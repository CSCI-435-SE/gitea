<!-- Generated by SpecStory, Markdown v2.1.0 -->

# 2026-09-16 14:19:43Z

<!-- Claude Code Session 13224e94-2112-44d3-9b1d-0e81e34d4ea5 (2026-09-16 14:19:43Z) -->

_**User (2026-09-16 14:19:43Z)**_

verify this issue branch

relavant issue:
Implement warning coloration on issues approaching their deadline similar to the red color used on items that have passed their deadline. Uses pre-defined warning color. Has tests for both new warning and existing red coloration. Some logic on the red coloration was changed as to not block other visual affects.

Surface validation, no need to go too too deep.

My personal look is like, arent these way too many changes for something as simply as showing the deadline?

---

_**Agent (claude-opus-5 2026-09-16 14:19:44Z)**_

I'll look at the branch's diff against main.

_**Agent (claude-opus-5 2026-09-16 14:19:47Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Show branch commits and diff stat

`git log --oneline main..HEAD && echo "---STAT---" && git diff main...HEAD --stat`

```text
10c9708788 Add AI logs
d0f3e1f999 Add warning color on date for issues due within 72 hours
---STAT---
 ...laude-vscode_issue-3-implement-warning-color.md | 12092 +++++++++++++++++++
 models/issues/deadline.go                          |    31 +
 models/issues/deadline_test.go                     |    86 +
 models/issues/issue.go                             |    12 +-
 models/issues/milestone.go                         |     7 +-
 options/locale/locale_en-US.json                   |     1 +
 templates/repo/issue/milestone_issues.tmpl         |    10 +-
 templates/repo/issue/milestones.tmpl               |     8 +-
 templates/repo/issue/sidebar/due_date.tmpl         |     8 +-
 templates/shared/issuelist.tmpl                    |     8 +-
 templates/user/dashboard/milestones.tmpl           |     8 +-
 tests/integration/issue_test.go                    |    50 +
 tests/integration/milestone_test.go                |    72 +
 13 files changed, 12378 insertions(+), 15 deletions(-)
```
</details></tool-use>

_**Agent (claude-opus-5 2026-09-16 14:19:49Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Show core diff

`git diff main...HEAD -- models options templates | cat`

```text
diff --git a/models/issues/deadline.go b/models/issues/deadline.go
new file mode 100644
index 0000000000..77e9678d27
--- /dev/null
+++ b/models/issues/deadline.go
@@ -0,0 +1,31 @@
+// Copyright 2026 The Gitea Authors. All rights reserved.
+// SPDX-License-Identifier: MIT
+
+package issues
+
+import (
+	"time"
+
+	"gitea.dev/modules/timeutil"
+)
+
+// NearDueDuration is the window before a deadline in which an open issue, pull request
+// or milestone counts as near due. Deadlines are pinned to 23:59:59 local time by
+// routers/common/deadline.go, so in practice this covers the next three calendar days.
+const NearDueDuration = 72 * time.Hour
+
+// calcDeadlineStatus computes the overdue / near-due state shared by Issue and Milestone.
+// The two are mutually exclusive: a passed deadline is overdue, never near due.
+func calcDeadlineStatus(deadlineUnix timeutil.TimeStamp, isClosed bool, closedUnix timeutil.TimeStamp) (isOverdue, isNearDue bool) {
+	if deadlineUnix == 0 {
+		return false, false
+	}
+	if isClosed {
+		return closedUnix >= deadlineUnix, false
+	}
+	now := timeutil.TimeStampNow()
+	if now >= deadlineUnix {
+		return true, false
+	}
+	return false, deadlineUnix <= now.AddDuration(NearDueDuration)
+}
diff --git a/models/issues/deadline_test.go b/models/issues/deadline_test.go
new file mode 100644
index 0000000000..c7562378fb
--- /dev/null
+++ b/models/issues/deadline_test.go
@@ -0,0 +1,86 @@
+// Copyright 2026 The Gitea Authors. All rights reserved.
+// SPDX-License-Identifier: MIT
+
+package issues_test
+
+import (
+	"testing"
+	"time"
+
+	issues_model "gitea.dev/models/issues"
+	"gitea.dev/modules/timeutil"
+
+	"github.com/stretchr/testify/assert"
+)
+
+func TestIssueDeadlineStatus(t *testing.T) {
+	now := time.Date(2024, 1, 10, 12, 0, 0, 0, time.UTC)
+	defer timeutil.MockSet(now)()
+	nowTS := timeutil.TimeStamp(now.Unix())
+
+	cases := []struct {
+		name    string
+		issue   issues_model.Issue
+		overdue bool
+		nearDue bool
+	}{
+		{"no deadline", issues_model.Issue{}, false, false},
+		{"far future", issues_model.Issue{DeadlineUnix: nowTS.AddDuration(96 * time.Hour)}, false, false},
+		{"just outside window", issues_model.Issue{DeadlineUnix: nowTS.AddDuration(issues_model.NearDueDuration).Add(1)}, false, false},
+		{"exactly at window edge", issues_model.Issue{DeadlineUnix: nowTS.AddDuration(issues_model.NearDueDuration)}, false, true},
+		{"inside window", issues_model.Issue{DeadlineUnix: nowTS.AddDuration(time.Hour)}, false, true},
+		{"deadline is now", issues_model.Issue{DeadlineUnix: nowTS}, true, false},
+		{"overdue", issues_model.Issue{DeadlineUnix: nowTS.Add(-1)}, true, false},
+		{"closed before deadline", issues_model.Issue{IsClosed: true, ClosedUnix: nowTS.Add(-3600), DeadlineUnix: nowTS.AddDuration(time.Hour)}, false, false},
+		{"closed after deadline", issues_model.Issue{IsClosed: true, ClosedUnix: nowTS, DeadlineUnix: nowTS.Add(-3600)}, true, false},
+		{"closed, no deadline", issues_model.Issue{IsClosed: true, ClosedUnix: nowTS}, false, false},
+	}
+	for _, c := range cases {
+		t.Run(c.name, func(t *testing.T) {
+			assert.Equal(t, c.overdue, c.issue.IsOverdue())
+			assert.Equal(t, c.nearDue, c.issue.IsNearDue())
+			assert.False(t, c.issue.IsOverdue() && c.issue.IsNearDue(), "overdue and near-due must be mutually exclusive")
+		})
+	}
+}
+
+func TestMilestoneDeadlineStatus(t *testing.T) {
+	now := time.Date(2024, 1, 10, 12, 0, 0, 0, time.UTC)
+	defer timeutil.MockSet(now)()
+	nowTS := timeutil.TimeStamp(now.Unix())
+
+	load := func(m *issues_model.Milestone) *issues_model.Milestone {
+		m.AfterLoad()
+		return m
+	}
+
+	noDeadline := load(&issues_model.Milestone{})
+	assert.False(t, noDeadline.IsOverdue)
+	assert.False(t, noDeadline.IsNearDue)
+	assert.Empty(t, noDeadline.DeadlineString)
+
+	soon := load(&issues_model.Milestone{DeadlineUnix: nowTS.AddDuration(time.Hour)})
+	assert.False(t, soon.IsOverdue)
+	assert.True(t, soon.IsNearDue)
+	assert.NotEmpty(t, soon.DeadlineString)
+
+	overdue := load(&issues_model.Milestone{DeadlineUnix: nowTS.Add(-1)})
+	assert.True(t, overdue.IsOverdue)
+	assert.False(t, overdue.IsNearDue)
+
+	far := load(&issues_model.Milestone{DeadlineUnix: nowTS.AddDuration(96 * time.Hour)})
+	assert.False(t, far.IsOverdue)
+	assert.False(t, far.IsNearDue)
+
+	closedInTime := load(&issues_model.Milestone{IsClosed: true, ClosedDateUnix: nowTS.Add(-3600), DeadlineUnix: nowTS.AddDuration(time.Hour)})
+	assert.False(t, closedInTime.IsOverdue)
+	assert.False(t, closedInTime.IsNearDue)
+
+	closedLate := load(&issues_model.Milestone{IsClosed: true, ClosedDateUnix: nowTS, DeadlineUnix: nowTS.Add(-3600)})
+	assert.True(t, closedLate.IsOverdue)
+	assert.False(t, closedLate.IsNearDue)
+
+	// NumOpenIssues is still computed as before.
+	m := load(&issues_model.Milestone{NumIssues: 5, NumClosedIssues: 2})
+	assert.Equal(t, 3, m.NumOpenIssues)
+}
diff --git a/models/issues/issue.go b/models/issues/issue.go
index a0af16db83..a7a5f837f1 100644
--- a/models/issues/issue.go
+++ b/models/issues/issue.go
@@ -135,10 +135,14 @@ func (issue *Issue) LoadTotalTimes(ctx context.Context) (err error) {
 
 // IsOverdue checks if the issue is overdue
 func (issue *Issue) IsOverdue() bool {
-	if issue.IsClosed {
-		return issue.ClosedUnix >= issue.DeadlineUnix
-	}
-	return timeutil.TimeStampNow() >= issue.DeadlineUnix
+	isOverdue, _ := calcDeadlineStatus(issue.DeadlineUnix, issue.IsClosed, issue.ClosedUnix)
+	return isOverdue
+}
+
+// IsNearDue checks if the issue's deadline is approaching but has not passed
+func (issue *Issue) IsNearDue() bool {
+	_, isNearDue := calcDeadlineStatus(issue.DeadlineUnix, issue.IsClosed, issue.ClosedUnix)
+	return isNearDue
 }
 
 // LoadRepo loads issue's repository
diff --git a/models/issues/milestone.go b/models/issues/milestone.go
index eb3c548c08..f5152e165b 100644
--- a/models/issues/milestone.go
+++ b/models/issues/milestone.go
@@ -57,6 +57,7 @@ type Milestone struct {
 	NumOpenIssues   int  `xorm:"-"`
 	Completeness    int  // Percentage(1-100).
 	IsOverdue       bool `xorm:"-"`
+	IsNearDue       bool `xorm:"-"`
 
 	CreatedUnix    timeutil.TimeStamp `xorm:"INDEX created"`
 	UpdatedUnix    timeutil.TimeStamp `xorm:"INDEX updated"`
@@ -90,11 +91,7 @@ func (m *Milestone) AfterLoad() {
 		return
 	}
 	m.DeadlineString = m.DeadlineUnix.FormatDate()
-	if m.IsClosed {
-		m.IsOverdue = m.ClosedDateUnix >= m.DeadlineUnix
-	} else {
-		m.IsOverdue = timeutil.TimeStampNow() >= m.DeadlineUnix
-	}
+	m.IsOverdue, m.IsNearDue = calcDeadlineStatus(m.DeadlineUnix, m.IsClosed, m.ClosedDateUnix)
 }
 
 // State returns string representation of milestone status.
diff --git a/options/locale/locale_en-US.json b/options/locale/locale_en-US.json
index 9373aa83a9..4cb9895bb1 100644
--- a/options/locale/locale_en-US.json
+++ b/options/locale/locale_en-US.json
@@ -1676,6 +1676,7 @@
   "repo.issues.due_date_modified": "modified the due date from %[2]s to %[1]s %[3]s",
   "repo.issues.due_date_remove": "removed the due date %s %s",
   "repo.issues.due_date_overdue": "Overdue",
+  "repo.issues.due_date_near": "Due soon",
   "repo.issues.due_date_invalid": "The due date is invalid or out of range. Please use the format 'yyyy-mm-dd'.",
   "repo.issues.dependency.title": "Dependencies",
   "repo.issues.dependency.issue_no_dependencies": "No dependencies set.",
diff --git a/templates/repo/issue/milestone_issues.tmpl b/templates/repo/issue/milestone_issues.tmpl
index 03cadfd295..1c152c502a 100644
--- a/templates/repo/issue/milestone_issues.tmpl
+++ b/templates/repo/issue/milestone_issues.tmpl
@@ -31,12 +31,18 @@
 			<div class="flex-text-block tw-gap-4">
 				<div class="flex-text-inline">
 					{{$closedDate:= DateUtils.TimeSince .Milestone.ClosedDateUnix}}
-					{{if .IsClosed}}
+					{{if .Milestone.IsClosed}}
 						{{svg "octicon-clock"}} {{ctx.Locale.Tr "repo.milestones.closed" $closedDate}}
 					{{else}}
 
 						{{if .Milestone.DeadlineString}}
-							<span{{if .IsOverdue}} class="tw-text-red"{{end}}>
+							{{$dueClass := ""}}{{$dueTooltip := ""}}
+							{{if .Milestone.IsOverdue}}
+								{{$dueClass = "tw-text-red"}}{{$dueTooltip = ctx.Locale.Tr "repo.issues.due_date_overdue"}}
+							{{else if .Milestone.IsNearDue}}
+								{{$dueClass = "tw-text-warning-text"}}{{$dueTooltip = ctx.Locale.Tr "repo.issues.due_date_near"}}
+							{{end}}
+							<span class="due-date {{$dueClass}}" {{if $dueTooltip}}data-tooltip-content="{{$dueTooltip}}"{{end}}>
 								{{svg "octicon-calendar"}}
 								{{DateUtils.AbsoluteShort (.Milestone.DeadlineString|DateUtils.ParseLegacy)}}
 							</span>
diff --git a/templates/repo/issue/milestones.tmpl b/templates/repo/issue/milestones.tmpl
index 4af4697b44..404c29b3f2 100644
--- a/templates/repo/issue/milestones.tmpl
+++ b/templates/repo/issue/milestones.tmpl
@@ -57,7 +57,13 @@
 									{{ctx.Locale.Tr "repo.milestones.closed" $closedDate}}
 								{{else}}
 									{{if .DeadlineString}}
-										<span class="flex-text-inline {{if .IsOverdue}}tw-text-red{{end}}">
+										{{$dueClass := ""}}{{$dueTooltip := ""}}
+										{{if .IsOverdue}}
+											{{$dueClass = "tw-text-red"}}{{$dueTooltip = ctx.Locale.Tr "repo.issues.due_date_overdue"}}
+										{{else if .IsNearDue}}
+											{{$dueClass = "tw-text-warning-text"}}{{$dueTooltip = ctx.Locale.Tr "repo.issues.due_date_near"}}
+										{{end}}
+										<span class="due-date flex-text-inline {{$dueClass}}" {{if $dueTooltip}}data-tooltip-content="{{$dueTooltip}}"{{end}}>
 											{{svg "octicon-calendar" 14}}
 											{{DateUtils.AbsoluteShort (.DeadlineString|DateUtils.ParseLegacy)}}
 										</span>
diff --git a/templates/repo/issue/sidebar/due_date.tmpl b/templates/repo/issue/sidebar/due_date.tmpl
index b312e8a889..224dcdb713 100644
--- a/templates/repo/issue/sidebar/due_date.tmpl
+++ b/templates/repo/issue/sidebar/due_date.tmpl
@@ -3,7 +3,13 @@
 <div class="ui form tw-mt-2">
 	{{if .Issue.DeadlineUnix}}
 		<div class="flex-left-right">
-			<div class="due-date {{if .Issue.IsOverdue}}tw-text-red{{end}}" {{if .Issue.IsOverdue}}data-tooltip-content="{{ctx.Locale.Tr "repo.issues.due_date_overdue"}}"{{end}}>
+			{{$dueClass := ""}}{{$dueTooltip := ""}}
+			{{if .Issue.IsOverdue}}
+				{{$dueClass = "tw-text-red"}}{{$dueTooltip = ctx.Locale.Tr "repo.issues.due_date_overdue"}}
+			{{else if .Issue.IsNearDue}}
+				{{$dueClass = "tw-text-warning-text"}}{{$dueTooltip = ctx.Locale.Tr "repo.issues.due_date_near"}}
+			{{end}}
+			<div class="due-date {{$dueClass}}" {{if $dueTooltip}}data-tooltip-content="{{$dueTooltip}}"{{end}}>
 				{{svg "octicon-calendar"}} {{DateUtils.AbsoluteLong .Issue.DeadlineUnix}}
 			</div>
 			<div class="flex-text-block">
diff --git a/templates/shared/issuelist.tmpl b/templates/shared/issuelist.tmpl
index 838caeb7b2..c5823729cb 100644
--- a/templates/shared/issuelist.tmpl
+++ b/templates/shared/issuelist.tmpl
@@ -103,7 +103,13 @@
 						</span>
 					{{end}}
 					{{if ne .DeadlineUnix 0}}
-						<span class="due-date flex-text-inline {{if .IsOverdue}}tw-text-red{{end}}" data-tooltip-content="{{ctx.Locale.Tr "repo.issues.due_date"}}">
+						{{$dueClass := ""}}{{$dueTooltip := ctx.Locale.Tr "repo.issues.due_date"}}
+						{{if .IsOverdue}}
+							{{$dueClass = "tw-text-red"}}{{$dueTooltip = ctx.Locale.Tr "repo.issues.due_date_overdue"}}
+						{{else if .IsNearDue}}
+							{{$dueClass = "tw-text-warning-text"}}{{$dueTooltip = ctx.Locale.Tr "repo.issues.due_date_near"}}
+						{{end}}
+						<span class="due-date flex-text-inline {{$dueClass}}" data-tooltip-content="{{$dueTooltip}}">
 							{{svg "octicon-calendar" 14}}
 							{{DateUtils.AbsoluteShort .DeadlineUnix}}
 						</span>
diff --git a/templates/user/dashboard/milestones.tmpl b/templates/user/dashboard/milestones.tmpl
index 62c2df7e8f..7453165d6d 100644
--- a/templates/user/dashboard/milestones.tmpl
+++ b/templates/user/dashboard/milestones.tmpl
@@ -116,7 +116,13 @@
 											{{ctx.Locale.Tr "repo.milestones.closed" $closedDate}}
 										{{else}}
 											{{if .DeadlineString}}
-												<span class="flex-text-inline {{if .IsOverdue}}tw-text-red{{end}}">
+												{{$dueClass := ""}}{{$dueTooltip := ""}}
+												{{if .IsOverdue}}
+													{{$dueClass = "tw-text-red"}}{{$dueTooltip = ctx.Locale.Tr "repo.issues.due_date_overdue"}}
+												{{else if .IsNearDue}}
+													{{$dueClass = "tw-text-warning-text"}}{{$dueTooltip = ctx.Locale.Tr "repo.issues.due_date_near"}}
+												{{end}}
+												<span class="due-date flex-text-inline {{$dueClass}}" {{if $dueTooltip}}data-tooltip-content="{{$dueTooltip}}"{{end}}>
 													{{svg "octicon-calendar" 14}}
 													{{DateUtils.AbsoluteShort (.DeadlineString|DateUtils.ParseLegacy)}}
 												</span>
```
</details></tool-use>

_**Agent (claude-opus-5 2026-09-16 14:20:00Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Check CSS class and callers

```bash
echo "=== warning-text usages ===" && grep -rn "warning-text" --include=*.css --include=*.js --include=*.ts --include=*.tmpl web_src templates tailwind.config.js 2>/dev/null | head -30
echo "=== due-date css ==="
grep -rn "due-date" web_src/css | head -20
echo "=== IsOverdue callers ==="
grep -rn "IsOverdue\|IsNearDue" --include=*.go --include=*.tmpl . | grep -v "_test.go" | grep -v "^./.claude" | head -30
```

```text
=== warning-text usages ===
(eval):1: no matches found: --include=*.css
=== due-date css ===
=== IsOverdue callers ===
(eval):5: no matches found: --include=*.go
```
</details></tool-use>

_**Agent (claude-opus-5 2026-09-16 14:20:03Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Check CSS class and callers

```bash
echo "=== warning-text defs ===" && grep -rn "warning-text" web_src templates tailwind.config.js 2>/dev/null | head -30
echo "=== due-date css ==="
grep -rn "due-date" web_src/css | head -20
echo "=== IsOverdue callers ==="
grep -rn "IsOverdue\|IsNearDue" --include="*.go" --include="*.tmpl" . 2>/dev/null | grep -v "_test.go" | grep -v "\.claude" | head -30
```

```text
=== warning-text defs ===
web_src/js/components/ActionRunJobView.vue:765:  color: var(--color-warning-text);
web_src/css/themes/theme-gitea-dark.css:189:  --color-warning-text: #d29922;
web_src/css/modules/header.css:161:  color: var(--color-warning-text) !important;
web_src/css/base.css:501:  border-left-color: var(--color-warning-text);
web_src/css/base.css:504:  color: var(--color-warning-text);
web_src/css/themes/theme-gitea-light.css:189:  --color-warning-text: #9a6700;
templates/repo/issue/sidebar/due_date.tmpl:10:				{{$dueClass = "tw-text-warning-text"}}{{$dueTooltip = ctx.Locale.Tr "repo.issues.due_date_near"}}
templates/repo/issue/milestone_issues.tmpl:43:								{{$dueClass = "tw-text-warning-text"}}{{$dueTooltip = ctx.Locale.Tr "repo.issues.due_date_near"}}
templates/devtest/severity-colors.tmpl:96:			<div class="tw-w-[100px] tw-h-[60px] tw-rounded tw-flex tw-items-center tw-justify-center tw-text-xs tw-bg-warning-bg tw-text-warning-text tw-border tw-border-warning-border">Text</div>
templates/repo/issue/milestones.tmpl:64:											{{$dueClass = "tw-text-warning-text"}}{{$dueTooltip = ctx.Locale.Tr "repo.issues.due_date_near"}}
templates/user/dashboard/milestones.tmpl:123:													{{$dueClass = "tw-text-warning-text"}}{{$dueTooltip = ctx.Locale.Tr "repo.issues.due_date_near"}}
templates/shared/issuelist.tmpl:110:							{{$dueClass = "tw-text-warning-text"}}{{$dueTooltip = ctx.Locale.Tr "repo.issues.due_date_near"}}
=== due-date css ===
=== IsOverdue callers ===
templates/repo/issue/milestones.tmpl:61:										{{if .IsOverdue}}
templates/repo/issue/milestones.tmpl:63:										{{else if .IsNearDue}}
templates/repo/issue/milestone_issues.tmpl:40:							{{if .Milestone.IsOverdue}}
templates/repo/issue/milestone_issues.tmpl:42:							{{else if .Milestone.IsNearDue}}
templates/repo/issue/sidebar/due_date.tmpl:7:			{{if .Issue.IsOverdue}}
templates/repo/issue/sidebar/due_date.tmpl:9:			{{else if .Issue.IsNearDue}}
templates/user/dashboard/milestones.tmpl:120:												{{if .IsOverdue}}
templates/user/dashboard/milestones.tmpl:122:												{{else if .IsNearDue}}
templates/shared/issuelist.tmpl:107:						{{if .IsOverdue}}
templates/shared/issuelist.tmpl:109:						{{else if .IsNearDue}}
models/issues/milestone.go:59:	IsOverdue       bool `xorm:"-"`
models/issues/milestone.go:60:	IsNearDue       bool `xorm:"-"`
models/issues/milestone.go:94:	m.IsOverdue, m.IsNearDue = calcDeadlineStatus(m.DeadlineUnix, m.IsClosed, m.ClosedDateUnix)
models/issues/issue.go:136:// IsOverdue checks if the issue is overdue
models/issues/issue.go:137:func (issue *Issue) IsOverdue() bool {
models/issues/issue.go:142:// IsNearDue checks if the issue's deadline is approaching but has not passed
models/issues/issue.go:143:func (issue *Issue) IsNearDue() bool {
```
</details></tool-use>

_**Agent (claude-opus-5 2026-09-16 14:20:10Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Inspect milestone router ctx and selectors

```bash
echo "=== ctxData IsClosed in milestone issues router ===" && grep -rn '"IsClosed"\|"Milestone"' routers/web/repo/milestone.go | head -20
echo "=== due-date selector usage ==="
grep -rn "due-date" --include="*.go" --include="*.ts" --include="*.js" . 2>/dev/null | grep -v "\.claude" | head
echo "=== tailwind warning ==="
grep -n "warning" tailwind.config.js | head
```

```text
=== ctxData IsClosed in milestone issues router ===
routers/web/repo/milestone.go:261:	ctx.Data["Milestone"] = milestone
=== due-date selector usage ===
tests/integration/milestone_test.go:47:		assert.True(t, row.Find(".due-date").HasClass(wantClass))
tests/integration/milestone_test.go:55:		assert.True(t, NewHTMLParser(t, resp.Body).Find(".due-date").HasClass(wantClass))
tests/integration/issue_test.go:700:		return NewHTMLParser(t, resp.Body).Find(".due-date")
=== tailwind warning ===
ugrep: warning: tailwind.config.js: No such file or directory
```
</details></tool-use>

_**Agent (claude-opus-5 2026-09-16 14:20:20Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Read milestone_issues template head

`sed -n 1,60p templates/repo/issue/milestone_issues.tmpl`

```text
{{template "base/head" .}}
<div role="main" aria-label="{{.Title}}" class="page-content repository milestone-issue-list">
	{{template "repo/header" .}}
	<div class="ui container">
		{{template "base/alert" .}}
		<div class="flex-text-block tw-flex-wrap tw-mb-2">
			<h1 class="tw-flex-1 tw-m-0">{{.Milestone.Name}}</h1>
			{{if not .Repository.IsArchived}}
				<div>
					{{if or .CanWriteIssues .CanWritePulls}}
						{{if .Milestone.IsClosed}}
							<a class="ui primary basic button link-action" href data-url="{{$.RepoLink}}/milestones/{{.MilestoneID}}/open">{{ctx.Locale.Tr "repo.milestones.open"}}
							</a>
						{{else}}
							<a class="ui red basic button link-action" href data-url="{{$.RepoLink}}/milestones/{{.MilestoneID}}/close">{{ctx.Locale.Tr "repo.milestones.close"}}
							</a>
						{{end}}
						<a class="ui button" href="{{.RepoLink}}/milestones/{{.MilestoneID}}/edit">{{ctx.Locale.Tr "repo.milestones.edit"}}</a>
					{{end}}
					<a class="ui primary button" href="{{.RepoLink}}/issues/new{{if .NewIssueChooseTemplate}}/choose{{end}}?milestone={{.MilestoneID}}">{{ctx.Locale.Tr "repo.issues.new"}}</a>
				</div>
			{{end}}
		</div>
		{{if .Milestone.RenderedContent}}
		<div class="render-content markup tw-mb-4">
				{{.Milestone.RenderedContent}}
		</div>
		{{end}}
		<div class="tw-flex tw-flex-col tw-gap-2">
			<progress class="milestone-progress-big" value="{{.Milestone.Completeness}}" max="100"></progress>
			<div class="flex-text-block tw-gap-4">
				<div class="flex-text-inline">
					{{$closedDate:= DateUtils.TimeSince .Milestone.ClosedDateUnix}}
					{{if .Milestone.IsClosed}}
						{{svg "octicon-clock"}} {{ctx.Locale.Tr "repo.milestones.closed" $closedDate}}
					{{else}}

						{{if .Milestone.DeadlineString}}
							{{$dueClass := ""}}{{$dueTooltip := ""}}
							{{if .Milestone.IsOverdue}}
								{{$dueClass = "tw-text-red"}}{{$dueTooltip = ctx.Locale.Tr "repo.issues.due_date_overdue"}}
							{{else if .Milestone.IsNearDue}}
								{{$dueClass = "tw-text-warning-text"}}{{$dueTooltip = ctx.Locale.Tr "repo.issues.due_date_near"}}
							{{end}}
							<span class="due-date {{$dueClass}}" {{if $dueTooltip}}data-tooltip-content="{{$dueTooltip}}"{{end}}>
								{{svg "octicon-calendar"}}
								{{DateUtils.AbsoluteShort (.Milestone.DeadlineString|DateUtils.ParseLegacy)}}
							</span>
						{{else}}
							{{svg "octicon-calendar"}}
							{{ctx.Locale.Tr "repo.milestones.no_due_date"}}
						{{end}}
					{{end}}
				</div>
				<div>{{ctx.Locale.Tr "repo.milestones.completeness" .Milestone.Completeness}}</div>
				{{if .TotalTrackedTime}}
					<div data-tooltip-content='{{ctx.Locale.Tr "tracked_time_summary"}}'>
						{{svg "octicon-clock"}}
						{{.TotalTrackedTime | Sec2Hour}}
					</div>
```
</details></tool-use>

_**Agent (claude-opus-5 2026-09-16 14:20:30Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Check ctx IsClosed and AI log convention

```bash
echo "=== IsClosed set in ctx.Data anywhere ===" && grep -rn 'Data\["IsClosed"\]' routers/ | head
echo "=== AI log location/convention ==="
git show --stat HEAD | head -20
ls .claude-students/ 2>/dev/null | head
git log --oneline -20 --name-only --diff-filter=A -- '*claude-vscode*' '*ai-log*' 2>/dev/null | head -20
```

```text
=== IsClosed set in ctx.Data anywhere ===
=== AI log location/convention ===
commit 10c970878845fbc370bcec3e5dd8cbe9ed78cba7
Author: Jack Donohue <jack.donohu3@gmail.com>
Date:   Wed Sep 16 00:35:39 2026 +0000

    Add AI logs

 ...laude-vscode_issue-3-implement-warning-color.md | 12092 +++++++++++++++++++
 1 file changed, 12092 insertions(+)
check.sh
docs
guide
INDEX.md
MAINTENANCE.md
README.md
_TEMPLATE.md
10c9708788 Add AI logs
ai-logs/sprint0/jdonohue/2026-09-15_claude-vscode_issue-3-implement-warning-color.md
3ef9323f91 chore(ai-log): add ai-log of the session
ai-logs/sprint0/ab-wm/2026-09-12_claude-code_issue-5-impl.md
d851fbed18 chore(ai-logs): added session log
ai-logs/sprint0/ab-wm/2026-09-10_16-40-09Z-this-is-the-gitea.md
c0092050a4 update to students' guide and adding folder structure
ai-logs/README.md
ai-logs/sprint0/.gitkeep
ai-logs/sprint1/.gitkeep
ai-logs/sprint2/.gitkeep
ai-logs/sprint3/.gitkeep
ai-logs/sprint4/.gitkeep
```
</details></tool-use>

_**Agent (claude-opus-5 2026-09-16 14:21:12Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Run new unit tests

`go test -run '^TestIssueDeadlineStatus$|^TestMilestoneDeadlineStatus$' ./models/issues/ 2>&1 | tail -20`

```text
ok  	gitea.dev/models/issues	0.396s
```
</details></tool-use>

_**Agent (claude-opus-5 2026-09-16 14:21:14Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Show integration test diff

`git diff main...HEAD -- tests/ | cat`

```text
diff --git a/tests/integration/issue_test.go b/tests/integration/issue_test.go
index 2872b721f3..49f0338f0c 100644
--- a/tests/integration/issue_test.go
+++ b/tests/integration/issue_test.go
@@ -680,6 +680,56 @@ func TestUpdateIssueDeadline(t *testing.T) {
 	assert.True(t, issueAfter.DeadlineUnix.IsZero())
 }
 
+func TestIssueDueDateHighlight(t *testing.T) {
+	defer tests.PrepareTestEnv(t)()
+
+	issue := unittest.AssertExistsAndLoadBean(t, &issues_model.Issue{ID: 10})
+	repo := unittest.AssertExistsAndLoadBean(t, &repo_model.Repository{ID: issue.RepoID})
+	owner := unittest.AssertExistsAndLoadBean(t, &user_model.User{ID: repo.OwnerID})
+
+	session := loginUser(t, owner.Name)
+	deadlineURL := fmt.Sprintf("/%s/%s/issues/%d/deadline", owner.Name, repo.Name, issue.Index)
+	issueURL := fmt.Sprintf("/%s/%s/issues/%d", owner.Name, repo.Name, issue.Index)
+
+	setDeadlineAndGetDueDate := func(t *testing.T, deadline string) *goquery.Selection {
+		req := NewRequestWithValues(t, "POST", deadlineURL, map[string]string{"deadline": deadline})
+		session.MakeRequest(t, req, http.StatusOK)
+
+		req = NewRequest(t, "GET", issueURL)
+		resp := session.MakeRequest(t, req, http.StatusOK)
+		return NewHTMLParser(t, resp.Body).Find(".due-date")
+	}
+
+	t.Run("near due", func(t *testing.T) {
+		tomorrow := time.Now().AddDate(0, 0, 1).Format("2006-01-02")
+		dueDate := setDeadlineAndGetDueDate(t, tomorrow)
+		assert.True(t, dueDate.HasClass("tw-text-warning-text"))
+		assert.False(t, dueDate.HasClass("tw-text-red"))
+		tooltip, _ := dueDate.Attr("data-tooltip-content")
+		assert.Equal(t, "Due soon", tooltip)
+	})
+
+	t.Run("overdue", func(t *testing.T) {
+		yesterday := time.Now().AddDate(0, 0, -1).Format("2006-01-02")
+		dueDate := setDeadlineAndGetDueDate(t, yesterday)
+		assert.True(t, dueDate.HasClass("tw-text-red"))
+		assert.False(t, dueDate.HasClass("tw-text-warning-text"))
+		tooltip, _ := dueDate.Attr("data-tooltip-content")
+		assert.Equal(t, "Overdue", tooltip)
+	})
+
+	t.Run("far future", func(t *testing.T) {
+		nextMonth := time.Now().AddDate(0, 1, 0).Format("2006-01-02")
+		dueDate := setDeadlineAndGetDueDate(t, nextMonth)
+		assert.False(t, dueDate.HasClass("tw-text-red"))
+		assert.False(t, dueDate.HasClass("tw-text-warning-text"))
+	})
+
+	// leave the issue without a deadline for other tests relying on fixture state
+	req := NewRequestWithValues(t, "POST", deadlineURL, map[string]string{"deadline": ""})
+	session.MakeRequest(t, req, http.StatusOK)
+}
+
 func TestUpdateIssueRefByPoster(t *testing.T) {
 	defer tests.PrepareTestEnv(t)()
 
diff --git a/tests/integration/milestone_test.go b/tests/integration/milestone_test.go
new file mode 100644
index 0000000000..f119534dae
--- /dev/null
+++ b/tests/integration/milestone_test.go
@@ -0,0 +1,72 @@
+// Copyright 2026 The Gitea Authors. All rights reserved.
+// SPDX-License-Identifier: MIT
+
+package integration
+
+import (
+	"fmt"
+	"net/http"
+	"testing"
+	"time"
+
+	issues_model "gitea.dev/models/issues"
+	repo_model "gitea.dev/models/repo"
+	"gitea.dev/models/unittest"
+	user_model "gitea.dev/models/user"
+	"gitea.dev/tests"
+
+	"github.com/stretchr/testify/assert"
+)
+
+// TestMilestoneDueDateHighlight also serves as the regression check for the
+// milestone_issues.tmpl context bug fix: before that fix the detail page never
+// rendered a milestone's due date in red, even when overdue.
+func TestMilestoneDueDateHighlight(t *testing.T) {
+	defer tests.PrepareTestEnv(t)()
+
+	repo := unittest.AssertExistsAndLoadBean(t, &repo_model.Repository{ID: 1})
+	owner := unittest.AssertExistsAndLoadBean(t, &user_model.User{ID: repo.OwnerID})
+	session := loginUser(t, owner.Name)
+
+	createMilestone := func(t *testing.T, name, deadline string) int64 {
+		req := NewRequestWithValues(t, "POST", fmt.Sprintf("/%s/%s/milestones/new", owner.Name, repo.Name), map[string]string{
+			"title":    name,
+			"deadline": deadline,
+		})
+		session.MakeRequest(t, req, http.StatusSeeOther)
+		milestone := unittest.AssertExistsAndLoadBean(t, &issues_model.Milestone{RepoID: repo.ID, Name: name})
+		return milestone.ID
+	}
+
+	// assertListDueDateClass scopes to the specific milestone's row, since the
+	// list page also renders the repo's other (fixture) milestones.
+	assertListDueDateClass := func(t *testing.T, id int64, wantClass string) {
+		req := NewRequest(t, "GET", fmt.Sprintf("/%s/%s/milestones", owner.Name, repo.Name))
+		resp := session.MakeRequest(t, req, http.StatusOK)
+		row := NewHTMLParser(t, resp.Body).Find(fmt.Sprintf(`a[href$="/milestone/%d"]`, id)).Closest(".item")
+		assert.True(t, row.Find(".due-date").HasClass(wantClass))
+	}
+
+	// assertDetailDueDateClass checks the milestone's own detail page, where it is
+	// the only milestone rendered.
+	assertDetailDueDateClass := func(t *testing.T, id int64, wantClass string) {
+		req := NewRequest(t, "GET", fmt.Sprintf("/%s/%s/milestone/%d", owner.Name, repo.Name, id))
+		resp := session.MakeRequest(t, req, http.StatusOK)
+		assert.True(t, NewHTMLParser(t, resp.Body).Find(".due-date").HasClass(wantClass))
+	}
+
+	t.Run("near due on milestone list", func(t *testing.T) {
+		tomorrow := time.Now().AddDate(0, 0, 1).Format("2006-01-02")
+		id := createMilestone(t, "e2e-near-due-list", tomorrow)
+		assertListDueDateClass(t, id, "tw-text-warning-text")
+	})
+
+	// This is the regression check for the milestone_issues.tmpl context bug fix:
+	// before that fix .IsOverdue read the page context instead of .Milestone, so an
+	// overdue milestone never rendered red on its own detail page.
+	t.Run("overdue on milestone detail page", func(t *testing.T) {
+		yesterday := time.Now().AddDate(0, 0, -1).Format("2006-01-02")
+		id := createMilestone(t, "e2e-overdue-detail", yesterday)
+		assertDetailDueDateClass(t, id, "tw-text-red")
+	})
+}
```
</details></tool-use>

_**Agent (claude-opus-5 2026-09-16 14:22:49Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Format and vet checks, compile integration tests

`gofmt -l models/issues tests/integration | head; echo "gofmt-done"; go vet ./models/issues/ 2>&1 | tail -5; echo "vet-done"; go test -run XXX -count=1 ./tests/integration/ 2>&1 | tail -5`

```text
gofmt-done
vet-done
ok  	gitea.dev/tests/integration	2.304s [no tests to run]
```
</details></tool-use>

_**Agent (claude-opus-5 2026-09-16 14:23:06Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Verify deadline pinning and run docs check

`ls routers/common/deadline.go 2>&1; grep -rn "23, 59, 59\|23:59:59" routers/common/*.go | head -5; echo "=== check.sh ==="; ./.claude-students/check.sh 2>&1 | tail -20`

```text
routers/common/deadline.go
routers/common/deadline.go:21:	deadline = time.Date(deadline.Year(), deadline.Month(), deadline.Day(), 23, 59, 59, 0, deadline.Location())
routers/common/deadline.go:29:	deadline := time.Date(t.Year(), t.Month(), t.Day(), 23, 59, 59, 0, setting.DefaultUILocation)
=== check.sh ===
check.sh: ok (34 docs, guide 34/34)
```
</details></tool-use>

_**Agent (claude-opus-5 2026-09-16 14:23:26Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Check for prebuilt binary and sqlite test target

`ls -la gitea 2>&1 | head -2; ls public/assets/js/index.js 2>&1 | head -2; grep -n "test-sqlite:" -A5 Makefile | head -20`

```text
-rwxr-xr-x. 1 mont9393 mont9393 112373920 Sep 15 11:33 gitea
ls: cannot access 'public/assets/js/index.js': No such file or directory
```
</details></tool-use>

_**Agent (claude-opus-5 2026-09-16 14:23:28Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Find sqlite integration test target

`grep -n "^test-sqlite" -A8 Makefile | head -30`

```text
(Bash completed with no output)
```
</details></tool-use>

_**Agent (claude-opus-5 2026-09-16 14:23:29Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Find sqlite targets in Makefile

`grep -n "sqlite" Makefile | head -20`

```text
35:# Use sqlite as default database if running tests, only do so for local tests, not in CI.
39:		GITEA_TEST_DATABASE ?= sqlite
47:ifneq (,$(findstring sqlite_mattn,$(TAGS))$(findstring pam,$(TAGS)))
189:	@printf "  \033[36m%-46s\033[0m %s\n" "test-backend[#TestSpecificName]" "run unit test (sqlite only)"
190:	@printf "  \033[36m%-46s\033[0m %s\n" "test-integration[#TestSpecificName]" "run integration test for GITEA_TEST_DATABASE (sqlite, mysql, pgsql, mssql)"
```
</details></tool-use>

_**Agent (claude-opus-5 2026-09-16 14:23:35Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Run the two new integration tests

`timeout 560 make 'test-integration#TestMilestoneDueDateHighlight|TestIssueDueDateHighlight' 2>&1 | tail -30`

```text
go test -timeout 40m -tags '' -run TestMilestoneDueDateHighlight|TestIssueDueDateHighlight gitea.dev/tests/integration
/bin/sh: line 1: TestIssueDueDateHighlight: command not found
make: *** [Makefile:464: test-integration#TestMilestoneDueDateHighlight|TestIssueDueDateHighlight] Error 127
```
</details></tool-use>

_**Agent (claude-opus-5 2026-09-16 14:23:54Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Run milestone integration test

`timeout 560 make 'test-integration#TestMilestoneDueDateHighlight' 2>&1 | tail -30`

```text
go test -timeout 40m -tags '' -run TestMilestoneDueDateHighlight gitea.dev/tests/integration
ok  	gitea.dev/tests/integration	8.250s
```
</details></tool-use>

_**Agent (claude-opus-5 2026-09-16 14:24:07Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Run issue integration test

`timeout 560 make 'test-integration#TestIssueDueDateHighlight' 2>&1 | tail -30`

```text
go test -timeout 40m -tags '' -run TestIssueDueDateHighlight gitea.dev/tests/integration
ok  	gitea.dev/tests/integration	2.578s
```
</details></tool-use>

_**Agent (claude-opus-5 2026-09-16 14:24:24Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Confirm regression test fails without the fix, then restore

`cp templates/repo/issue/milestone_issues.tmpl /tmp/claude-1000/-home-mont9393-Documents-CSCI435-gitea/13224e94-2112-44d3-9b1d-0e81e34d4ea5/scratchpad/mi.bak && sed -i 's/{{if \.Milestone\.IsOverdue}}/{{if .IsOverdue}}/' templates/repo/issue/milestone_issues.tmpl && timeout 400 make 'test-integration#TestMilestoneDueDateHighlight' 2>&1 | tail -12; cp /tmp/claude-1000/-home-mont9393-Documents-CSCI435-gitea/13224e94-2112-44d3-9b1d-0e81e34d4ea5/scratchpad/mi.bak templates/repo/issue/milestone_issues.tmpl; git status --short`

```text
    testlogger.go:62: 2026/09/16 10:24:24 HTTPRequest [I] router: completed POST /user2/repo1/milestones/new for test-mock:12345, 303 See Other in 8.2ms @ repo/milestone.go:107(repo.NewMilestonePost)
    testlogger.go:62: 2026/09/16 10:24:24 HTTPRequest [I] router: completed GET /user2/repo1/milestone/7 for test-mock:12345, 200 OK in 28.5ms @ repo/milestone.go:239(repo.MilestoneIssuesAndPulls)
    --- FAIL: TestMilestoneDueDateHighlight/overdue_on_milestone_detail_page (0.04s)
        milestone_test.go:55: 
            	Error Trace:	/home/mont9393/Documents/CSCI435/gitea/tests/integration/milestone_test.go:55
            	            				/home/mont9393/Documents/CSCI435/gitea/tests/integration/milestone_test.go:70
            	Error:      	Should be true
            	Test:       	TestMilestoneDueDateHighlight/overdue_on_milestone_detail_page
FAIL
FAIL	gitea.dev/tests/integration	2.441s
FAIL
make: *** [Makefile:464: test-integration#TestMilestoneDueDateHighlight] Error 1
```
</details></tool-use>

_**Agent (claude-opus-5 2026-09-16 14:24:28Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Diff size excluding AI logs

`git diff main...HEAD --shortstat -- . ':(exclude)ai-logs'`

```text
 12 files changed, 286 insertions(+), 15 deletions(-)
```
</details></tool-use>

_**Agent (claude-opus-5 2026-09-16 14:24:45Z)**_

**Verdict: the branch is sound. Everything claimed in the issue is actually there and actually works.**

## What I ran

| Check | Result |
|---|---|
| `go test ./models/issues/` (new unit tests) | ok |
| `make test-integration#TestIssueDueDateHighlight` | ok (2.6s) |
| `make test-integration#TestMilestoneDueDateHighlight` | ok (8.3s) |
| `gofmt -l`, `go vet ./models/issues/` | clean |
| `./.claude-students/check.sh` | ok (34 docs, guide 34/34) |
| Regression claim: reverted `.Milestone.IsOverdue` → `.IsOverdue`, re-ran | **FAILS** — the test really does guard the bug fix. Tree restored clean. |

`tw-text-warning-text` is a real token (`--color-warning-text`, defined in both light and dark themes), so the color works in both themes. The `due-date` class added to the milestone templates has no CSS behind it — it's purely a test hook, harmless.

## On "aren't these way too many changes?"

The 12,378-line stat is misleading. 12,092 of it is the AI session log (`ai-logs/sprint0/jdonohue/…`), which is a course convention, not code. The actual change is **286 insertions / 15 deletions across 12 files**, and it splits like this:

- **~60 lines of production code** — one new `deadline.go` helper (31 lines), plus small edits to `issue.go`, `milestone.go`, one locale key.
- **~160 lines of tests** — unit + integration, which the issue explicitly asked for.
- **~40 lines across 5 templates.**

So the real footprint is small. Two things do inflate it legitimately:

**1. The 5 templates are the same 7-line block copy-pasted.** Every due-date render site (issue sidebar, issue list, milestone list, milestone detail, dashboard milestones) got an identical `{{$dueClass}}/{{$dueTooltip}}` if/else-if. That's the one real code-quality critique — a shared partial or a `DueDateClass` helper would collapse it to one definition and five one-line call sites. Worth raising in review, not a blocker.

**2. It touches milestones, not just issues.** The issue says "issues," and 3 of the 5 templates are milestone pages. Defensible — milestones share the deadline model and the helper is genuinely shared — but it *is* scope beyond the literal ask.

## Two behavior changes worth calling out explicitly

The issue's line "some logic on the red coloration was changed as to not block other visual effects" undersells what happened. In `milestone_issues.tmpl`, `{{if .IsClosed}}` and `{{if .IsOverdue}}` were reading the **page context**, not `.Milestone` — those keys don't exist in `ctx.Data`, so they silently evaluated nil. Consequences of the fix:

- Overdue milestones never rendered red on their own detail page. Now they do. (Pre-existing bug, correctly fixed, correctly tested.)
- Closed milestones on that page used to show the due date; they now show "Closed X ago" like every other milestone view. This is the intended original behavior, but it's a **visible change nobody asked for** and it isn't covered by a test.

Also: `Issue.IsOverdue()` previously returned `true` for issues with no deadline at all (`TimeStampNow() >= 0`). The new helper returns `false`. Every template guards on `DeadlineUnix != 0` so nothing rendered wrong before, and there are no other Go callers — but it's a quiet semantic fix riding along in the same commit.

Neither is wrong. Both should be in the PR description rather than buried under "some logic was changed."

---

_**User (2026-09-16 14:33:15Z)**_

This was the original issue. These changes absolutely exceeded the requires?

What problem would this solve?

On the issues page, any issues with a deadline present the due date on the page. Once the deadline is passed, the data gets colored red to indicate the deadline has been passed. However, there is no indication that an issue is near a deadline without carefully scrutinizing the page.
What do you propose?

As a user, I want the deadline dates, if available, to be highlighted with an appropriate color to indicate the status of the issue, and the urgency that it needs to settle the issue in a timely manner.
Scope

Small: Should be a simple logic change. The main effort would be finding the code location.
Source

Personal use.

---

_**Agent (claude-opus-5 2026-09-16 14:33:52Z)**_

Yes. Roughly 60% of the code is outside what that ticket asked for.

The issue is explicit on two points that the branch overran: **"On the issues page"** and **"Scope: Small… a simple logic change. The main effort would be finding the code location."** That's a ticket author telling you they expect ~20 lines once you've located `issuelist.tmpl`.

## What the ticket actually needed

| | |
|---|---|
| `Issue.IsNearDue()` in `issue.go` | ~6 lines |
| `repo.issues.due_date_near` locale key | 1 line |
| `templates/shared/issuelist.tmpl` | the target — 8 lines |
| `templates/repo/issue/sidebar/due_date.tmpl` | 8 lines — not literally "the issues page," but the same widget, fair to include |
| Unit test + one integration test | ~90 lines |

≈115 lines. No new file, no `milestone.go`, no helper extraction.

## What got added on top

| | |
|---|---|
| `models/issues/deadline.go` | 31 lines — this file exists *only* because milestones were brought in; for issues alone it's a method on `Issue` |
| `milestone.go` + `IsNearDue` field | +7/-5 |
| `milestones.tmpl`, `milestone_issues.tmpl`, `dashboard/milestones.tmpl` | 3 templates, ~26 lines |
| `TestMilestoneDeadlineStatus` + `milestone_test.go` | ~110 lines |
| The `.IsClosed`/`.IsOverdue` context bug fix | unrequested, and it changes how *closed* milestones render |

≈170 lines, none of it requested.

## How I'd read this in review

None of the extra work is *bad*. The milestone extension is a defensible reading of "deadline dates, if available" — milestones have deadlines and share the model. The `.IsOverdue` context bug is a genuine find and it's now covered by a test that I confirmed fails without the fix. On a ticket scoped "Medium: make deadline coloring consistent across the app," this would be a good PR.

But it wasn't that ticket. The concrete costs of the overrun:

1. **A behavior change nobody asked for and no test covers** — closed milestones on the detail page used to show their due date, now they show "Closed X ago." Correct, but it shipped silently inside a coloring ticket.
2. **The helper extraction is self-justifying.** `deadline.go` is only needed because milestones were added; the diff then points to the shared helper as evidence the work was necessary.
3. **A reviewer expecting 20 lines gets 286** and has to re-derive which parts map to the ticket.

## Recommendation

Don't make them split it — the work is done, tested, and passing, and unpicking it now costs more than it's worth on a course fork. Instead:

- Rewrite the PR description to say plainly: implements near-due coloring for issues *and* milestones, fixes a pre-existing context bug in `milestone_issues.tmpl`, and changes closed-milestone display as a consequence.
- Fix the one thing that's a real defect regardless of scope: the same 7-line `$dueClass`/`$dueTooltip` block is pasted 5 times. Collapse it into a partial or helper.
- The scope note is worth saying out loud to the author, because next time the ticket might be one where the extra 170 lines land in code someone else is actively changing.
