<!-- Generated by SpecStory, Markdown v2.1.0 -->

# 2026-09-15 01:41:03Z

<!-- Claude Code Session f1c2aa49-dab6-4b47-9277-85993d369c13 (2026-09-15 01:41:03Z) -->

_**User (2026-09-15 01:41:03Z)**_

I do not see any problems with this PR I am reviewing. Do you see anything?

Relavant issue
What problem would this solve?

When a repository has no releases, navigating to the Releases tab shows a blank page with no explanation or call to action. The page should display a friendly message guiding users on how to create their first release.
What do you propose?

As a user, I want to see a helpful message on the Releases page when no releases exist, so that I understand the page is working correctly and know how to get started.

---

_**Agent (claude-opus-5 2026-09-15 01:41:08Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Show the release empty-state commit

`git show --stat aa1063d30d && echo "=====DIFF=====" && git show aa1063d30d`

```text
commit aa1063d30df3de6edcc8308756dd2be892c69227
Author: CarsonRackley <cdrackley@wm.edu>
Date:   Mon Sep 14 19:06:13 2026 -0400

    feat(repo): show empty state on releases page
    
    Assisted-by: Claude Code:claude-opus-5

 options/locale/locale_en-US.json  |  3 +++
 templates/repo/release/list.tmpl  | 11 +++++++++++
 tests/integration/release_test.go | 22 ++++++++++++++++++++++
 3 files changed, 36 insertions(+)
=====DIFF=====
commit aa1063d30df3de6edcc8308756dd2be892c69227
Author: CarsonRackley <cdrackley@wm.edu>
Date:   Mon Sep 14 19:06:13 2026 -0400

    feat(repo): show empty state on releases page
    
    Assisted-by: Claude Code:claude-opus-5

diff --git a/options/locale/locale_en-US.json b/options/locale/locale_en-US.json
index 9373aa83a9..66742a73d0 100644
--- a/options/locale/locale_en-US.json
+++ b/options/locale/locale_en-US.json
@@ -2617,6 +2617,9 @@
   "repo.release.detail": "Release details",
   "repo.release.tags": "Tags",
   "repo.release.new_release": "New Release",
+  "repo.release.empty": "No releases yet.",
+  "repo.release.empty_description": "Releases package software versions for download. This repository has not published one yet.",
+  "repo.release.empty_description_writer": "Create a release from a tag to publish a version of this repository.",
   "repo.release.draft": "Draft",
   "repo.release.prerelease": "Pre-Release",
   "repo.release.stable": "Stable",
diff --git a/templates/repo/release/list.tmpl b/templates/repo/release/list.tmpl
index 1009d22437..8d798deb67 100644
--- a/templates/repo/release/list.tmpl
+++ b/templates/repo/release/list.tmpl
@@ -4,6 +4,7 @@
 	<div class="ui container">
 		{{template "base/alert" .}}
 		{{template "repo/release_tag_header" .}}
+		{{if .Releases}}
 		<ul id="release-list">
 			{{range $idx, $info := .Releases}}
 				{{$release := $info.Release}}
@@ -111,6 +112,16 @@
 				</li>
 			{{end}}
 		</ul>
+		{{else}}
+			<div class="empty-placeholder">
+				{{svg "octicon-tag" 48}}
+				<h2>{{ctx.Locale.Tr "repo.release.empty"}}</h2>
+				<p>{{if .CanCreateRelease}}{{ctx.Locale.Tr "repo.release.empty_description_writer"}}{{else}}{{ctx.Locale.Tr "repo.release.empty_description"}}{{end}}</p>
+				{{if .CanCreateRelease}}
+					<a class="ui small primary button" href="{{$.RepoLink}}/releases/new">{{ctx.Locale.Tr "repo.release.new_release"}}</a>
+				{{end}}
+			</div>
+		{{end}}
 
 		{{template "base/paginate" .}}
 	</div>
diff --git a/tests/integration/release_test.go b/tests/integration/release_test.go
index f495e417d9..83a352ed31 100644
--- a/tests/integration/release_test.go
+++ b/tests/integration/release_test.go
@@ -162,6 +162,28 @@ func TestViewReleaseListNoLogin(t *testing.T) {
 	}, commitsToMain)
 }
 
+func TestViewReleaseListEmpty(t *testing.T) {
+	defer tests.PrepareTestEnv(t)()
+
+	link := "/user5/repo4/releases" // repo4 has no release fixtures
+
+	t.Run("NoLogin", func(t *testing.T) {
+		req := NewRequest(t, "GET", link)
+		resp := MakeRequest(t, req, http.StatusOK)
+		htmlDoc := NewHTMLParser(t, resp.Body)
+		assert.Equal(t, 1, htmlDoc.Find(".empty-placeholder").Length())
+		assert.Equal(t, 0, htmlDoc.Find("#release-list").Length())
+		assert.Equal(t, 0, htmlDoc.Find(`.empty-placeholder a[href="/user5/repo4/releases/new"]`).Length())
+	})
+	t.Run("Owner", func(t *testing.T) {
+		session := loginUser(t, "user5")
+		req := NewRequest(t, "GET", link)
+		resp := session.MakeRequest(t, req, http.StatusOK)
+		htmlDoc := NewHTMLParser(t, resp.Body)
+		assert.Equal(t, 1, htmlDoc.Find(`.empty-placeholder a[href="/user5/repo4/releases/new"]`).Length())
+	})
+}
+
 func TestViewSingleRelease(t *testing.T) {
 	defer tests.PrepareTestEnv(t)()
```
</details></tool-use>

_**Agent (claude-opus-5 2026-09-15 01:41:15Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Find CanCreateRelease definition and usages

`echo "=== CanCreateRelease usages ===" && grep -rn "CanCreateRelease" --include=*.go --include=*.tmpl . | head -50`

```text
=== CanCreateRelease usages ===
(eval):1: no matches found: --include=*.go
```
</details></tool-use>

_**Agent (claude-opus-5 2026-09-15 01:41:18Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Check empty-placeholder class existence

`echo "=== empty-placeholder ===" && grep -rn "empty-placeholder" --include=*.tmpl --include=*.css --include=*.ts . | head -30; echo "=== flex-center / empty states in other tmpl ===" && grep -rln "no_results\|empty_placeholder\|empty-placeholder" templates/ | head -20`

```text
=== empty-placeholder ===
(eval):1: no matches found: --include=*.tmpl
=== flex-center / empty states in other tmpl ===
templates/admin/notice.tmpl
templates/admin/auth/list.tmpl
templates/admin/user/list.tmpl
templates/admin/org/list.tmpl
templates/admin/packages/list.tmpl
templates/admin/repo/list.tmpl
templates/admin/emails/list.tmpl
templates/package/shared/list.tmpl
templates/user/notification/notification_div.tmpl
templates/shared/search/code/search.tmpl
templates/projects/list.tmpl
templates/org/worktime/table_repos.tmpl
templates/org/home.tmpl
templates/org/worktime/empty_placeholder.tmpl
templates/shared/issuelist.tmpl
templates/org/member/members.tmpl
templates/org/worktime/table_members.tmpl
templates/shared/repo/list.tmpl
templates/repo/issue/branch_selector_field.tmpl
templates/repo/release/list.tmpl
```
</details></tool-use>

_**Agent (claude-opus-5 2026-09-15 01:41:22Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Search CanCreateRelease and empty-placeholder

`echo "=== CanCreateRelease ===" && grep -rn "CanCreateRelease" --include='*.go' --include='*.tmpl' . | head -50; echo; echo "=== empty-placeholder class def ===" && grep -rn "empty-placeholder" web_src/ templates/ | head -30`

```text
=== CanCreateRelease ===
routers/web/repo/release.go:173:	ctx.Data["CanCreateRelease"] = writeAccess && !ctx.Repo.Repository.IsArchived
routers/web/repo/release.go:204:	ctx.Data["CanCreateRelease"] = ctx.Repo.Permission.CanWrite(unit.TypeReleases) && !ctx.Repo.Repository.IsArchived
routers/web/repo/release.go:282:	ctx.Data["CanCreateRelease"] = writeAccess && !ctx.Repo.Repository.IsArchived
templates/repo/release_tag_header.tmpl:19:		{{if and (not .PageIsTagList) .CanCreateRelease}}
templates/repo/tag/list.tmpl:41:								{{if (and $canReadReleases $.CanCreateRelease $release.IsTag)}}
templates/repo/release/list.tmpl:41:								{{if and $.CanCreateRelease (not $.PageIsSingleTag)}}
templates/repo/release/list.tmpl:119:				<p>{{if .CanCreateRelease}}{{ctx.Locale.Tr "repo.release.empty_description_writer"}}{{else}}{{ctx.Locale.Tr "repo.release.empty_description"}}{{end}}</p>
templates/repo/release/list.tmpl:120:				{{if .CanCreateRelease}}

=== empty-placeholder class def ===
web_src/css/base.css:358:.empty-placeholder {
templates/package/shared/list.tmpl:46:			<div class="empty-placeholder">
templates/user/notification/notification_div.tmpl:83:				<div class="empty-placeholder">
templates/org/home.tmpl:14:					<div class="empty-placeholder">
templates/org/worktime/empty_placeholder.tmpl:3:		<div class="empty-placeholder">
templates/projects/list.tmpl:80:			<div class="empty-placeholder">
templates/projects/list.tmpl:86:			<div class="empty-placeholder">
templates/shared/search/code/search.tmpl:28:			<div class="empty-placeholder">
templates/repo/release/list.tmpl:116:			<div class="empty-placeholder">
templates/repo/actions/runs_list.tmpl:7:	<div class="empty-placeholder">
templates/repo/actions/no_workflows.tmpl:1:<div class="empty-placeholder">
```
</details></tool-use>

_**Agent (claude-opus-5 2026-09-15 01:41:23Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Inspect release list template and header

`sed -n '1,40p' templates/repo/release/list.tmpl; echo "=== header ==="; cat templates/repo/release_tag_header.tmpl`

```text
{{template "base/head" .}}
<div role="main" aria-label="{{.Title}}" class="page-content repository releases">
	{{template "repo/header" .}}
	<div class="ui container">
		{{template "base/alert" .}}
		{{template "repo/release_tag_header" .}}
		{{if .Releases}}
		<ul id="release-list">
			{{range $idx, $info := .Releases}}
				{{$release := $info.Release}}
				<li class="release-entry">
					<div class="meta">
						<a class="muted" href="{{if not (and $release.Sha1 ($.Permission.CanRead ctx.Consts.RepoUnitTypeCode))}}#{{else}}{{$.RepoLink}}/src/tag/{{$release.TagName | PathEscapeSegments}}{{end}}" rel="nofollow">{{svg "octicon-tag" 16 "tw-mr-1"}}{{$release.TagName}}</a>
						{{if and $release.Sha1 ($.Permission.CanRead ctx.Consts.RepoUnitTypeCode)}}
							<a class="muted tw-font-mono" href="{{$.RepoLink}}/src/commit/{{$release.Sha1}}" rel="nofollow">{{svg "octicon-git-commit" 16 "tw-mr-1"}}{{ShortSha $release.Sha1}}</a>
							{{$compareTarget := ""}}
							{{if $release.IsDraft}}
									{{$compareTarget = $release.Target}}
							{{else if $release.TagName}}
									{{$compareTarget = $release.TagName}}
							{{else}}
								{{$compareTarget = $release.Sha1}}
							{{end}}
							{{template "repo/branch_dropdown" dict
								"ContainerClasses" "release-branch-tag-selector"
								"Repository" $.Repository
								"ShowTabTags" true
								"DropdownFixedText" (ctx.Locale.Tr "repo.release.compare")
								"RefLinkTemplate" (print "{RepoLink}/compare/{RefShortName}" "..." (PathEscapeSegments $compareTarget))
							}}
						{{end}}
					</div>
					<div class="ui segment detail">
						<div class="flex-left-right tw-mb-2">
							<h4 class="release-list-title tw-break-anywhere">
								{{if $.PageIsSingleTag}}{{$release.Title}}{{else}}<a class="muted" href="{{$.RepoLink}}/releases/tag/{{$release.TagName | PathEscapeSegments}}">{{$release.Title}}</a>{{end}}
								{{template "repo/commit_statuses" dict "Status" $info.CommitStatus "Statuses" $info.CommitStatuses "AdditionalClasses" "tw-flex"}}
								{{template "repo/release/label" (dict "Release" $release)}}
							</h4>
							<div>
=== header ===
{{$canReadReleases := $.Permission.CanRead ctx.Consts.RepoUnitTypeReleases}}
{{$canReadCode := $.Permission.CanRead ctx.Consts.RepoUnitTypeCode}}

{{if $canReadReleases}}
	<div class="flex-text-block">
		<div class="tw-flex-1 tw-flex tw-items-center">
			<h2 class="ui compact small menu small-menu-items">
				<a class="{{if and .PageIsReleaseList (not .PageIsSingleTag)}}active {{end}}item" href="{{.RepoLink}}/releases">{{ctx.Locale.PrettyNumber .NumReleases}} {{ctx.Locale.TrN .NumReleases "repo.release" "repo.releases"}}</a>
				{{if $canReadCode}}
					<a class="{{if or .PageIsTagList .PageIsSingleTag}}active {{end}}item" href="{{.RepoLink}}/tags">{{ctx.Locale.PrettyNumber .NumTags}} {{ctx.Locale.TrN .NumTags "repo.tag" "repo.tags"}}</a>
				{{end}}
			</h2>
		</div>
		{{if .EnableFeed}}
			<a class="ui small button" href="{{.RepoLink}}/{{if .PageIsTagList}}tags{{else}}releases{{end}}.rss">
				{{svg "octicon-rss" 16}} {{ctx.Locale.Tr "rss_feed"}}
			</a>
		{{end}}
		{{if and (not .PageIsTagList) .CanCreateRelease}}
			<a class="ui small primary button" href="{{$.RepoLink}}/releases/new{{if .PageIsSingleTag}}?tag={{.SingleReleaseTagName}}{{end}}">
				{{ctx.Locale.Tr "repo.release.new_release"}}
			</a>
		{{end}}
	</div>
	<div class="divider"></div>
{{else if $canReadCode}}
	{{/* if the "repo.releases" unit is disabled, only show the "commits / branches / tags" sub menu */}}
	{{template "repo/sub_menu" .}}
{{end}}
```
</details></tool-use>

_**Agent (claude-opus-5 2026-09-15 01:41:30Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Read release router handlers

`sed -n '100,300p' routers/web/repo/release.go`

```text
	canReadActions := ctx.Repo.Permission.CanRead(unit.TypeActions)

	// Bulk-load commit statuses for all releases in one query.
	var commitStatusMap map[string][]*git_model.CommitStatus
	if canReadActions && len(releases) > 0 {
		shas := make([]string, 0, len(releases))
		for _, r := range releases {
			shas = append(shas, r.Sha1)
		}
		commitStatusMap, err = git_model.GetLatestCommitStatusForRepoCommitIDs(ctx, ctx.Repo.Repository.ID, shas)
		if err != nil {
			return nil, err
		}
	}

	releaseInfos := make([]*ReleaseInfo, 0, len(releases))
	for _, r := range releases {
		if r.Publisher, ok = cacheUsers[r.PublisherID]; !ok {
			r.PublisherID, r.Publisher, err = user_model.GetPossibleUserByID(ctx, r.PublisherID)
			if err != nil {
				return nil, err
			}
			cacheUsers[r.PublisherID] = r.Publisher
		}

		rctx := renderhelper.NewRenderContextRepoComment(ctx, r.Repo, renderhelper.RepoCommentOptions{
			FootnoteContextID: strconv.FormatInt(r.ID, 10),
		})
		r.RenderedNote, err = markdown.RenderString(rctx, r.Note)
		if err != nil {
			return nil, err
		}

		if !r.IsDraft {
			if err := calReleaseNumCommitsBehind(ctx, ctx.Repo, r, countCache); err != nil {
				return nil, err
			}
		}

		info := &ReleaseInfo{
			Release: r,
		}

		if canReadActions {
			statuses := commitStatusMap[r.Sha1]
			info.CommitStatus = git_model.CalcCommitStatus(statuses)
			info.CommitStatuses = statuses
		}

		releaseInfos = append(releaseInfos, info)
	}

	return releaseInfos, nil
}

// Releases render releases list page
func Releases(ctx *context.Context) {
	ctx.Data["PageIsReleaseList"] = true
	ctx.Data["Title"] = ctx.Tr("repo.release.releases")

	listOptions := db.ListOptions{
		Page:     ctx.FormInt("page"),
		PageSize: ctx.FormInt("limit"),
	}
	if listOptions.PageSize == 0 {
		listOptions.PageSize = setting.Repository.Release.DefaultPagingNum
	}
	if listOptions.PageSize > setting.API.MaxResponseItems {
		listOptions.PageSize = setting.API.MaxResponseItems
	}

	writeAccess := ctx.Repo.Permission.CanWrite(unit.TypeReleases)
	ctx.Data["CanCreateRelease"] = writeAccess && !ctx.Repo.Repository.IsArchived

	releases, err := getReleaseInfos(ctx, &repo_model.FindReleasesOptions{
		ListOptions: listOptions,
		// only show draft releases for users who can write, read-only users shouldn't see draft releases.
		IncludeDrafts: writeAccess,
		RepoID:        ctx.Repo.Repository.ID,
	})
	if err != nil {
		ctx.ServerError("getReleaseInfos", err)
		return
	}
	for _, rel := range releases {
		if rel.Release.IsTag && rel.Release.Title == "" {
			rel.Release.Title = rel.Release.TagName
		}
	}

	ctx.Data["Releases"] = releases

	numReleases := ctx.Data["NumReleases"].(int64)
	pager := context.NewPagination(numReleases, listOptions.PageSize, listOptions.Page, 5)
	pager.AddParamFromRequest(ctx.Req)
	ctx.Data["Page"] = pager
	ctx.HTML(http.StatusOK, tplReleasesList)
}

// TagsList render tags list page
func TagsList(ctx *context.Context) {
	ctx.Data["PageIsTagList"] = true
	ctx.Data["Title"] = ctx.Tr("repo.release.tags")
	ctx.Data["CanCreateRelease"] = ctx.Repo.Permission.CanWrite(unit.TypeReleases) && !ctx.Repo.Repository.IsArchived

	namePattern := ctx.FormTrim("q")

	listOptions := db.ListOptions{
		Page:     ctx.FormInt("page"),
		PageSize: ctx.FormInt("limit"),
	}
	if listOptions.PageSize == 0 {
		listOptions.PageSize = setting.Repository.Release.DefaultPagingNum
	}
	if listOptions.PageSize > setting.API.MaxResponseItems {
		listOptions.PageSize = setting.API.MaxResponseItems
	}

	opts := repo_model.FindReleasesOptions{
		ListOptions: listOptions,
		// for the tags list page, show all releases with real tags (having real commit-id),
		// the drafts should also be included because a real tag might be used as a draft.
		IncludeDrafts: true,
		IncludeTags:   true,
		HasSha1:       optional.Some(true),
		RepoID:        ctx.Repo.Repository.ID,
		NamePattern:   optional.Some(namePattern),
	}

	releases, err := db.Find[repo_model.Release](ctx, opts)
	if err != nil {
		ctx.ServerError("GetReleasesByRepoID", err)
		return
	}

	count, err := db.Count[repo_model.Release](ctx, opts)
	if err != nil {
		ctx.ServerError("GetReleasesByRepoID", err)
		return
	}

	ctx.Data["Keyword"] = namePattern
	ctx.Data["Releases"] = releases
	ctx.Data["TagCount"] = count

	pager := context.NewPagination(count, opts.PageSize, opts.Page, 5)
	pager.AddParamFromRequest(ctx.Req)
	ctx.Data["Page"] = pager
	ctx.Data["PageIsViewCode"] = !ctx.Repo.Repository.UnitEnabled(ctx, unit.TypeReleases)
	ctx.HTML(http.StatusOK, tplTagsList)
}

// ReleasesFeedRSS get feeds for releases in RSS format
func ReleasesFeedRSS(ctx *context.Context) {
	releasesOrTagsFeed(ctx, true, "rss")
}

// TagsListFeedRSS get feeds for tags in RSS format
func TagsListFeedRSS(ctx *context.Context) {
	releasesOrTagsFeed(ctx, false, "rss")
}

// ReleasesFeedAtom get feeds for releases in Atom format
func ReleasesFeedAtom(ctx *context.Context) {
	releasesOrTagsFeed(ctx, true, "atom")
}

// TagsListFeedAtom get feeds for tags in RSS format
func TagsListFeedAtom(ctx *context.Context) {
	releasesOrTagsFeed(ctx, false, "atom")
}

func releasesOrTagsFeed(ctx *context.Context, isReleasesOnly bool, formatType string) {
	feed.ShowReleaseFeed(ctx, ctx.Repo.Repository, isReleasesOnly, formatType)
}

// SingleRelease renders a single release's page
func SingleRelease(ctx *context.Context) {
	ctx.Data["PageIsReleaseList"] = true

	writeAccess := ctx.Repo.Permission.CanWrite(unit.TypeReleases)
	ctx.Data["CanCreateRelease"] = writeAccess && !ctx.Repo.Repository.IsArchived

	releases, err := getReleaseInfos(ctx, &repo_model.FindReleasesOptions{
		ListOptions: db.ListOptions{Page: 1, PageSize: 1},
		RepoID:      ctx.Repo.Repository.ID,
		TagNames:    []string{ctx.PathParam("*")},
		// only show draft releases for users who can write, read-only users shouldn't see draft releases.
		IncludeDrafts: writeAccess,
		IncludeTags:   true,
	})
	if err != nil {
		ctx.ServerError("getReleaseInfos", err)
		return
	}
	if len(releases) != 1 {
		ctx.NotFound(err)
		return
	}
```
</details></tool-use>

_**Agent (claude-opus-5 2026-09-15 01:41:51Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Compare with existing empty-placeholder usages

`echo "=== base.css .empty-placeholder ===" && sed -n '350,375p' web_src/css/base.css; echo; echo "=== no_workflows.tmpl ==="; cat templates/repo/actions/no_workflows.tmpl; echo; echo "=== projects/list.tmpl 75-95 ==="; sed -n '75,95p' templates/projects/list.tmpl; echo; echo "=== package/shared/list.tmpl 42-55 ==="; sed -n '42,55p' templates/package/shared/list.tmpl`

```text
=== base.css .empty-placeholder ===
  content: "";
  width: 16px;
  height: 16px;
  mask-size: cover;
  mask-image: var(--select-arrows);
  background: currentcolor;
}

.empty-placeholder {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding-top: 40px;
  padding-bottom: 40px;
  text-align: center;
  text-wrap: balance;
}

.inline-code-block {
  padding: 2px 4px;
  border-radius: .24em;
  background-color: var(--color-label-bg);
}

.ui.menu .dropdown.item:hover,
.ui.menu a.item:hover,

=== no_workflows.tmpl ===
<div class="empty-placeholder">
	{{svg "octicon-no-entry" 48}}
	<h2>{{ctx.Locale.Tr "actions.runs.no_workflows"}}</h2>
	{{if and .CanWriteCode .CanWriteActions}}
		<p>{{ctx.Locale.Tr "actions.runs.no_workflows.quick_start" "https://docs.gitea.com/usage/actions/quickstart/"}}</p>
	{{end}}
	<p>{{ctx.Locale.Tr "actions.runs.no_workflows.documentation" "https://docs.gitea.com/usage/actions/overview/"}}</p>
</div>

=== projects/list.tmpl 75-95 ===
				<div class="render-content markup">{{.RenderedContent}}</div>
			{{end}}
		</div>
	{{else}}
		{{if and (eq .OpenCount 0) (eq .ClosedCount 0)}}
			<div class="empty-placeholder">
				{{svg "octicon-project-symlink" 48}}
				<h2>{{ctx.Locale.Tr "repo.projects.empty"}}</h2>
				<p>{{ctx.Locale.Tr "repo.projects.empty_description"}}</p>
			</div>
		{{else}}
			<div class="empty-placeholder">
				{{svg "octicon-search" 48}}
				<h2>{{ctx.Locale.Tr "repo.projects.no_results"}}</h2>
			</div>
		{{end}}
	{{end}}

	{{template "base/paginate" .}}
</div>


=== package/shared/list.tmpl 42-55 ===
		</div>
	</div>
	{{else}}
		{{if not .HasPackages}}
			<div class="empty-placeholder">
				{{svg "octicon-package" 48}}
				<h2>{{ctx.Locale.Tr "packages.empty"}}</h2>
				{{if and .Repository .CanWritePackages}}
					{{$packagesUrl := print .Owner.HomeLink "/-/packages"}}
					<p>{{ctx.Locale.Tr "packages.empty.repo" $packagesUrl}}</p>
				{{end}}
				<p>{{ctx.Locale.Tr "packages.empty.documentation" "https://docs.gitea.com/usage/packages/overview/"}}</p>
			</div>
		{{else}}
```
</details></tool-use>

_**Agent (claude-opus-5 2026-09-15 01:41:55Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Check repo4 and user5 fixtures

`echo "=== repo4 fixture ===" && grep -n -A14 "id: 4$" models/fixtures/repository.yml | head -40; echo "=== releases for repo 4 ==="; grep -n "repo_id: 4$" models/fixtures/release.yml; echo "=== user5 ==="; grep -n -A6 "^- id: 5$" models/fixtures/user.yml | head -20`

```text
=== repo4 fixture ===
96:  id: 4
97-  owner_id: 5
98-  owner_name: user5
99-  lower_name: repo4
100-  name: repo4
101-  default_branch: master
102-  num_watches: 0
103-  num_stars: 1
104-  num_forks: 0
105-  num_issues: 0
106-  num_closed_issues: 0
107-  num_pulls: 0
108-  num_closed_pulls: 0
109-  num_milestones: 0
110-  num_closed_milestones: 0
=== releases for repo 4 ===
=== user5 ===
```
</details></tool-use>

_**Agent (claude-opus-5 2026-09-15 01:42:04Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Check repo4 visibility, user5, repo units

`sed -n '96,135p' models/fixtures/repository.yml; echo "=== user5 ==="; grep -n -B2 -A10 "lower_name: user5" models/fixtures/user.yml | head -30; echo "=== repo_unit for repo 4 ==="; grep -n -B3 -A4 "repo_id: 4$" models/fixtures/repo_unit.yml`

```text
  id: 4
  owner_id: 5
  owner_name: user5
  lower_name: repo4
  name: repo4
  default_branch: master
  num_watches: 0
  num_stars: 1
  num_forks: 0
  num_issues: 0
  num_closed_issues: 0
  num_pulls: 0
  num_closed_pulls: 0
  num_milestones: 0
  num_closed_milestones: 0
  num_projects: 0
  num_closed_projects: 1
  num_action_runs: 4
  num_closed_action_runs: 3
  is_private: false
  is_empty: false
  is_archived: false
  is_mirror: false
  status: 0
  is_fork: false
  fork_id: 0
  is_template: false
  template_id: 0
  size: 0
  is_fsck_enabled: true
  close_issues_via_commit_in_any_branch: false

-
  id: 5
  owner_id: 3
  owner_name: org3
  lower_name: repo5
  name: repo5
  num_watches: 0
  num_stars: 0
=== user5 ===
152--
153-  id: 5
154:  lower_name: user5
155-  name: user5
156-  full_name: User Five
157-  email: user5@example.com
158-  keep_email_private: false
159-  email_notifications_preference: enabled
160-  passwd: ZogKvWdyEx:password
161-  passwd_hash_algo: dummy
162-  must_change_password: false
163-  login_source: 0
164-  login_name: user5
=== repo_unit for repo 4 ===
113-
114--
115-  id: 17
116:  repo_id: 4
117-  type: 4
118-  config: "{}"
119-  created_unix: 946684810
120-
121--
122-  id: 18
123:  repo_id: 4
124-  type: 5
125-  config: "{}"
126-  created_unix: 946684810
127-
128--
129-  id: 19
130:  repo_id: 4
131-  type: 1
132-  config: "{}"
133-  created_unix: 946684810
134-
135--
136-  id: 20
137:  repo_id: 4
138-  type: 2
139-  config: "{\"EnableTimetracker\":true,\"AllowOnlyContributorsToTrackTime\":true}"
140-  created_unix: 946684810
141-
142--
143-  id: 21
144:  repo_id: 4
145-  type: 3
146-  config: "{\"IgnoreWhitespaceConflicts\":false,\"AllowMerge\":true,\"AllowRebase\":true,\"AllowSquash\":true}"
147-  created_unix: 946684810
148-
--
743-
744--
745-  id: 112
746:  repo_id: 4
747-  type: 10
748-  config: "{}"
749-  created_unix: 946684810
750-
```
</details></tool-use>

_**Agent (claude-opus-5 2026-09-15 01:42:04Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Check paginate template and lint targets

`cat templates/base/paginate.tmpl; echo "=== lint targets ==="; grep -n "lint-templates\|djlint\|lint-backend\|lint-frontend" Makefile | head -20`

```text
{{$page := or $.Page ctx.RootData.Page}}
{{$paginationLink := $.Link}}
{{if eq $paginationLink nil}}{{$paginationLink = ctx.RootData.Link}}{{end}}
{{if eq $paginationLink AppSubUrl}}{{$paginationLink = print $paginationLink "/"}}{{end}}
{{$paginationParams := $page.GetParams}}
{{with $page.Paginater}}
	{{if or (eq .TotalPages -1) (gt .TotalPages 1)}}
		{{$showFirstLast := gt .TotalPages 1}}
		<div class="center page buttons">
			<div class="ui borderless pagination menu">
				{{if $showFirstLast}}
				<a class="{{if .IsFirst}}disabled{{end}} item navigation" {{if not .IsFirst}}href="{{$paginationLink}}{{if $paginationParams}}?{{$paginationParams}}{{end}}"{{end}}>
					{{svg "gitea-double-chevron-left" 16 "tw-mr-1"}}
					<span class="navigation_label">{{ctx.Locale.Tr "admin.first_page"}}</span>
				</a>
				{{end}}

				<a class="{{if not .HasPrevious}}disabled{{end}} item navigation" {{if .HasPrevious}}href="{{$paginationLink}}?page={{.Previous}}{{if $paginationParams}}&{{$paginationParams}}{{end}}"{{end}}>
					{{svg "octicon-chevron-left" 16 "tw-mr-1"}}
					<span class="navigation_label">{{ctx.Locale.Tr "repo.issues.previous"}}</span>
				</a>
				{{$pages := .Pages}}
				{{$pagesLen := len $pages}}
				{{range $pages}}
					{{if eq .Num -1}}
						<a class="disabled item">...</a>
					{{else}}
						{{/* do not highlight the current page if there is only one page */}}
						<a class="{{if and .IsCurrent (gt $pagesLen 1)}}active {{end}}item" {{if not .IsCurrent}}href="{{$paginationLink}}?page={{.Num}}{{if $paginationParams}}&{{$paginationParams}}{{end}}"{{end}}>{{.Num}}</a>
					{{end}}
				{{end}}
				<a class="{{if not .HasNext}}disabled{{end}} item navigation" {{if .HasNext}}href="{{$paginationLink}}?page={{.Next}}{{if $paginationParams}}&{{$paginationParams}}{{end}}"{{end}}>
					<span class="navigation_label">{{ctx.Locale.Tr "repo.issues.next"}}</span>
					{{svg "octicon-chevron-right" 16 "tw-ml-1"}}
				</a>

				{{if $showFirstLast}}
				<a class="{{if .IsLast}}disabled{{end}} item navigation" {{if not .IsLast}}href="{{$paginationLink}}?page={{.TotalPages}}{{if $paginationParams}}&{{$paginationParams}}{{end}}"{{end}}>
					<span class="navigation_label">{{ctx.Locale.Tr "admin.last_page"}}</span>
					{{svg "gitea-double-chevron-right" 16 "tw-ml-1"}}
				</a>
				{{end}}
			</div>
		</div>
	{{end}}
{{end}}
=== lint targets ===
282:lint: lint-frontend lint-backend lint-templates lint-swagger lint-spell lint-md lint-actions lint-json lint-yaml lint-shell ## lint everything
285:lint-fix: lint-frontend-fix lint-backend-fix lint-spell-fix ## lint everything and fix issues
287:.PHONY: lint-frontend
288:lint-frontend: lint-js lint-css ## lint frontend files
290:.PHONY: lint-frontend-fix
291:lint-frontend-fix: lint-js-fix lint-css-fix ## lint frontend files and fix issues
293:.PHONY: lint-backend
294:lint-backend: lint-go lint-editorconfig ## lint backend files
296:.PHONY: lint-backend-fix
297:lint-backend-fix: lint-go-fix lint-editorconfig ## lint backend files and fix issues
359:.PHONY: lint-templates
360:lint-templates: .venv node_modules ## lint template files
361:	@node tools/lint-templates-svg.ts
362:	@uv run --frozen djlint $(shell find templates -type f -iname '*.tmpl')
```
</details></tool-use>

_**Agent (claude-opus-5 2026-09-15 01:42:29Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Check locale layout, djlint and editorconfig config

`ls options/locale* -d; ls options/locale/ | head; echo "=== djlint config ==="; grep -rn -A25 "djlint" pyproject.toml 2>/dev/null | head -40; echo "=== editorconfig lint ==="; grep -n -A5 "lint-editorconfig" Makefile`

```text
options/locale
locale_cs-CZ.json
locale_de-DE.json
locale_el-GR.json
locale_en-US.json
locale_es-ES.json
locale_fa-IR.json
locale_fi-FI.json
locale_fr-FR.json
locale_ga-IE.json
locale_hu-HU.json
=== djlint config ===
pyproject.toml:8:  "djlint==1.40.1",
pyproject.toml-9-  "yamllint==1.38.0",
pyproject.toml-10-  "zizmor==1.26.1",
pyproject.toml-11-]
pyproject.toml-12-
pyproject.toml:13:[tool.djlint]
pyproject.toml-14-profile="golang"
pyproject.toml-15-ignore="H005,H006,H013,H016,H020,H021,H030,H031"
=== editorconfig lint ===
294:lint-backend: lint-go lint-editorconfig ## lint backend files
295-
296-.PHONY: lint-backend-fix
297:lint-backend-fix: lint-go-fix lint-editorconfig ## lint backend files and fix issues
298-
299-.PHONY: lint-js
300-lint-js: node_modules ## lint js and ts files
301-	pnpm exec eslint --color --max-warnings=0 --concurrency $(ESLINT_CONCURRENCY) $(ESLINT_FILES)
302-	pnpm exec vue-tsc
--
345:.PHONY: lint-editorconfig
346:lint-editorconfig:
347-	@echo "Running editorconfig check..."
348-	@$(GO) run $(EDITORCONFIG_CHECKER_PACKAGE) $(EDITORCONFIG_FILES)
349-
350-.PHONY: lint-actions
351-lint-actions: .venv ## lint action workflow files
```
</details></tool-use>

_**Agent (claude-opus-5 2026-09-15 01:42:41Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Inspect whitespace of new template block

`sed -n '108,128p' templates/repo/release/list.tmpl | cat -A | sed -e 's/\$$/<EOL>/' | head -30`

```text
^I^I^I^I^I^I^I^I{{end}}<EOL>
^I^I^I^I^I^I^I</ul><EOL>
^I^I^I^I^I^I</details><EOL>
^I^I^I^I^I</div><EOL>
^I^I^I^I</li><EOL>
^I^I^I{{end}}<EOL>
^I^I</ul><EOL>
^I^I{{else}}<EOL>
^I^I^I<div class="empty-placeholder"><EOL>
^I^I^I^I{{svg "octicon-tag" 48}}<EOL>
^I^I^I^I<h2>{{ctx.Locale.Tr "repo.release.empty"}}</h2><EOL>
^I^I^I^I<p>{{if .CanCreateRelease}}{{ctx.Locale.Tr "repo.release.empty_description_writer"}}{{else}}{{ctx.Locale.Tr "repo.release.empty_description"}}{{end}}</p><EOL>
^I^I^I^I{{if .CanCreateRelease}}<EOL>
^I^I^I^I^I<a class="ui small primary button" href="{{$.RepoLink}}/releases/new">{{ctx.Locale.Tr "repo.release.new_release"}}</a><EOL>
^I^I^I^I{{end}}<EOL>
^I^I^I</div><EOL>
^I^I{{end}}<EOL>
<EOL>
^I^I{{template "base/paginate" .}}<EOL>
^I</div><EOL>
</div><EOL>
```
</details></tool-use>

_**Agent (claude-opus-5 2026-09-15 01:42:54Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
List existing release locale keys

`echo "=== locale keys with 'release' and empty/no ===" && grep -n '"repo.release' options/locale/locale_en-US.json | head -60`

```text
=== locale keys with 'release' and empty/no ===
1193:  "repo.release": "Release",
1194:  "repo.releases": "Releases",
1197:  "repo.released_this": "released this",
2615:  "repo.releases.desc": "Track project versions and downloads.",
2616:  "repo.release.releases": "Releases",
2617:  "repo.release.detail": "Release details",
2618:  "repo.release.tags": "Tags",
2619:  "repo.release.new_release": "New Release",
2620:  "repo.release.empty": "No releases yet.",
2621:  "repo.release.empty_description": "Releases package software versions for download. This repository has not published one yet.",
2622:  "repo.release.empty_description_writer": "Create a release from a tag to publish a version of this repository.",
2623:  "repo.release.draft": "Draft",
2624:  "repo.release.prerelease": "Pre-Release",
2625:  "repo.release.stable": "Stable",
2626:  "repo.release.latest": "Latest",
2627:  "repo.release.compare": "Compare",
2628:  "repo.release.edit": "edit",
2629:  "repo.release.ahead.commits": "<strong>%d</strong> commits",
2630:  "repo.release.ahead.target": "to %s since this release",
2632:  "repo.release.source_code": "Source Code",
2633:  "repo.release.new_subheader": "Releases organize project versions.",
2634:  "repo.release.edit_subheader": "Releases organize project versions.",
2635:  "repo.release.tag_name": "Tag name",
2636:  "repo.release.target": "Target",
2637:  "repo.release.tag_helper": "Choose an existing tag or create a new tag.",
2638:  "repo.release.tag_helper_new": "New tag. This tag will be created from the target.",
2639:  "repo.release.tag_helper_existing": "Existing tag.",
2640:  "repo.release.title": "Release title",
2641:  "repo.release.title_empty": "Title cannot be empty.",
2642:  "repo.release.message": "Describe this release",
2643:  "repo.release.prerelease_desc": "Mark as Pre-Release",
2644:  "repo.release.prerelease_helper": "Mark this release unsuitable for production use.",
2645:  "repo.release.cancel": "Cancel",
2646:  "repo.release.publish": "Publish Release",
2647:  "repo.release.save_draft": "Save Draft",
2648:  "repo.release.edit_release": "Update Release",
2649:  "repo.release.delete_release": "Delete Release",
2650:  "repo.release.delete_tag": "Delete Tag",
2651:  "repo.release.deletion": "Delete Release",
2652:  "repo.release.deletion_desc": "Deleting a release only removes it from Gitea. It will not affect the Git tag, the contents of your repository or its history. Continue?",
2653:  "repo.release.deletion_success": "The release has been deleted.",
2654:  "repo.release.deletion_tag_desc": "Will delete this tag from repository. Repository contents and history remain unchanged. Continue?",
2655:  "repo.release.deletion_tag_success": "The tag has been deleted.",
2656:  "repo.release.tag_name_already_exist": "A release with this tag name already exists.",
2657:  "repo.release.tag_name_invalid": "The tag name is not valid.",
2658:  "repo.release.tag_name_protected": "The tag name is protected.",
2659:  "repo.release.tag_already_exist": "This tag name already exists.",
2660:  "repo.release.downloads": "Downloads",
2661:  "repo.release.download_count": "Downloads: %s",
2662:  "repo.release.add_tag_msg": "Use the title and content of release as tag message.",
2663:  "repo.release.add_tag": "Create Tag Only",
2664:  "repo.release.releases_for": "Releases for %s",
2665:  "repo.release.tags_for": "Tags for %s",
2666:  "repo.release.notes": "Release notes",
2667:  "repo.release.generate_notes": "Generate release notes",
2668:  "repo.release.generate_notes_desc": "Automatically add merged pull requests and a changelog link for this release.",
2669:  "repo.release.previous_tag": "Previous tag",
2670:  "repo.release.generate_notes_tag_not_found": "Tag \"%s\" does not exist in this repository.",
2671:  "repo.release.generate_notes_target_not_found": "The release target \"%s\" cannot be found.",
2672:  "repo.release.generate_notes_missing_tag": "Enter a tag name to generate release notes.",
```
</details></tool-use>

_**Agent (claude-opus-5 2026-09-15 01:42:55Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Check test file header and tags template

`sed -n '1,20p' tests/integration/release_test.go; echo "=== tags empty state? ==="; grep -n "empty\|no_results" templates/repo/tag/list.tmpl`

```text
// Copyright 2017 The Gitea Authors. All rights reserved.
// SPDX-License-Identifier: MIT

package integration

import (
	"fmt"
	"net/http"
	"testing"

	repo_model "gitea.dev/models/repo"
	"gitea.dev/models/unittest"
	user_model "gitea.dev/models/user"
	"gitea.dev/modules/setting"
	"gitea.dev/modules/test"
	"gitea.dev/modules/translation"
	"gitea.dev/tests"

	"github.com/PuerkitoBio/goquery"
	"github.com/stretchr/testify/assert"
=== tags empty state? ===
61:					<p class="tw-p-4">{{ctx.Locale.Tr "no_results_found"}}</p>
```
</details></tool-use>

_**Agent (claude-opus-5 2026-09-15 01:43:04Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Find NumReleases assignment

`grep -rn "NumReleases" --include='*.go' modules/ services/ routers/ models/ | head -20`

```text
services/context/repo.go:608:	ctx.Data["NumReleases"], err = db.Count[repo_model.Release](ctx, repo_model.FindReleasesOptions{
routers/web/repo/release.go:193:	numReleases := ctx.Data["NumReleases"].(int64)
```
</details></tool-use>

_**Agent (claude-opus-5 2026-09-15 01:43:06Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Read NumReleases computation

`sed -n '600,625p' services/context/repo.go`

```text
		IncludeTags:   true,
		HasSha1:       optional.Some(true), // only draft releases which are created with existing tags
		RepoID:        ctx.Repo.Repository.ID,
	})
	if err != nil {
		ctx.ServerError("GetReleaseCountByRepoID", err)
		return
	}
	ctx.Data["NumReleases"], err = db.Count[repo_model.Release](ctx, repo_model.FindReleasesOptions{
		// only show draft releases for users who can write, read-only users shouldn't see draft releases.
		IncludeDrafts: ctx.Repo.Permission.CanWrite(unit_model.TypeReleases),
		RepoID:        ctx.Repo.Repository.ID,
	})
	if err != nil {
		ctx.ServerError("GetReleaseCountByRepoID", err)
		return
	}

	ctx.Data["Title"] = repo.Owner.Name + "/" + repo.Name
	ctx.Data["PageTitleCommon"] = repo.Name + " - " + setting.AppName
	ctx.Data["Repository"] = repo
	ctx.Data["Owner"] = ctx.Repo.Repository.Owner
	ctx.Data["CanWriteCode"] = ctx.Repo.Permission.CanWrite(unit_model.TypeCode)
	ctx.Data["CanWriteIssues"] = ctx.Repo.Permission.CanWrite(unit_model.TypeIssues)
	ctx.Data["CanWritePulls"] = ctx.Repo.Permission.CanWrite(unit_model.TypePullRequests)
	ctx.Data["CanWriteActions"] = ctx.Repo.Permission.CanWrite(unit_model.TypeActions)
```
</details></tool-use>

_**Agent (claude-opus-5 2026-09-15 01:43:21Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Check empty-state copy conventions and paging default

`grep -n '"repo.projects.empty\|"packages.empty\|"actions.runs.no_workflows' options/locale/locale_en-US.json; echo "=== DefaultPagingNum ==="; grep -rn "DefaultPagingNum" modules/setting/repository.go | head`

```text
1358:  "repo.projects.empty": "No projects yet.",
1359:  "repo.projects.empty_description": "Create a project to coordinate issues and pull requests.",
3522:  "packages.empty": "There are no packages yet.",
3524:  "packages.empty.documentation": "For more information on the package registry, see <a target=\"_blank\" rel=\"noopener noreferrer\" href=\"%s\">the documentation</a>.",
3525:  "packages.empty.repo": "Did you upload a package, but it's not shown here? Go to <a href=\"%[1]s\">package settings</a> and link it to this repo.",
3800:  "actions.runs.no_workflows": "There are no workflows yet.",
3801:  "actions.runs.no_workflows.quick_start": "Don't know how to start with Gitea Actions? See <a target=\"_blank\" rel=\"noopener noreferrer\" href=\"%s\">the quick start guide</a>.",
3802:  "actions.runs.no_workflows.documentation": "For more information on Gitea Actions, see <a target=\"_blank\" rel=\"noopener noreferrer\" href=\"%s\">the documentation</a>.",
=== DefaultPagingNum ===
modules/setting/repository.go:112:			DefaultPagingNum int
modules/setting/repository.go:259:			DefaultPagingNum int
modules/setting/repository.go:264:			DefaultPagingNum: 10,
```
</details></tool-use>

_**Agent (claude-opus-5 2026-09-15 01:43:35Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Get exact line numbers

`grep -n "{{if .Releases}}\|{{else}}\|empty-placeholder\|base/paginate" templates/repo/release/list.tmpl; echo "---"; grep -n "OpenCount 0" templates/projects/list.tmpl`

```text
7:		{{if .Releases}}
13:						<a class="muted" href="{{if not (and $release.Sha1 ($.Permission.CanRead ctx.Consts.RepoUnitTypeCode))}}#{{else}}{{$.RepoLink}}/src/tag/{{$release.TagName | PathEscapeSegments}}{{end}}" rel="nofollow">{{svg "octicon-tag" 16 "tw-mr-1"}}{{$release.TagName}}</a>
21:							{{else}}
36:								{{if $.PageIsSingleTag}}{{$release.Title}}{{else}}<a class="muted" href="{{$.RepoLink}}/releases/tag/{{$release.TagName | PathEscapeSegments}}">{{$release.Title}}</a>{{end}}
56:								{{else}}
59:							{{else}}
115:		{{else}}
116:			<div class="empty-placeholder">
119:				<p>{{if .CanCreateRelease}}{{ctx.Locale.Tr "repo.release.empty_description_writer"}}{{else}}{{ctx.Locale.Tr "repo.release.empty_description"}}{{end}}</p>
126:		{{template "base/paginate" .}}
---
79:		{{if and (eq .OpenCount 0) (eq .ClosedCount 0)}}
```
</details></tool-use>

_**Agent (claude-opus-5 2026-09-15 01:43:56Z)**_

Yes — three things, one of which is a real bug.

## 1. False empty state on out-of-range pages (`templates/repo/release/list.tmpl:7`)

`{{if .Releases}}` tests the *current page's* slice, not whether the repo has releases. `Releases()` doesn't clamp the page number (`routers/web/repo/release.go:159-167` reads `page` straight from the query and `DefaultPagingNum` is 10, `modules/setting/repository.go:264`). So on a repo with 12 releases, `/owner/repo/releases?page=3` now renders **"No releases yet."** above a pager that still shows pages 1–2.

Gitea already has the correct pattern for this — `templates/projects/list.tmpl:79` guards on the counts, not the slice:

```
{{if and (eq .OpenCount 0) (eq .ClosedCount 0)}}
```

The equivalent here is `.NumReleases`, and it's an exact match for the guard you want: `services/context/repo.go:608` computes it with the same `RepoID` + `IncludeDrafts: CanWrite(TypeReleases)` filter the handler uses at `release.go:172-178`, so `NumReleases == 0` ⟺ the repo genuinely has nothing to show *this viewer*. Worth asking them to fold that into the condition.

## 2. Duplicate "New Release" button

`templates/repo/release_tag_header.tmpl:19-23` already renders `<a class="ui small primary button">New Release</a>` whenever `.CanCreateRelease` and not the tag list — which is exactly the state the empty placeholder fires in. A writer hitting an empty releases page now gets two identical primary buttons about 100px apart.

Worth noting too: none of the existing `.empty-placeholder` blocks in this repo carry a CTA button — `no_workflows.tmpl`, `package/shared/list.tmpl:46`, `projects/list.tmpl:80` are all icon + `<h2>` + `<p>`. So dropping the button (lines 120-122) both removes the duplicate and lines up with convention.

## 3. The test can't catch either of the above

`tests/integration/release_test.go:162+`:

- Both assertions are scoped to `.empty-placeholder a[href=...]`, so the header's button is invisible to the test — it passes whether or not the page has one button or two. A `htmlDoc.Find('a[href="/user5/repo4/releases/new"]').Length()` assertion would pin the count.
- There's no case for a repo that *has* releases on an out-of-range page, which is where #1 bites.

## Nits

- `list.tmpl:7-114` — the `{{if}}` branch wasn't re-indented, so `<ul>` sits at the same depth as its `{{if}}` while the `{{else}}` branch at 115 is indented normally. Cosmetic, but the asymmetry makes the diff harder to read than it needs to be.
- `repo.release.empty_description` ("…has not published one yet") restates the `<h2>` above it ("No releases yet."). Compare `repo.projects.empty_description`, which spends its sentence on what a project *is for* instead of repeating the heading.

Nothing here is a correctness risk for the common path — the feature works as intended on page 1, the locale keys follow the `*.empty` / `*.empty_description` naming convention, `octicon-tag` is already used in this file, indentation is tabs with no trailing whitespace, and repo4/user5 are valid fixtures for the test. #1 is the one I'd block on.

Want me to draft these as PR review comments?

---

_**User (2026-09-15 01:50:15Z)**_

where does .Releases come from. I know nothing of go templates

---

_**Agent (claude-opus-5 2026-09-15 01:50:28Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Trace Releases data and route

`grep -n "tplReleasesList\|ctx.Data\[\"Releases\"\]\|func Releases" routers/web/repo/release.go | head; echo "=== route registration ==="; grep -rn "repo.Releases\b\|releases\"" routers/web/web.go | head -20`

```text
37:	tplReleasesList templates.TplName = "repo/release/list"
157:func Releases(ctx *context.Context) {
191:	ctx.Data["Releases"] = releases
197:	ctx.HTML(http.StatusOK, tplReleasesList)
243:	ctx.Data["Releases"] = releases
254:func ReleasesFeedRSS(ctx *context.Context) {
264:func ReleasesFeedAtom(ctx *context.Context) {
314:	ctx.Data["Releases"] = releases
315:	ctx.HTML(http.StatusOK, tplReleasesList)
=== route registration ===
routers/web/web.go:1482:		m.Group("/releases", func() {
routers/web/web.go:1483:			m.Get("", repo.Releases)
routers/web/web.go:1491:		m.Group("/releases", func() {
```
</details></tool-use>

_**Agent (claude-opus-5 2026-09-15 01:50:29Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Find ctx.Data and ctx.HTML definitions

`grep -rn "func (ctx \*Context) HTML\|Data      map\|Data  *map\[string\]any" services/context/*.go modules/web/*.go 2>/dev/null | head -20; echo "=== ReleaseInfo type ==="; grep -n -B3 -A8 "type ReleaseInfo struct" routers/web/repo/release.go`

```text
services/context/context_response.go:81:func (ctx *Context) HTML(status int, name templates.TplName) {
services/context/context.go:47:	PageData map[string]any // data used by JavaScript modules in one page, it's `window.config.pageData`
=== ReleaseInfo type ===
70-	return nil
71-}
72-
73:type ReleaseInfo struct {
74-	Release        *repo_model.Release
75-	CommitStatus   *git_model.CommitStatus
76-	CommitStatuses []*git_model.CommitStatus
77-}
78-
79-func getReleaseInfos(ctx *context.Context, opts *repo_model.FindReleasesOptions) ([]*ReleaseInfo, error) {
80-	releases, err := db.Find[repo_model.Release](ctx, opts)
81-	if err != nil {
```
</details></tool-use>

_**Agent (claude-opus-5 2026-09-15 01:50:33Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Find ctx.Data type and HTML renderer

`grep -rn "Data reqctx.ContextData\|Data .*ContextData\|type ContextData" services/context/context.go modules/reqctx/*.go | head; echo "=== ctx.HTML body ==="; sed -n '75,110p' services/context/context_response.go`

```text
modules/reqctx/datastore.go:15:type ContextDataProvider interface {
modules/reqctx/datastore.go:19:type ContextData map[string]any
modules/reqctx/datastore.go:66:// GetData and the underlying ContextData are not thread-safe, callers should ensure thread-safety.
=== ctx.HTML body ===
	ctx.Redirect(setting.AppSubURL + "/")
}

const tplStatus500 templates.TplName = "status/500"

// HTML calls Context.HTML and renders the template to HTTP response
func (ctx *Context) HTML(status int, name templates.TplName) {
	log.Debug("Template: %s", name)

	tmplStartTime := time.Now()
	if !setting.IsProd {
		ctx.Data["TemplateName"] = name
	}
	ctx.Data["TemplateLoadTimes"] = func() string {
		return strconv.FormatInt(time.Since(tmplStartTime).Nanoseconds()/1e6, 10) + "ms"
	}

	err := ctx.Render.HTML(ctx.Resp, status, name, ctx.Data, ctx.TemplateContext)
	if err == nil || errors.Is(err, syscall.EPIPE) {
		return
	}

	// if rendering fails, show error page
	if name != tplStatus500 {
		err = fmt.Errorf("failed to render template: %s, error: %s", name, templates.HandleTemplateRenderingError(err))
		ctx.ServerError("Render failed", err) // show the 500 error page
	} else {
		ctx.PlainText(http.StatusInternalServerError, "Unable to render status/500 page, the template system is broken, or Gitea can't find your template files.")
		return
	}
}

// JSONTemplate renders the template as JSON response
// keep in mind that the template is processed in HTML context, so JSON things should be handled carefully, e.g.: use JSEscape
func (ctx *Context) JSONTemplate(tmpl templates.TplName) {
	t, err := ctx.Render.TemplateLookup(string(tmpl), nil)
```
</details></tool-use>

_**Agent (claude-opus-5 2026-09-15 01:51:06Z)**_

Short answer: `.Releases` is a lookup in a `map[string]any` that the route handler fills in before rendering. There's no type, no declaration, no import — just a string key that has to match on both sides.

## The chain

```
routers/web/web.go:1483        m.Get("", repo.Releases)          ← route → handler
routers/web/repo/release.go:157  func Releases(ctx *context.Context)
                        :191     ctx.Data["Releases"] = releases   ← the write
                        :197     ctx.HTML(http.StatusOK, tplReleasesList)
                        :37      tplReleasesList = "repo/release/list"  → templates/repo/release/list.tmpl
services/context/context_response.go:93
                                 ctx.Render.HTML(..., ctx.Data, ...)    ← map handed to the template
templates/repo/release/list.tmpl:7   {{if .Releases}}                   ← the read
```

`ctx.Data` is `reqctx.ContextData`, and that's literally `map[string]any` (`modules/reqctx/datastore.go:19`). One bag per request. The handler stuffs keys in; `ctx.HTML` hands the whole bag to the renderer; inside the template, **`.` ("dot") *is* that map**, so `.Releases` means `ctx.Data["Releases"]` — here a `[]*ReleaseInfo` (`release.go:73-77`).

Same story for every other name in that file: `.CanCreateRelease` exists only because `release.go:173` sets it. `.RepoLink`, `.Permission`, `.NumReleases` come from repo middleware that runs before the handler (`services/context/repo.go:608` for `NumReleases`) — which is why every repo page can use them.

## Three places your React instincts will lie to you

**1. `{{if .Releases}}` is `.length > 0`, not `!= null`.** Go template truthiness counts an empty slice as *false*. In JS `if ([])` is true. This is exactly the hinge of the bug in my first point — the author almost certainly read it as "did we get a list back", when it actually means "does this page have items on it".

**2. A typo renders as nothing, silently.** Because dot is a *map*, `{{.Releaess}}` is a missing key → zero value → empty output, no error. (Missing *struct fields* do error; map keys don't.) So "the page loads" proves nothing about whether a name is spelled right. Worth knowing when reviewing.

**3. `range` rebinds dot — there's no callback parameter.** Inside `{{range $idx, $info := .Releases}}` (line 8), `.` is now the current element, not the root map. That's why lines 13–41 are full of `$.RepoLink` and `$.Permission`: `$` is the root dot, pinned for the whole template. Imagine if `.map()` reassigned `this` instead of passing you an argument.

A fourth, smaller one: `{{template "base/paginate" .}}` at line 126 — that trailing `.` is the argument. Sub-templates don't close over anything; you pass dot explicitly. Closest thing to props in this system.

## To check it yourself

For any name in a template, grep for the write side:

```
grep -rn 'Data\["Releases"\]' routers/ services/
```

Three hits in `release.go` (191, 243, 314) — the list page, the tags feed path, and `SingleRelease`. That last one is worth noting for the review: `SingleRelease` renders the *same* template (line 315), which is why I checked whether the new `{{else}}` branch could fire there. It can't — line 288 does `ctx.NotFound()` when the release count isn't exactly 1.

---

_**User (2026-09-15 02:12:56Z)**_

<command-name>/effort</command-name>
            <command-message>effort</command-message>
            <command-args></command-args>

---

_**User (2026-09-15 02:12:56Z)**_

<local-command-stdout>Set effort level to ultracode (this session only): xhigh + dynamic workflow orchestration</local-command-stdout>

---

_**User (2026-09-15 02:12:56Z)**_

<local-command-caveat>Caveat: The messages below were generated by the user while running local commands. DO NOT respond to these messages or otherwise consider them in your response unless the user explicitly asks you to.</local-command-caveat>

---

_**User (2026-09-15 02:13:32Z)**_

Now, a new issue, the ability to rename passkeys. Given this deals with passkeys, ensure it is all good. The code seems fine.

---

_**User (2026-09-15 02:13:32Z)**_

<command-message>workflow-authoring</command-message>
<command-name>workflow-authoring</command-name>
<skill-format>true</skill-format>

---

_**User (2026-09-15 02:13:32Z)**_

# Workflow authoring reference

A workflow structures work across many agents — to be comprehensive (decompose and cover in parallel), to be confident (independent perspectives and adversarial checks before committing), or to take on scale one context can't hold (migrations, audits, broad sweeps). The script is where you encode that structure: what fans out, what verifies, what synthesizes.

When you do call it, the right move is often **hybrid**: scout inline first (list the files, find the channels, scope the diff) to discover the work-list, then call Workflow to pipeline over it. You don't need to know the shape before the *task* — only before the *orchestration step*.

Common single-phase workflows you can chain across turns:
- **Understand** — parallel readers over relevant subsystems → structured map
- **Design** — judge panel of N independent approaches → scored synthesis
- **Review** — dimensions → find → adversarially verify (the review-changes example)
- **Research** — multi-modal sweep → deep-read → synthesize
- **Migrate** — discover sites → transform each (worktree isolation) → verify

For larger work, run several in sequence — read each result before deciding the next phase. You stay in the loop; each workflow is one well-scoped fan-out.

**Ultracode.** When a system-reminder confirms ultracode is on, that opt-in is standing: author and run a workflow for every substantive task by default. The goal is the most exhaustive, correct answer you can produce — token cost is not a constraint. For multi-phase work (understand → design → implement → review), that often means several workflows in sequence — one per phase — so you stay in the loop between them. The quality patterns below (adversarial verify, multi-modal sweep, completeness critic, loop-until-dry) are the tools; pick what fits the task. Lean toward orchestrating with workflows and adversarially verifying your findings — unless the work is trivial or already verified. Solo only on conversational turns or trivial mechanical edits. When a reminder says ultracode is off, revert to the opt-in rule in the Workflow tool description.

Pass the script inline via `script` — do not Write it to a file first. Every invocation automatically persists its script to a file under the session directory and returns the path in the tool result. To iterate on a workflow, edit that file with Write/Edit and re-invoke Workflow with `{scriptPath: "<path>"}` instead of resending the full script.

Every script must begin with `export const meta = {...}`:
  export const meta = {
    name: 'find-flaky-tests',
    description: 'Find flaky tests and propose fixes',   // one-line, shown in permission dialog
    phases: [                                            // one entry per phase() call
      { title: 'Scan', detail: 'grep test logs for retries' },
      { title: 'Fix', detail: 'one agent per flaky test' },
    ],
  }
  // script body starts here — use agent()/parallel()/pipeline()/phase()/log()
  phase('Scan')
  const flaky = await agent('grep CI logs for retry markers', {schema: FLAKY_SCHEMA})
  ...

The `meta` object must be a PURE LITERAL — no variables, function calls, spreads, or template interpolation. Required fields: `name`, `description`. Optional: `whenToUse` (shown in the workflow list), `phases`. Use the SAME phase titles in meta.phases as in phase() calls — titles are matched exactly; a phase() call with no matching meta entry just gets its own progress group. Add `model` to a phase entry when that phase uses a specific model override.

Script body hooks:
- agent(prompt: string, opts?: {label?: string, phase?: string, schema?: object, model?: string, effort?: string, isolation?: 'worktree', agentType?: string}): Promise<any> — spawn a subagent. Without schema, returns its final text as a string. With schema (a JSON Schema), the subagent is forced to call a StructuredOutput tool and agent() returns the validated object — no parsing needed. Returns null if the user skips the agent mid-run or the subagent dies on a terminal API error after retries (filter with .filter(Boolean)). opts.label overrides the display label. opts.phase explicitly assigns this agent to a progress group (use this inside pipeline()/parallel() stages to avoid races on the global phase() state — same phase string → same group box). opts.model overrides the model for this agent call. Default to omitting it — the agent inherits the main-loop model (the resolved session model), which is almost always correct. Only set it when you're highly confident a different tier fits the task; when unsure, omit. opts.effort overrides the reasoning effort for this agent call ('low' | 'medium' | 'high' | 'xhigh' | 'max') — omit to inherit the session effort; use 'low' for cheap mechanical stages and higher tiers only for the hardest verify/judge stages. opts.isolation: 'worktree' runs the agent in a fresh git worktree — EXPENSIVE (~200-500ms setup + disk per agent), use ONLY when agents mutate files in parallel and would otherwise conflict; the worktree is auto-removed if unchanged. opts.agentType uses a custom subagent type (e.g. 'general-purpose', 'code-reviewer') instead of the default workflow subagent — resolved from the same registry as the Agent tool; composes with schema (the custom agent's system prompt gets a StructuredOutput instruction appended).
- pipeline(items, stage1, stage2, ...): Promise<any[]> — run each item through all stages independently, NO barrier between stages. Item A can be in stage 3 while item B is still in stage 1. This is the DEFAULT for multi-stage work. Wall-clock = slowest single-item chain, not sum-of-slowest-per-stage. Every stage callback receives (prevResult, originalItem, index) — use originalItem/index in later stages to label work without threading context through stage 1's return value. A stage that throws drops that item to `null` and skips its remaining stages.
- parallel(thunks: Array<() => Promise<any>>): Promise<any[]> — run tasks concurrently. This is a BARRIER: awaits all thunks before returning. A thunk that throws (or whose agent errors) resolves to `null` in the result array — the call itself never rejects, so `.filter(Boolean)` before using the results. Use ONLY when you genuinely need all results together.
- log(message: string): void — emit a progress message to the user (shown as a narrator line above the progress tree)
- phase(title: string): void — start a new phase; subsequent agent() calls are grouped under this title in the progress display
- args: any — the value passed as Workflow's `args` input, verbatim (undefined if not provided). Pass arrays/objects as actual JSON values in the tool call, NOT as a JSON-encoded string — `args: ["a.ts", "b.ts"]`, not `args: "[\"a.ts\", ...]"` (a stringified list reaches the script as one string, so `args.filter`/`args.map` throw). Use this to parameterize named workflows — e.g. pass a research question, target path, or config object directly instead of via a side-channel file.
- budget: {total: number|null, spent(): number, remaining(): number} — the turn's token target from the user's "+500k"-style directive. `budget.total` is null if no target was set. `budget.spent()` returns output tokens spent this turn across the main loop and all workflows — the pool is shared, not per-workflow. `budget.remaining()` returns `max(0, total - spent())`, or `Infinity` if no target. The target is a HARD ceiling, not advisory: once `spent()` reaches `total`, further `agent()` calls throw. Use for dynamic loops: `while (budget.total && budget.remaining() > 50_000) { ... }`, or static scaling: `const FLEET = budget.total ? Math.floor(budget.total / 100_000) : 5`.
- workflow(nameOrRef: string | {scriptPath: string}, args?: any): Promise<any> — run another workflow inline as a sub-step and return whatever it returns. Pass a name to invoke a saved workflow (same registry as {name: "..."}), or {scriptPath} to run a script file you Wrote earlier. The child shares this run's concurrency cap, agent counter, abort signal, and token budget — its agents appear under a "▸ name" group in /workflows and its tokens count toward budget.spent(). The args param becomes the child's `args` global. Nesting is one level only: workflow() inside a child throws. Throws on unknown name / unreadable scriptPath / child syntax error; catch to handle gracefully.

Subagents are told their final text IS the return value (not a human-facing message), so they return raw data. For structured output, use the schema option — validation happens at the tool-call layer so the model retries on mismatch.
Schemas need {type: 'object', properties: {...}} at root and required ⊆ properties; unsatisfiable ones throw at agent().

Workflow agents can reach all session-connected MCP tools via ToolSearch — schemas load on demand per agent. Caveat: interactively-authenticated MCP servers (e.g. claude.ai) may be absent in headless/cron runs.

Subagents get the same CLAUDE.md files injected at start that you did (except built-in agent types that omit them, such as Explore and Plan) — don't tell them to re-read those or paste their rules into the prompt; name the specific rule a stage needs, if any.

Scripts are plain JavaScript, NOT TypeScript — type annotations (`: string[]`), interfaces, and generics fail to parse. The script body runs in an async context — use await directly. Standard JS built-ins (JSON, Math, Array, etc.) are available — EXCEPT `Date.now()`/`Math.random()`/argless `new Date()`, which throw (they would break resume); pass timestamps in via `args`, stamp results after the workflow returns, and for randomness vary the agent prompt/label by index. No filesystem or Node.js API access.

DEFAULT TO pipeline(). Only reach for a barrier (parallel between stages) when you genuinely need ALL prior-stage results together.

A barrier is correct ONLY when stage N needs cross-item context from all of stage N-1:
- Dedup/merge across the full result set before expensive downstream work
- Early-exit if the total count is zero ("0 bugs found → skip verification entirely")
- Stage N's prompt references "the other findings" for comparison

A barrier is NOT justified by:
- "I need to flatten/map/filter first" — do it inside a pipeline stage: pipeline(items, stageA, r => transform([r]).flat(), stageB)
- "The stages are conceptually separate" — that's what pipeline() models. Separate stages ≠ synchronized stages.
- "It's cleaner code" — barrier latency is real. If 5 finders run and the slowest takes 3× the fastest, a barrier wastes 2/3 of the fast finders' idle time.

Smell test: if you wrote
  const a = await parallel(...)
  const b = transform(a)        // flatten, map, filter — no cross-item dependency
  const c = await parallel(b.map(...))
that middle transform doesn't need the barrier. Rewrite as a pipeline with the transform inside a stage. When in doubt: pipeline.

Concurrent agent() calls are capped at min(16, available CPUs - 2) per workflow — excess calls queue and run as slots free up. You can still pass 100 items to parallel()/pipeline() and they all complete; only ~10 run at any moment. Total agent count across a workflow's lifetime is capped at 1000 — a runaway-loop backstop set far above any real workflow. A single parallel()/pipeline() call accepts at most 4096 items; passing more is an explicit error, not a silent truncation.

When a barrier IS correct — dedup across all findings before expensive verification:
  const all = await parallel(DIMENSIONS.map(d => () => agent(d.prompt, {schema: FINDINGS_SCHEMA})))
  const deduped = dedupeByFileAndLine(all.filter(Boolean).flatMap(r => r.findings))  // <-- genuinely needs ALL at once
  const verified = await parallel(deduped.map(f => () => agent(verifyPrompt(f), {schema: VERDICT_SCHEMA})))

Loop-until-count pattern — accumulate to a target:
  const bugs = []
  while (bugs.length < 10) {
    const result = await agent("Find bugs in this codebase.", {schema: BUGS_SCHEMA})
    bugs.push(...result.bugs)
    log(`${bugs.length}/10 found`)
  }

Loop-until-budget pattern — scale depth to the user's "+500k" directive. Guard on budget.total: with no target set, remaining() is Infinity and the loop would run straight to the 1000-agent cap.
  const bugs = []
  while (budget.total && budget.remaining() > 50_000) {
    const result = await agent("Find bugs in this codebase.", {schema: BUGS_SCHEMA})
    bugs.push(...result.bugs)
    log(`${bugs.length} found, ${Math.round(budget.remaining()/1000)}k remaining`)
  }

Composing patterns — exhaustive review (find → dedup vs seen → diverse-lens panel → loop-until-dry):
  const seen = new Set(), confirmed = []
  let dry = 0
  while (dry < 2) {                                              // loop-until-dry
    const found = (await parallel(FINDERS.map(f => () =>          // barrier: collect all finders this round
      agent(f.prompt, {phase: 'Find', schema: BUGS})))).filter(Boolean).flatMap(r => r.bugs)
    const fresh = found.filter(b => !seen.has(key(b)))           // dedup vs ALL seen — plain code, not an agent
    if (!fresh.length) { dry++; continue }
    dry = 0; fresh.forEach(b => seen.add(key(b)))
    const judged = await parallel(fresh.map(b => () =>           // every fresh bug judged concurrently...
      parallel(['correctness','security','repro'].map(lens => () =>   // ...each by 3 distinct lenses
        agent(`Judge "${b.desc}" via the ${lens} lens — real?`, {phase: 'Verify', schema: VERDICT})))
        .then(vs => ({ b, real: vs.filter(Boolean).filter(v => v.real).length >= 2 }))))
    confirmed.push(...judged.filter(v => v.real).map(v => v.b))
  }
  return confirmed
  // dedup vs `seen`, NOT `confirmed` — else judge-rejected findings reappear every round and it never converges.

Quality patterns — common shapes; pick by task and compose freely:
- Adversarial verify: spawn N independent skeptics per finding, each prompted to REFUTE. Kill if ≥majority refute. Prevents plausible-but-wrong findings from surviving.
    const votes = await parallel(Array.from({length: 3}, () => () =>
      agent(`Try to refute: ${claim}. Default to refuted=true if uncertain.`, {schema: VERDICT})))
    const survives = votes.filter(Boolean).filter(v => !v.refuted).length >= 2
- Perspective-diverse verify: when a finding can fail in more than one way, give each verifier a distinct lens (correctness, security, perf, does-it-reproduce) instead of N identical refuters — diversity catches failure modes redundancy can't.
- Judge panel: generate N independent attempts from different angles (e.g. MVP-first, risk-first, user-first), score with parallel judges, synthesize from the winner while grafting the best ideas from runners-up. Beats one-attempt-iterated when the solution space is wide.
- Loop-until-dry: for unknown-size discovery (bugs, issues, edge cases), keep spawning finders until K consecutive rounds return nothing new. Simple counters (while count < N) miss the tail.
- Multi-modal sweep: parallel agents each searching a different way (by-container, by-content, by-entity, by-time). Each is blind to what the others surface; useful when one search angle won't find everything.
- Completeness critic: a final agent that asks "what's missing — modality not run, claim unverified, source unread?" What it finds becomes the next round of work.
- No silent caps: if a workflow bounds coverage (top-N, no-retry, sampling), `log()` what was dropped — silent truncation reads as "covered everything" when it didn't.

Scale to what the user asked for. "find any bugs" → a few finders, single-vote verify. "thoroughly audit this" or "be comprehensive" → larger finder pool, 3–5 vote adversarial pass, synthesis stage. When unsure, lean toward thoroughness for research/review/audit requests and toward brevity for quick checks.

These patterns aren't exhaustive — compose novel harnesses when the task calls for it (tournament brackets, self-repair loops, staged escalation, whatever fits).

Use this tool for multi-step orchestration where control flow should be deterministic (loops, conditionals, fan-out) rather than model-driven.

## Resume

The tool result includes a runId. To resume after a pause, kill, or script edit, relaunch with Workflow({scriptPath, resumeFromRunId}) — the longest unchanged prefix of agent() calls returns cached results instantly; the first edited/new call and everything after it runs live. Same script + same args → 100% cache hit. Before diagnosing why a completed workflow returned an empty or unexpected result, Read <transcriptDir>/journal.jsonl — it records each agent's actual return value; do not assume cached results are non-empty. Date.now()/Math.random()/new Date() are unavailable in scripts (they would break this) — stamp results after the workflow returns, or pass timestamps via args. Fallback when no journal is available: Read agent-<id>.jsonl files in the transcript directory and hand-author a continuation script.

---

_**Agent (claude-opus-5 2026-09-15 02:13:40Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Survey branches and recent commits

`git log --oneline -15 --all | head -40; echo "=== branches ==="; git branch -a -v | head -30; echo "=== status ==="; git status --short | head -20`

```text
4ee4d928f0 chore(ai-logs): add session log for issues #1 and #17
63d75ebf23 Merge branch 'main' into feat/issue-1-empty-releases-message
98e73f51f3 fix(user): reject whitespace-only security key nicknames
e9dc83b736 Merge pull request #15 from CSCI-435-SE/chore/fix-students-md
faaf8222b6 Merge pull request #10 from CSCI-435-SE/feat/issue-5-robots-txt
aff9a20450 feat(user): allow renaming security keys
aa1063d30d feat(repo): show empty state on releases page
e336a6871d fix(ui): correctly reset counter when the field is made empty, and then cancel is clicked
b49c5109cb chore(docs): fixed students.md
e2ef046642 chore(docs): fixed students.md
6374b0eeed chore(docs): Fixed students.md
9e470e9a5d chore(docs): ran lint fix on students.md
3ef9323f91 chore(ai-log): add ai-log of the session
b4786e6a51 test(web): assert robots.txt source precedence
c2fe526bcd feat(web): serve a default robots.txt
=== branches ===
  feat/issue-1-empty-releases-message                4ee4d928f0 chore(ai-logs): add session log for issues #1 and #17
* feat/issue-17-rename-passkeys                      98e73f51f3 fix(user): reject whitespace-only security key nicknames
  feat/issue-2-show-character-field                  500a0708df [behind 2] chose(ai-logs): added personal log
  feat/issue-5-robots-txt                            3ef9323f91 chore(ai-log): add ai-log of the session
  main                                               e9dc83b736 Merge pull request #15 from CSCI-435-SE/chore/fix-students-md
  remotes/origin/HEAD                                -> origin/main
  remotes/origin/chore/claude-students-docs          6374b0eeed chore(docs): Fixed students.md
  remotes/origin/feat/issue-1-empty-releases-message 4ee4d928f0 chore(ai-logs): add session log for issues #1 and #17
  remotes/origin/feat/issue-17-rename-passkeys       98e73f51f3 fix(user): reject whitespace-only security key nicknames
  remotes/origin/feat/issue-2-show-character-field   e336a6871d fix(ui): correctly reset counter when the field is made empty, and then cancel is clicked
  remotes/origin/feat/issue-5-robots-txt             3ef9323f91 chore(ai-log): add ai-log of the session
  remotes/origin/main                                e9dc83b736 Merge pull request #15 from CSCI-435-SE/chore/fix-students-md
=== status ===
?? .specstory/
```
</details></tool-use>

_**Agent (claude-opus-5 2026-09-15 02:13:59Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Get passkey branch diff stat

`BASE=$(git merge-base main HEAD) && echo "merge-base: $BASE" && git log --oneline $BASE..HEAD && echo "=== STAT ===" && git diff --stat $BASE..HEAD`

```text
merge-base: c0092050a4c81926ab8d9f24dfd56faf55a54739
98e73f51f3 fix(user): reject whitespace-only security key nicknames
aff9a20450 feat(user): allow renaming security keys
=== STAT ===
 models/auth/webauthn.go                        |  6 +++
 models/auth/webauthn_test.go                   | 11 +++++
 options/locale/locale_en-US.json               |  3 ++
 routers/web/user/setting/security/webauthn.go  | 47 +++++++++++++++++++
 routers/web/web.go                             |  1 +
 services/forms/user_form.go                    | 11 +++++
 templates/user/settings/security/webauthn.tmpl | 19 ++++++++
 tests/integration/user_settings_test.go        | 64 ++++++++++++++++++++++++++
 8 files changed, 162 insertions(+)
```
</details></tool-use>

_**Agent (claude-opus-5 2026-09-15 02:14:09Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Show full passkey rename diff

`git diff $(git merge-base main HEAD)..HEAD`

```text
diff --git a/models/auth/webauthn.go b/models/auth/webauthn.go
index 11297556d2..b37e9f51ef 100644
--- a/models/auth/webauthn.go
+++ b/models/auth/webauthn.go
@@ -195,6 +195,12 @@ func CreateCredential(ctx context.Context, userID int64, name string, cred *weba
 	return c, nil
 }
 
+// UpdateCredentialName will rename the WebAuthnCredential with the given id if it belongs to userID
+func UpdateCredentialName(ctx context.Context, id, userID int64, name string) error {
+	_, err := db.GetEngine(ctx).ID(id).Where("user_id = ?", userID).Cols("name", "lower_name").Update(&WebAuthnCredential{Name: name})
+	return err
+}
+
 // DeleteCredential will delete WebAuthnCredential
 func DeleteCredential(ctx context.Context, id, userID int64) (bool, error) {
 	had, err := db.GetEngine(ctx).ID(id).Where("user_id = ?", userID).Delete(&WebAuthnCredential{})
diff --git a/models/auth/webauthn_test.go b/models/auth/webauthn_test.go
index a648319226..d7d848f71e 100644
--- a/models/auth/webauthn_test.go
+++ b/models/auth/webauthn_test.go
@@ -64,3 +64,14 @@ func TestCreateCredential(t *testing.T) {
 
 	unittest.AssertExistsAndLoadBean(t, &auth_model.WebAuthnCredential{Name: "WebAuthn Created Credential", UserID: 1})
 }
+
+func TestUpdateCredentialName(t *testing.T) {
+	assert.NoError(t, unittest.PrepareTestDatabase())
+
+	// credential 1 belongs to user 32, so another user's update must not change it
+	assert.NoError(t, auth_model.UpdateCredentialName(t.Context(), 1, 2, "Hijacked"))
+	unittest.AssertExistsAndLoadBean(t, &auth_model.WebAuthnCredential{ID: 1, Name: "WebAuthn credential"})
+
+	assert.NoError(t, auth_model.UpdateCredentialName(t.Context(), 1, 32, "Renamed Key"))
+	unittest.AssertExistsAndLoadBean(t, &auth_model.WebAuthnCredential{ID: 1, Name: "Renamed Key", LowerName: "renamed key", UserID: 32})
+}
diff --git a/options/locale/locale_en-US.json b/options/locale/locale_en-US.json
index 9373aa83a9..694d0bf99b 100644
--- a/options/locale/locale_en-US.json
+++ b/options/locale/locale_en-US.json
@@ -919,6 +919,9 @@
   "settings.webauthn_desc": "Security keys are hardware devices containing cryptographic keys. They can be used for two-factor authentication. Security keys must support the <a rel=\"noreferrer\" target=\"_blank\" href=\"%s\">WebAuthn Authenticator</a> standard.",
   "settings.webauthn_register_key": "Add Security Key",
   "settings.webauthn_nickname": "Nickname",
+  "settings.webauthn_rename_key": "Rename Security Key",
+  "settings.webauthn_rename_key_success": "The security key has been renamed.",
+  "settings.webauthn_nickname_taken": "Another security key already uses this nickname.",
   "settings.webauthn_delete_key": "Remove Security Key",
   "settings.webauthn_delete_key_desc": "If you remove a security key, you can no longer sign in with it. Continue?",
   "settings.webauthn_key_loss_warning": "If you lose your security keys, you will lose access to your account.",
diff --git a/routers/web/user/setting/security/webauthn.go b/routers/web/user/setting/security/webauthn.go
index 4db7b47879..5e93abbbf1 100644
--- a/routers/web/user/setting/security/webauthn.go
+++ b/routers/web/user/setting/security/webauthn.go
@@ -7,6 +7,7 @@ import (
 	"errors"
 	"net/http"
 	"strconv"
+	"strings"
 	"time"
 
 	"gitea.dev/models/auth"
@@ -125,6 +126,52 @@ func WebauthnRegisterPost(ctx *context.Context) {
 	ctx.JSON(http.StatusCreated, cred)
 }
 
+// WebauthnRename renames a security key by id
+func WebauthnRename(ctx *context.Context) {
+	if user_model.IsFeatureDisabledWithLoginType(ctx.Doer, setting.UserFeatureManageMFA) {
+		ctx.HTTPError(http.StatusNotFound)
+		return
+	}
+
+	if ctx.HasError() { // form binding validation error
+		ctx.JSONError(ctx.GetErrMsg())
+		return
+	}
+
+	cred, err := auth.GetWebAuthnCredentialByID(ctx, ctx.FormInt64("id"))
+	if err != nil && !auth.IsErrWebAuthnCredentialNotExist(err) {
+		ctx.ServerError("GetWebAuthnCredentialByID", err)
+		return
+	}
+	if cred == nil || cred.UserID != ctx.Doer.ID { // hide other users' keys as not found
+		ctx.JSONErrorNotFound()
+		return
+	}
+
+	form := web.GetForm(ctx).(*forms.WebauthnRenameForm)
+	form.Name = strings.TrimSpace(form.Name)
+	if form.Name == "" { // "Required" binding only rejects an exactly empty value, not whitespace
+		ctx.JSONError(ctx.Locale.TrString("settings.webauthn_nickname") + ctx.Locale.TrString("form.require_error"))
+		return
+	}
+	sameName, err := auth.GetWebAuthnCredentialByName(ctx, ctx.Doer.ID, form.Name)
+	if err != nil && !auth.IsErrWebAuthnCredentialNotExist(err) {
+		ctx.ServerError("GetWebAuthnCredentialByName", err)
+		return
+	}
+	if sameName != nil && sameName.ID != cred.ID { // renaming a key to its own name with different casing is allowed
+		ctx.JSONError(ctx.Tr("settings.webauthn_nickname_taken"))
+		return
+	}
+
+	if err := auth.UpdateCredentialName(ctx, cred.ID, ctx.Doer.ID, form.Name); err != nil {
+		ctx.ServerError("UpdateCredentialName", err)
+		return
+	}
+	ctx.Flash.Success(ctx.Tr("settings.webauthn_rename_key_success"))
+	ctx.JSONRedirect(setting.AppSubURL + "/user/settings/security")
+}
+
 // WebauthnDelete deletes an security key by id
 func WebauthnDelete(ctx *context.Context) {
 	if user_model.IsFeatureDisabledWithLoginType(ctx.Doer, setting.UserFeatureManageMFA) {
diff --git a/routers/web/web.go b/routers/web/web.go
index 0bfae44e86..f8e48f99fb 100644
--- a/routers/web/web.go
+++ b/routers/web/web.go
@@ -652,6 +652,7 @@ func registerWebRoutes(m *web.Router, webAuth *AuthMiddleware) {
 			m.Group("/webauthn", func() {
 				m.Post("/request_register", web.Bind(forms.WebauthnRegistrationForm{}), security.WebAuthnRegister)
 				m.Post("/register", security.WebauthnRegisterPost)
+				m.Post("/rename", web.Bind(forms.WebauthnRenameForm{}), security.WebauthnRename)
 				m.Post("/delete", security.WebauthnDelete)
 			})
 			m.Group("/openid", func() {
diff --git a/services/forms/user_form.go b/services/forms/user_form.go
index 75275fb5c7..95f3ae0b16 100644
--- a/services/forms/user_form.go
+++ b/services/forms/user_form.go
@@ -419,6 +419,17 @@ func (f *WebauthnRegistrationForm) Validate(req *http.Request, errs binding.Erro
 	return middleware.Validate(errs, ctx.Data, f, ctx.Locale)
 }
 
+// WebauthnRenameForm for renaming an existing WebAuthn credential
+type WebauthnRenameForm struct {
+	Name string `binding:"Required;MaxSize(255)" locale:"settings.webauthn_nickname"` // 255 is the size of the name column
+}
+
+// Validate validates the fields
+func (f *WebauthnRenameForm) Validate(req *http.Request, errs binding.Errors) binding.Errors {
+	ctx := context.GetValidateContext(req)
+	return middleware.Validate(errs, ctx.Data, f, ctx.Locale)
+}
+
 // PackageSettingForm form for package settings
 type PackageSettingForm struct {
 	Action   string
diff --git a/templates/user/settings/security/webauthn.tmpl b/templates/user/settings/security/webauthn.tmpl
index 1bf0159d37..6bfaf11d6b 100644
--- a/templates/user/settings/security/webauthn.tmpl
+++ b/templates/user/settings/security/webauthn.tmpl
@@ -16,6 +16,13 @@
 					</div>
 				</div>
 				<div class="item-trailing">
+					<button class="ui tiny button show-modal"
+						data-modal="#rename-webauthn-modal"
+						data-modal-form.url="{{$.Link}}/webauthn/rename?id={{.ID}}"
+						data-modal-rename-webauthn-nickname="{{.Name}}"
+					>
+						{{svg "octicon-pencil"}} {{ctx.Locale.Tr "settings.webauthn_rename_key"}}
+					</button>
 					<button class="ui red tiny button link-action" data-modal-confirm="#delete-registration" data-url="{{$.Link}}/webauthn/delete?id={{.ID}}">
 					{{ctx.Locale.Tr "settings.delete_key"}}
 					</button>
@@ -30,6 +37,18 @@
 		</div>
 		<button id="register-webauthn" class="ui primary button">{{svg "octicon-key"}} {{ctx.Locale.Tr "settings.webauthn_register_key"}}</button>
 	</div>
+	<div class="ui small modal" id="rename-webauthn-modal">
+		<div class="header">{{svg "octicon-pencil"}} {{ctx.Locale.Tr "settings.webauthn_rename_key"}}</div>
+		<form class="ui form form-fetch-action" method="post">
+			<div class="content">
+				<div class="required field">
+					<label for="rename-webauthn-nickname">{{ctx.Locale.Tr "settings.webauthn_nickname"}}</label>
+					<input id="rename-webauthn-nickname" name="name" type="text" maxlength="255" required>
+				</div>
+			</div>
+			{{template "base/modal_actions_confirm" (dict "ModalButtonTypes" "confirm")}}
+		</form>
+	</div>
 	<div class="ui small modal" id="delete-registration">
 		<div class="header">
 			{{svg "octicon-trash"}}
diff --git a/tests/integration/user_settings_test.go b/tests/integration/user_settings_test.go
index d837da71cd..4df3f9f534 100644
--- a/tests/integration/user_settings_test.go
+++ b/tests/integration/user_settings_test.go
@@ -4,15 +4,21 @@
 package integration
 
 import (
+	"fmt"
 	"net/http"
+	"net/http/httptest"
 	"strings"
 	"testing"
 
+	auth_model "gitea.dev/models/auth"
+	"gitea.dev/models/unittest"
 	"gitea.dev/modules/container"
 	"gitea.dev/modules/setting"
 	"gitea.dev/modules/test"
+	"gitea.dev/modules/translation"
 	"gitea.dev/tests"
 
+	"github.com/go-webauthn/webauthn/webauthn"
 	"github.com/stretchr/testify/assert"
 )
 
@@ -269,6 +275,64 @@ func TestUserSettingsSecurity(t *testing.T) {
 	})
 }
 
+func TestUserSettingsWebAuthnRename(t *testing.T) {
+	defer tests.PrepareTestEnv(t)()
+
+	session := loginUser(t, "user2") // log in before adding keys, otherwise login would require the second factor
+	own, err := auth_model.CreateCredential(t.Context(), 2, "Laptop Key", &webauthn.Credential{ID: []byte("rename-1")})
+	assert.NoError(t, err)
+	_, err = auth_model.CreateCredential(t.Context(), 2, "Phone Key", &webauthn.Credential{ID: []byte("rename-2")})
+	assert.NoError(t, err)
+	other, err := auth_model.CreateCredential(t.Context(), 4, "Other User Key", &webauthn.Credential{ID: []byte("rename-3")})
+	assert.NoError(t, err)
+
+	rename := func(id int64, name string, expectedStatus int) *httptest.ResponseRecorder {
+		req := NewRequestWithValues(t, "POST", fmt.Sprintf("/user/settings/security/webauthn/rename?id=%d", id), map[string]string{"name": name})
+		return session.MakeRequest(t, req, expectedStatus)
+	}
+
+	t.Run("Success", func(t *testing.T) {
+		resp := rename(own.ID, "Work Laptop", http.StatusOK)
+		assert.Equal(t, "/user/settings/security", *test.ParseJSONRedirect(resp.Body.Bytes()).Redirect)
+		cred := unittest.AssertExistsAndLoadBean(t, &auth_model.WebAuthnCredential{ID: own.ID, Name: "Work Laptop"})
+		assert.Equal(t, []byte("rename-1"), cred.CredentialID) // the credential itself is untouched
+	})
+
+	t.Run("CaseChangeOfOwnName", func(t *testing.T) {
+		rename(own.ID, "WORK LAPTOP", http.StatusOK)
+		unittest.AssertExistsAndLoadBean(t, &auth_model.WebAuthnCredential{ID: own.ID, Name: "WORK LAPTOP"})
+	})
+
+	t.Run("InvalidNames", func(t *testing.T) {
+		for _, name := range []string{"", "   ", strings.Repeat("a", 256)} {
+			resp := rename(own.ID, name, http.StatusBadRequest)
+			assert.NotEmpty(t, test.ParseJSONError(resp.Body.Bytes()).ErrorMessage)
+		}
+		unittest.AssertExistsAndLoadBean(t, &auth_model.WebAuthnCredential{ID: own.ID, Name: "WORK LAPTOP"})
+	})
+
+	t.Run("SurroundingSpacesTrimmed", func(t *testing.T) {
+		rename(own.ID, "  Spaced Key  ", http.StatusOK)
+		unittest.AssertExistsAndLoadBean(t, &auth_model.WebAuthnCredential{ID: own.ID, Name: "Spaced Key"})
+		rename(own.ID, "WORK LAPTOP", http.StatusOK) // restore the name the following subtests expect
+	})
+
+	t.Run("NameTaken", func(t *testing.T) {
+		resp := rename(own.ID, "phone key", http.StatusBadRequest)
+		assert.Equal(t, translation.NewLocale("en-US").TrString("settings.webauthn_nickname_taken"), test.ParseJSONError(resp.Body.Bytes()).ErrorMessage)
+		unittest.AssertExistsAndLoadBean(t, &auth_model.WebAuthnCredential{ID: own.ID, Name: "WORK LAPTOP"})
+	})
+
+	t.Run("OtherUsersKey", func(t *testing.T) {
+		rename(other.ID, "Stolen", http.StatusNotFound)
+		unittest.AssertExistsAndLoadBean(t, &auth_model.WebAuthnCredential{ID: other.ID, Name: "Other User Key"})
+	})
+
+	t.Run("NonexistentKey", func(t *testing.T) {
+		rename(99999, "Missing", http.StatusNotFound)
+	})
+}
+
 func TestUserSettingsApplications(t *testing.T) {
 	defer tests.PrepareTestEnv(t)()
```
</details></tool-use>

_**Agent (claude-opus-5 2026-09-15 02:14:28Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Read webauthn model top half

`sed -n '1,120p' models/auth/webauthn.go`

```text
// Copyright 2020 The Gitea Authors. All rights reserved.
// SPDX-License-Identifier: MIT

package auth

import (
	"context"
	"fmt"
	"strings"

	"gitea.dev/models/db"
	"gitea.dev/modules/timeutil"
	"gitea.dev/modules/util"

	"github.com/go-webauthn/webauthn/protocol"
	"github.com/go-webauthn/webauthn/webauthn"
)

// ErrWebAuthnCredentialNotExist represents a "ErrWebAuthnCRedentialNotExist" kind of error.
type ErrWebAuthnCredentialNotExist struct {
	ID           int64
	CredentialID []byte
}

func (err ErrWebAuthnCredentialNotExist) Error() string {
	if len(err.CredentialID) == 0 {
		return fmt.Sprintf("WebAuthn credential does not exist [id: %d]", err.ID)
	}
	return fmt.Sprintf("WebAuthn credential does not exist [credential_id: %x]", err.CredentialID)
}

// Unwrap unwraps this as a ErrNotExist err
func (err ErrWebAuthnCredentialNotExist) Unwrap() error {
	return util.ErrNotExist
}

// IsErrWebAuthnCredentialNotExist checks if an error is a ErrWebAuthnCredentialNotExist.
func IsErrWebAuthnCredentialNotExist(err error) bool {
	_, ok := err.(ErrWebAuthnCredentialNotExist)
	return ok
}

// WebAuthnCredential represents the WebAuthn credential data for a public-key
// credential conformant to WebAuthn Level 1
type WebAuthnCredential struct {
	ID              int64 `xorm:"pk autoincr"`
	Name            string
	LowerName       string `xorm:"unique(s)"`
	UserID          int64  `xorm:"INDEX unique(s)"`
	CredentialID    []byte `xorm:"INDEX VARBINARY(1024)"`
	PublicKey       []byte
	AttestationType string
	AAGUID          []byte
	SignCount       uint32 `xorm:"BIGINT"`
	CloneWarning    bool
	CreatedUnix     timeutil.TimeStamp `xorm:"INDEX created"`
	UpdatedUnix     timeutil.TimeStamp `xorm:"INDEX updated"`
}

func init() {
	db.RegisterModel(new(WebAuthnCredential))
}

// TableName returns a better table name for WebAuthnCredential
func (cred WebAuthnCredential) TableName() string {
	return "webauthn_credential"
}

// UpdateSignCount will update the database value of SignCount
func (cred *WebAuthnCredential) UpdateSignCount(ctx context.Context) error {
	_, err := db.GetEngine(ctx).ID(cred.ID).Cols("sign_count").Update(cred)
	return err
}

// BeforeInsert will be invoked by XORM before updating a record
func (cred *WebAuthnCredential) BeforeInsert() {
	cred.LowerName = strings.ToLower(cred.Name)
}

// BeforeUpdate will be invoked by XORM before updating a record
func (cred *WebAuthnCredential) BeforeUpdate() {
	cred.LowerName = strings.ToLower(cred.Name)
}

// AfterLoad is invoked from XORM after setting the values of all fields of this object.
func (cred *WebAuthnCredential) AfterLoad() {
	cred.LowerName = strings.ToLower(cred.Name)
}

// WebAuthnCredentialList is a list of *WebAuthnCredential
type WebAuthnCredentialList []*WebAuthnCredential

// newCredentialFlagsFromAuthenticatorFlags is copied from https://github.com/go-webauthn/webauthn/pull/337
// to convert protocol.AuthenticatorFlags to webauthn.CredentialFlags
func newCredentialFlagsFromAuthenticatorFlags(flags protocol.AuthenticatorFlags) webauthn.CredentialFlags {
	return webauthn.CredentialFlags{
		UserPresent:    flags.HasUserPresent(),
		UserVerified:   flags.HasUserVerified(),
		BackupEligible: flags.HasBackupEligible(),
		BackupState:    flags.HasBackupState(),
	}
}

// ToCredentials will convert all WebAuthnCredentials to webauthn.Credentials
func (list WebAuthnCredentialList) ToCredentials(defaultAuthFlags ...protocol.AuthenticatorFlags) []webauthn.Credential {
	// TODO: at the moment, Gitea doesn't store or check the flags
	// so we need to use the default flags from the authenticator to make the login validation pass
	// In the future, we should:
	// 1. store the flags when registering the credential
	// 2. provide the stored flags when converting the credentials (for login)
	// 3. for old users, still use this fallback to the default flags
	defAuthFlags := util.OptionalArg(defaultAuthFlags)
	creds := make([]webauthn.Credential, 0, len(list))
	for _, cred := range list {
		creds = append(creds, webauthn.Credential{
			ID:              cred.CredentialID,
			PublicKey:       cred.PublicKey,
			AttestationType: cred.AttestationType,
			Flags:           newCredentialFlagsFromAuthenticatorFlags(defAuthFlags),
			Authenticator: webauthn.Authenticator{
```
</details></tool-use>

_**Agent (claude-opus-5 2026-09-15 02:14:29Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Read webauthn model bottom half

`sed -n '120,230p' models/auth/webauthn.go`

```text
			Authenticator: webauthn.Authenticator{
				AAGUID:       cred.AAGUID,
				SignCount:    cred.SignCount,
				CloneWarning: cred.CloneWarning,
			},
		})
	}
	return creds
}

// GetWebAuthnCredentialsByUID returns all WebAuthn credentials of the given user
func GetWebAuthnCredentialsByUID(ctx context.Context, uid int64) (WebAuthnCredentialList, error) {
	creds := make(WebAuthnCredentialList, 0)
	return creds, db.GetEngine(ctx).Where("user_id = ?", uid).Find(&creds)
}

// ExistsWebAuthnCredentialsForUID returns if the given user has credentials
func ExistsWebAuthnCredentialsForUID(ctx context.Context, uid int64) (bool, error) {
	return db.GetEngine(ctx).Where("user_id = ?", uid).Exist(&WebAuthnCredential{})
}

// GetWebAuthnCredentialByName returns WebAuthn credential by id
func GetWebAuthnCredentialByName(ctx context.Context, uid int64, name string) (*WebAuthnCredential, error) {
	cred := new(WebAuthnCredential)
	if found, err := db.GetEngine(ctx).Where("user_id = ? AND lower_name = ?", uid, strings.ToLower(name)).Get(cred); err != nil {
		return nil, err
	} else if !found {
		return nil, ErrWebAuthnCredentialNotExist{}
	}
	return cred, nil
}

// GetWebAuthnCredentialByID returns WebAuthn credential by id
func GetWebAuthnCredentialByID(ctx context.Context, id int64) (*WebAuthnCredential, error) {
	cred := new(WebAuthnCredential)
	if found, err := db.GetEngine(ctx).ID(id).Get(cred); err != nil {
		return nil, err
	} else if !found {
		return nil, ErrWebAuthnCredentialNotExist{ID: id}
	}
	return cred, nil
}

// HasWebAuthnRegistrationsByUID returns whether a given user has WebAuthn registrations
func HasWebAuthnRegistrationsByUID(ctx context.Context, uid int64) (bool, error) {
	return db.GetEngine(ctx).Where("user_id = ?", uid).Exist(&WebAuthnCredential{})
}

// GetWebAuthnCredentialByCredID returns WebAuthn credential by credential ID
func GetWebAuthnCredentialByCredID(ctx context.Context, userID int64, credID []byte) (*WebAuthnCredential, error) {
	cred := new(WebAuthnCredential)
	if found, err := db.GetEngine(ctx).Where("user_id = ? AND credential_id = ?", userID, credID).Get(cred); err != nil {
		return nil, err
	} else if !found {
		return nil, ErrWebAuthnCredentialNotExist{CredentialID: credID}
	}
	return cred, nil
}

// CreateCredential will create a new WebAuthnCredential from the given Credential
func CreateCredential(ctx context.Context, userID int64, name string, cred *webauthn.Credential) (*WebAuthnCredential, error) {
	c := &WebAuthnCredential{
		UserID:          userID,
		Name:            name,
		CredentialID:    cred.ID,
		PublicKey:       cred.PublicKey,
		AttestationType: cred.AttestationType,
		AAGUID:          cred.Authenticator.AAGUID,
		SignCount:       cred.Authenticator.SignCount,
		CloneWarning:    false,
	}

	if err := db.Insert(ctx, c); err != nil {
		return nil, err
	}
	return c, nil
}

// UpdateCredentialName will rename the WebAuthnCredential with the given id if it belongs to userID
func UpdateCredentialName(ctx context.Context, id, userID int64, name string) error {
	_, err := db.GetEngine(ctx).ID(id).Where("user_id = ?", userID).Cols("name", "lower_name").Update(&WebAuthnCredential{Name: name})
	return err
}

// DeleteCredential will delete WebAuthnCredential
func DeleteCredential(ctx context.Context, id, userID int64) (bool, error) {
	had, err := db.GetEngine(ctx).ID(id).Where("user_id = ?", userID).Delete(&WebAuthnCredential{})
	return had > 0, err
}
```
</details></tool-use>

_**Agent (claude-opus-5 2026-09-15 02:16:12Z)**_

Let me scout a couple more facts inline, then fan this out properly.

_**Agent (claude-opus-5 2026-09-15 02:16:14Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Check CSRF token usage in settings templates

`echo "=== CsrfTokenHtml in settings templates ===" && grep -rn "CsrfTokenHtml" templates/user/settings/security/ templates/user/settings/*.tmpl | head -20; echo; echo "=== form-fetch-action usages ===" && grep -rn "form-fetch-action" templates/ | head -15`

```text
=== CsrfTokenHtml in settings templates ===

=== form-fetch-action usages ===
templates/admin/badge/edit.tmpl:7:				<form class="ui form form-fetch-action" action="./edit" method="post">
templates/admin/packages/list.tmpl:91:<form class="ui small modal form-fetch-action" method="post" id="admin-package-delete-modal">
templates/base/head_navbar.tmpl:163:					<form class="stopwatch-commit form-fetch-action" method="post" action="{{$activeStopwatch.IssueLink}}/times/stopwatch/stop">
templates/base/head_navbar.tmpl:170:					<form class="stopwatch-cancel form-fetch-action" method="post" action="{{$activeStopwatch.IssueLink}}/times/stopwatch/cancel">
templates/admin/repo/list.tmpl:104:<form class="ui small modal form-fetch-action" id="admin-repo-delete-modal" method="post">
templates/admin/badge/new.tmpl:7:			<form class="ui form form-fetch-action" action="{{.Link}}" method="post">
templates/user/settings/security/webauthn.tmpl:42:		<form class="ui form form-fetch-action" method="post">
templates/user/settings/organization.tmpl:41:<form class="ui small modal form-fetch-action" method="post" id="leave-organization">
templates/user/notification/notification_div.tmpl:56:					<form class="notifications-buttons form-fetch-action" action="{{AppSubUrl}}/notifications/status?type={{$.PageType}}&page={{$.Page.Paginater.Current}}&perPage={{$.Page.Paginater.PagingNum}}" method="post"
templates/user/settings/account.tmpl:118:			<form class="ui form ignore-dirty form-fetch-action" action="{{AppSubUrl}}/user/settings/account/delete" method="post">
templates/org/team/members.tmpl:75:<form class="ui small modal form-fetch-action" method="post" id="remove-team-member">
templates/user/settings/security/twofa.tmpl:12:	<form class="ui form form-fetch-action" action="{{AppSubUrl}}/user/settings/security/two_factor/disable" method="post">
templates/shared/user/blocked_users.tmpl:8:	<form class="ui form form-fetch-action ignore-dirty" action="{{$.Link}}" method="post">
templates/shared/user/blocked_users.tmpl:43:					<form class="form-fetch-action" action="{{$.Link}}" method="post">
templates/shared/user/blocked_users.tmpl:58:		<form class="ui form form-fetch-action" action="{{$.Link}}" method="post">
```
</details></tool-use>

_**Agent (claude-opus-5 2026-09-15 02:16:16Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Find modal data attribute convention

`grep -rn "data-modal-form.url\|data-modal-form\b" templates/ web_src/js/ | head -20; echo "=== modal attr impl ==="; grep -rn "data-modal-" web_src/js/features/common-page.ts web_src/js/modules/*.ts web_src/js/features/*.ts 2>/dev/null | head -20`

```text
templates/admin/packages/list.tmpl:76:									data-modal-form.url="{{$.Link}}/delete?page={{$.Page.Paginater.Current}}&sort={{$.SortType}}&id={{.Version.ID}}"
templates/admin/repo/list.tmpl:89:									data-modal-form.url="{{$.Link}}/delete?page={{$.Page.Paginater.Current}}&sort={{$.SortType}}&id={{.ID}}"
templates/user/settings/organization.tmpl:27:											data-modal-form.url="{{.OrganisationLink}}/members/action/leave?uid={{$.SignedUser.ID}}"
templates/user/settings/security/webauthn.tmpl:21:						data-modal-form.url="{{$.Link}}/webauthn/rename?id={{.ID}}"
templates/org/team/teams.tmpl:40:									data-modal-form.url="{{$.OrgLink}}/teams/{{.LowerName | PathEscape}}/action/leave?uid={{$.SignedUser.ID}}"
templates/org/team/members.tmpl:38:											data-modal-form.url="{{$.OrgLink}}/teams/{{$.Team.LowerName | PathEscape}}/action/remove?uid={{.ID}}"
templates/shared/variables/variable_list.tmpl:6:			data-modal-form.url="{{.Link}}/new"
templates/shared/variables/variable_list.tmpl:42:					data-modal-form.url="{{$.Link}}/{{.ID}}/edit"
templates/shared/secrets/add_list.tmpl:6:			data-modal-form.url="{{.Link}}"
templates/shared/secrets/add_list.tmpl:42:					data-modal-form.url="{{$.Link}}"
templates/repo/commit_page.tmpl:34:										data-modal-form="#create-tag-form">
web_src/js/features/common-button.ts:54:    // To prevent template developers from making mistakes like `data-modal-form.action="?k={{ValueWithSpecialChars}}" (no escaping),
web_src/js/features/common-button.ts:55:    // here we use `data-modal-form.url="?k={{ValueWithSpecialChars}}", then the value gets correctly escaped by Golang HTML template.
web_src/js/features/common-button.ts:56:    if (kebabName === 'action') throw new Error(`don't assign element property "action" by value, use "data-modal-form.url" instead`);
web_src/js/features/common-button.ts:86:  // HINT: GOLANG-HTML-TEMPLATE-URL-ESCAPING: Form element's "action" property must be set by "data-modal-form.url"
templates/org/team/sidebar.tmpl:16:					data-modal-form.url="{{$.OrgLink}}/teams/{{$.Team.LowerName | PathEscape}}/action/leave?uid={{$.SignedUser.ID}}"
templates/repo/branch/list.tmpl:203:										<button class="btn interact-bg tw-p-2 show-modal delete-branch-button tw-text-red" data-modal="#delete-branch-modal" data-modal-form.url="{{$.Link}}/delete?name={{.DBBranch.Name}}&page={{$.Page.Paginater.Current}}" data-tooltip-content="{{ctx.Locale.Tr "repo.branch.delete" (.DBBranch.Name)}}" data-modal-name="{{.DBBranch.Name}}">
web_src/js/features/repo-branch.ts:13:      const modalFormName = el.getAttribute('data-modal-form') || '#create-branch-form';
=== modal attr impl ===
web_src/js/features/repo-projects.ts:91:  const attrDataColumnId = 'data-modal-project-column-id';
web_src/js/features/repo-projects.ts:92:  const attrDataColumnTitle = 'data-modal-project-column-title-input';
web_src/js/features/repo-projects.ts:93:  const attrDataColumnColor = 'data-modal-project-column-color-input';
web_src/js/features/common-button.ts:54:    // To prevent template developers from making mistakes like `data-modal-form.action="?k={{ValueWithSpecialChars}}" (no escaping),
web_src/js/features/common-button.ts:55:    // here we use `data-modal-form.url="?k={{ValueWithSpecialChars}}", then the value gets correctly escaped by Golang HTML template.
web_src/js/features/common-button.ts:56:    if (kebabName === 'action') throw new Error(`don't assign element property "action" by value, use "data-modal-form.url" instead`);
web_src/js/features/common-button.ts:78:  // Each "data-modal-{target}" attribute will be filled to target element's value or text-content.
web_src/js/features/common-button.ts:83:  // If there's a ".{prop-name}" part like "data-modal-input.value", the "input" element's "value" property will be set,
web_src/js/features/common-button.ts:84:  // the "prop-name" will be camel-cased to "propName" (e.g.: "data-modal-input.read-only" for "readOnly" property).
web_src/js/features/common-button.ts:86:  // HINT: GOLANG-HTML-TEMPLATE-URL-ESCAPING: Form element's "action" property must be set by "data-modal-form.url"
web_src/js/features/common-button.ts:93:  const modalAttrPrefix = 'data-modal-';
web_src/js/features/repo-branch.ts:13:      const modalFormName = el.getAttribute('data-modal-form') || '#create-branch-form';
web_src/js/features/repo-branch.ts:18:      const fromSpanName = el.getAttribute('data-modal-from-span') || '#modal-create-branch-from-span';
web_src/js/features/common-fetch-action.ts:198:  const dataModalConfirm = el.getAttribute('data-modal-confirm') || '';
web_src/js/features/common-fetch-action.ts:208:    const modalConfirmContent = dataModalConfirm || el.getAttribute('data-modal-confirm-content') || '';
web_src/js/features/common-fetch-action.ts:212:        header: el.getAttribute('data-modal-confirm-header') || '',
web_src/js/features/common-fetch-action.ts:392:  // * data-modal-confirm: a "confirm modal dialog" will be shown before taking action.
```
</details></tool-use>

_**Agent (claude-opus-5 2026-09-15 02:16:27Z)**_

Scoping to the diff only, adversarially. Fanning out now.

_**Agent (claude-opus-5 2026-09-15 02:19:29Z)**_

<tool-use data-tool-type="unknown" data-tool-name="Workflow"><details>
<summary>Tool use: **Workflow**</summary>
Adversarial multi-lens review of passkey rename diff

```text
Workflow launched in background. Task ID: wdswt5slj
Summary: Adversarially attack the WebAuthn passkey-rename diff (changed lines only) across 8 lenses, then verify each finding with skeptics
Transcript dir: /home/mantmant/.claude/projects/-home-mantmant-Documents-CSCI435-gitea/f1c2aa49-dab6-4b47-9277-85993d369c13/subagents/workflows/wf_a3b632f3-d0d
Script file: /home/mantmant/.claude/projects/-home-mantmant-Documents-CSCI435-gitea/f1c2aa49-dab6-4b47-9277-85993d369c13/workflows/scripts/passkey-rename-adversarial-wf_a3b632f3-d0d.js
(Edit this file with Write/Edit and re-invoke Workflow with {scriptPath: "/home/mantmant/.claude/projects/-home-mantmant-Documents-CSCI435-gitea/f1c2aa49-dab6-4b47-9277-85993d369c13/workflows/scripts/passkey-rename-adversarial-wf_a3b632f3-d0d.js"} to iterate without resending the script.)
Run ID: wf_a3b632f3-d0d
To resume after editing the script: Workflow({scriptPath: "/home/mantmant/.claude/projects/-home-mantmant-Documents-CSCI435-gitea/f1c2aa49-dab6-4b47-9277-85993d369c13/workflows/scripts/passkey-rename-adversarial-wf_a3b632f3-d0d.js", resumeFromRunId: "wf_a3b632f3-d0d"}) — completed agents return cached results (cached results may themselves be empty — inspect journal.jsonl before assuming there is something to recover).

You will be notified when it completes. Use /workflows to watch live progress.
```
</details></tool-use>

_**Agent - sidechain (2026-09-15 02:19:31Z)**_

REPO: /home/mantmant/Documents/CSCI435/gitea
This is a FORK of Gitea. The Go module path is "gitea.dev", NOT "code.gitea.io/gitea".
BRANCH UNDER REVIEW: feat/issue-17-rename-passkeys (currently checked out).

See the change with:  git diff $(git merge-base main HEAD)..HEAD
Two commits: aff9a20450 "feat(user): allow renaming security keys", 98e73f51f3 "fix(user): reject whitespace-only security key nicknames".

FEATURE: let a signed-in user rename their own WebAuthn security key (passkey) from /user/settings/security.
Files touched:
  models/auth/webauthn.go                         -> new UpdateCredentialName()
  models/auth/webauthn_test.go                    -> new TestUpdateCredentialName
  routers/web/user/setting/security/webauthn.go   -> new WebauthnRename() handler
  routers/web/web.go                              -> new POST /user/settings/security/webauthn/rename route
  services/forms/user_form.go                     -> new WebauthnRenameForm
  templates/user/settings/security/webauthn.tmpl  -> rename button + shared modal
  options/locale/locale_en-US.json                -> 3 new keys
  tests/integration/user_settings_test.go         -> new TestUserSettingsWebAuthnRename

=== SCOPE — THE USER WAS EXPLICIT ===
Attack THE CHANGES ONLY. Pre-existing Gitea defects, pre-existing design choices, and the general
state of the WebAuthn subsystem are OUT OF SCOPE and must NOT be reported.
A pre-existing issue is in scope ONLY if this diff newly reaches it, newly widens it, or newly depends
on it being safe. If you report one, you MUST state explicitly which added line newly exposes it.

=== ALREADY ESTABLISHED (do not re-derive; DO challenge if you believe it is wrong) ===
- WebAuthnCredential.LowerName is xorm:"unique(s)" and UserID is xorm:"INDEX unique(s)", so there IS a
  DB unique index over (lower_name, user_id).
- BeforeUpdate() sets LowerName = strings.ToLower(Name); UpdateCredentialName passes Cols("name","lower_name").
- The new unit test does exercise the cross-user case (renaming user 32 credential as user 2 is a no-op).
- data-modal-form.url IS a real supported convention (web_src/js/features/common-button.ts:54-56).
- No settings template anywhere in this repo uses {{.CsrfTokenHtml}}, so CSRF for form-fetch-action forms
  is presumably injected by JS. CONFIRM the mechanism in web_src/js before claiming anything about CSRF.

=== RULES ===
- READ-ONLY. Never edit, stage, commit, or checkout. Read-only commands (git, grep, cat, go build, go vet,
  go test) are encouraged.
- Ground EVERY claim in a file:line you actually opened, and quote the line. A claim you cannot anchor to
  real code you read is a hallucination — drop it.
- Report ONLY defects, risks, and gaps. No praise, no summary of what the code does.
- Do not report style preferences unless this repo demonstrably does it differently elsewhere; cite the
  counter-example as file:line.
- Prefer few high-confidence findings over many speculative ones. An empty findings list is a valid answer.

========================================
LENS: divergence from this repo own conventions, in the changed lines only. Every claim needs a
counter-example file:line from the existing codebase.

- ERROR MESSAGE CONSTRUCTION. The handler builds a message by string-concatenating two locale strings:
  ctx.Locale.TrString("settings.webauthn_nickname") + ctx.Locale.TrString("form.require_error").
  Find how the rest of this codebase produces a "field is required" message (grep for form.require_error and
  for the middleware.Validate error rendering). Is manual concatenation done anywhere else? Does it produce a
  correct sentence in the en-US locale (look up both strings verbatim in options/locale/locale_en-US.json)?
  Is it i18n-safe for languages with different word order? Could the binding layer have produced this message
  itself if the form used a different validator?
- Note the mix of ctx.Locale.TrString(...) in one error and ctx.Tr(...) in another, within the same handler.
  Which is the convention for JSONError in this repo?
- LOCALE KEYS. Check the 3 new keys in options/locale/locale_en-US.json: naming against neighbouring
  settings.webauthn_* keys, placement, capitalisation and punctuation style vs adjacent values, and whether an
  existing key already says the same thing (search for an existing "already exists"/"taken" style message and
  for an existing rename label). Is settings.delete_key vs settings.webauthn_delete_key inconsistency relevant
  to how the new button labels itself?
- HANDLER SHAPE. Compare WebauthnRename line-by-line against WebauthnDelete and WebauthnRegisterPost in the
  same file: ordering of the feature gate vs the binding-error check, use of ctx.HasError() with web.Bind,
  whether other JSON handlers in this repo check ctx.HasError() at all or rely on the bind middleware, and the
  choice of ctx.JSONErrorNotFound vs ctx.NotFound. Verify ctx.JSONErrorNotFound actually exists and returns
  the status the integration test expects.
- FORM STRUCT. Compare WebauthnRenameForm and its Validate method against the sibling forms in
  services/forms/user_form.go. Is the boilerplate Validate identical to the others? Is MaxSize(255) the value
  the siblings use for the same column? Is the locale: tag right?
- TEMPLATE. Compare the new button and modal against the delete button directly beneath it and against another
  rename-style modal in this repo (templates/shared/secrets/add_list.tmpl, templates/shared/variables/). Check:
  button size/colour classes, svg sizing argument, whether other modals include a cancel button, label/for
  wiring, aria attributes, and whether the shared-modal pattern here matches the repo pattern.
- AGENTS.md rules relevant to the diff: trailing whitespace, copyright year on new .go files (are there any?),
  conventional commit subjects for the two commits, and the Assisted-by trailer requirement. Check the two
  commit messages with git log --format=full.

_**Agent (claude-opus-5 2026-09-15 02:19:32Z)**_

While that runs, let me independently check the two hottest leads myself.

_**Agent - sidechain (2026-09-15 02:19:32Z)**_

REPO: /home/mantmant/Documents/CSCI435/gitea
This is a FORK of Gitea. The Go module path is "gitea.dev", NOT "code.gitea.io/gitea".
BRANCH UNDER REVIEW: feat/issue-17-rename-passkeys (currently checked out).

See the change with:  git diff $(git merge-base main HEAD)..HEAD
Two commits: aff9a20450 "feat(user): allow renaming security keys", 98e73f51f3 "fix(user): reject whitespace-only security key nicknames".

FEATURE: let a signed-in user rename their own WebAuthn security key (passkey) from /user/settings/security.
Files touched:
  models/auth/webauthn.go                         -> new UpdateCredentialName()
  models/auth/webauthn_test.go                    -> new TestUpdateCredentialName
  routers/web/user/setting/security/webauthn.go   -> new WebauthnRename() handler
  routers/web/web.go                              -> new POST /user/settings/security/webauthn/rename route
  services/forms/user_form.go                     -> new WebauthnRenameForm
  templates/user/settings/security/webauthn.tmpl  -> rename button + shared modal
  options/locale/locale_en-US.json                -> 3 new keys
  tests/integration/user_settings_test.go         -> new TestUserSettingsWebAuthnRename

=== SCOPE — THE USER WAS EXPLICIT ===
Attack THE CHANGES ONLY. Pre-existing Gitea defects, pre-existing design choices, and the general
state of the WebAuthn subsystem are OUT OF SCOPE and must NOT be reported.
A pre-existing issue is in scope ONLY if this diff newly reaches it, newly widens it, or newly depends
on it being safe. If you report one, you MUST state explicitly which added line newly exposes it.

=== ALREADY ESTABLISHED (do not re-derive; DO challenge if you believe it is wrong) ===
- WebAuthnCredential.LowerName is xorm:"unique(s)" and UserID is xorm:"INDEX unique(s)", so there IS a
  DB unique index over (lower_name, user_id).
- BeforeUpdate() sets LowerName = strings.ToLower(Name); UpdateCredentialName passes Cols("name","lower_name").
- The new unit test does exercise the cross-user case (renaming user 32 credential as user 2 is a no-op).
- data-modal-form.url IS a real supported convention (web_src/js/features/common-button.ts:54-56).
- No settings template anywhere in this repo uses {{.CsrfTokenHtml}}, so CSRF for form-fetch-action forms
  is presumably injected by JS. CONFIRM the mechanism in web_src/js before claiming anything about CSRF.

=== RULES ===
- READ-ONLY. Never edit, stage, commit, or checkout. Read-only commands (git, grep, cat, go build, go vet,
  go test) are encouraged.
- Ground EVERY claim in a file:line you actually opened, and quote the line. A claim you cannot anchor to
  real code you read is a hallucination — drop it.
- Report ONLY defects, risks, and gaps. No praise, no summary of what the code does.
- Do not report style preferences unless this repo demonstrably does it differently elsewhere; cite the
  counter-example as file:line.
- Prefer few high-confidence findings over many speculative ones. An empty findings list is a valid answer.

========================================
LENS: CSRF and whether the front-end wiring actually works in a real browser.

This is the highest-value lens. Be extremely concrete; read the actual TypeScript.

1) CSRF. The new modal form in templates/user/settings/security/webauthn.tmpl has class "form-fetch-action"
   and NO csrf token in the markup. Read web_src/js/features/common-fetch-action.ts end to end and determine
   exactly how (and whether) a csrf token is attached to a form-fetch-action submission. Then confirm the
   server side: find the CSRF middleware in services/context or routers/web and determine whether this POST
   route is protected. State the mechanism with file:line. If CSRF IS handled, say so plainly — do not invent
   a finding. If it is NOT, that is a high-severity finding with a concrete cross-site POST as the attack.

2) THE PREFILL — likely bug, verify hard. The button carries data-modal-rename-webauthn-nickname="{{.Name}}"
   and the modal input is <input id="rename-webauthn-nickname" ...>. Read web_src/js/features/common-button.ts
   lines ~50-130 and work out EXACTLY how the kebab suffix after "data-modal-" is turned into a selector, and
   whether it matches by id, by class, by name, or something else. Look at the working examples in
   templates/repo/branch/list.tmpl (data-modal-name) and templates/shared/secrets/add_list.tmpl and
   web_src/js/features/repo-projects.ts (attrDataColumnTitle) to see what markup the mechanism actually
   expects on the receiving element. If the selector cannot match an element carrying only an id, the prefill
   silently does nothing and the user gets an EMPTY required field every time they open the rename dialog.
   Report exactly which of the two (attribute name, or the target element markup) is wrong, and what the
   matching convention would be. Also check: does a non-matching data-modal-* attribute throw, warn, or fail
   silently in that code path?

3) MODAL REUSE. There is ONE shared #rename-webauthn-modal reused by every key row. Trace whether state
   (the previously typed value, the previous form action url) is reset between openings. If a user opens the
   dialog for key A, types a name, cancels, then opens it for key B, what is in the field and what is the form
   action? Compare with how the other shared-modal templates in this repo handle it.

4) SUBMIT PATH. Confirm base/modal_actions_confirm with ModalButtonTypes "confirm" actually renders a submit
   button that submits THIS form (read templates/base/modal_actions_confirm.tmpl). Confirm the JSONRedirect
   response the handler returns is understood by common-fetch-action.ts, and that ctx.Flash.Success set before
   a JSONRedirect actually survives to be rendered after the redirect (check how other JSON handlers in this
   repo pair Flash with JSONRedirect).

5) The button is a bare <button> inside a form-less container but ALSO inside the page. Confirm it cannot
   accidentally submit an enclosing form (check the surrounding markup for a wrapping <form>).

_**Agent - sidechain (2026-09-15 02:19:32Z)**_

REPO: /home/mantmant/Documents/CSCI435/gitea
This is a FORK of Gitea. The Go module path is "gitea.dev", NOT "code.gitea.io/gitea".
BRANCH UNDER REVIEW: feat/issue-17-rename-passkeys (currently checked out).

See the change with:  git diff $(git merge-base main HEAD)..HEAD
Two commits: aff9a20450 "feat(user): allow renaming security keys", 98e73f51f3 "fix(user): reject whitespace-only security key nicknames".

FEATURE: let a signed-in user rename their own WebAuthn security key (passkey) from /user/settings/security.
Files touched:
  models/auth/webauthn.go                         -> new UpdateCredentialName()
  models/auth/webauthn_test.go                    -> new TestUpdateCredentialName
  routers/web/user/setting/security/webauthn.go   -> new WebauthnRename() handler
  routers/web/web.go                              -> new POST /user/settings/security/webauthn/rename route
  services/forms/user_form.go                     -> new WebauthnRenameForm
  templates/user/settings/security/webauthn.tmpl  -> rename button + shared modal
  options/locale/locale_en-US.json                -> 3 new keys
  tests/integration/user_settings_test.go         -> new TestUserSettingsWebAuthnRename

=== SCOPE — THE USER WAS EXPLICIT ===
Attack THE CHANGES ONLY. Pre-existing Gitea defects, pre-existing design choices, and the general
state of the WebAuthn subsystem are OUT OF SCOPE and must NOT be reported.
A pre-existing issue is in scope ONLY if this diff newly reaches it, newly widens it, or newly depends
on it being safe. If you report one, you MUST state explicitly which added line newly exposes it.

=== ALREADY ESTABLISHED (do not re-derive; DO challenge if you believe it is wrong) ===
- WebAuthnCredential.LowerName is xorm:"unique(s)" and UserID is xorm:"INDEX unique(s)", so there IS a
  DB unique index over (lower_name, user_id).
- BeforeUpdate() sets LowerName = strings.ToLower(Name); UpdateCredentialName passes Cols("name","lower_name").
- The new unit test does exercise the cross-user case (renaming user 32 credential as user 2 is a no-op).
- data-modal-form.url IS a real supported convention (web_src/js/features/common-button.ts:54-56).
- No settings template anywhere in this repo uses {{.CsrfTokenHtml}}, so CSRF for form-fetch-action forms
  is presumably injected by JS. CONFIRM the mechanism in web_src/js before claiming anything about CSRF.

=== RULES ===
- READ-ONLY. Never edit, stage, commit, or checkout. Read-only commands (git, grep, cat, go build, go vet,
  go test) are encouraged.
- Ground EVERY claim in a file:line you actually opened, and quote the line. A claim you cannot anchor to
  real code you read is a hallucination — drop it.
- Report ONLY defects, risks, and gaps. No praise, no summary of what the code does.
- Do not report style preferences unless this repo demonstrably does it differently elsewhere; cite the
  counter-example as file:line.
- Prefer few high-confidence findings over many speculative ones. An empty findings list is a valid answer.

========================================
LENS: adversarial test review. Assume the tests are theatre until proven otherwise.

Read models/auth/webauthn_test.go TestUpdateCredentialName and tests/integration/user_settings_test.go
TestUserSettingsWebAuthnRename in full, plus the helpers they use.

For EACH assertion, answer: what specific mutation of the production code would leave this assertion passing?
Then report the mutations that a reasonable bug could produce and that NO test catches. Specifically:
- If the template forgot a csrf token entirely, would the integration test still pass? Read
  NewRequestWithValues / MakeRequest in tests/integration and determine whether they inject _csrf automatically
  from the session. If they do, the test proves nothing about the real browser path — that is a finding.
- If the data-modal-* prefill selector never matches (empty field on open), would ANY test catch it? Is there
  a playwright e2e test covering the modal? Check tests/e2e for the security settings page.
- If UpdateCredentialName wrote lower_name as "" would TestUpdateCredentialName fail? Determine precisely how
  unittest.AssertExistsAndLoadBean builds its query (read the helper). Note the model has AfterLoad() that
  recomputes LowerName in memory — reason about whether that defeats the assertion.
- The subtests share mutable state and run in sequence (Success -> CaseChangeOfOwnName -> InvalidNames ->
  SurroundingSpacesTrimmed, which renames back to "WORK LAPTOP" for later subtests). What breaks if one
  subtest fails, if they are reordered, or if -run selects a single subtest? Is per-test isolation the norm
  in this file (compare the neighbouring tests)?
- The test creates credentials with auth_model.CreateCredential directly rather than through the HTTP API, and
  with fabricated webauthn.Credential{ID: []byte("rename-1")}. Does that leave fixtures polluted for other
  tests, and is that pattern used elsewhere in tests/integration?
- AGENTS.md requires: prefer unit tests where logic is testable in isolation, and sub-2s local runtime for
  integration/e2e tests. Judge the new integration test against both. Which of these subtests could have been
  unit tests?
- What behaviours of the handler have NO test at all? Enumerate them (feature-disabled gate, ServerError paths,
  concurrent-rename collision, unicode-case collision, exactly-255 boundary, etc).

_**Agent - sidechain (2026-09-15 02:19:32Z)**_

REPO: /home/mantmant/Documents/CSCI435/gitea
This is a FORK of Gitea. The Go module path is "gitea.dev", NOT "code.gitea.io/gitea".
BRANCH UNDER REVIEW: feat/issue-17-rename-passkeys (currently checked out).

See the change with:  git diff $(git merge-base main HEAD)..HEAD
Two commits: aff9a20450 "feat(user): allow renaming security keys", 98e73f51f3 "fix(user): reject whitespace-only security key nicknames".

FEATURE: let a signed-in user rename their own WebAuthn security key (passkey) from /user/settings/security.
Files touched:
  models/auth/webauthn.go                         -> new UpdateCredentialName()
  models/auth/webauthn_test.go                    -> new TestUpdateCredentialName
  routers/web/user/setting/security/webauthn.go   -> new WebauthnRename() handler
  routers/web/web.go                              -> new POST /user/settings/security/webauthn/rename route
  services/forms/user_form.go                     -> new WebauthnRenameForm
  templates/user/settings/security/webauthn.tmpl  -> rename button + shared modal
  options/locale/locale_en-US.json                -> 3 new keys
  tests/integration/user_settings_test.go         -> new TestUserSettingsWebAuthnRename

=== SCOPE — THE USER WAS EXPLICIT ===
Attack THE CHANGES ONLY. Pre-existing Gitea defects, pre-existing design choices, and the general
state of the WebAuthn subsystem are OUT OF SCOPE and must NOT be reported.
A pre-existing issue is in scope ONLY if this diff newly reaches it, newly widens it, or newly depends
on it being safe. If you report one, you MUST state explicitly which added line newly exposes it.

=== ALREADY ESTABLISHED (do not re-derive; DO challenge if you believe it is wrong) ===
- WebAuthnCredential.LowerName is xorm:"unique(s)" and UserID is xorm:"INDEX unique(s)", so there IS a
  DB unique index over (lower_name, user_id).
- BeforeUpdate() sets LowerName = strings.ToLower(Name); UpdateCredentialName passes Cols("name","lower_name").
- The new unit test does exercise the cross-user case (renaming user 32 credential as user 2 is a no-op).
- data-modal-form.url IS a real supported convention (web_src/js/features/common-button.ts:54-56).
- No settings template anywhere in this repo uses {{.CsrfTokenHtml}}, so CSRF for form-fetch-action forms
  is presumably injected by JS. CONFIRM the mechanism in web_src/js before claiming anything about CSRF.

=== RULES ===
- READ-ONLY. Never edit, stage, commit, or checkout. Read-only commands (git, grep, cat, go build, go vet,
  go test) are encouraged.
- Ground EVERY claim in a file:line you actually opened, and quote the line. A claim you cannot anchor to
  real code you read is a hallucination — drop it.
- Report ONLY defects, risks, and gaps. No praise, no summary of what the code does.
- Do not report style preferences unless this repo demonstrably does it differently elsewhere; cite the
  counter-example as file:line.
- Prefer few high-confidence findings over many speculative ones. An empty findings list is a valid answer.

========================================
LENS: actually build and run it. You are the empirical check on every other reviewer.

Do these in order and report EXACT output (commands, exit codes, failure text). Do not summarise away errors.
1) go build ./... (and go vet on the three changed Go packages: ./models/auth/ ./routers/web/user/setting/security/ ./services/forms/).
2) Run the new unit test: go test -run "^TestUpdateCredentialName$" ./models/auth/ -v
3) Run the neighbouring existing test to confirm you did not just get a vacuous pass:
   go test -run "^TestCreateCredential$" ./models/auth/ -v
4) MUTATION CHECK (read-only requirement still applies to committed state, but you MAY edit a file and then
   restore it EXACTLY with git checkout -- <file> when done; verify with git status --short that the tree is
   clean again before you finish — this is the one exception to read-only, and you must leave the tree clean):
   temporarily break UpdateCredentialName by dropping "lower_name" from the Cols(...) call, re-run the unit
   test, and report whether it FAILS. Then restore. Repeat by dropping the .Where("user_id = ?", userID) clause
   and report whether the test FAILS. A mutation that does NOT fail the test is a test-coverage finding.
5) Attempt the integration test. Find how this repo runs them (check the Makefile for test-sqlite / test-backend
   targets and any required build tags or TEST_* env vars). Try:
   go test -tags "sqlite sqlite_unlock_notify" -run "^TestUserSettingsWebAuthnRename$" ./tests/integration/ -v
   Give it up to ~8 minutes including build. If it cannot run in this environment, report exactly why (missing
   tag, missing config, missing fixture dir) rather than guessing whether it would pass.
6) Run make lint-go if it completes in reasonable time; if not, run gofmt -l on the changed .go files and report.
7) Report whether the working tree is clean at the end (git status --short).

Report every failure as a finding. If everything passes, report findings: [] and put the evidence in the notes
field. Never claim a test passed that you did not actually see pass.

_**Agent - sidechain (2026-09-15 02:19:32Z)**_

REPO: /home/mantmant/Documents/CSCI435/gitea
This is a FORK of Gitea. The Go module path is "gitea.dev", NOT "code.gitea.io/gitea".
BRANCH UNDER REVIEW: feat/issue-17-rename-passkeys (currently checked out).

See the change with:  git diff $(git merge-base main HEAD)..HEAD
Two commits: aff9a20450 "feat(user): allow renaming security keys", 98e73f51f3 "fix(user): reject whitespace-only security key nicknames".

FEATURE: let a signed-in user rename their own WebAuthn security key (passkey) from /user/settings/security.
Files touched:
  models/auth/webauthn.go                         -> new UpdateCredentialName()
  models/auth/webauthn_test.go                    -> new TestUpdateCredentialName
  routers/web/user/setting/security/webauthn.go   -> new WebauthnRename() handler
  routers/web/web.go                              -> new POST /user/settings/security/webauthn/rename route
  services/forms/user_form.go                     -> new WebauthnRenameForm
  templates/user/settings/security/webauthn.tmpl  -> rename button + shared modal
  options/locale/locale_en-US.json                -> 3 new keys
  tests/integration/user_settings_test.go         -> new TestUserSettingsWebAuthnRename

=== SCOPE — THE USER WAS EXPLICIT ===
Attack THE CHANGES ONLY. Pre-existing Gitea defects, pre-existing design choices, and the general
state of the WebAuthn subsystem are OUT OF SCOPE and must NOT be reported.
A pre-existing issue is in scope ONLY if this diff newly reaches it, newly widens it, or newly depends
on it being safe. If you report one, you MUST state explicitly which added line newly exposes it.

=== ALREADY ESTABLISHED (do not re-derive; DO challenge if you believe it is wrong) ===
- WebAuthnCredential.LowerName is xorm:"unique(s)" and UserID is xorm:"INDEX unique(s)", so there IS a
  DB unique index over (lower_name, user_id).
- BeforeUpdate() sets LowerName = strings.ToLower(Name); UpdateCredentialName passes Cols("name","lower_name").
- The new unit test does exercise the cross-user case (renaming user 32 credential as user 2 is a no-op).
- data-modal-form.url IS a real supported convention (web_src/js/features/common-button.ts:54-56).
- No settings template anywhere in this repo uses {{.CsrfTokenHtml}}, so CSRF for form-fetch-action forms
  is presumably injected by JS. CONFIRM the mechanism in web_src/js before claiming anything about CSRF.

=== RULES ===
- READ-ONLY. Never edit, stage, commit, or checkout. Read-only commands (git, grep, cat, go build, go vet,
  go test) are encouraged.
- Ground EVERY claim in a file:line you actually opened, and quote the line. A claim you cannot anchor to
  real code you read is a hallucination — drop it.
- Report ONLY defects, risks, and gaps. No praise, no summary of what the code does.
- Do not report style preferences unless this repo demonstrably does it differently elsewhere; cite the
  counter-example as file:line.
- Prefer few high-confidence findings over many speculative ones. An empty findings list is a valid answer.

========================================
LENS: persistence correctness — xorm semantics, constraints, races, and data integrity of the new write path.

Attack models/auth/webauthn.go UpdateCredentialName and the handler uniqueness check around it:
- Verify against real xorm source (locate the xorm package in the go module cache) that calling
  .Cols("name","lower_name").Update(&WebAuthnCredential{Name: name}) actually invokes BeforeUpdate() BEFORE the
  SET clause is built. If the hook ran after, lower_name would be written as empty string and the unique index
  would then collide across ALL renamed keys. Read the xorm Update implementation and settle this definitively.
- Note WebAuthnCredential also has an AfterLoad() that recomputes LowerName in memory. Does that mask a broken
  lower_name column from the new unit test assertions? Determine whether unittest.AssertExistsAndLoadBean uses
  the bean fields as SQL WHERE conditions (making it a real DB assertion) or as post-load comparisons (making
  it vacuous). Read the unittest helper source and settle it.
- UpdatedUnix is xorm:"INDEX updated". With an explicit Cols(...) list that omits updated_unix, does xorm still
  bump it? Check whether the repo cares (compare UpdateSignCount in the same file).
- TOCTOU: the handler does a SELECT-then-UPDATE with no transaction. Two concurrent renames to the same new
  name pass the check and one hits the DB unique index. What does the user see? Trace the error path: does the
  raw DB error reach ctx.ServerError and produce a 500 + logged stack? Compare with how CreateCredential
  collisions are handled during registration in the same repo. Is db.WithTx used by comparable write paths?
- The uniqueness check compares against GetWebAuthnCredentialByName which matches on lower_name. Are there
  name pairs that the Go-side check treats as distinct but the DB unique index treats as equal, or vice versa?
  Think about strings.ToLower vs the DB collation (sqlite NOCASE / MySQL utf8mb4_general_ci / Postgres) and
  about Unicode: Turkish dotless i, Kelvin sign U+212A vs K, fullwidth forms, combining marks. Check what
  collation this repo creates the column with (look in models/migrations and models/db).
- MaxSize(255) is validated BEFORE the trim, and the DB column is the xorm default VARCHAR(255). Does MaxSize
  count bytes or runes in this binding library? If runes, can a 255-rune multibyte name exceed 255 BYTES and
  get truncated or rejected by the DB? Find the MaxSize implementation and settle it.

_**Agent - sidechain (2026-09-15 02:19:32Z)**_

REPO: /home/mantmant/Documents/CSCI435/gitea
This is a FORK of Gitea. The Go module path is "gitea.dev", NOT "code.gitea.io/gitea".
BRANCH UNDER REVIEW: feat/issue-17-rename-passkeys (currently checked out).

See the change with:  git diff $(git merge-base main HEAD)..HEAD
Two commits: aff9a20450 "feat(user): allow renaming security keys", 98e73f51f3 "fix(user): reject whitespace-only security key nicknames".

FEATURE: let a signed-in user rename their own WebAuthn security key (passkey) from /user/settings/security.
Files touched:
  models/auth/webauthn.go                         -> new UpdateCredentialName()
  models/auth/webauthn_test.go                    -> new TestUpdateCredentialName
  routers/web/user/setting/security/webauthn.go   -> new WebauthnRename() handler
  routers/web/web.go                              -> new POST /user/settings/security/webauthn/rename route
  services/forms/user_form.go                     -> new WebauthnRenameForm
  templates/user/settings/security/webauthn.tmpl  -> rename button + shared modal
  options/locale/locale_en-US.json                -> 3 new keys
  tests/integration/user_settings_test.go         -> new TestUserSettingsWebAuthnRename

=== SCOPE — THE USER WAS EXPLICIT ===
Attack THE CHANGES ONLY. Pre-existing Gitea defects, pre-existing design choices, and the general
state of the WebAuthn subsystem are OUT OF SCOPE and must NOT be reported.
A pre-existing issue is in scope ONLY if this diff newly reaches it, newly widens it, or newly depends
on it being safe. If you report one, you MUST state explicitly which added line newly exposes it.

=== ALREADY ESTABLISHED (do not re-derive; DO challenge if you believe it is wrong) ===
- WebAuthnCredential.LowerName is xorm:"unique(s)" and UserID is xorm:"INDEX unique(s)", so there IS a
  DB unique index over (lower_name, user_id).
- BeforeUpdate() sets LowerName = strings.ToLower(Name); UpdateCredentialName passes Cols("name","lower_name").
- The new unit test does exercise the cross-user case (renaming user 32 credential as user 2 is a no-op).
- data-modal-form.url IS a real supported convention (web_src/js/features/common-button.ts:54-56).
- No settings template anywhere in this repo uses {{.CsrfTokenHtml}}, so CSRF for form-fetch-action forms
  is presumably injected by JS. CONFIRM the mechanism in web_src/js before claiming anything about CSRF.

=== RULES ===
- READ-ONLY. Never edit, stage, commit, or checkout. Read-only commands (git, grep, cat, go build, go vet,
  go test) are encouraged.
- Ground EVERY claim in a file:line you actually opened, and quote the line. A claim you cannot anchor to
  real code you read is a hallucination — drop it.
- Report ONLY defects, risks, and gaps. No praise, no summary of what the code does.
- Do not report style preferences unless this repo demonstrably does it differently elsewhere; cite the
  counter-example as file:line.
- Prefer few high-confidence findings over many speculative ones. An empty findings list is a valid answer.

========================================
LENS: authorization, IDOR, and privilege boundaries — attack the new rename path.

Attack the new WebauthnRename handler and UpdateCredentialName as an authenticated attacker trying to
touch a credential that is not theirs, or to act when they should not be allowed to act at all. Work through:
- Read routers/web/user/setting/security/webauthn.go fully. Trace every branch of WebauthnRename.
- Compare its guards against the sibling WebauthnDelete and WebauthnRegisterPost handlers in the SAME file.
  Any guard the siblings have that rename lacks is a finding. Any guard rename has that is weaker is a finding.
- Read routers/web/web.go around the new route registration (~line 652). What middleware chain does this
  route inherit? Walk OUT to the enclosing m.Group calls and confirm every middleware the sibling routes get.
  Is the route inside the same auth/reqSignIn/2FA group? Does the group ordering matter?
- The handler resolves the credential with ctx.FormInt64("id") from the QUERY STRING while binding the form
  from the BODY. Is there any way to make those disagree, or to smuggle an "id" the handler does not expect?
  Check how ctx.FormInt64 resolves a parameter present in BOTH query and body in this codebase.
- UpdateCredentialName uses .ID(id).Where("user_id = ?", userID). Verify against real xorm source (find it
  under the go module cache or vendor dir) that ID() and Where() are ANDed and that neither is dropped when
  Cols() is also set. If the handler already checked ownership, is the model-level check load-bearing anywhere?
- Is there any way to reach UpdateCredentialName with a userID that is not the doer?
- Does anything let a user rename a credential belonging to a DISABLED/prohibited-login account, or rename
  while impersonating/admin-acting-as another user? Check how ctx.Doer is set for this route group.

_**Agent - sidechain (2026-09-15 02:19:32Z)**_

REPO: /home/mantmant/Documents/CSCI435/gitea
This is a FORK of Gitea. The Go module path is "gitea.dev", NOT "code.gitea.io/gitea".
BRANCH UNDER REVIEW: feat/issue-17-rename-passkeys (currently checked out).

See the change with:  git diff $(git merge-base main HEAD)..HEAD
Two commits: aff9a20450 "feat(user): allow renaming security keys", 98e73f51f3 "fix(user): reject whitespace-only security key nicknames".

FEATURE: let a signed-in user rename their own WebAuthn security key (passkey) from /user/settings/security.
Files touched:
  models/auth/webauthn.go                         -> new UpdateCredentialName()
  models/auth/webauthn_test.go                    -> new TestUpdateCredentialName
  routers/web/user/setting/security/webauthn.go   -> new WebauthnRename() handler
  routers/web/web.go                              -> new POST /user/settings/security/webauthn/rename route
  services/forms/user_form.go                     -> new WebauthnRenameForm
  templates/user/settings/security/webauthn.tmpl  -> rename button + shared modal
  options/locale/locale_en-US.json                -> 3 new keys
  tests/integration/user_settings_test.go         -> new TestUserSettingsWebAuthnRename

=== SCOPE — THE USER WAS EXPLICIT ===
Attack THE CHANGES ONLY. Pre-existing Gitea defects, pre-existing design choices, and the general
state of the WebAuthn subsystem are OUT OF SCOPE and must NOT be reported.
A pre-existing issue is in scope ONLY if this diff newly reaches it, newly widens it, or newly depends
on it being safe. If you report one, you MUST state explicitly which added line newly exposes it.

=== ALREADY ESTABLISHED (do not re-derive; DO challenge if you believe it is wrong) ===
- WebAuthnCredential.LowerName is xorm:"unique(s)" and UserID is xorm:"INDEX unique(s)", so there IS a
  DB unique index over (lower_name, user_id).
- BeforeUpdate() sets LowerName = strings.ToLower(Name); UpdateCredentialName passes Cols("name","lower_name").
- The new unit test does exercise the cross-user case (renaming user 32 credential as user 2 is a no-op).
- data-modal-form.url IS a real supported convention (web_src/js/features/common-button.ts:54-56).
- No settings template anywhere in this repo uses {{.CsrfTokenHtml}}, so CSRF for form-fetch-action forms
  is presumably injected by JS. CONFIRM the mechanism in web_src/js before claiming anything about CSRF.

=== RULES ===
- READ-ONLY. Never edit, stage, commit, or checkout. Read-only commands (git, grep, cat, go build, go vet,
  go test) are encouraged.
- Ground EVERY claim in a file:line you actually opened, and quote the line. A claim you cannot anchor to
  real code you read is a hallucination — drop it.
- Report ONLY defects, risks, and gaps. No praise, no summary of what the code does.
- Do not report style preferences unless this repo demonstrably does it differently elsewhere; cite the
  counter-example as file:line.
- Prefer few high-confidence findings over many speculative ones. An empty findings list is a valid answer.

========================================
LENS: injection and rendering of attacker-controlled key names introduced by this diff.

The rename feature lets a user put arbitrary text (up to 255) into a field that is rendered in HTML and may
flow elsewhere. Before this diff a name could only be set at registration; confirm whether that materially
changes reachability (if registration already allowed arbitrary names, say so and only report what is NEW).

- templates/user/settings/security/webauthn.tmpl now renders the name into an HTML ATTRIBUTE:
  data-modal-rename-webauthn-nickname="{{.Name}}". Confirm Go html/template applies attribute-context escaping
  here and that no |safeHTML / |Str2html / |SafeAttr style pipeline is involved. Check whether the value later
  reaches JS via getAttribute and is then inserted with innerHTML anywhere in common-button.ts (that would turn
  a safe attribute into a DOM-XSS sink). Read the actual assignment code, do not assume.
- Can a name contain control characters, newlines, RTL overrides (U+202E), zero-width joiners, or HTML-looking
  text? What does the settings list look like then, and does the confirm/flash message echo it? Trace whether
  ctx.Flash.Success / the JSONError strings ever embed the user-supplied name (they appear not to — confirm).
- The form action is built as {{$.Link}}/webauthn/rename?id={{.ID}} inside data-modal-form.url. Read the
  comment at web_src/js/features/common-button.ts:54-56 explaining WHY .url exists rather than .action, and
  confirm this specific usage satisfies that requirement. Is $.Link attacker-influenced on this page?
- Trailing/leading whitespace is trimmed but INTERIOR whitespace is not normalized. Can a user create two keys
  that are visually identical in the settings list (double space, NBSP U+00A0, tab) and defeat the uniqueness
  check? That is a real spoofing concern for a security-key list where the name is the only way a user tells
  their keys apart. Verify the uniqueness check would indeed let "My  Key" and "My Key" coexist.
- Does the 255-char name break the settings page layout or the modal? Low severity, but check the CSS classes
  used on the row.

_**Agent (claude-opus-5 2026-09-15 02:19:33Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Read show-modal data attribute handling

`sed -n '40,140p' web_src/js/features/common-button.ts`

```text
  }
  throw new Error('no panel to hide'); // should never happen, otherwise there is a bug in code
}

export type ElementWithAssignableProperties = {
  nodeName: string;
  getAttribute: (name: string) => string | null;
  setAttribute: (name: string, value: string) => void;
} & Record<string, any>;

export function assignElementProperty(el: ElementWithAssignableProperties, kebabName: string, val: string) {
  if (el.nodeName === 'FORM') {
    // HINT: GOLANG-HTML-TEMPLATE-URL-ESCAPING: a special case for Golang HTML template escaping.
    // Golang HTML template only handles some "known" attribute names as URL (e.g.: when the name is "action" or contains "url")
    // To prevent template developers from making mistakes like `data-modal-form.action="?k={{ValueWithSpecialChars}}" (no escaping),
    // here we use `data-modal-form.url="?k={{ValueWithSpecialChars}}", then the value gets correctly escaped by Golang HTML template.
    if (kebabName === 'action') throw new Error(`don't assign element property "action" by value, use "data-modal-form.url" instead`);
    if (kebabName === 'url') kebabName = 'action';
  }
  const camelizedName = camelize(kebabName);
  const old = el[camelizedName];
  if (typeof old === 'boolean') {
    el[camelizedName] = val === 'true';
  } else if (typeof old === 'number') {
    el[camelizedName] = parseFloat(val);
  } else if (typeof old === 'string') {
    el[camelizedName] = val;
  } else if (old?.nodeName) {
    // "form" has an edge case: its "<input name=action>" element overwrites the "action" property, we can only set attribute
    el.setAttribute(kebabName, val);
  } else {
    // in the future, maybe we could introduce a better typing system if it is really needed
    throw new Error(`cannot assign element property "${camelizedName}" by value "${val}"`);
  }
}

function onShowModalClick(el: HTMLElement, e: MouseEvent) {
  // A ".show-modal" button will show a modal dialog defined by its "data-modal" attribute.
  // Each "data-modal-{target}" attribute will be filled to target element's value or text-content.
  // * First, try to query '#target'
  // * Then, try to query '[name=target]'
  // * Then, try to query '.target'
  // * Then, try to query 'target' as HTML tag
  // If there's a ".{prop-name}" part like "data-modal-input.value", the "input" element's "value" property will be set,
  // the "prop-name" will be camel-cased to "propName" (e.g.: "data-modal-input.read-only" for "readOnly" property).
  //
  // HINT: GOLANG-HTML-TEMPLATE-URL-ESCAPING: Form element's "action" property must be set by "data-modal-form.url"
  // to make the template variables get correctly escaped in the URL.
  e.preventDefault();
  const modalSelector = el.getAttribute('data-modal')!;
  const elModal = document.querySelector(modalSelector);
  if (!elModal) throw new Error('no modal for this action');

  const modalAttrPrefix = 'data-modal-';
  for (const attrib of el.attributes) {
    if (!attrib.name.startsWith(modalAttrPrefix)) {
      continue;
    }

    const attrTargetCombo = attrib.name.substring(modalAttrPrefix.length);
    const [attrTargetName, attrTargetProp] = attrTargetCombo.split('.');
    // try to find target by: "#target" -> "[name=target]" -> ".target" -> "<target> tag", and then try the modal itself
    const attrTarget = elModal.querySelector(`#${attrTargetName}`) ||
      elModal.querySelector(`[name=${CSS.escape(attrTargetName)}]`) ||
      elModal.querySelector(`.${attrTargetName}`) ||
      elModal.querySelector(attrTargetName) ||
      (elModal.matches(attrTargetName) || elModal.matches(`#${attrTargetName}`) || elModal.matches(`.${attrTargetName}`) ? elModal : null);
    if (!attrTarget) {
      if (!window.config.runModeIsProd) throw new Error(`attr target "${attrTargetCombo}" not found for modal`);
      continue;
    }

    if (attrTargetProp) {
      assignElementProperty(attrTarget, attrTargetProp, attrib.value);
    } else if (attrTarget.matches('input, textarea')) {
      (attrTarget as HTMLInputElement | HTMLTextAreaElement).value = attrib.value; // FIXME: add more supports like checkbox
    } else {
      attrTarget.textContent = attrib.value; // FIXME: it should be more strict here, only handle div/span/p
    }
  }

  showFomanticModal(elModal);
}

export function initGlobalButtons(): void {
  // There are many "cancel button" elements in modal dialogs, Fomantic UI expects they are button-like elements but never submit a form.
  // However, Gitea misuses the modal dialog and put the cancel buttons inside forms, so we must prevent the form submission.
  // There are a few cancel buttons in non-modal forms, and there are some dynamically created forms (eg: the "Edit Issue Content")
  addDelegatedEventListener(document, 'click', 'form button.ui.cancel.button', (_ /* el */, e) => e.preventDefault());

  // Ideally these "button" events should be handled by registerGlobalEventFunc
  // Refactoring would involve too many changes, so at the moment, just use the global event listener.
  addDelegatedEventListener(document, 'click', '.show-panel, .hide-panel, .show-modal', (el, e: MouseEvent) => {
    if (el.classList.contains('show-panel')) {
      onShowPanelClick(el, e);
    } else if (el.classList.contains('hide-panel')) {
      onHidePanelClick(el, e);
    } else if (el.classList.contains('show-modal')) {
      onShowModalClick(el, e);
    }
  });
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:19:33Z)**_

I'll start by reading the diff and the changed files.

_**Agent - sidechain (2026-09-15 02:19:34Z)**_

REPO: /home/mantmant/Documents/CSCI435/gitea
This is a FORK of Gitea. The Go module path is "gitea.dev", NOT "code.gitea.io/gitea".
BRANCH UNDER REVIEW: feat/issue-17-rename-passkeys (currently checked out).

See the change with:  git diff $(git merge-base main HEAD)..HEAD
Two commits: aff9a20450 "feat(user): allow renaming security keys", 98e73f51f3 "fix(user): reject whitespace-only security key nicknames".

FEATURE: let a signed-in user rename their own WebAuthn security key (passkey) from /user/settings/security.
Files touched:
  models/auth/webauthn.go                         -> new UpdateCredentialName()
  models/auth/webauthn_test.go                    -> new TestUpdateCredentialName
  routers/web/user/setting/security/webauthn.go   -> new WebauthnRename() handler
  routers/web/web.go                              -> new POST /user/settings/security/webauthn/rename route
  services/forms/user_form.go                     -> new WebauthnRenameForm
  templates/user/settings/security/webauthn.tmpl  -> rename button + shared modal
  options/locale/locale_en-US.json                -> 3 new keys
  tests/integration/user_settings_test.go         -> new TestUserSettingsWebAuthnRename

=== SCOPE — THE USER WAS EXPLICIT ===
Attack THE CHANGES ONLY. Pre-existing Gitea defects, pre-existing design choices, and the general
state of the WebAuthn subsystem are OUT OF SCOPE and must NOT be reported.
A pre-existing issue is in scope ONLY if this diff newly reaches it, newly widens it, or newly depends
on it being safe. If you report one, you MUST state explicitly which added line newly exposes it.

=== ALREADY ESTABLISHED (do not re-derive; DO challenge if you believe it is wrong) ===
- WebAuthnCredential.LowerName is xorm:"unique(s)" and UserID is xorm:"INDEX unique(s)", so there IS a
  DB unique index over (lower_name, user_id).
- BeforeUpdate() sets LowerName = strings.ToLower(Name); UpdateCredentialName passes Cols("name","lower_name").
- The new unit test does exercise the cross-user case (renaming user 32 credential as user 2 is a no-op).
- data-modal-form.url IS a real supported convention (web_src/js/features/common-button.ts:54-56).
- No settings template anywhere in this repo uses {{.CsrfTokenHtml}}, so CSRF for form-fetch-action forms
  is presumably injected by JS. CONFIRM the mechanism in web_src/js before claiming anything about CSRF.

=== RULES ===
- READ-ONLY. Never edit, stage, commit, or checkout. Read-only commands (git, grep, cat, go build, go vet,
  go test) are encouraged.
- Ground EVERY claim in a file:line you actually opened, and quote the line. A claim you cannot anchor to
  real code you read is a hallucination — drop it.
- Report ONLY defects, risks, and gaps. No praise, no summary of what the code does.
- Do not report style preferences unless this repo demonstrably does it differently elsewhere; cite the
  counter-example as file:line.
- Prefer few high-confidence findings over many speculative ones. An empty findings list is a valid answer.

========================================
LENS: passkey/WebAuthn-specific security semantics. The user specifically asked for extra care because this
touches passkeys.

- Prove or disprove that the rename path can touch credential MATERIAL. Read UpdateCredentialName and confirm
  the Cols() list cannot cause xorm to write zero values into credential_id, public_key, sign_count,
  attestation_type, aaguid, or clone_warning. What exactly happens to columns NOT in Cols()? Check the xorm
  source. A bug here would silently brick a users second factor, so verify it rather than assuming.
- Is the credential name security-relevant anywhere beyond display? Grep the whole repo for reads of
  WebAuthnCredential.Name / LowerName / GetWebAuthnCredentialByName and check every call site: login flows
  (routers/web/auth/webauthn.go), the API, notifications, audit. Does renaming break, confuse, or bypass any
  of them? In particular, does registration rely on name lookup in a way a rename can now defeat (for example,
  can a user rename key A to free up a name and create a confusing duplicate-ish state)?
- The handler gates on user_model.IsFeatureDisabledWithLoginType(ctx.Doer, setting.UserFeatureManageMFA).
  Confirm this is the same gate the delete/register siblings use and that it is the CORRECT gate for a rename
  (read the setting definition). Is a rename "managing MFA"? Would an admin expect a user with MFA management
  disabled to still be able to rename?
- Security-sensitive account changes in Gitea sometimes notify the user by email or record an audit entry.
  Determine what THIS repo does on add/remove of a security key (grep for mailer/notify calls near the
  webauthn register and delete handlers, and any audit/log package). If add and delete notify but rename does
  not, that is a gap worth reporting; if none of them notify, it is pre-existing and OUT OF SCOPE.
- Does upstream Gitea already have a passkey-rename feature or a rejected proposal for one? Check git log/tags
  and any API routes (routers/api/v1) for an existing rename endpoint this web handler should be consistent
  with, or duplicates.
- Does the change alter anything about how many keys a user has, or any "last second factor" invariant? A
  rename should not, but confirm the handler cannot delete/insert under any branch.

_**Agent - sidechain (claude-opus-5 2026-09-15 02:19:34Z)**_

I'll start by reading the diff and the files it touches.

_**Agent - sidechain (claude-opus-5 2026-09-15 02:19:34Z)**_

I'll work through the build/test/mutation checks in order.

_**Agent - sidechain (claude-opus-5 2026-09-15 02:19:46Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Show the full diff under review

`git diff $(git merge-base main HEAD)..HEAD`

```text
diff --git a/models/auth/webauthn.go b/models/auth/webauthn.go
index 11297556d2..b37e9f51ef 100644
--- a/models/auth/webauthn.go
+++ b/models/auth/webauthn.go
@@ -195,6 +195,12 @@ func CreateCredential(ctx context.Context, userID int64, name string, cred *weba
 	return c, nil
 }
 
+// UpdateCredentialName will rename the WebAuthnCredential with the given id if it belongs to userID
+func UpdateCredentialName(ctx context.Context, id, userID int64, name string) error {
+	_, err := db.GetEngine(ctx).ID(id).Where("user_id = ?", userID).Cols("name", "lower_name").Update(&WebAuthnCredential{Name: name})
+	return err
+}
+
 // DeleteCredential will delete WebAuthnCredential
 func DeleteCredential(ctx context.Context, id, userID int64) (bool, error) {
 	had, err := db.GetEngine(ctx).ID(id).Where("user_id = ?", userID).Delete(&WebAuthnCredential{})
diff --git a/models/auth/webauthn_test.go b/models/auth/webauthn_test.go
index a648319226..d7d848f71e 100644
--- a/models/auth/webauthn_test.go
+++ b/models/auth/webauthn_test.go
@@ -64,3 +64,14 @@ func TestCreateCredential(t *testing.T) {
 
 	unittest.AssertExistsAndLoadBean(t, &auth_model.WebAuthnCredential{Name: "WebAuthn Created Credential", UserID: 1})
 }
+
+func TestUpdateCredentialName(t *testing.T) {
+	assert.NoError(t, unittest.PrepareTestDatabase())
+
+	// credential 1 belongs to user 32, so another user's update must not change it
+	assert.NoError(t, auth_model.UpdateCredentialName(t.Context(), 1, 2, "Hijacked"))
+	unittest.AssertExistsAndLoadBean(t, &auth_model.WebAuthnCredential{ID: 1, Name: "WebAuthn credential"})
+
+	assert.NoError(t, auth_model.UpdateCredentialName(t.Context(), 1, 32, "Renamed Key"))
+	unittest.AssertExistsAndLoadBean(t, &auth_model.WebAuthnCredential{ID: 1, Name: "Renamed Key", LowerName: "renamed key", UserID: 32})
+}
diff --git a/options/locale/locale_en-US.json b/options/locale/locale_en-US.json
index 9373aa83a9..694d0bf99b 100644
--- a/options/locale/locale_en-US.json
+++ b/options/locale/locale_en-US.json
@@ -919,6 +919,9 @@
   "settings.webauthn_desc": "Security keys are hardware devices containing cryptographic keys. They can be used for two-factor authentication. Security keys must support the <a rel=\"noreferrer\" target=\"_blank\" href=\"%s\">WebAuthn Authenticator</a> standard.",
   "settings.webauthn_register_key": "Add Security Key",
   "settings.webauthn_nickname": "Nickname",
+  "settings.webauthn_rename_key": "Rename Security Key",
+  "settings.webauthn_rename_key_success": "The security key has been renamed.",
+  "settings.webauthn_nickname_taken": "Another security key already uses this nickname.",
   "settings.webauthn_delete_key": "Remove Security Key",
   "settings.webauthn_delete_key_desc": "If you remove a security key, you can no longer sign in with it. Continue?",
   "settings.webauthn_key_loss_warning": "If you lose your security keys, you will lose access to your account.",
diff --git a/routers/web/user/setting/security/webauthn.go b/routers/web/user/setting/security/webauthn.go
index 4db7b47879..5e93abbbf1 100644
--- a/routers/web/user/setting/security/webauthn.go
+++ b/routers/web/user/setting/security/webauthn.go
@@ -7,6 +7,7 @@ import (
 	"errors"
 	"net/http"
 	"strconv"
+	"strings"
 	"time"
 
 	"gitea.dev/models/auth"
@@ -125,6 +126,52 @@ func WebauthnRegisterPost(ctx *context.Context) {
 	ctx.JSON(http.StatusCreated, cred)
 }
 
+// WebauthnRename renames a security key by id
+func WebauthnRename(ctx *context.Context) {
+	if user_model.IsFeatureDisabledWithLoginType(ctx.Doer, setting.UserFeatureManageMFA) {
+		ctx.HTTPError(http.StatusNotFound)
+		return
+	}
+
+	if ctx.HasError() { // form binding validation error
+		ctx.JSONError(ctx.GetErrMsg())
+		return
+	}
+
+	cred, err := auth.GetWebAuthnCredentialByID(ctx, ctx.FormInt64("id"))
+	if err != nil && !auth.IsErrWebAuthnCredentialNotExist(err) {
+		ctx.ServerError("GetWebAuthnCredentialByID", err)
+		return
+	}
+	if cred == nil || cred.UserID != ctx.Doer.ID { // hide other users' keys as not found
+		ctx.JSONErrorNotFound()
+		return
+	}
+
+	form := web.GetForm(ctx).(*forms.WebauthnRenameForm)
+	form.Name = strings.TrimSpace(form.Name)
+	if form.Name == "" { // "Required" binding only rejects an exactly empty value, not whitespace
+		ctx.JSONError(ctx.Locale.TrString("settings.webauthn_nickname") + ctx.Locale.TrString("form.require_error"))
+		return
+	}
+	sameName, err := auth.GetWebAuthnCredentialByName(ctx, ctx.Doer.ID, form.Name)
+	if err != nil && !auth.IsErrWebAuthnCredentialNotExist(err) {
+		ctx.ServerError("GetWebAuthnCredentialByName", err)
+		return
+	}
+	if sameName != nil && sameName.ID != cred.ID { // renaming a key to its own name with different casing is allowed
+		ctx.JSONError(ctx.Tr("settings.webauthn_nickname_taken"))
+		return
+	}
+
+	if err := auth.UpdateCredentialName(ctx, cred.ID, ctx.Doer.ID, form.Name); err != nil {
+		ctx.ServerError("UpdateCredentialName", err)
+		return
+	}
+	ctx.Flash.Success(ctx.Tr("settings.webauthn_rename_key_success"))
+	ctx.JSONRedirect(setting.AppSubURL + "/user/settings/security")
+}
+
 // WebauthnDelete deletes an security key by id
 func WebauthnDelete(ctx *context.Context) {
 	if user_model.IsFeatureDisabledWithLoginType(ctx.Doer, setting.UserFeatureManageMFA) {
diff --git a/routers/web/web.go b/routers/web/web.go
index 0bfae44e86..f8e48f99fb 100644
--- a/routers/web/web.go
+++ b/routers/web/web.go
@@ -652,6 +652,7 @@ func registerWebRoutes(m *web.Router, webAuth *AuthMiddleware) {
 			m.Group("/webauthn", func() {
 				m.Post("/request_register", web.Bind(forms.WebauthnRegistrationForm{}), security.WebAuthnRegister)
 				m.Post("/register", security.WebauthnRegisterPost)
+				m.Post("/rename", web.Bind(forms.WebauthnRenameForm{}), security.WebauthnRename)
 				m.Post("/delete", security.WebauthnDelete)
 			})
 			m.Group("/openid", func() {
diff --git a/services/forms/user_form.go b/services/forms/user_form.go
index 75275fb5c7..95f3ae0b16 100644
--- a/services/forms/user_form.go
+++ b/services/forms/user_form.go
@@ -419,6 +419,17 @@ func (f *WebauthnRegistrationForm) Validate(req *http.Request, errs binding.Erro
 	return middleware.Validate(errs, ctx.Data, f, ctx.Locale)
 }
 
+// WebauthnRenameForm for renaming an existing WebAuthn credential
+type WebauthnRenameForm struct {
+	Name string `binding:"Required;MaxSize(255)" locale:"settings.webauthn_nickname"` // 255 is the size of the name column
+}
+
+// Validate validates the fields
+func (f *WebauthnRenameForm) Validate(req *http.Request, errs binding.Errors) binding.Errors {
+	ctx := context.GetValidateContext(req)
+	return middleware.Validate(errs, ctx.Data, f, ctx.Locale)
+}
+
 // PackageSettingForm form for package settings
 type PackageSettingForm struct {
 	Action   string
diff --git a/templates/user/settings/security/webauthn.tmpl b/templates/user/settings/security/webauthn.tmpl
index 1bf0159d37..6bfaf11d6b 100644
--- a/templates/user/settings/security/webauthn.tmpl
+++ b/templates/user/settings/security/webauthn.tmpl
@@ -16,6 +16,13 @@
 					</div>
 				</div>
 				<div class="item-trailing">
+					<button class="ui tiny button show-modal"
+						data-modal="#rename-webauthn-modal"
+						data-modal-form.url="{{$.Link}}/webauthn/rename?id={{.ID}}"
+						data-modal-rename-webauthn-nickname="{{.Name}}"
+					>
+						{{svg "octicon-pencil"}} {{ctx.Locale.Tr "settings.webauthn_rename_key"}}
+					</button>
 					<button class="ui red tiny button link-action" data-modal-confirm="#delete-registration" data-url="{{$.Link}}/webauthn/delete?id={{.ID}}">
 					{{ctx.Locale.Tr "settings.delete_key"}}
 					</button>
@@ -30,6 +37,18 @@
 		</div>
 		<button id="register-webauthn" class="ui primary button">{{svg "octicon-key"}} {{ctx.Locale.Tr "settings.webauthn_register_key"}}</button>
 	</div>
+	<div class="ui small modal" id="rename-webauthn-modal">
+		<div class="header">{{svg "octicon-pencil"}} {{ctx.Locale.Tr "settings.webauthn_rename_key"}}</div>
+		<form class="ui form form-fetch-action" method="post">
+			<div class="content">
+				<div class="required field">
+					<label for="rename-webauthn-nickname">{{ctx.Locale.Tr "settings.webauthn_nickname"}}</label>
+					<input id="rename-webauthn-nickname" name="name" type="text" maxlength="255" required>
+				</div>
+			</div>
+			{{template "base/modal_actions_confirm" (dict "ModalButtonTypes" "confirm")}}
+		</form>
+	</div>
 	<div class="ui small modal" id="delete-registration">
 		<div class="header">
 			{{svg "octicon-trash"}}
diff --git a/tests/integration/user_settings_test.go b/tests/integration/user_settings_test.go
index d837da71cd..4df3f9f534 100644
--- a/tests/integration/user_settings_test.go
+++ b/tests/integration/user_settings_test.go
@@ -4,15 +4,21 @@
 package integration
 
 import (
+	"fmt"
 	"net/http"
+	"net/http/httptest"
 	"strings"
 	"testing"
 
+	auth_model "gitea.dev/models/auth"
+	"gitea.dev/models/unittest"
 	"gitea.dev/modules/container"
 	"gitea.dev/modules/setting"
 	"gitea.dev/modules/test"
+	"gitea.dev/modules/translation"
 	"gitea.dev/tests"
 
+	"github.com/go-webauthn/webauthn/webauthn"
 	"github.com/stretchr/testify/assert"
 )
 
@@ -269,6 +275,64 @@ func TestUserSettingsSecurity(t *testing.T) {
 	})
 }
 
+func TestUserSettingsWebAuthnRename(t *testing.T) {
+	defer tests.PrepareTestEnv(t)()
+
+	session := loginUser(t, "user2") // log in before adding keys, otherwise login would require the second factor
+	own, err := auth_model.CreateCredential(t.Context(), 2, "Laptop Key", &webauthn.Credential{ID: []byte("rename-1")})
+	assert.NoError(t, err)
+	_, err = auth_model.CreateCredential(t.Context(), 2, "Phone Key", &webauthn.Credential{ID: []byte("rename-2")})
+	assert.NoError(t, err)
+	other, err := auth_model.CreateCredential(t.Context(), 4, "Other User Key", &webauthn.Credential{ID: []byte("rename-3")})
+	assert.NoError(t, err)
+
+	rename := func(id int64, name string, expectedStatus int) *httptest.ResponseRecorder {
+		req := NewRequestWithValues(t, "POST", fmt.Sprintf("/user/settings/security/webauthn/rename?id=%d", id), map[string]string{"name": name})
+		return session.MakeRequest(t, req, expectedStatus)
+	}
+
+	t.Run("Success", func(t *testing.T) {
+		resp := rename(own.ID, "Work Laptop", http.StatusOK)
+		assert.Equal(t, "/user/settings/security", *test.ParseJSONRedirect(resp.Body.Bytes()).Redirect)
+		cred := unittest.AssertExistsAndLoadBean(t, &auth_model.WebAuthnCredential{ID: own.ID, Name: "Work Laptop"})
+		assert.Equal(t, []byte("rename-1"), cred.CredentialID) // the credential itself is untouched
+	})
+
+	t.Run("CaseChangeOfOwnName", func(t *testing.T) {
+		rename(own.ID, "WORK LAPTOP", http.StatusOK)
+		unittest.AssertExistsAndLoadBean(t, &auth_model.WebAuthnCredential{ID: own.ID, Name: "WORK LAPTOP"})
+	})
+
+	t.Run("InvalidNames", func(t *testing.T) {
+		for _, name := range []string{"", "   ", strings.Repeat("a", 256)} {
+			resp := rename(own.ID, name, http.StatusBadRequest)
+			assert.NotEmpty(t, test.ParseJSONError(resp.Body.Bytes()).ErrorMessage)
+		}
+		unittest.AssertExistsAndLoadBean(t, &auth_model.WebAuthnCredential{ID: own.ID, Name: "WORK LAPTOP"})
+	})
+
+	t.Run("SurroundingSpacesTrimmed", func(t *testing.T) {
+		rename(own.ID, "  Spaced Key  ", http.StatusOK)
+		unittest.AssertExistsAndLoadBean(t, &auth_model.WebAuthnCredential{ID: own.ID, Name: "Spaced Key"})
+		rename(own.ID, "WORK LAPTOP", http.StatusOK) // restore the name the following subtests expect
+	})
+
+	t.Run("NameTaken", func(t *testing.T) {
+		resp := rename(own.ID, "phone key", http.StatusBadRequest)
+		assert.Equal(t, translation.NewLocale("en-US").TrString("settings.webauthn_nickname_taken"), test.ParseJSONError(resp.Body.Bytes()).ErrorMessage)
+		unittest.AssertExistsAndLoadBean(t, &auth_model.WebAuthnCredential{ID: own.ID, Name: "WORK LAPTOP"})
+	})
+
+	t.Run("OtherUsersKey", func(t *testing.T) {
+		rename(other.ID, "Stolen", http.StatusNotFound)
+		unittest.AssertExistsAndLoadBean(t, &auth_model.WebAuthnCredential{ID: other.ID, Name: "Other User Key"})
+	})
+
+	t.Run("NonexistentKey", func(t *testing.T) {
+		rename(99999, "Missing", http.StatusNotFound)
+	})
+}
+
 func TestUserSettingsApplications(t *testing.T) {
 	defer tests.PrepareTestEnv(t)()
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:19:34Z)**_

I'll start by reading the core files under review.

_**Agent (claude-opus-5 2026-09-15 02:19:34Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Check CSRF handling in fetch-action JS

`grep -n "csrf\|Csrf\|CSRF" web_src/js/features/common-fetch-action.ts web_src/js/modules/fetch.ts 2>/dev/null | head -20`

```text
(Bash completed with no output)
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:19:34Z)**_

I'll start by reading the diff and the test files in full.

_**Agent - sidechain (claude-opus-5 2026-09-15 02:19:34Z)**_

I'll start by reading the diff and the touched files.

_**Agent - sidechain (claude-opus-5 2026-09-15 02:19:34Z)**_

I'll start by reading the diff and the key front-end files.

_**Agent - sidechain (claude-opus-5 2026-09-15 02:19:49Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Show commit messages in full

`git log --format=full $(git merge-base main HEAD)..HEAD`

```text
commit 98e73f51f3fce30bb58094613a5c4cff7e725fe0
Author: CarsonRackley <cdrackley@wm.edu>
Commit: CarsonRackley <cdrackley@wm.edu>

    fix(user): reject whitespace-only security key nicknames
    
    Assisted-by: Claude Code:claude-opus-5

commit aff9a2045013a5fa89be6692091dac435c92df3f
Author: CarsonRackley <cdrackley@wm.edu>
Commit: CarsonRackley <cdrackley@wm.edu>

    feat(user): allow renaming security keys
    
    Assisted-by: Claude Code:claude-opus-5
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:19:45Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Show the full diff under review

`git diff $(git merge-base main HEAD)..HEAD`

```text
diff --git a/models/auth/webauthn.go b/models/auth/webauthn.go
index 11297556d2..b37e9f51ef 100644
--- a/models/auth/webauthn.go
+++ b/models/auth/webauthn.go
@@ -195,6 +195,12 @@ func CreateCredential(ctx context.Context, userID int64, name string, cred *weba
 	return c, nil
 }
 
+// UpdateCredentialName will rename the WebAuthnCredential with the given id if it belongs to userID
+func UpdateCredentialName(ctx context.Context, id, userID int64, name string) error {
+	_, err := db.GetEngine(ctx).ID(id).Where("user_id = ?", userID).Cols("name", "lower_name").Update(&WebAuthnCredential{Name: name})
+	return err
+}
+
 // DeleteCredential will delete WebAuthnCredential
 func DeleteCredential(ctx context.Context, id, userID int64) (bool, error) {
 	had, err := db.GetEngine(ctx).ID(id).Where("user_id = ?", userID).Delete(&WebAuthnCredential{})
diff --git a/models/auth/webauthn_test.go b/models/auth/webauthn_test.go
index a648319226..d7d848f71e 100644
--- a/models/auth/webauthn_test.go
+++ b/models/auth/webauthn_test.go
@@ -64,3 +64,14 @@ func TestCreateCredential(t *testing.T) {
 
 	unittest.AssertExistsAndLoadBean(t, &auth_model.WebAuthnCredential{Name: "WebAuthn Created Credential", UserID: 1})
 }
+
+func TestUpdateCredentialName(t *testing.T) {
+	assert.NoError(t, unittest.PrepareTestDatabase())
+
+	// credential 1 belongs to user 32, so another user's update must not change it
+	assert.NoError(t, auth_model.UpdateCredentialName(t.Context(), 1, 2, "Hijacked"))
+	unittest.AssertExistsAndLoadBean(t, &auth_model.WebAuthnCredential{ID: 1, Name: "WebAuthn credential"})
+
+	assert.NoError(t, auth_model.UpdateCredentialName(t.Context(), 1, 32, "Renamed Key"))
+	unittest.AssertExistsAndLoadBean(t, &auth_model.WebAuthnCredential{ID: 1, Name: "Renamed Key", LowerName: "renamed key", UserID: 32})
+}
diff --git a/options/locale/locale_en-US.json b/options/locale/locale_en-US.json
index 9373aa83a9..694d0bf99b 100644
--- a/options/locale/locale_en-US.json
+++ b/options/locale/locale_en-US.json
@@ -919,6 +919,9 @@
   "settings.webauthn_desc": "Security keys are hardware devices containing cryptographic keys. They can be used for two-factor authentication. Security keys must support the <a rel=\"noreferrer\" target=\"_blank\" href=\"%s\">WebAuthn Authenticator</a> standard.",
   "settings.webauthn_register_key": "Add Security Key",
   "settings.webauthn_nickname": "Nickname",
+  "settings.webauthn_rename_key": "Rename Security Key",
+  "settings.webauthn_rename_key_success": "The security key has been renamed.",
+  "settings.webauthn_nickname_taken": "Another security key already uses this nickname.",
   "settings.webauthn_delete_key": "Remove Security Key",
   "settings.webauthn_delete_key_desc": "If you remove a security key, you can no longer sign in with it. Continue?",
   "settings.webauthn_key_loss_warning": "If you lose your security keys, you will lose access to your account.",
diff --git a/routers/web/user/setting/security/webauthn.go b/routers/web/user/setting/security/webauthn.go
index 4db7b47879..5e93abbbf1 100644
--- a/routers/web/user/setting/security/webauthn.go
+++ b/routers/web/user/setting/security/webauthn.go
@@ -7,6 +7,7 @@ import (
 	"errors"
 	"net/http"
 	"strconv"
+	"strings"
 	"time"
 
 	"gitea.dev/models/auth"
@@ -125,6 +126,52 @@ func WebauthnRegisterPost(ctx *context.Context) {
 	ctx.JSON(http.StatusCreated, cred)
 }
 
+// WebauthnRename renames a security key by id
+func WebauthnRename(ctx *context.Context) {
+	if user_model.IsFeatureDisabledWithLoginType(ctx.Doer, setting.UserFeatureManageMFA) {
+		ctx.HTTPError(http.StatusNotFound)
+		return
+	}
+
+	if ctx.HasError() { // form binding validation error
+		ctx.JSONError(ctx.GetErrMsg())
+		return
+	}
+
+	cred, err := auth.GetWebAuthnCredentialByID(ctx, ctx.FormInt64("id"))
+	if err != nil && !auth.IsErrWebAuthnCredentialNotExist(err) {
+		ctx.ServerError("GetWebAuthnCredentialByID", err)
+		return
+	}
+	if cred == nil || cred.UserID != ctx.Doer.ID { // hide other users' keys as not found
+		ctx.JSONErrorNotFound()
+		return
+	}
+
+	form := web.GetForm(ctx).(*forms.WebauthnRenameForm)
+	form.Name = strings.TrimSpace(form.Name)
+	if form.Name == "" { // "Required" binding only rejects an exactly empty value, not whitespace
+		ctx.JSONError(ctx.Locale.TrString("settings.webauthn_nickname") + ctx.Locale.TrString("form.require_error"))
+		return
+	}
+	sameName, err := auth.GetWebAuthnCredentialByName(ctx, ctx.Doer.ID, form.Name)
+	if err != nil && !auth.IsErrWebAuthnCredentialNotExist(err) {
+		ctx.ServerError("GetWebAuthnCredentialByName", err)
+		return
+	}
+	if sameName != nil && sameName.ID != cred.ID { // renaming a key to its own name with different casing is allowed
+		ctx.JSONError(ctx.Tr("settings.webauthn_nickname_taken"))
+		return
+	}
+
+	if err := auth.UpdateCredentialName(ctx, cred.ID, ctx.Doer.ID, form.Name); err != nil {
+		ctx.ServerError("UpdateCredentialName", err)
+		return
+	}
+	ctx.Flash.Success(ctx.Tr("settings.webauthn_rename_key_success"))
+	ctx.JSONRedirect(setting.AppSubURL + "/user/settings/security")
+}
+
 // WebauthnDelete deletes an security key by id
 func WebauthnDelete(ctx *context.Context) {
 	if user_model.IsFeatureDisabledWithLoginType(ctx.Doer, setting.UserFeatureManageMFA) {
diff --git a/routers/web/web.go b/routers/web/web.go
index 0bfae44e86..f8e48f99fb 100644
--- a/routers/web/web.go
+++ b/routers/web/web.go
@@ -652,6 +652,7 @@ func registerWebRoutes(m *web.Router, webAuth *AuthMiddleware) {
 			m.Group("/webauthn", func() {
 				m.Post("/request_register", web.Bind(forms.WebauthnRegistrationForm{}), security.WebAuthnRegister)
 				m.Post("/register", security.WebauthnRegisterPost)
+				m.Post("/rename", web.Bind(forms.WebauthnRenameForm{}), security.WebauthnRename)
 				m.Post("/delete", security.WebauthnDelete)
 			})
 			m.Group("/openid", func() {
diff --git a/services/forms/user_form.go b/services/forms/user_form.go
index 75275fb5c7..95f3ae0b16 100644
--- a/services/forms/user_form.go
+++ b/services/forms/user_form.go
@@ -419,6 +419,17 @@ func (f *WebauthnRegistrationForm) Validate(req *http.Request, errs binding.Erro
 	return middleware.Validate(errs, ctx.Data, f, ctx.Locale)
 }
 
+// WebauthnRenameForm for renaming an existing WebAuthn credential
+type WebauthnRenameForm struct {
+	Name string `binding:"Required;MaxSize(255)" locale:"settings.webauthn_nickname"` // 255 is the size of the name column
+}
+
+// Validate validates the fields
+func (f *WebauthnRenameForm) Validate(req *http.Request, errs binding.Errors) binding.Errors {
+	ctx := context.GetValidateContext(req)
+	return middleware.Validate(errs, ctx.Data, f, ctx.Locale)
+}
+
 // PackageSettingForm form for package settings
 type PackageSettingForm struct {
 	Action   string
diff --git a/templates/user/settings/security/webauthn.tmpl b/templates/user/settings/security/webauthn.tmpl
index 1bf0159d37..6bfaf11d6b 100644
--- a/templates/user/settings/security/webauthn.tmpl
+++ b/templates/user/settings/security/webauthn.tmpl
@@ -16,6 +16,13 @@
 					</div>
 				</div>
 				<div class="item-trailing">
+					<button class="ui tiny button show-modal"
+						data-modal="#rename-webauthn-modal"
+						data-modal-form.url="{{$.Link}}/webauthn/rename?id={{.ID}}"
+						data-modal-rename-webauthn-nickname="{{.Name}}"
+					>
+						{{svg "octicon-pencil"}} {{ctx.Locale.Tr "settings.webauthn_rename_key"}}
+					</button>
 					<button class="ui red tiny button link-action" data-modal-confirm="#delete-registration" data-url="{{$.Link}}/webauthn/delete?id={{.ID}}">
 					{{ctx.Locale.Tr "settings.delete_key"}}
 					</button>
@@ -30,6 +37,18 @@
 		</div>
 		<button id="register-webauthn" class="ui primary button">{{svg "octicon-key"}} {{ctx.Locale.Tr "settings.webauthn_register_key"}}</button>
 	</div>
+	<div class="ui small modal" id="rename-webauthn-modal">
+		<div class="header">{{svg "octicon-pencil"}} {{ctx.Locale.Tr "settings.webauthn_rename_key"}}</div>
+		<form class="ui form form-fetch-action" method="post">
+			<div class="content">
+				<div class="required field">
+					<label for="rename-webauthn-nickname">{{ctx.Locale.Tr "settings.webauthn_nickname"}}</label>
+					<input id="rename-webauthn-nickname" name="name" type="text" maxlength="255" required>
+				</div>
+			</div>
+			{{template "base/modal_actions_confirm" (dict "ModalButtonTypes" "confirm")}}
+		</form>
+	</div>
 	<div class="ui small modal" id="delete-registration">
 		<div class="header">
 			{{svg "octicon-trash"}}
diff --git a/tests/integration/user_settings_test.go b/tests/integration/user_settings_test.go
index d837da71cd..4df3f9f534 100644
--- a/tests/integration/user_settings_test.go
+++ b/tests/integration/user_settings_test.go
@@ -4,15 +4,21 @@
 package integration
 
 import (
+	"fmt"
 	"net/http"
+	"net/http/httptest"
 	"strings"
 	"testing"
 
+	auth_model "gitea.dev/models/auth"
+	"gitea.dev/models/unittest"
 	"gitea.dev/modules/container"
 	"gitea.dev/modules/setting"
 	"gitea.dev/modules/test"
+	"gitea.dev/modules/translation"
 	"gitea.dev/tests"
 
+	"github.com/go-webauthn/webauthn/webauthn"
 	"github.com/stretchr/testify/assert"
 )
 
@@ -269,6 +275,64 @@ func TestUserSettingsSecurity(t *testing.T) {
 	})
 }
 
+func TestUserSettingsWebAuthnRename(t *testing.T) {
+	defer tests.PrepareTestEnv(t)()
+
+	session := loginUser(t, "user2") // log in before adding keys, otherwise login would require the second factor
+	own, err := auth_model.CreateCredential(t.Context(), 2, "Laptop Key", &webauthn.Credential{ID: []byte("rename-1")})
+	assert.NoError(t, err)
+	_, err = auth_model.CreateCredential(t.Context(), 2, "Phone Key", &webauthn.Credential{ID: []byte("rename-2")})
+	assert.NoError(t, err)
+	other, err := auth_model.CreateCredential(t.Context(), 4, "Other User Key", &webauthn.Credential{ID: []byte("rename-3")})
+	assert.NoError(t, err)
+
+	rename := func(id int64, name string, expectedStatus int) *httptest.ResponseRecorder {
+		req := NewRequestWithValues(t, "POST", fmt.Sprintf("/user/settings/security/webauthn/rename?id=%d", id), map[string]string{"name": name})
+		return session.MakeRequest(t, req, expectedStatus)
+	}
+
+	t.Run("Success", func(t *testing.T) {
+		resp := rename(own.ID, "Work Laptop", http.StatusOK)
+		assert.Equal(t, "/user/settings/security", *test.ParseJSONRedirect(resp.Body.Bytes()).Redirect)
+		cred := unittest.AssertExistsAndLoadBean(t, &auth_model.WebAuthnCredential{ID: own.ID, Name: "Work Laptop"})
+		assert.Equal(t, []byte("rename-1"), cred.CredentialID) // the credential itself is untouched
+	})
+
+	t.Run("CaseChangeOfOwnName", func(t *testing.T) {
+		rename(own.ID, "WORK LAPTOP", http.StatusOK)
+		unittest.AssertExistsAndLoadBean(t, &auth_model.WebAuthnCredential{ID: own.ID, Name: "WORK LAPTOP"})
+	})
+
+	t.Run("InvalidNames", func(t *testing.T) {
+		for _, name := range []string{"", "   ", strings.Repeat("a", 256)} {
+			resp := rename(own.ID, name, http.StatusBadRequest)
+			assert.NotEmpty(t, test.ParseJSONError(resp.Body.Bytes()).ErrorMessage)
+		}
+		unittest.AssertExistsAndLoadBean(t, &auth_model.WebAuthnCredential{ID: own.ID, Name: "WORK LAPTOP"})
+	})
+
+	t.Run("SurroundingSpacesTrimmed", func(t *testing.T) {
+		rename(own.ID, "  Spaced Key  ", http.StatusOK)
+		unittest.AssertExistsAndLoadBean(t, &auth_model.WebAuthnCredential{ID: own.ID, Name: "Spaced Key"})
+		rename(own.ID, "WORK LAPTOP", http.StatusOK) // restore the name the following subtests expect
+	})
+
+	t.Run("NameTaken", func(t *testing.T) {
+		resp := rename(own.ID, "phone key", http.StatusBadRequest)
+		assert.Equal(t, translation.NewLocale("en-US").TrString("settings.webauthn_nickname_taken"), test.ParseJSONError(resp.Body.Bytes()).ErrorMessage)
+		unittest.AssertExistsAndLoadBean(t, &auth_model.WebAuthnCredential{ID: own.ID, Name: "WORK LAPTOP"})
+	})
+
+	t.Run("OtherUsersKey", func(t *testing.T) {
+		rename(other.ID, "Stolen", http.StatusNotFound)
+		unittest.AssertExistsAndLoadBean(t, &auth_model.WebAuthnCredential{ID: other.ID, Name: "Other User Key"})
+	})
+
+	t.Run("NonexistentKey", func(t *testing.T) {
+		rename(99999, "Missing", http.StatusNotFound)
+	})
+}
+
 func TestUserSettingsApplications(t *testing.T) {
 	defer tests.PrepareTestEnv(t)()
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:19:44Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Show the full branch diff

`git diff $(git merge-base main HEAD)..HEAD`

```text
diff --git a/models/auth/webauthn.go b/models/auth/webauthn.go
index 11297556d2..b37e9f51ef 100644
--- a/models/auth/webauthn.go
+++ b/models/auth/webauthn.go
@@ -195,6 +195,12 @@ func CreateCredential(ctx context.Context, userID int64, name string, cred *weba
 	return c, nil
 }
 
+// UpdateCredentialName will rename the WebAuthnCredential with the given id if it belongs to userID
+func UpdateCredentialName(ctx context.Context, id, userID int64, name string) error {
+	_, err := db.GetEngine(ctx).ID(id).Where("user_id = ?", userID).Cols("name", "lower_name").Update(&WebAuthnCredential{Name: name})
+	return err
+}
+
 // DeleteCredential will delete WebAuthnCredential
 func DeleteCredential(ctx context.Context, id, userID int64) (bool, error) {
 	had, err := db.GetEngine(ctx).ID(id).Where("user_id = ?", userID).Delete(&WebAuthnCredential{})
diff --git a/models/auth/webauthn_test.go b/models/auth/webauthn_test.go
index a648319226..d7d848f71e 100644
--- a/models/auth/webauthn_test.go
+++ b/models/auth/webauthn_test.go
@@ -64,3 +64,14 @@ func TestCreateCredential(t *testing.T) {
 
 	unittest.AssertExistsAndLoadBean(t, &auth_model.WebAuthnCredential{Name: "WebAuthn Created Credential", UserID: 1})
 }
+
+func TestUpdateCredentialName(t *testing.T) {
+	assert.NoError(t, unittest.PrepareTestDatabase())
+
+	// credential 1 belongs to user 32, so another user's update must not change it
+	assert.NoError(t, auth_model.UpdateCredentialName(t.Context(), 1, 2, "Hijacked"))
+	unittest.AssertExistsAndLoadBean(t, &auth_model.WebAuthnCredential{ID: 1, Name: "WebAuthn credential"})
+
+	assert.NoError(t, auth_model.UpdateCredentialName(t.Context(), 1, 32, "Renamed Key"))
+	unittest.AssertExistsAndLoadBean(t, &auth_model.WebAuthnCredential{ID: 1, Name: "Renamed Key", LowerName: "renamed key", UserID: 32})
+}
diff --git a/options/locale/locale_en-US.json b/options/locale/locale_en-US.json
index 9373aa83a9..694d0bf99b 100644
--- a/options/locale/locale_en-US.json
+++ b/options/locale/locale_en-US.json
@@ -919,6 +919,9 @@
   "settings.webauthn_desc": "Security keys are hardware devices containing cryptographic keys. They can be used for two-factor authentication. Security keys must support the <a rel=\"noreferrer\" target=\"_blank\" href=\"%s\">WebAuthn Authenticator</a> standard.",
   "settings.webauthn_register_key": "Add Security Key",
   "settings.webauthn_nickname": "Nickname",
+  "settings.webauthn_rename_key": "Rename Security Key",
+  "settings.webauthn_rename_key_success": "The security key has been renamed.",
+  "settings.webauthn_nickname_taken": "Another security key already uses this nickname.",
   "settings.webauthn_delete_key": "Remove Security Key",
   "settings.webauthn_delete_key_desc": "If you remove a security key, you can no longer sign in with it. Continue?",
   "settings.webauthn_key_loss_warning": "If you lose your security keys, you will lose access to your account.",
diff --git a/routers/web/user/setting/security/webauthn.go b/routers/web/user/setting/security/webauthn.go
index 4db7b47879..5e93abbbf1 100644
--- a/routers/web/user/setting/security/webauthn.go
+++ b/routers/web/user/setting/security/webauthn.go
@@ -7,6 +7,7 @@ import (
 	"errors"
 	"net/http"
 	"strconv"
+	"strings"
 	"time"
 
 	"gitea.dev/models/auth"
@@ -125,6 +126,52 @@ func WebauthnRegisterPost(ctx *context.Context) {
 	ctx.JSON(http.StatusCreated, cred)
 }
 
+// WebauthnRename renames a security key by id
+func WebauthnRename(ctx *context.Context) {
+	if user_model.IsFeatureDisabledWithLoginType(ctx.Doer, setting.UserFeatureManageMFA) {
+		ctx.HTTPError(http.StatusNotFound)
+		return
+	}
+
+	if ctx.HasError() { // form binding validation error
+		ctx.JSONError(ctx.GetErrMsg())
+		return
+	}
+
+	cred, err := auth.GetWebAuthnCredentialByID(ctx, ctx.FormInt64("id"))
+	if err != nil && !auth.IsErrWebAuthnCredentialNotExist(err) {
+		ctx.ServerError("GetWebAuthnCredentialByID", err)
+		return
+	}
+	if cred == nil || cred.UserID != ctx.Doer.ID { // hide other users' keys as not found
+		ctx.JSONErrorNotFound()
+		return
+	}
+
+	form := web.GetForm(ctx).(*forms.WebauthnRenameForm)
+	form.Name = strings.TrimSpace(form.Name)
+	if form.Name == "" { // "Required" binding only rejects an exactly empty value, not whitespace
+		ctx.JSONError(ctx.Locale.TrString("settings.webauthn_nickname") + ctx.Locale.TrString("form.require_error"))
+		return
+	}
+	sameName, err := auth.GetWebAuthnCredentialByName(ctx, ctx.Doer.ID, form.Name)
+	if err != nil && !auth.IsErrWebAuthnCredentialNotExist(err) {
+		ctx.ServerError("GetWebAuthnCredentialByName", err)
+		return
+	}
+	if sameName != nil && sameName.ID != cred.ID { // renaming a key to its own name with different casing is allowed
+		ctx.JSONError(ctx.Tr("settings.webauthn_nickname_taken"))
+		return
+	}
+
+	if err := auth.UpdateCredentialName(ctx, cred.ID, ctx.Doer.ID, form.Name); err != nil {
+		ctx.ServerError("UpdateCredentialName", err)
+		return
+	}
+	ctx.Flash.Success(ctx.Tr("settings.webauthn_rename_key_success"))
+	ctx.JSONRedirect(setting.AppSubURL + "/user/settings/security")
+}
+
 // WebauthnDelete deletes an security key by id
 func WebauthnDelete(ctx *context.Context) {
 	if user_model.IsFeatureDisabledWithLoginType(ctx.Doer, setting.UserFeatureManageMFA) {
diff --git a/routers/web/web.go b/routers/web/web.go
index 0bfae44e86..f8e48f99fb 100644
--- a/routers/web/web.go
+++ b/routers/web/web.go
@@ -652,6 +652,7 @@ func registerWebRoutes(m *web.Router, webAuth *AuthMiddleware) {
 			m.Group("/webauthn", func() {
 				m.Post("/request_register", web.Bind(forms.WebauthnRegistrationForm{}), security.WebAuthnRegister)
 				m.Post("/register", security.WebauthnRegisterPost)
+				m.Post("/rename", web.Bind(forms.WebauthnRenameForm{}), security.WebauthnRename)
 				m.Post("/delete", security.WebauthnDelete)
 			})
 			m.Group("/openid", func() {
diff --git a/services/forms/user_form.go b/services/forms/user_form.go
index 75275fb5c7..95f3ae0b16 100644
--- a/services/forms/user_form.go
+++ b/services/forms/user_form.go
@@ -419,6 +419,17 @@ func (f *WebauthnRegistrationForm) Validate(req *http.Request, errs binding.Erro
 	return middleware.Validate(errs, ctx.Data, f, ctx.Locale)
 }
 
+// WebauthnRenameForm for renaming an existing WebAuthn credential
+type WebauthnRenameForm struct {
+	Name string `binding:"Required;MaxSize(255)" locale:"settings.webauthn_nickname"` // 255 is the size of the name column
+}
+
+// Validate validates the fields
+func (f *WebauthnRenameForm) Validate(req *http.Request, errs binding.Errors) binding.Errors {
+	ctx := context.GetValidateContext(req)
+	return middleware.Validate(errs, ctx.Data, f, ctx.Locale)
+}
+
 // PackageSettingForm form for package settings
 type PackageSettingForm struct {
 	Action   string
diff --git a/templates/user/settings/security/webauthn.tmpl b/templates/user/settings/security/webauthn.tmpl
index 1bf0159d37..6bfaf11d6b 100644
--- a/templates/user/settings/security/webauthn.tmpl
+++ b/templates/user/settings/security/webauthn.tmpl
@@ -16,6 +16,13 @@
 					</div>
 				</div>
 				<div class="item-trailing">
+					<button class="ui tiny button show-modal"
+						data-modal="#rename-webauthn-modal"
+						data-modal-form.url="{{$.Link}}/webauthn/rename?id={{.ID}}"
+						data-modal-rename-webauthn-nickname="{{.Name}}"
+					>
+						{{svg "octicon-pencil"}} {{ctx.Locale.Tr "settings.webauthn_rename_key"}}
+					</button>
 					<button class="ui red tiny button link-action" data-modal-confirm="#delete-registration" data-url="{{$.Link}}/webauthn/delete?id={{.ID}}">
 					{{ctx.Locale.Tr "settings.delete_key"}}
 					</button>
@@ -30,6 +37,18 @@
 		</div>
 		<button id="register-webauthn" class="ui primary button">{{svg "octicon-key"}} {{ctx.Locale.Tr "settings.webauthn_register_key"}}</button>
 	</div>
+	<div class="ui small modal" id="rename-webauthn-modal">
+		<div class="header">{{svg "octicon-pencil"}} {{ctx.Locale.Tr "settings.webauthn_rename_key"}}</div>
+		<form class="ui form form-fetch-action" method="post">
+			<div class="content">
+				<div class="required field">
+					<label for="rename-webauthn-nickname">{{ctx.Locale.Tr "settings.webauthn_nickname"}}</label>
+					<input id="rename-webauthn-nickname" name="name" type="text" maxlength="255" required>
+				</div>
+			</div>
+			{{template "base/modal_actions_confirm" (dict "ModalButtonTypes" "confirm")}}
+		</form>
+	</div>
 	<div class="ui small modal" id="delete-registration">
 		<div class="header">
 			{{svg "octicon-trash"}}
diff --git a/tests/integration/user_settings_test.go b/tests/integration/user_settings_test.go
index d837da71cd..4df3f9f534 100644
--- a/tests/integration/user_settings_test.go
+++ b/tests/integration/user_settings_test.go
@@ -4,15 +4,21 @@
 package integration
 
 import (
+	"fmt"
 	"net/http"
+	"net/http/httptest"
 	"strings"
 	"testing"
 
+	auth_model "gitea.dev/models/auth"
+	"gitea.dev/models/unittest"
 	"gitea.dev/modules/container"
 	"gitea.dev/modules/setting"
 	"gitea.dev/modules/test"
+	"gitea.dev/modules/translation"
 	"gitea.dev/tests"
 
+	"github.com/go-webauthn/webauthn/webauthn"
 	"github.com/stretchr/testify/assert"
 )
 
@@ -269,6 +275,64 @@ func TestUserSettingsSecurity(t *testing.T) {
 	})
 }
 
+func TestUserSettingsWebAuthnRename(t *testing.T) {
+	defer tests.PrepareTestEnv(t)()
+
+	session := loginUser(t, "user2") // log in before adding keys, otherwise login would require the second factor
+	own, err := auth_model.CreateCredential(t.Context(), 2, "Laptop Key", &webauthn.Credential{ID: []byte("rename-1")})
+	assert.NoError(t, err)
+	_, err = auth_model.CreateCredential(t.Context(), 2, "Phone Key", &webauthn.Credential{ID: []byte("rename-2")})
+	assert.NoError(t, err)
+	other, err := auth_model.CreateCredential(t.Context(), 4, "Other User Key", &webauthn.Credential{ID: []byte("rename-3")})
+	assert.NoError(t, err)
+
+	rename := func(id int64, name string, expectedStatus int) *httptest.ResponseRecorder {
+		req := NewRequestWithValues(t, "POST", fmt.Sprintf("/user/settings/security/webauthn/rename?id=%d", id), map[string]string{"name": name})
+		return session.MakeRequest(t, req, expectedStatus)
+	}
+
+	t.Run("Success", func(t *testing.T) {
+		resp := rename(own.ID, "Work Laptop", http.StatusOK)
+		assert.Equal(t, "/user/settings/security", *test.ParseJSONRedirect(resp.Body.Bytes()).Redirect)
+		cred := unittest.AssertExistsAndLoadBean(t, &auth_model.WebAuthnCredential{ID: own.ID, Name: "Work Laptop"})
+		assert.Equal(t, []byte("rename-1"), cred.CredentialID) // the credential itself is untouched
+	})
+
+	t.Run("CaseChangeOfOwnName", func(t *testing.T) {
+		rename(own.ID, "WORK LAPTOP", http.StatusOK)
+		unittest.AssertExistsAndLoadBean(t, &auth_model.WebAuthnCredential{ID: own.ID, Name: "WORK LAPTOP"})
+	})
+
+	t.Run("InvalidNames", func(t *testing.T) {
+		for _, name := range []string{"", "   ", strings.Repeat("a", 256)} {
+			resp := rename(own.ID, name, http.StatusBadRequest)
+			assert.NotEmpty(t, test.ParseJSONError(resp.Body.Bytes()).ErrorMessage)
+		}
+		unittest.AssertExistsAndLoadBean(t, &auth_model.WebAuthnCredential{ID: own.ID, Name: "WORK LAPTOP"})
+	})
+
+	t.Run("SurroundingSpacesTrimmed", func(t *testing.T) {
+		rename(own.ID, "  Spaced Key  ", http.StatusOK)
+		unittest.AssertExistsAndLoadBean(t, &auth_model.WebAuthnCredential{ID: own.ID, Name: "Spaced Key"})
+		rename(own.ID, "WORK LAPTOP", http.StatusOK) // restore the name the following subtests expect
+	})
+
+	t.Run("NameTaken", func(t *testing.T) {
+		resp := rename(own.ID, "phone key", http.StatusBadRequest)
+		assert.Equal(t, translation.NewLocale("en-US").TrString("settings.webauthn_nickname_taken"), test.ParseJSONError(resp.Body.Bytes()).ErrorMessage)
+		unittest.AssertExistsAndLoadBean(t, &auth_model.WebAuthnCredential{ID: own.ID, Name: "WORK LAPTOP"})
+	})
+
+	t.Run("OtherUsersKey", func(t *testing.T) {
+		rename(other.ID, "Stolen", http.StatusNotFound)
+		unittest.AssertExistsAndLoadBean(t, &auth_model.WebAuthnCredential{ID: other.ID, Name: "Other User Key"})
+	})
+
+	t.Run("NonexistentKey", func(t *testing.T) {
+		rename(99999, "Missing", http.StatusNotFound)
+	})
+}
+
 func TestUserSettingsApplications(t *testing.T) {
 	defer tests.PrepareTestEnv(t)()
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:19:41Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Show the full diff under review

`git diff $(git merge-base main HEAD)..HEAD`

```text
diff --git a/models/auth/webauthn.go b/models/auth/webauthn.go
index 11297556d2..b37e9f51ef 100644
--- a/models/auth/webauthn.go
+++ b/models/auth/webauthn.go
@@ -195,6 +195,12 @@ func CreateCredential(ctx context.Context, userID int64, name string, cred *weba
 	return c, nil
 }
 
+// UpdateCredentialName will rename the WebAuthnCredential with the given id if it belongs to userID
+func UpdateCredentialName(ctx context.Context, id, userID int64, name string) error {
+	_, err := db.GetEngine(ctx).ID(id).Where("user_id = ?", userID).Cols("name", "lower_name").Update(&WebAuthnCredential{Name: name})
+	return err
+}
+
 // DeleteCredential will delete WebAuthnCredential
 func DeleteCredential(ctx context.Context, id, userID int64) (bool, error) {
 	had, err := db.GetEngine(ctx).ID(id).Where("user_id = ?", userID).Delete(&WebAuthnCredential{})
diff --git a/models/auth/webauthn_test.go b/models/auth/webauthn_test.go
index a648319226..d7d848f71e 100644
--- a/models/auth/webauthn_test.go
+++ b/models/auth/webauthn_test.go
@@ -64,3 +64,14 @@ func TestCreateCredential(t *testing.T) {
 
 	unittest.AssertExistsAndLoadBean(t, &auth_model.WebAuthnCredential{Name: "WebAuthn Created Credential", UserID: 1})
 }
+
+func TestUpdateCredentialName(t *testing.T) {
+	assert.NoError(t, unittest.PrepareTestDatabase())
+
+	// credential 1 belongs to user 32, so another user's update must not change it
+	assert.NoError(t, auth_model.UpdateCredentialName(t.Context(), 1, 2, "Hijacked"))
+	unittest.AssertExistsAndLoadBean(t, &auth_model.WebAuthnCredential{ID: 1, Name: "WebAuthn credential"})
+
+	assert.NoError(t, auth_model.UpdateCredentialName(t.Context(), 1, 32, "Renamed Key"))
+	unittest.AssertExistsAndLoadBean(t, &auth_model.WebAuthnCredential{ID: 1, Name: "Renamed Key", LowerName: "renamed key", UserID: 32})
+}
diff --git a/options/locale/locale_en-US.json b/options/locale/locale_en-US.json
index 9373aa83a9..694d0bf99b 100644
--- a/options/locale/locale_en-US.json
+++ b/options/locale/locale_en-US.json
@@ -919,6 +919,9 @@
   "settings.webauthn_desc": "Security keys are hardware devices containing cryptographic keys. They can be used for two-factor authentication. Security keys must support the <a rel=\"noreferrer\" target=\"_blank\" href=\"%s\">WebAuthn Authenticator</a> standard.",
   "settings.webauthn_register_key": "Add Security Key",
   "settings.webauthn_nickname": "Nickname",
+  "settings.webauthn_rename_key": "Rename Security Key",
+  "settings.webauthn_rename_key_success": "The security key has been renamed.",
+  "settings.webauthn_nickname_taken": "Another security key already uses this nickname.",
   "settings.webauthn_delete_key": "Remove Security Key",
   "settings.webauthn_delete_key_desc": "If you remove a security key, you can no longer sign in with it. Continue?",
   "settings.webauthn_key_loss_warning": "If you lose your security keys, you will lose access to your account.",
diff --git a/routers/web/user/setting/security/webauthn.go b/routers/web/user/setting/security/webauthn.go
index 4db7b47879..5e93abbbf1 100644
--- a/routers/web/user/setting/security/webauthn.go
+++ b/routers/web/user/setting/security/webauthn.go
@@ -7,6 +7,7 @@ import (
 	"errors"
 	"net/http"
 	"strconv"
+	"strings"
 	"time"
 
 	"gitea.dev/models/auth"
@@ -125,6 +126,52 @@ func WebauthnRegisterPost(ctx *context.Context) {
 	ctx.JSON(http.StatusCreated, cred)
 }
 
+// WebauthnRename renames a security key by id
+func WebauthnRename(ctx *context.Context) {
+	if user_model.IsFeatureDisabledWithLoginType(ctx.Doer, setting.UserFeatureManageMFA) {
+		ctx.HTTPError(http.StatusNotFound)
+		return
+	}
+
+	if ctx.HasError() { // form binding validation error
+		ctx.JSONError(ctx.GetErrMsg())
+		return
+	}
+
+	cred, err := auth.GetWebAuthnCredentialByID(ctx, ctx.FormInt64("id"))
+	if err != nil && !auth.IsErrWebAuthnCredentialNotExist(err) {
+		ctx.ServerError("GetWebAuthnCredentialByID", err)
+		return
+	}
+	if cred == nil || cred.UserID != ctx.Doer.ID { // hide other users' keys as not found
+		ctx.JSONErrorNotFound()
+		return
+	}
+
+	form := web.GetForm(ctx).(*forms.WebauthnRenameForm)
+	form.Name = strings.TrimSpace(form.Name)
+	if form.Name == "" { // "Required" binding only rejects an exactly empty value, not whitespace
+		ctx.JSONError(ctx.Locale.TrString("settings.webauthn_nickname") + ctx.Locale.TrString("form.require_error"))
+		return
+	}
+	sameName, err := auth.GetWebAuthnCredentialByName(ctx, ctx.Doer.ID, form.Name)
+	if err != nil && !auth.IsErrWebAuthnCredentialNotExist(err) {
+		ctx.ServerError("GetWebAuthnCredentialByName", err)
+		return
+	}
+	if sameName != nil && sameName.ID != cred.ID { // renaming a key to its own name with different casing is allowed
+		ctx.JSONError(ctx.Tr("settings.webauthn_nickname_taken"))
+		return
+	}
+
+	if err := auth.UpdateCredentialName(ctx, cred.ID, ctx.Doer.ID, form.Name); err != nil {
+		ctx.ServerError("UpdateCredentialName", err)
+		return
+	}
+	ctx.Flash.Success(ctx.Tr("settings.webauthn_rename_key_success"))
+	ctx.JSONRedirect(setting.AppSubURL + "/user/settings/security")
+}
+
 // WebauthnDelete deletes an security key by id
 func WebauthnDelete(ctx *context.Context) {
 	if user_model.IsFeatureDisabledWithLoginType(ctx.Doer, setting.UserFeatureManageMFA) {
diff --git a/routers/web/web.go b/routers/web/web.go
index 0bfae44e86..f8e48f99fb 100644
--- a/routers/web/web.go
+++ b/routers/web/web.go
@@ -652,6 +652,7 @@ func registerWebRoutes(m *web.Router, webAuth *AuthMiddleware) {
 			m.Group("/webauthn", func() {
 				m.Post("/request_register", web.Bind(forms.WebauthnRegistrationForm{}), security.WebAuthnRegister)
 				m.Post("/register", security.WebauthnRegisterPost)
+				m.Post("/rename", web.Bind(forms.WebauthnRenameForm{}), security.WebauthnRename)
 				m.Post("/delete", security.WebauthnDelete)
 			})
 			m.Group("/openid", func() {
diff --git a/services/forms/user_form.go b/services/forms/user_form.go
index 75275fb5c7..95f3ae0b16 100644
--- a/services/forms/user_form.go
+++ b/services/forms/user_form.go
@@ -419,6 +419,17 @@ func (f *WebauthnRegistrationForm) Validate(req *http.Request, errs binding.Erro
 	return middleware.Validate(errs, ctx.Data, f, ctx.Locale)
 }
 
+// WebauthnRenameForm for renaming an existing WebAuthn credential
+type WebauthnRenameForm struct {
+	Name string `binding:"Required;MaxSize(255)" locale:"settings.webauthn_nickname"` // 255 is the size of the name column
+}
+
+// Validate validates the fields
+func (f *WebauthnRenameForm) Validate(req *http.Request, errs binding.Errors) binding.Errors {
+	ctx := context.GetValidateContext(req)
+	return middleware.Validate(errs, ctx.Data, f, ctx.Locale)
+}
+
 // PackageSettingForm form for package settings
 type PackageSettingForm struct {
 	Action   string
diff --git a/templates/user/settings/security/webauthn.tmpl b/templates/user/settings/security/webauthn.tmpl
index 1bf0159d37..6bfaf11d6b 100644
--- a/templates/user/settings/security/webauthn.tmpl
+++ b/templates/user/settings/security/webauthn.tmpl
@@ -16,6 +16,13 @@
 					</div>
 				</div>
 				<div class="item-trailing">
+					<button class="ui tiny button show-modal"
+						data-modal="#rename-webauthn-modal"
+						data-modal-form.url="{{$.Link}}/webauthn/rename?id={{.ID}}"
+						data-modal-rename-webauthn-nickname="{{.Name}}"
+					>
+						{{svg "octicon-pencil"}} {{ctx.Locale.Tr "settings.webauthn_rename_key"}}
+					</button>
 					<button class="ui red tiny button link-action" data-modal-confirm="#delete-registration" data-url="{{$.Link}}/webauthn/delete?id={{.ID}}">
 					{{ctx.Locale.Tr "settings.delete_key"}}
 					</button>
@@ -30,6 +37,18 @@
 		</div>
 		<button id="register-webauthn" class="ui primary button">{{svg "octicon-key"}} {{ctx.Locale.Tr "settings.webauthn_register_key"}}</button>
 	</div>
+	<div class="ui small modal" id="rename-webauthn-modal">
+		<div class="header">{{svg "octicon-pencil"}} {{ctx.Locale.Tr "settings.webauthn_rename_key"}}</div>
+		<form class="ui form form-fetch-action" method="post">
+			<div class="content">
+				<div class="required field">
+					<label for="rename-webauthn-nickname">{{ctx.Locale.Tr "settings.webauthn_nickname"}}</label>
+					<input id="rename-webauthn-nickname" name="name" type="text" maxlength="255" required>
+				</div>
+			</div>
+			{{template "base/modal_actions_confirm" (dict "ModalButtonTypes" "confirm")}}
+		</form>
+	</div>
 	<div class="ui small modal" id="delete-registration">
 		<div class="header">
 			{{svg "octicon-trash"}}
diff --git a/tests/integration/user_settings_test.go b/tests/integration/user_settings_test.go
index d837da71cd..4df3f9f534 100644
--- a/tests/integration/user_settings_test.go
+++ b/tests/integration/user_settings_test.go
@@ -4,15 +4,21 @@
 package integration
 
 import (
+	"fmt"
 	"net/http"
+	"net/http/httptest"
 	"strings"
 	"testing"
 
+	auth_model "gitea.dev/models/auth"
+	"gitea.dev/models/unittest"
 	"gitea.dev/modules/container"
 	"gitea.dev/modules/setting"
 	"gitea.dev/modules/test"
+	"gitea.dev/modules/translation"
 	"gitea.dev/tests"
 
+	"github.com/go-webauthn/webauthn/webauthn"
 	"github.com/stretchr/testify/assert"
 )
 
@@ -269,6 +275,64 @@ func TestUserSettingsSecurity(t *testing.T) {
 	})
 }
 
+func TestUserSettingsWebAuthnRename(t *testing.T) {
+	defer tests.PrepareTestEnv(t)()
+
+	session := loginUser(t, "user2") // log in before adding keys, otherwise login would require the second factor
+	own, err := auth_model.CreateCredential(t.Context(), 2, "Laptop Key", &webauthn.Credential{ID: []byte("rename-1")})
+	assert.NoError(t, err)
+	_, err = auth_model.CreateCredential(t.Context(), 2, "Phone Key", &webauthn.Credential{ID: []byte("rename-2")})
+	assert.NoError(t, err)
+	other, err := auth_model.CreateCredential(t.Context(), 4, "Other User Key", &webauthn.Credential{ID: []byte("rename-3")})
+	assert.NoError(t, err)
+
+	rename := func(id int64, name string, expectedStatus int) *httptest.ResponseRecorder {
+		req := NewRequestWithValues(t, "POST", fmt.Sprintf("/user/settings/security/webauthn/rename?id=%d", id), map[string]string{"name": name})
+		return session.MakeRequest(t, req, expectedStatus)
+	}
+
+	t.Run("Success", func(t *testing.T) {
+		resp := rename(own.ID, "Work Laptop", http.StatusOK)
+		assert.Equal(t, "/user/settings/security", *test.ParseJSONRedirect(resp.Body.Bytes()).Redirect)
+		cred := unittest.AssertExistsAndLoadBean(t, &auth_model.WebAuthnCredential{ID: own.ID, Name: "Work Laptop"})
+		assert.Equal(t, []byte("rename-1"), cred.CredentialID) // the credential itself is untouched
+	})
+
+	t.Run("CaseChangeOfOwnName", func(t *testing.T) {
+		rename(own.ID, "WORK LAPTOP", http.StatusOK)
+		unittest.AssertExistsAndLoadBean(t, &auth_model.WebAuthnCredential{ID: own.ID, Name: "WORK LAPTOP"})
+	})
+
+	t.Run("InvalidNames", func(t *testing.T) {
+		for _, name := range []string{"", "   ", strings.Repeat("a", 256)} {
+			resp := rename(own.ID, name, http.StatusBadRequest)
+			assert.NotEmpty(t, test.ParseJSONError(resp.Body.Bytes()).ErrorMessage)
+		}
+		unittest.AssertExistsAndLoadBean(t, &auth_model.WebAuthnCredential{ID: own.ID, Name: "WORK LAPTOP"})
+	})
+
+	t.Run("SurroundingSpacesTrimmed", func(t *testing.T) {
+		rename(own.ID, "  Spaced Key  ", http.StatusOK)
+		unittest.AssertExistsAndLoadBean(t, &auth_model.WebAuthnCredential{ID: own.ID, Name: "Spaced Key"})
+		rename(own.ID, "WORK LAPTOP", http.StatusOK) // restore the name the following subtests expect
+	})
+
+	t.Run("NameTaken", func(t *testing.T) {
+		resp := rename(own.ID, "phone key", http.StatusBadRequest)
+		assert.Equal(t, translation.NewLocale("en-US").TrString("settings.webauthn_nickname_taken"), test.ParseJSONError(resp.Body.Bytes()).ErrorMessage)
+		unittest.AssertExistsAndLoadBean(t, &auth_model.WebAuthnCredential{ID: own.ID, Name: "WORK LAPTOP"})
+	})
+
+	t.Run("OtherUsersKey", func(t *testing.T) {
+		rename(other.ID, "Stolen", http.StatusNotFound)
+		unittest.AssertExistsAndLoadBean(t, &auth_model.WebAuthnCredential{ID: other.ID, Name: "Other User Key"})
+	})
+
+	t.Run("NonexistentKey", func(t *testing.T) {
+		rename(99999, "Missing", http.StatusNotFound)
+	})
+}
+
 func TestUserSettingsApplications(t *testing.T) {
 	defer tests.PrepareTestEnv(t)()
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:19:41Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Show diff stat for branch

`git log --oneline -3 && git diff $(git merge-base main HEAD)..HEAD --stat`

```text
98e73f51f3 fix(user): reject whitespace-only security key nicknames
aff9a20450 feat(user): allow renaming security keys
c0092050a4 update to students' guide and adding folder structure
 models/auth/webauthn.go                        |  6 +++
 models/auth/webauthn_test.go                   | 11 +++++
 options/locale/locale_en-US.json               |  3 ++
 routers/web/user/setting/security/webauthn.go  | 47 +++++++++++++++++++
 routers/web/web.go                             |  1 +
 services/forms/user_form.go                    | 11 +++++
 templates/user/settings/security/webauthn.tmpl | 19 ++++++++
 tests/integration/user_settings_test.go        | 64 ++++++++++++++++++++++++++
 8 files changed, 162 insertions(+)
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:19:40Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Show the full diff under review

`git diff $(git merge-base main HEAD)..HEAD`

```text
diff --git a/models/auth/webauthn.go b/models/auth/webauthn.go
index 11297556d2..b37e9f51ef 100644
--- a/models/auth/webauthn.go
+++ b/models/auth/webauthn.go
@@ -195,6 +195,12 @@ func CreateCredential(ctx context.Context, userID int64, name string, cred *weba
 	return c, nil
 }
 
+// UpdateCredentialName will rename the WebAuthnCredential with the given id if it belongs to userID
+func UpdateCredentialName(ctx context.Context, id, userID int64, name string) error {
+	_, err := db.GetEngine(ctx).ID(id).Where("user_id = ?", userID).Cols("name", "lower_name").Update(&WebAuthnCredential{Name: name})
+	return err
+}
+
 // DeleteCredential will delete WebAuthnCredential
 func DeleteCredential(ctx context.Context, id, userID int64) (bool, error) {
 	had, err := db.GetEngine(ctx).ID(id).Where("user_id = ?", userID).Delete(&WebAuthnCredential{})
diff --git a/models/auth/webauthn_test.go b/models/auth/webauthn_test.go
index a648319226..d7d848f71e 100644
--- a/models/auth/webauthn_test.go
+++ b/models/auth/webauthn_test.go
@@ -64,3 +64,14 @@ func TestCreateCredential(t *testing.T) {
 
 	unittest.AssertExistsAndLoadBean(t, &auth_model.WebAuthnCredential{Name: "WebAuthn Created Credential", UserID: 1})
 }
+
+func TestUpdateCredentialName(t *testing.T) {
+	assert.NoError(t, unittest.PrepareTestDatabase())
+
+	// credential 1 belongs to user 32, so another user's update must not change it
+	assert.NoError(t, auth_model.UpdateCredentialName(t.Context(), 1, 2, "Hijacked"))
+	unittest.AssertExistsAndLoadBean(t, &auth_model.WebAuthnCredential{ID: 1, Name: "WebAuthn credential"})
+
+	assert.NoError(t, auth_model.UpdateCredentialName(t.Context(), 1, 32, "Renamed Key"))
+	unittest.AssertExistsAndLoadBean(t, &auth_model.WebAuthnCredential{ID: 1, Name: "Renamed Key", LowerName: "renamed key", UserID: 32})
+}
diff --git a/options/locale/locale_en-US.json b/options/locale/locale_en-US.json
index 9373aa83a9..694d0bf99b 100644
--- a/options/locale/locale_en-US.json
+++ b/options/locale/locale_en-US.json
@@ -919,6 +919,9 @@
   "settings.webauthn_desc": "Security keys are hardware devices containing cryptographic keys. They can be used for two-factor authentication. Security keys must support the <a rel=\"noreferrer\" target=\"_blank\" href=\"%s\">WebAuthn Authenticator</a> standard.",
   "settings.webauthn_register_key": "Add Security Key",
   "settings.webauthn_nickname": "Nickname",
+  "settings.webauthn_rename_key": "Rename Security Key",
+  "settings.webauthn_rename_key_success": "The security key has been renamed.",
+  "settings.webauthn_nickname_taken": "Another security key already uses this nickname.",
   "settings.webauthn_delete_key": "Remove Security Key",
   "settings.webauthn_delete_key_desc": "If you remove a security key, you can no longer sign in with it. Continue?",
   "settings.webauthn_key_loss_warning": "If you lose your security keys, you will lose access to your account.",
diff --git a/routers/web/user/setting/security/webauthn.go b/routers/web/user/setting/security/webauthn.go
index 4db7b47879..5e93abbbf1 100644
--- a/routers/web/user/setting/security/webauthn.go
+++ b/routers/web/user/setting/security/webauthn.go
@@ -7,6 +7,7 @@ import (
 	"errors"
 	"net/http"
 	"strconv"
+	"strings"
 	"time"
 
 	"gitea.dev/models/auth"
@@ -125,6 +126,52 @@ func WebauthnRegisterPost(ctx *context.Context) {
 	ctx.JSON(http.StatusCreated, cred)
 }
 
+// WebauthnRename renames a security key by id
+func WebauthnRename(ctx *context.Context) {
+	if user_model.IsFeatureDisabledWithLoginType(ctx.Doer, setting.UserFeatureManageMFA) {
+		ctx.HTTPError(http.StatusNotFound)
+		return
+	}
+
+	if ctx.HasError() { // form binding validation error
+		ctx.JSONError(ctx.GetErrMsg())
+		return
+	}
+
+	cred, err := auth.GetWebAuthnCredentialByID(ctx, ctx.FormInt64("id"))
+	if err != nil && !auth.IsErrWebAuthnCredentialNotExist(err) {
+		ctx.ServerError("GetWebAuthnCredentialByID", err)
+		return
+	}
+	if cred == nil || cred.UserID != ctx.Doer.ID { // hide other users' keys as not found
+		ctx.JSONErrorNotFound()
+		return
+	}
+
+	form := web.GetForm(ctx).(*forms.WebauthnRenameForm)
+	form.Name = strings.TrimSpace(form.Name)
+	if form.Name == "" { // "Required" binding only rejects an exactly empty value, not whitespace
+		ctx.JSONError(ctx.Locale.TrString("settings.webauthn_nickname") + ctx.Locale.TrString("form.require_error"))
+		return
+	}
+	sameName, err := auth.GetWebAuthnCredentialByName(ctx, ctx.Doer.ID, form.Name)
+	if err != nil && !auth.IsErrWebAuthnCredentialNotExist(err) {
+		ctx.ServerError("GetWebAuthnCredentialByName", err)
+		return
+	}
+	if sameName != nil && sameName.ID != cred.ID { // renaming a key to its own name with different casing is allowed
+		ctx.JSONError(ctx.Tr("settings.webauthn_nickname_taken"))
+		return
+	}
+
+	if err := auth.UpdateCredentialName(ctx, cred.ID, ctx.Doer.ID, form.Name); err != nil {
+		ctx.ServerError("UpdateCredentialName", err)
+		return
+	}
+	ctx.Flash.Success(ctx.Tr("settings.webauthn_rename_key_success"))
+	ctx.JSONRedirect(setting.AppSubURL + "/user/settings/security")
+}
+
 // WebauthnDelete deletes an security key by id
 func WebauthnDelete(ctx *context.Context) {
 	if user_model.IsFeatureDisabledWithLoginType(ctx.Doer, setting.UserFeatureManageMFA) {
diff --git a/routers/web/web.go b/routers/web/web.go
index 0bfae44e86..f8e48f99fb 100644
--- a/routers/web/web.go
+++ b/routers/web/web.go
@@ -652,6 +652,7 @@ func registerWebRoutes(m *web.Router, webAuth *AuthMiddleware) {
 			m.Group("/webauthn", func() {
 				m.Post("/request_register", web.Bind(forms.WebauthnRegistrationForm{}), security.WebAuthnRegister)
 				m.Post("/register", security.WebauthnRegisterPost)
+				m.Post("/rename", web.Bind(forms.WebauthnRenameForm{}), security.WebauthnRename)
 				m.Post("/delete", security.WebauthnDelete)
 			})
 			m.Group("/openid", func() {
diff --git a/services/forms/user_form.go b/services/forms/user_form.go
index 75275fb5c7..95f3ae0b16 100644
--- a/services/forms/user_form.go
+++ b/services/forms/user_form.go
@@ -419,6 +419,17 @@ func (f *WebauthnRegistrationForm) Validate(req *http.Request, errs binding.Erro
 	return middleware.Validate(errs, ctx.Data, f, ctx.Locale)
 }
 
+// WebauthnRenameForm for renaming an existing WebAuthn credential
+type WebauthnRenameForm struct {
+	Name string `binding:"Required;MaxSize(255)" locale:"settings.webauthn_nickname"` // 255 is the size of the name column
+}
+
+// Validate validates the fields
+func (f *WebauthnRenameForm) Validate(req *http.Request, errs binding.Errors) binding.Errors {
+	ctx := context.GetValidateContext(req)
+	return middleware.Validate(errs, ctx.Data, f, ctx.Locale)
+}
+
 // PackageSettingForm form for package settings
 type PackageSettingForm struct {
 	Action   string
diff --git a/templates/user/settings/security/webauthn.tmpl b/templates/user/settings/security/webauthn.tmpl
index 1bf0159d37..6bfaf11d6b 100644
--- a/templates/user/settings/security/webauthn.tmpl
+++ b/templates/user/settings/security/webauthn.tmpl
@@ -16,6 +16,13 @@
 					</div>
 				</div>
 				<div class="item-trailing">
+					<button class="ui tiny button show-modal"
+						data-modal="#rename-webauthn-modal"
+						data-modal-form.url="{{$.Link}}/webauthn/rename?id={{.ID}}"
+						data-modal-rename-webauthn-nickname="{{.Name}}"
+					>
+						{{svg "octicon-pencil"}} {{ctx.Locale.Tr "settings.webauthn_rename_key"}}
+					</button>
 					<button class="ui red tiny button link-action" data-modal-confirm="#delete-registration" data-url="{{$.Link}}/webauthn/delete?id={{.ID}}">
 					{{ctx.Locale.Tr "settings.delete_key"}}
 					</button>
@@ -30,6 +37,18 @@
 		</div>
 		<button id="register-webauthn" class="ui primary button">{{svg "octicon-key"}} {{ctx.Locale.Tr "settings.webauthn_register_key"}}</button>
 	</div>
+	<div class="ui small modal" id="rename-webauthn-modal">
+		<div class="header">{{svg "octicon-pencil"}} {{ctx.Locale.Tr "settings.webauthn_rename_key"}}</div>
+		<form class="ui form form-fetch-action" method="post">
+			<div class="content">
+				<div class="required field">
+					<label for="rename-webauthn-nickname">{{ctx.Locale.Tr "settings.webauthn_nickname"}}</label>
+					<input id="rename-webauthn-nickname" name="name" type="text" maxlength="255" required>
+				</div>
+			</div>
+			{{template "base/modal_actions_confirm" (dict "ModalButtonTypes" "confirm")}}
+		</form>
+	</div>
 	<div class="ui small modal" id="delete-registration">
 		<div class="header">
 			{{svg "octicon-trash"}}
diff --git a/tests/integration/user_settings_test.go b/tests/integration/user_settings_test.go
index d837da71cd..4df3f9f534 100644
--- a/tests/integration/user_settings_test.go
+++ b/tests/integration/user_settings_test.go
@@ -4,15 +4,21 @@
 package integration
 
 import (
+	"fmt"
 	"net/http"
+	"net/http/httptest"
 	"strings"
 	"testing"
 
+	auth_model "gitea.dev/models/auth"
+	"gitea.dev/models/unittest"
 	"gitea.dev/modules/container"
 	"gitea.dev/modules/setting"
 	"gitea.dev/modules/test"
+	"gitea.dev/modules/translation"
 	"gitea.dev/tests"
 
+	"github.com/go-webauthn/webauthn/webauthn"
 	"github.com/stretchr/testify/assert"
 )
 
@@ -269,6 +275,64 @@ func TestUserSettingsSecurity(t *testing.T) {
 	})
 }
 
+func TestUserSettingsWebAuthnRename(t *testing.T) {
+	defer tests.PrepareTestEnv(t)()
+
+	session := loginUser(t, "user2") // log in before adding keys, otherwise login would require the second factor
+	own, err := auth_model.CreateCredential(t.Context(), 2, "Laptop Key", &webauthn.Credential{ID: []byte("rename-1")})
+	assert.NoError(t, err)
+	_, err = auth_model.CreateCredential(t.Context(), 2, "Phone Key", &webauthn.Credential{ID: []byte("rename-2")})
+	assert.NoError(t, err)
+	other, err := auth_model.CreateCredential(t.Context(), 4, "Other User Key", &webauthn.Credential{ID: []byte("rename-3")})
+	assert.NoError(t, err)
+
+	rename := func(id int64, name string, expectedStatus int) *httptest.ResponseRecorder {
+		req := NewRequestWithValues(t, "POST", fmt.Sprintf("/user/settings/security/webauthn/rename?id=%d", id), map[string]string{"name": name})
+		return session.MakeRequest(t, req, expectedStatus)
+	}
+
+	t.Run("Success", func(t *testing.T) {
+		resp := rename(own.ID, "Work Laptop", http.StatusOK)
+		assert.Equal(t, "/user/settings/security", *test.ParseJSONRedirect(resp.Body.Bytes()).Redirect)
+		cred := unittest.AssertExistsAndLoadBean(t, &auth_model.WebAuthnCredential{ID: own.ID, Name: "Work Laptop"})
+		assert.Equal(t, []byte("rename-1"), cred.CredentialID) // the credential itself is untouched
+	})
+
+	t.Run("CaseChangeOfOwnName", func(t *testing.T) {
+		rename(own.ID, "WORK LAPTOP", http.StatusOK)
+		unittest.AssertExistsAndLoadBean(t, &auth_model.WebAuthnCredential{ID: own.ID, Name: "WORK LAPTOP"})
+	})
+
+	t.Run("InvalidNames", func(t *testing.T) {
+		for _, name := range []string{"", "   ", strings.Repeat("a", 256)} {
+			resp := rename(own.ID, name, http.StatusBadRequest)
+			assert.NotEmpty(t, test.ParseJSONError(resp.Body.Bytes()).ErrorMessage)
+		}
+		unittest.AssertExistsAndLoadBean(t, &auth_model.WebAuthnCredential{ID: own.ID, Name: "WORK LAPTOP"})
+	})
+
+	t.Run("SurroundingSpacesTrimmed", func(t *testing.T) {
+		rename(own.ID, "  Spaced Key  ", http.StatusOK)
+		unittest.AssertExistsAndLoadBean(t, &auth_model.WebAuthnCredential{ID: own.ID, Name: "Spaced Key"})
+		rename(own.ID, "WORK LAPTOP", http.StatusOK) // restore the name the following subtests expect
+	})
+
+	t.Run("NameTaken", func(t *testing.T) {
+		resp := rename(own.ID, "phone key", http.StatusBadRequest)
+		assert.Equal(t, translation.NewLocale("en-US").TrString("settings.webauthn_nickname_taken"), test.ParseJSONError(resp.Body.Bytes()).ErrorMessage)
+		unittest.AssertExistsAndLoadBean(t, &auth_model.WebAuthnCredential{ID: own.ID, Name: "WORK LAPTOP"})
+	})
+
+	t.Run("OtherUsersKey", func(t *testing.T) {
+		rename(other.ID, "Stolen", http.StatusNotFound)
+		unittest.AssertExistsAndLoadBean(t, &auth_model.WebAuthnCredential{ID: other.ID, Name: "Other User Key"})
+	})
+
+	t.Run("NonexistentKey", func(t *testing.T) {
+		rename(99999, "Missing", http.StatusNotFound)
+	})
+}
+
 func TestUserSettingsApplications(t *testing.T) {
 	defer tests.PrepareTestEnv(t)()
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:19:37Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Check tree state and recent commits

`git status --short && echo "---EXIT:$?" && git log --oneline -3`

```text
?? .specstory/
---EXIT:0
98e73f51f3 fix(user): reject whitespace-only security key nicknames
aff9a20450 feat(user): allow renaming security keys
c0092050a4 update to students' guide and adding folder structure
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:19:35Z)**_

I'll start by reading the diff and the key files.

_**Agent - sidechain (claude-opus-5 2026-09-15 02:19:41Z)**_

<tool-use data-tool-type="read" data-tool-name="Read"><details>
<summary>Tool use: **Read**</summary>
 `./routers/web/user/setting/security/webauthn.go`

```text
1	// Copyright 2018 The Gitea Authors. All rights reserved.
2	// SPDX-License-Identifier: MIT
3	
4	package security
5	
6	import (
7		"errors"
8		"net/http"
9		"strconv"
10		"strings"
11		"time"
12	
13		"gitea.dev/models/auth"
14		user_model "gitea.dev/models/user"
15		wa "gitea.dev/modules/auth/webauthn"
16		"gitea.dev/modules/log"
17		"gitea.dev/modules/session"
18		"gitea.dev/modules/setting"
19		"gitea.dev/modules/web"
20		"gitea.dev/services/context"
21		"gitea.dev/services/forms"
22	
23		"github.com/go-webauthn/webauthn/protocol"
24		"github.com/go-webauthn/webauthn/webauthn"
25	)
26	
27	// WebAuthnRegister initializes the webauthn registration procedure
28	func WebAuthnRegister(ctx *context.Context) {
29		if user_model.IsFeatureDisabledWithLoginType(ctx.Doer, setting.UserFeatureManageMFA) {
30			ctx.HTTPError(http.StatusNotFound)
31			return
32		}
33	
34		form := web.GetForm(ctx).(*forms.WebauthnRegistrationForm)
35		if form.Name == "" {
36			// Set name to the hexadecimal of the current time
37			form.Name = strconv.FormatInt(time.Now().UnixNano(), 16)
38		}
39	
40		cred, err := auth.GetWebAuthnCredentialByName(ctx, ctx.Doer.ID, form.Name)
41		if err != nil && !auth.IsErrWebAuthnCredentialNotExist(err) {
42			ctx.ServerError("GetWebAuthnCredentialsByUID", err)
43			return
44		}
45		if cred != nil {
46			ctx.HTTPError(http.StatusConflict, "Name already taken")
47			return
48		}
49	
50		_ = ctx.Session.Delete("webauthnRegistration")
51		if err := ctx.Session.Set("webauthnName", form.Name); err != nil {
52			ctx.ServerError("Unable to set session key for webauthnName", err)
53			return
54		}
55	
56		webAuthnUser := wa.NewWebAuthnUser(ctx, ctx.Doer)
57		credentialOptions, sessionData, err := wa.WebAuthn.BeginRegistration(webAuthnUser, webauthn.WithAuthenticatorSelection(protocol.AuthenticatorSelection{
58			ResidentKey: protocol.ResidentKeyRequirementRequired,
59		}))
60		if err != nil {
61			ctx.ServerError("Unable to BeginRegistration", err)
62			return
63		}
64	
65		// Save the session data as marshaled JSON
66		if err = ctx.Session.Set("webauthnRegistration", sessionData); err != nil {
67			ctx.ServerError("Unable to set session", err)
68			return
69		}
70	
71		ctx.JSON(http.StatusOK, credentialOptions)
72	}
73	
74	// WebauthnRegisterPost receives the response of the security key
75	func WebauthnRegisterPost(ctx *context.Context) {
76		if user_model.IsFeatureDisabledWithLoginType(ctx.Doer, setting.UserFeatureManageMFA) {
77			ctx.HTTPError(http.StatusNotFound)
78			return
79		}
80	
81		name, ok := ctx.Session.Get("webauthnName").(string)
82		if !ok || name == "" {
83			ctx.ServerError("Get webauthnName", errors.New("no webauthnName"))
84			return
85		}
86	
87		// Load the session data
88		sessionData, ok := ctx.Session.Get("webauthnRegistration").(*webauthn.SessionData)
89		if !ok || sessionData == nil {
90			ctx.ServerError("Get registration", errors.New("no registration"))
91			return
92		}
93		defer func() {
94			_ = ctx.Session.Delete("webauthnRegistration")
95		}()
96	
97		// Verify that the challenge succeeded
98		webAuthnUser := wa.NewWebAuthnUser(ctx, ctx.Doer)
99		cred, err := wa.WebAuthn.FinishRegistration(webAuthnUser, *sessionData, ctx.Req)
100		if err != nil {
101			if pErr, ok := err.(*protocol.Error); ok {
102				log.Error("Unable to finish registration due to error: %v\nDevInfo: %s", pErr, pErr.DevInfo)
103			}
104			ctx.ServerError("CreateCredential", err)
105			return
106		}
107	
108		dbCred, err := auth.GetWebAuthnCredentialByName(ctx, ctx.Doer.ID, name)
109		if err != nil && !auth.IsErrWebAuthnCredentialNotExist(err) {
110			ctx.ServerError("GetWebAuthnCredentialsByUID", err)
111			return
112		}
113		if dbCred != nil {
114			ctx.HTTPError(http.StatusConflict, "Name already taken")
115			return
116		}
117	
118		// Create the credential
119		_, err = auth.CreateCredential(ctx, ctx.Doer.ID, name, cred)
120		if err != nil {
121			ctx.ServerError("CreateCredential", err)
122			return
123		}
124		_ = ctx.Session.Delete("webauthnName")
125		_ = ctx.Session.Set(session.KeyUserHasTwoFactorAuth, true)
126		ctx.JSON(http.StatusCreated, cred)
127	}
128	
129	// WebauthnRename renames a security key by id
130	func WebauthnRename(ctx *context.Context) {
131		if user_model.IsFeatureDisabledWithLoginType(ctx.Doer, setting.UserFeatureManageMFA) {
132			ctx.HTTPError(http.StatusNotFound)
133			return
134		}
135	
136		if ctx.HasError() { // form binding validation error
137			ctx.JSONError(ctx.GetErrMsg())
138			return
139		}
140	
141		cred, err := auth.GetWebAuthnCredentialByID(ctx, ctx.FormInt64("id"))
142		if err != nil && !auth.IsErrWebAuthnCredentialNotExist(err) {
143			ctx.ServerError("GetWebAuthnCredentialByID", err)
144			return
145		}
146		if cred == nil || cred.UserID != ctx.Doer.ID { // hide other users' keys as not found
147			ctx.JSONErrorNotFound()
148			return
149		}
150	
151		form := web.GetForm(ctx).(*forms.WebauthnRenameForm)
152		form.Name = strings.TrimSpace(form.Name)
153		if form.Name == "" { // "Required" binding only rejects an exactly empty value, not whitespace
154			ctx.JSONError(ctx.Locale.TrString("settings.webauthn_nickname") + ctx.Locale.TrString("form.require_error"))
155			return
156		}
157		sameName, err := auth.GetWebAuthnCredentialByName(ctx, ctx.Doer.ID, form.Name)
158		if err != nil && !auth.IsErrWebAuthnCredentialNotExist(err) {
159			ctx.ServerError("GetWebAuthnCredentialByName", err)
160			return
161		}
162		if sameName != nil && sameName.ID != cred.ID { // renaming a key to its own name with different casing is allowed
163			ctx.JSONError(ctx.Tr("settings.webauthn_nickname_taken"))
164			return
165		}
166	
167		if err := auth.UpdateCredentialName(ctx, cred.ID, ctx.Doer.ID, form.Name); err != nil {
168			ctx.ServerError("UpdateCredentialName", err)
169			return
170		}
171		ctx.Flash.Success(ctx.Tr("settings.webauthn_rename_key_success"))
172		ctx.JSONRedirect(setting.AppSubURL + "/user/settings/security")
173	}
174	
175	// WebauthnDelete deletes an security key by id
176	func WebauthnDelete(ctx *context.Context) {
177		if user_model.IsFeatureDisabledWithLoginType(ctx.Doer, setting.UserFeatureManageMFA) {
178			ctx.HTTPError(http.StatusNotFound)
179			return
180		}
181	
182		if _, err := auth.DeleteCredential(ctx, ctx.FormInt64("id"), ctx.Doer.ID); err != nil {
183			ctx.ServerError("GetWebAuthnCredentialByID", err)
184			return
185		}
186		ctx.JSONRedirect(setting.AppSubURL + "/user/settings/security")
187	}
188
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:19:45Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Read webauthn model file

`cat models/auth/webauthn.go`

```text
// Copyright 2020 The Gitea Authors. All rights reserved.
// SPDX-License-Identifier: MIT

package auth

import (
	"context"
	"fmt"
	"strings"

	"gitea.dev/models/db"
	"gitea.dev/modules/timeutil"
	"gitea.dev/modules/util"

	"github.com/go-webauthn/webauthn/protocol"
	"github.com/go-webauthn/webauthn/webauthn"
)

// ErrWebAuthnCredentialNotExist represents a "ErrWebAuthnCRedentialNotExist" kind of error.
type ErrWebAuthnCredentialNotExist struct {
	ID           int64
	CredentialID []byte
}

func (err ErrWebAuthnCredentialNotExist) Error() string {
	if len(err.CredentialID) == 0 {
		return fmt.Sprintf("WebAuthn credential does not exist [id: %d]", err.ID)
	}
	return fmt.Sprintf("WebAuthn credential does not exist [credential_id: %x]", err.CredentialID)
}

// Unwrap unwraps this as a ErrNotExist err
func (err ErrWebAuthnCredentialNotExist) Unwrap() error {
	return util.ErrNotExist
}

// IsErrWebAuthnCredentialNotExist checks if an error is a ErrWebAuthnCredentialNotExist.
func IsErrWebAuthnCredentialNotExist(err error) bool {
	_, ok := err.(ErrWebAuthnCredentialNotExist)
	return ok
}

// WebAuthnCredential represents the WebAuthn credential data for a public-key
// credential conformant to WebAuthn Level 1
type WebAuthnCredential struct {
	ID              int64 `xorm:"pk autoincr"`
	Name            string
	LowerName       string `xorm:"unique(s)"`
	UserID          int64  `xorm:"INDEX unique(s)"`
	CredentialID    []byte `xorm:"INDEX VARBINARY(1024)"`
	PublicKey       []byte
	AttestationType string
	AAGUID          []byte
	SignCount       uint32 `xorm:"BIGINT"`
	CloneWarning    bool
	CreatedUnix     timeutil.TimeStamp `xorm:"INDEX created"`
	UpdatedUnix     timeutil.TimeStamp `xorm:"INDEX updated"`
}

func init() {
	db.RegisterModel(new(WebAuthnCredential))
}

// TableName returns a better table name for WebAuthnCredential
func (cred WebAuthnCredential) TableName() string {
	return "webauthn_credential"
}

// UpdateSignCount will update the database value of SignCount
func (cred *WebAuthnCredential) UpdateSignCount(ctx context.Context) error {
	_, err := db.GetEngine(ctx).ID(cred.ID).Cols("sign_count").Update(cred)
	return err
}

// BeforeInsert will be invoked by XORM before updating a record
func (cred *WebAuthnCredential) BeforeInsert() {
	cred.LowerName = strings.ToLower(cred.Name)
}

// BeforeUpdate will be invoked by XORM before updating a record
func (cred *WebAuthnCredential) BeforeUpdate() {
	cred.LowerName = strings.ToLower(cred.Name)
}

// AfterLoad is invoked from XORM after setting the values of all fields of this object.
func (cred *WebAuthnCredential) AfterLoad() {
	cred.LowerName = strings.ToLower(cred.Name)
}

// WebAuthnCredentialList is a list of *WebAuthnCredential
type WebAuthnCredentialList []*WebAuthnCredential

// newCredentialFlagsFromAuthenticatorFlags is copied from https://github.com/go-webauthn/webauthn/pull/337
// to convert protocol.AuthenticatorFlags to webauthn.CredentialFlags
func newCredentialFlagsFromAuthenticatorFlags(flags protocol.AuthenticatorFlags) webauthn.CredentialFlags {
	return webauthn.CredentialFlags{
		UserPresent:    flags.HasUserPresent(),
		UserVerified:   flags.HasUserVerified(),
		BackupEligible: flags.HasBackupEligible(),
		BackupState:    flags.HasBackupState(),
	}
}

// ToCredentials will convert all WebAuthnCredentials to webauthn.Credentials
func (list WebAuthnCredentialList) ToCredentials(defaultAuthFlags ...protocol.AuthenticatorFlags) []webauthn.Credential {
	// TODO: at the moment, Gitea doesn't store or check the flags
	// so we need to use the default flags from the authenticator to make the login validation pass
	// In the future, we should:
	// 1. store the flags when registering the credential
	// 2. provide the stored flags when converting the credentials (for login)
	// 3. for old users, still use this fallback to the default flags
	defAuthFlags := util.OptionalArg(defaultAuthFlags)
	creds := make([]webauthn.Credential, 0, len(list))
	for _, cred := range list {
		creds = append(creds, webauthn.Credential{
			ID:              cred.CredentialID,
			PublicKey:       cred.PublicKey,
			AttestationType: cred.AttestationType,
			Flags:           newCredentialFlagsFromAuthenticatorFlags(defAuthFlags),
			Authenticator: webauthn.Authenticator{
				AAGUID:       cred.AAGUID,
				SignCount:    cred.SignCount,
				CloneWarning: cred.CloneWarning,
			},
		})
	}
	return creds
}

// GetWebAuthnCredentialsByUID returns all WebAuthn credentials of the given user
func GetWebAuthnCredentialsByUID(ctx context.Context, uid int64) (WebAuthnCredentialList, error) {
	creds := make(WebAuthnCredentialList, 0)
	return creds, db.GetEngine(ctx).Where("user_id = ?", uid).Find(&creds)
}

// ExistsWebAuthnCredentialsForUID returns if the given user has credentials
func ExistsWebAuthnCredentialsForUID(ctx context.Context, uid int64) (bool, error) {
	return db.GetEngine(ctx).Where("user_id = ?", uid).Exist(&WebAuthnCredential{})
}

// GetWebAuthnCredentialByName returns WebAuthn credential by id
func GetWebAuthnCredentialByName(ctx context.Context, uid int64, name string) (*WebAuthnCredential, error) {
	cred := new(WebAuthnCredential)
	if found, err := db.GetEngine(ctx).Where("user_id = ? AND lower_name = ?", uid, strings.ToLower(name)).Get(cred); err != nil {
		return nil, err
	} else if !found {
		return nil, ErrWebAuthnCredentialNotExist{}
	}
	return cred, nil
}

// GetWebAuthnCredentialByID returns WebAuthn credential by id
func GetWebAuthnCredentialByID(ctx context.Context, id int64) (*WebAuthnCredential, error) {
	cred := new(WebAuthnCredential)
	if found, err := db.GetEngine(ctx).ID(id).Get(cred); err != nil {
		return nil, err
	} else if !found {
		return nil, ErrWebAuthnCredentialNotExist{ID: id}
	}
	return cred, nil
}

// HasWebAuthnRegistrationsByUID returns whether a given user has WebAuthn registrations
func HasWebAuthnRegistrationsByUID(ctx context.Context, uid int64) (bool, error) {
	return db.GetEngine(ctx).Where("user_id = ?", uid).Exist(&WebAuthnCredential{})
}

// GetWebAuthnCredentialByCredID returns WebAuthn credential by credential ID
func GetWebAuthnCredentialByCredID(ctx context.Context, userID int64, credID []byte) (*WebAuthnCredential, error) {
	cred := new(WebAuthnCredential)
	if found, err := db.GetEngine(ctx).Where("user_id = ? AND credential_id = ?", userID, credID).Get(cred); err != nil {
		return nil, err
	} else if !found {
		return nil, ErrWebAuthnCredentialNotExist{CredentialID: credID}
	}
	return cred, nil
}

// CreateCredential will create a new WebAuthnCredential from the given Credential
func CreateCredential(ctx context.Context, userID int64, name string, cred *webauthn.Credential) (*WebAuthnCredential, error) {
	c := &WebAuthnCredential{
		UserID:          userID,
		Name:            name,
		CredentialID:    cred.ID,
		PublicKey:       cred.PublicKey,
		AttestationType: cred.AttestationType,
		AAGUID:          cred.Authenticator.AAGUID,
		SignCount:       cred.Authenticator.SignCount,
		CloneWarning:    false,
	}

	if err := db.Insert(ctx, c); err != nil {
		return nil, err
	}
	return c, nil
}

// UpdateCredentialName will rename the WebAuthnCredential with the given id if it belongs to userID
func UpdateCredentialName(ctx context.Context, id, userID int64, name string) error {
	_, err := db.GetEngine(ctx).ID(id).Where("user_id = ?", userID).Cols("name", "lower_name").Update(&WebAuthnCredential{Name: name})
	return err
}

// DeleteCredential will delete WebAuthnCredential
func DeleteCredential(ctx context.Context, id, userID int64) (bool, error) {
	had, err := db.GetEngine(ctx).ID(id).Where("user_id = ?", userID).Delete(&WebAuthnCredential{})
	return had > 0, err
}
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:19:45Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Show diffstat

`git diff --stat $(git merge-base main HEAD)..HEAD`

```text
 models/auth/webauthn.go                        |  6 +++
 models/auth/webauthn_test.go                   | 11 +++++
 options/locale/locale_en-US.json               |  3 ++
 routers/web/user/setting/security/webauthn.go  | 47 +++++++++++++++++++
 routers/web/web.go                             |  1 +
 services/forms/user_form.go                    | 11 +++++
 templates/user/settings/security/webauthn.tmpl | 19 ++++++++
 tests/integration/user_settings_test.go        | 64 ++++++++++++++++++++++++++
 8 files changed, 162 insertions(+)
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:19:44Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Show commit stats

`git log --oneline $(git merge-base main HEAD)..HEAD --stat`

```text
98e73f51f3 fix(user): reject whitespace-only security key nicknames
 routers/web/user/setting/security/webauthn.go | 6 ++++++
 tests/integration/user_settings_test.go       | 8 +++++++-
 2 files changed, 13 insertions(+), 1 deletion(-)
aff9a20450 feat(user): allow renaming security keys
 models/auth/webauthn.go                        |  6 +++
 models/auth/webauthn_test.go                   | 11 +++++
 options/locale/locale_en-US.json               |  3 ++
 routers/web/user/setting/security/webauthn.go  | 41 ++++++++++++++++++
 routers/web/web.go                             |  1 +
 services/forms/user_form.go                    | 11 +++++
 templates/user/settings/security/webauthn.tmpl | 19 +++++++++
 tests/integration/user_settings_test.go        | 58 ++++++++++++++++++++++++++
 8 files changed, 150 insertions(+)
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:19:40Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Read common-fetch-action.ts

`cat web_src/js/features/common-fetch-action.ts`

```text
import {GET, request} from '../modules/fetch.ts';
import {hideToastsAll, showErrorToast} from '../modules/toast.ts';
import {activePageTimerRefresh, addDelegatedEventListener, createElementFromHTML} from '../utils/dom.ts';
import {errorMessage, errorName} from '../modules/errors.ts';
import {confirmModal, createConfirmModal} from './comp/ConfirmModal.ts';
import {ignoreAreYouSure} from '../vendor/jquery.are-you-sure.ts';
import {registerGlobalSelectorFunc} from '../modules/observer.ts';
import {Idiomorph} from 'idiomorph';
import {parseDom} from '../utils.ts';
import {html} from '../utils/html.ts';

const {appSubUrl, runModeIsProd} = window.config;

type FetchActionOpts = {
  method: string;
  url: string;
  headers?: HeadersInit;
  body?: FormData;
  formSubmitter?: HTMLElement | null;

  // pseudo selectors/commands to update the current page with the response text when the response is text (html)
  // e.g.: "$this", "$innerHTML", "$closest(tr) td .the-class", "$body #the-id"
  successSync: string;

  // the loading indicator element selector, it uses the same syntax as "data-fetch-sync" to find the element(s)
  // empty means no loading indicator, "$this" means the element itself
  loadingIndicator: string;
};

// fetchActionDoRedirect does real redirection to bypass the browser's limitations of "location"
// more details are in the backend's fetch-redirect handler
function fetchActionDoRedirect(redirect: string) {
  // In production, if the link can be directly navigated by browser, we just do normal redirection, which is faster.
  // Otherwise, need to use backend to do redirection:
  // * Also do so in development, to make sure the redirection logic is always tested by real users
  const needBackendHelp = redirect.includes('#');
  if (runModeIsProd && !needBackendHelp) {
    window.location.assign(redirect);
    return;
  }

  // use backend to do redirection, which can bypass the browser's limitations of "location"
  const form = createElementFromHTML<HTMLFormElement>(html`<form method="post"></form>`);
  form.action = `${appSubUrl}/-/fetch-redirect?redirect=${encodeURIComponent(redirect)}`;
  document.body.append(form);
  form.submit();
}

function toggleLoadingIndicator(el: HTMLElement, opt: FetchActionOpts, isLoading: boolean) {
  const loadingIndicatorElems = opt.loadingIndicator ? execPseudoSelectorCommands(el, opt.loadingIndicator).targets : [];
  for (const indicatorEl of loadingIndicatorElems) {
    if (isLoading) {
      // for button or input element, we can directly disable it, it looks better than adding a loading spinner
      if ('disabled' in indicatorEl) {
        indicatorEl.disabled = true;
      } else {
        indicatorEl.classList.add('is-loading');
        if (indicatorEl.clientHeight < 50) indicatorEl.classList.add('loading-icon-2px');
      }
    } else {
      if ('disabled' in indicatorEl) {
        indicatorEl.disabled = false;
      } else {
        indicatorEl.classList.remove('is-loading', 'loading-icon-2px');
      }
    }
  }
}

export async function handleFetchActionSuccessJson(el: HTMLElement, respJson: any) {
  ignoreAreYouSure(el); // ignore the areYouSure check before reloading
  const redirect = respJson?.redirect;
  if (typeof redirect === 'string' && redirect) {
    fetchActionDoRedirect(redirect);
  } else {
    // reserved behavior, in the future, there can be more fields to introduce more behaviors
    window.location.reload();
  }
}

async function handleFetchActionSuccess(el: HTMLElement, opt: FetchActionOpts, resp: Response) {
  const isRespJson = resp.headers.get('content-type')?.includes('application/json');
  const respText = await resp.text();
  const respJson = isRespJson ? JSON.parse(respText) : null;
  if (isRespJson) {
    await handleFetchActionSuccessJson(el, respJson);
  } else if (opt.successSync) {
    await handleFetchActionSuccessSync(el, opt.successSync, respText);
  } else {
    showErrorToast(`Unsupported fetch action response, expected JSON but got: ${respText.substring(0, 200)}`);
  }
}

async function handleFetchActionError(resp: Response) {
  const isRespJson = resp.headers.get('content-type')?.includes('application/json');
  const respText = await resp.text();
  const respJson = isRespJson ? JSON.parse(respText) : null;
  if (respJson?.errorMessage) {
    // the code was quite messy, sometimes the backend uses "err", sometimes it uses "error", and even "user_error"
    // but at the moment, as a new approach, we only use "errorMessage" here, backend can use JSONError() to respond.
    showErrorToast(respJson.errorMessage, {useHtmlBody: respJson.renderFormat === 'html'});
  } else {
    showErrorToast(`Error ${resp.status} ${resp.statusText}. Response: ${respText.substring(0, 200)}`);
  }
}

function buildFetchActionUrl(el: HTMLElement, opt: FetchActionOpts) {
  let url = opt.url;
  if ('name' in el && 'value' in el) {
    // ref: https://htmx.org/attributes/hx-get/
    // If the element with the hx-get attribute also has a value, this will be included as a parameter
    const name = (el as HTMLInputElement).name;
    const val = (el as HTMLInputElement).value;
    const u = new URL(url, window.location.href);
    if (name && !u.searchParams.has(name)) {
      u.searchParams.set(name, val);
      url = u.href;
    }
  }
  return url;
}

async function performActionRequest(el: HTMLElement, opt: FetchActionOpts) {
  const attrIsLoading = 'data-fetch-is-loading';
  if (el.getAttribute(attrIsLoading)) return;
  if (!await confirmFetchAction(opt.formSubmitter ?? el)) return;

  el.setAttribute(attrIsLoading, 'true');
  toggleLoadingIndicator(el, opt, true);

  try {
    const url = buildFetchActionUrl(el, opt);
    const headers = new Headers(opt.headers);
    headers.set('X-Gitea-Fetch-Action', '1');
    const resp = await request(url, {method: opt.method, body: opt.body, headers});
    if (resp.ok) {
      await handleFetchActionSuccess(el, opt, resp);
      return;
    }
    await handleFetchActionError(resp);
  } catch (err) {
    if (errorName(err) !== 'AbortError') {
      console.error(`Fetch action request error:`, err);
      showErrorToast(`Error: ${errorMessage(err)}`);
    }
  } finally {
    toggleLoadingIndicator(el, opt, false);
    el.removeAttribute(attrIsLoading);
  }
}

type SubmitFormFetchActionOpts = {
  formSubmitter?: HTMLElement | null;
  formData?: FormData;
};

function prepareFormFetchActionOpts(formEl: HTMLFormElement, opts: SubmitFormFetchActionOpts = {}): FetchActionOpts {
  const formMethodUpper = formEl.getAttribute('method')?.toUpperCase() || 'GET';
  const formActionUrl = formEl.getAttribute('action') || window.location.href;
  const formData = opts.formData ?? new FormData(formEl);
  const [submitterName, submitterValue] = [opts.formSubmitter?.getAttribute('name'), opts.formSubmitter?.getAttribute('value')];
  if (submitterName) {
    formData.append(submitterName, submitterValue || '');
  }

  let reqUrl = formActionUrl;
  let reqBody: FormData | undefined;
  if (formMethodUpper === 'GET') {
    const params = new URLSearchParams();
    for (const [key, value] of formData) {
      params.append(key, value as string);
    }
    const pos = reqUrl.indexOf('?');
    if (pos !== -1) {
      reqUrl = reqUrl.slice(0, pos);
    }
    reqUrl += `?${params.toString()}`;
  } else {
    reqBody = formData;
  }
  return {
    method: formMethodUpper,
    url: reqUrl,
    body: reqBody,
    formSubmitter: opts.formSubmitter,
    loadingIndicator: '$this', // for form submit, by default, the loading indicator is the whole form
    successSync: formEl.getAttribute('data-fetch-sync') ?? '', // by default, no fetch sync for form submit
  };
}

export async function submitFormFetchAction(formEl: HTMLFormElement, opts: SubmitFormFetchActionOpts = {}) {
  hideToastsAll();
  await performActionRequest(formEl, prepareFormFetchActionOpts(formEl, opts));
}

async function confirmFetchAction(el: HTMLElement) {
  let elModal: HTMLElement | null = null;
  const dataModalConfirm = el.getAttribute('data-modal-confirm') || '';
  if (dataModalConfirm.startsWith('#')) {
    // eslint-disable-next-line unicorn/prefer-query-selector
    elModal = document.getElementById(dataModalConfirm.substring(1));
    if (elModal) {
      elModal = createElementFromHTML(elModal.outerHTML);
      elModal.removeAttribute('id');
    }
  }
  if (!elModal) {
    const modalConfirmContent = dataModalConfirm || el.getAttribute('data-modal-confirm-content') || '';
    if (modalConfirmContent) {
      const isRisky = el.classList.contains('red') || el.classList.contains('negative');
      elModal = createConfirmModal({
        header: el.getAttribute('data-modal-confirm-header') || '',
        content: modalConfirmContent,
        confirmButtonColor: isRisky ? 'red' : 'primary',
      });
    }
  }
  if (!elModal) return true;
  return await confirmModal(elModal);
}

async function performLinkFetchAction(el: HTMLElement) {
  hideToastsAll();
  await performActionRequest(el, {
    method: el.getAttribute('data-fetch-method') || 'POST', // by default, the method is POST for link-action
    url: el.getAttribute('data-url')!,
    loadingIndicator: el.getAttribute('data-fetch-indicator') ?? '$this', // by default, the link-action itself is the loading indicator
    successSync: el.getAttribute('data-fetch-sync') ?? '', // by default, no fetch sync for link-action
  });
}

type FetchActionTriggerType = 'click' | 'change' | 'every' | 'load' | 'fetch-reload';

export async function performFetchActionTrigger(el: HTMLElement, triggerType: FetchActionTriggerType) {
  const isUserInitiated = triggerType === 'click' || triggerType === 'change';
  // for user initiated action, by default, the loading indicator is the element itself, otherwise no loading indicator
  const defaultLoadingIndicator = isUserInitiated ? '$this' : '';

  if (isUserInitiated) hideToastsAll();
  await performActionRequest(el, {
    method: el.getAttribute('data-fetch-method') || 'GET', // by default, the method is GET for fetch trigger action
    url: el.getAttribute('data-fetch-url')!,
    loadingIndicator: el.getAttribute('data-fetch-indicator') ?? defaultLoadingIndicator,
    successSync: el.getAttribute('data-fetch-sync') ?? '$this', // by default, the response will replace the current element
  });
}

type PseudoSelectorCommandResult = {
  targets: Element[];
  cmdInnerHTML: boolean;
  cmdMorph: boolean;
};

export function execPseudoSelectorCommands(el: Element, fullCommand: string): PseudoSelectorCommandResult {
  const cmds = fullCommand.split(' ').map((s) => s.trim()).filter(Boolean) || [];
  let targets = [el], cmdInnerHTML = false, cmdMorph = false;
  for (const cmd of cmds) {
    if (cmd === '$this') {
      targets = [el];
    } else if (cmd === '$body') {
      targets = [document.body];
    } else if (cmd === '$innerHTML') {
      cmdInnerHTML = true;
    } else if (cmd === '$morph') {
      cmdMorph = true;
    } else if (cmd.startsWith('$closest(') && cmd.endsWith(')')) {
      const selector = cmd.substring('$closest('.length, cmd.length - 1);
      const newTargets: Element[] = [];
      for (const target of targets) {
        const closest = target.closest(selector);
        if (closest) newTargets.push(closest);
      }
      targets = newTargets;
    } else {
      const newTargets: Element[] = [];
      for (const target of targets) {
        newTargets.push(...target.querySelectorAll(cmd));
      }
      targets = newTargets;
    }
  }
  return {targets, cmdInnerHTML, cmdMorph};
}

async function handleFetchActionSuccessSync(el: Element, successSync: string, respText: string) {
  const res = execPseudoSelectorCommands(el, successSync);
  if (!res.targets.length) throw new Error(`Fetch-sync command "${successSync}" did not find any target element to update`);
  if (res.targets.length > 1) throw new Error(`Fetch-sync command "${successSync}" found multiple target elements, which is not supported`);
  const target = res.targets[0];
  if (res.cmdMorph) {
    Idiomorph.morph(target, respText, {morphStyle: res.cmdInnerHTML ? 'innerHTML' : 'outerHTML'});
  } else if (res.cmdInnerHTML) {
    target.innerHTML = respText;
  } else {
    target.outerHTML = respText;
  }
  await fetchActionReloadOutdatedElements();
}

async function fetchActionReloadOutdatedElements() {
  const outdatedElems: HTMLElement[] = [];
  for (const outdated of document.querySelectorAll<HTMLElement>('[data-fetch-trigger~="fetch-reload"]')) {
    if (!outdated.id) throw new Error(`Elements with "fetch-reload" trigger must have an id to be reloaded after fetch sync: ${outdated.outerHTML.substring(0, 100)}`);
    outdatedElems.push(outdated);
  }
  if (!outdatedElems.length) return;

  const resp = await GET(window.location.href);
  if (!resp.ok) {
    showErrorToast(`Failed to reload page content after fetch action: ${resp.status} ${resp.statusText}`);
    return;
  }
  const newPageHtml = await resp.text();
  const newPageDom = parseDom(newPageHtml, 'text/html');
  for (const oldEl of outdatedElems) {
    // eslint-disable-next-line unicorn/prefer-query-selector
    const newEl = newPageDom.getElementById(oldEl.id);
    if (newEl) {
      oldEl.replaceWith(newEl);
    } else {
      oldEl.remove();
    }
  }
}

function initFetchActionTriggerEvery(el: HTMLElement, trigger: string) {
  const interval = trigger.substring('every '.length);
  const match = /^(\d+)(ms|s)$/.exec(interval);
  if (!match) throw new Error(`Invalid interval format: ${interval}`);

  const num = parseInt(match[1], 10), unit = match[2];
  const intervalMs = unit === 's' ? num * 1000 : num;
  activePageTimerRefresh({
    interval: () => document.contains(el) ? intervalMs : 0, // only continue if the element is still in the document
    async callback() { await performFetchActionTrigger(el, 'every') },
  });
}

function initFetchActionTrigger(el: HTMLElement) {
  const trigger = el.getAttribute('data-fetch-trigger');

  // this trigger is managed internally, only triggered after fetch sync success, not triggered by event or timer
  if (trigger === 'fetch-reload') return;

  if (trigger === 'load') {
    performFetchActionTrigger(el, trigger);
  } else if (trigger === 'change') {
    el.addEventListener('change', () => performFetchActionTrigger(el, trigger));
  } else if (trigger?.startsWith('every ')) {
    initFetchActionTriggerEvery(el, trigger);
  } else if (!trigger || trigger === 'click') {
    el.addEventListener('click', (e) => {
      e.preventDefault();
      performFetchActionTrigger(el, 'click');
    });
  } else {
    throw new Error(`Unsupported fetch trigger: ${trigger}`);
  }
}

export function initGlobalFetchAction() {
  // The "fetch-action" framework is a general approach for elements to trigger fetch requests:
  // show confirm dialog (if any), show loading indicators, send fetch request, and redirect or update UI after success.
  //
  // If you need more fine-grained control more details, sometimes it's clearer to write the logic in JavaScript, instead of using this generic framework.
  //
  // Attributes:
  //
  // * data-fetch-method: the HTTP method to use
  //   * default to "GET" for "data-fetch-url" actions, "POST" for "link-action" elements
  //   * this attribute is ignored, the method will be determined by the form's "method" attribute, and default to "GET"
  //
  // * data-fetch-url: the URL for the request
  //
  // * data-fetch-trigger: the event to trigger the fetch action, can be:
  //   * "click", "change" (user-initiated events)
  //   * "load" (triggered on page load)
  //   * "every 5s" (also support "ms" unit)
  //   * "fetch-reload" (only triggered by fetch sync success to reload outdated content)
  //
  // * data-fetch-indicator: the loading indicator element selector, it uses the same syntax as "data-fetch-sync" to find the element(s)
  //
  // * data-fetch-sync: when the response is text (html), the pseudo selectors/commands defined in "data-fetch-sync"
  //   will be used to update the content in the current page. It only supports some simple syntaxes that we need.
  //   "$" prefix means it is our private command (for special logic), the selectors are run one by one from current element.
  //   * "$this": replace the current element with the response
  //   * "$innerHTML": replace innerHTML of the current element with the response, instead of replacing the whole element (outerHTML)
  //   * "$morph": use morph algorithm to update the target element
  //   * "$body #the-id .the-class": query the selector one by one from body
  //   * "$closest(tr) td": pseudo command can help to find the target element in a more flexible way
  //
  // * data-modal-confirm: a "confirm modal dialog" will be shown before taking action.
  //   * it can be a string for the content of the modal dialog
  //   * it has "-header" and "-content" variants to set the header and content of the "confirm modal"
  //   * it can refer an existing modal element by "#the-modal-id"

  addDelegatedEventListener<HTMLFormElement, SubmitEvent>(document, 'submit', '.form-fetch-action', async (el, e) => {
    // "fetch-action" will use the form's data to send the request
    e.preventDefault();
    await submitFormFetchAction(el, {formSubmitter: e.submitter});
  });

  addDelegatedEventListener(document, 'click', '.link-action', async (el, e) => {
    // `<a class="link-action" data-url="...">` is a shorthand for
    // `<a data-fetch-trigger="click" data-fetch-method="post" data-fetch-url="..." data-fetch-indicator="$this">`
    e.preventDefault();
    await performLinkFetchAction(el);
  });

  registerGlobalSelectorFunc('[data-fetch-url]', initFetchActionTrigger);
}
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:19:39Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Show diff stat for branch

`git diff $(git merge-base main HEAD)..HEAD --stat`

```text
 models/auth/webauthn.go                        |  6 +++
 models/auth/webauthn_test.go                   | 11 +++++
 options/locale/locale_en-US.json               |  3 ++
 routers/web/user/setting/security/webauthn.go  | 47 +++++++++++++++++++
 routers/web/web.go                             |  1 +
 services/forms/user_form.go                    | 11 +++++
 templates/user/settings/security/webauthn.tmpl | 19 ++++++++
 tests/integration/user_settings_test.go        | 64 ++++++++++++++++++++++++++
 8 files changed, 162 insertions(+)
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:19:40Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Show the full diff under review

`git diff $(git merge-base main HEAD)..HEAD`

```text
diff --git a/models/auth/webauthn.go b/models/auth/webauthn.go
index 11297556d2..b37e9f51ef 100644
--- a/models/auth/webauthn.go
+++ b/models/auth/webauthn.go
@@ -195,6 +195,12 @@ func CreateCredential(ctx context.Context, userID int64, name string, cred *weba
 	return c, nil
 }
 
+// UpdateCredentialName will rename the WebAuthnCredential with the given id if it belongs to userID
+func UpdateCredentialName(ctx context.Context, id, userID int64, name string) error {
+	_, err := db.GetEngine(ctx).ID(id).Where("user_id = ?", userID).Cols("name", "lower_name").Update(&WebAuthnCredential{Name: name})
+	return err
+}
+
 // DeleteCredential will delete WebAuthnCredential
 func DeleteCredential(ctx context.Context, id, userID int64) (bool, error) {
 	had, err := db.GetEngine(ctx).ID(id).Where("user_id = ?", userID).Delete(&WebAuthnCredential{})
diff --git a/models/auth/webauthn_test.go b/models/auth/webauthn_test.go
index a648319226..d7d848f71e 100644
--- a/models/auth/webauthn_test.go
+++ b/models/auth/webauthn_test.go
@@ -64,3 +64,14 @@ func TestCreateCredential(t *testing.T) {
 
 	unittest.AssertExistsAndLoadBean(t, &auth_model.WebAuthnCredential{Name: "WebAuthn Created Credential", UserID: 1})
 }
+
+func TestUpdateCredentialName(t *testing.T) {
+	assert.NoError(t, unittest.PrepareTestDatabase())
+
+	// credential 1 belongs to user 32, so another user's update must not change it
+	assert.NoError(t, auth_model.UpdateCredentialName(t.Context(), 1, 2, "Hijacked"))
+	unittest.AssertExistsAndLoadBean(t, &auth_model.WebAuthnCredential{ID: 1, Name: "WebAuthn credential"})
+
+	assert.NoError(t, auth_model.UpdateCredentialName(t.Context(), 1, 32, "Renamed Key"))
+	unittest.AssertExistsAndLoadBean(t, &auth_model.WebAuthnCredential{ID: 1, Name: "Renamed Key", LowerName: "renamed key", UserID: 32})
+}
diff --git a/options/locale/locale_en-US.json b/options/locale/locale_en-US.json
index 9373aa83a9..694d0bf99b 100644
--- a/options/locale/locale_en-US.json
+++ b/options/locale/locale_en-US.json
@@ -919,6 +919,9 @@
   "settings.webauthn_desc": "Security keys are hardware devices containing cryptographic keys. They can be used for two-factor authentication. Security keys must support the <a rel=\"noreferrer\" target=\"_blank\" href=\"%s\">WebAuthn Authenticator</a> standard.",
   "settings.webauthn_register_key": "Add Security Key",
   "settings.webauthn_nickname": "Nickname",
+  "settings.webauthn_rename_key": "Rename Security Key",
+  "settings.webauthn_rename_key_success": "The security key has been renamed.",
+  "settings.webauthn_nickname_taken": "Another security key already uses this nickname.",
   "settings.webauthn_delete_key": "Remove Security Key",
   "settings.webauthn_delete_key_desc": "If you remove a security key, you can no longer sign in with it. Continue?",
   "settings.webauthn_key_loss_warning": "If you lose your security keys, you will lose access to your account.",
diff --git a/routers/web/user/setting/security/webauthn.go b/routers/web/user/setting/security/webauthn.go
index 4db7b47879..5e93abbbf1 100644
--- a/routers/web/user/setting/security/webauthn.go
+++ b/routers/web/user/setting/security/webauthn.go
@@ -7,6 +7,7 @@ import (
 	"errors"
 	"net/http"
 	"strconv"
+	"strings"
 	"time"
 
 	"gitea.dev/models/auth"
@@ -125,6 +126,52 @@ func WebauthnRegisterPost(ctx *context.Context) {
 	ctx.JSON(http.StatusCreated, cred)
 }
 
+// WebauthnRename renames a security key by id
+func WebauthnRename(ctx *context.Context) {
+	if user_model.IsFeatureDisabledWithLoginType(ctx.Doer, setting.UserFeatureManageMFA) {
+		ctx.HTTPError(http.StatusNotFound)
+		return
+	}
+
+	if ctx.HasError() { // form binding validation error
+		ctx.JSONError(ctx.GetErrMsg())
+		return
+	}
+
+	cred, err := auth.GetWebAuthnCredentialByID(ctx, ctx.FormInt64("id"))
+	if err != nil && !auth.IsErrWebAuthnCredentialNotExist(err) {
+		ctx.ServerError("GetWebAuthnCredentialByID", err)
+		return
+	}
+	if cred == nil || cred.UserID != ctx.Doer.ID { // hide other users' keys as not found
+		ctx.JSONErrorNotFound()
+		return
+	}
+
+	form := web.GetForm(ctx).(*forms.WebauthnRenameForm)
+	form.Name = strings.TrimSpace(form.Name)
+	if form.Name == "" { // "Required" binding only rejects an exactly empty value, not whitespace
+		ctx.JSONError(ctx.Locale.TrString("settings.webauthn_nickname") + ctx.Locale.TrString("form.require_error"))
+		return
+	}
+	sameName, err := auth.GetWebAuthnCredentialByName(ctx, ctx.Doer.ID, form.Name)
+	if err != nil && !auth.IsErrWebAuthnCredentialNotExist(err) {
+		ctx.ServerError("GetWebAuthnCredentialByName", err)
+		return
+	}
+	if sameName != nil && sameName.ID != cred.ID { // renaming a key to its own name with different casing is allowed
+		ctx.JSONError(ctx.Tr("settings.webauthn_nickname_taken"))
+		return
+	}
+
+	if err := auth.UpdateCredentialName(ctx, cred.ID, ctx.Doer.ID, form.Name); err != nil {
+		ctx.ServerError("UpdateCredentialName", err)
+		return
+	}
+	ctx.Flash.Success(ctx.Tr("settings.webauthn_rename_key_success"))
+	ctx.JSONRedirect(setting.AppSubURL + "/user/settings/security")
+}
+
 // WebauthnDelete deletes an security key by id
 func WebauthnDelete(ctx *context.Context) {
 	if user_model.IsFeatureDisabledWithLoginType(ctx.Doer, setting.UserFeatureManageMFA) {
diff --git a/routers/web/web.go b/routers/web/web.go
index 0bfae44e86..f8e48f99fb 100644
--- a/routers/web/web.go
+++ b/routers/web/web.go
@@ -652,6 +652,7 @@ func registerWebRoutes(m *web.Router, webAuth *AuthMiddleware) {
 			m.Group("/webauthn", func() {
 				m.Post("/request_register", web.Bind(forms.WebauthnRegistrationForm{}), security.WebAuthnRegister)
 				m.Post("/register", security.WebauthnRegisterPost)
+				m.Post("/rename", web.Bind(forms.WebauthnRenameForm{}), security.WebauthnRename)
 				m.Post("/delete", security.WebauthnDelete)
 			})
 			m.Group("/openid", func() {
diff --git a/services/forms/user_form.go b/services/forms/user_form.go
index 75275fb5c7..95f3ae0b16 100644
--- a/services/forms/user_form.go
+++ b/services/forms/user_form.go
@@ -419,6 +419,17 @@ func (f *WebauthnRegistrationForm) Validate(req *http.Request, errs binding.Erro
 	return middleware.Validate(errs, ctx.Data, f, ctx.Locale)
 }
 
+// WebauthnRenameForm for renaming an existing WebAuthn credential
+type WebauthnRenameForm struct {
+	Name string `binding:"Required;MaxSize(255)" locale:"settings.webauthn_nickname"` // 255 is the size of the name column
+}
+
+// Validate validates the fields
+func (f *WebauthnRenameForm) Validate(req *http.Request, errs binding.Errors) binding.Errors {
+	ctx := context.GetValidateContext(req)
+	return middleware.Validate(errs, ctx.Data, f, ctx.Locale)
+}
+
 // PackageSettingForm form for package settings
 type PackageSettingForm struct {
 	Action   string
diff --git a/templates/user/settings/security/webauthn.tmpl b/templates/user/settings/security/webauthn.tmpl
index 1bf0159d37..6bfaf11d6b 100644
--- a/templates/user/settings/security/webauthn.tmpl
+++ b/templates/user/settings/security/webauthn.tmpl
@@ -16,6 +16,13 @@
 					</div>
 				</div>
 				<div class="item-trailing">
+					<button class="ui tiny button show-modal"
+						data-modal="#rename-webauthn-modal"
+						data-modal-form.url="{{$.Link}}/webauthn/rename?id={{.ID}}"
+						data-modal-rename-webauthn-nickname="{{.Name}}"
+					>
+						{{svg "octicon-pencil"}} {{ctx.Locale.Tr "settings.webauthn_rename_key"}}
+					</button>
 					<button class="ui red tiny button link-action" data-modal-confirm="#delete-registration" data-url="{{$.Link}}/webauthn/delete?id={{.ID}}">
 					{{ctx.Locale.Tr "settings.delete_key"}}
 					</button>
@@ -30,6 +37,18 @@
 		</div>
 		<button id="register-webauthn" class="ui primary button">{{svg "octicon-key"}} {{ctx.Locale.Tr "settings.webauthn_register_key"}}</button>
 	</div>
+	<div class="ui small modal" id="rename-webauthn-modal">
+		<div class="header">{{svg "octicon-pencil"}} {{ctx.Locale.Tr "settings.webauthn_rename_key"}}</div>
+		<form class="ui form form-fetch-action" method="post">
+			<div class="content">
+				<div class="required field">
+					<label for="rename-webauthn-nickname">{{ctx.Locale.Tr "settings.webauthn_nickname"}}</label>
+					<input id="rename-webauthn-nickname" name="name" type="text" maxlength="255" required>
+				</div>
+			</div>
+			{{template "base/modal_actions_confirm" (dict "ModalButtonTypes" "confirm")}}
+		</form>
+	</div>
 	<div class="ui small modal" id="delete-registration">
 		<div class="header">
 			{{svg "octicon-trash"}}
diff --git a/tests/integration/user_settings_test.go b/tests/integration/user_settings_test.go
index d837da71cd..4df3f9f534 100644
--- a/tests/integration/user_settings_test.go
+++ b/tests/integration/user_settings_test.go
@@ -4,15 +4,21 @@
 package integration
 
 import (
+	"fmt"
 	"net/http"
+	"net/http/httptest"
 	"strings"
 	"testing"
 
+	auth_model "gitea.dev/models/auth"
+	"gitea.dev/models/unittest"
 	"gitea.dev/modules/container"
 	"gitea.dev/modules/setting"
 	"gitea.dev/modules/test"
+	"gitea.dev/modules/translation"
 	"gitea.dev/tests"
 
+	"github.com/go-webauthn/webauthn/webauthn"
 	"github.com/stretchr/testify/assert"
 )
 
@@ -269,6 +275,64 @@ func TestUserSettingsSecurity(t *testing.T) {
 	})
 }
 
+func TestUserSettingsWebAuthnRename(t *testing.T) {
+	defer tests.PrepareTestEnv(t)()
+
+	session := loginUser(t, "user2") // log in before adding keys, otherwise login would require the second factor
+	own, err := auth_model.CreateCredential(t.Context(), 2, "Laptop Key", &webauthn.Credential{ID: []byte("rename-1")})
+	assert.NoError(t, err)
+	_, err = auth_model.CreateCredential(t.Context(), 2, "Phone Key", &webauthn.Credential{ID: []byte("rename-2")})
+	assert.NoError(t, err)
+	other, err := auth_model.CreateCredential(t.Context(), 4, "Other User Key", &webauthn.Credential{ID: []byte("rename-3")})
+	assert.NoError(t, err)
+
+	rename := func(id int64, name string, expectedStatus int) *httptest.ResponseRecorder {
+		req := NewRequestWithValues(t, "POST", fmt.Sprintf("/user/settings/security/webauthn/rename?id=%d", id), map[string]string{"name": name})
+		return session.MakeRequest(t, req, expectedStatus)
+	}
+
+	t.Run("Success", func(t *testing.T) {
+		resp := rename(own.ID, "Work Laptop", http.StatusOK)
+		assert.Equal(t, "/user/settings/security", *test.ParseJSONRedirect(resp.Body.Bytes()).Redirect)
+		cred := unittest.AssertExistsAndLoadBean(t, &auth_model.WebAuthnCredential{ID: own.ID, Name: "Work Laptop"})
+		assert.Equal(t, []byte("rename-1"), cred.CredentialID) // the credential itself is untouched
+	})
+
+	t.Run("CaseChangeOfOwnName", func(t *testing.T) {
+		rename(own.ID, "WORK LAPTOP", http.StatusOK)
+		unittest.AssertExistsAndLoadBean(t, &auth_model.WebAuthnCredential{ID: own.ID, Name: "WORK LAPTOP"})
+	})
+
+	t.Run("InvalidNames", func(t *testing.T) {
+		for _, name := range []string{"", "   ", strings.Repeat("a", 256)} {
+			resp := rename(own.ID, name, http.StatusBadRequest)
+			assert.NotEmpty(t, test.ParseJSONError(resp.Body.Bytes()).ErrorMessage)
+		}
+		unittest.AssertExistsAndLoadBean(t, &auth_model.WebAuthnCredential{ID: own.ID, Name: "WORK LAPTOP"})
+	})
+
+	t.Run("SurroundingSpacesTrimmed", func(t *testing.T) {
+		rename(own.ID, "  Spaced Key  ", http.StatusOK)
+		unittest.AssertExistsAndLoadBean(t, &auth_model.WebAuthnCredential{ID: own.ID, Name: "Spaced Key"})
+		rename(own.ID, "WORK LAPTOP", http.StatusOK) // restore the name the following subtests expect
+	})
+
+	t.Run("NameTaken", func(t *testing.T) {
+		resp := rename(own.ID, "phone key", http.StatusBadRequest)
+		assert.Equal(t, translation.NewLocale("en-US").TrString("settings.webauthn_nickname_taken"), test.ParseJSONError(resp.Body.Bytes()).ErrorMessage)
+		unittest.AssertExistsAndLoadBean(t, &auth_model.WebAuthnCredential{ID: own.ID, Name: "WORK LAPTOP"})
+	})
+
+	t.Run("OtherUsersKey", func(t *testing.T) {
+		rename(other.ID, "Stolen", http.StatusNotFound)
+		unittest.AssertExistsAndLoadBean(t, &auth_model.WebAuthnCredential{ID: other.ID, Name: "Other User Key"})
+	})
+
+	t.Run("NonexistentKey", func(t *testing.T) {
+		rename(99999, "Missing", http.StatusNotFound)
+	})
+}
+
 func TestUserSettingsApplications(t *testing.T) {
 	defer tests.PrepareTestEnv(t)()
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:19:40Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Show recent commits and merge base

`git log --oneline -5 && echo "---" && git merge-base main HEAD`

```text
98e73f51f3 fix(user): reject whitespace-only security key nicknames
aff9a20450 feat(user): allow renaming security keys
c0092050a4 update to students' guide and adding folder structure
43889d7588 docs: rename student.md to STUDENTS.md
e1fa7da836 Updated student.md with references
---
c0092050a4c81926ab8d9f24dfd56faf55a54739
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:19:42Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Show Go-side diff

`git diff $(git merge-base main HEAD)..HEAD -- models/auth/webauthn.go models/auth/webauthn_test.go services/forms/user_form.go routers/web/web.go routers/web/user/setting/security/webauthn.go`

```text
diff --git a/models/auth/webauthn.go b/models/auth/webauthn.go
index 11297556d2..b37e9f51ef 100644
--- a/models/auth/webauthn.go
+++ b/models/auth/webauthn.go
@@ -195,6 +195,12 @@ func CreateCredential(ctx context.Context, userID int64, name string, cred *weba
 	return c, nil
 }
 
+// UpdateCredentialName will rename the WebAuthnCredential with the given id if it belongs to userID
+func UpdateCredentialName(ctx context.Context, id, userID int64, name string) error {
+	_, err := db.GetEngine(ctx).ID(id).Where("user_id = ?", userID).Cols("name", "lower_name").Update(&WebAuthnCredential{Name: name})
+	return err
+}
+
 // DeleteCredential will delete WebAuthnCredential
 func DeleteCredential(ctx context.Context, id, userID int64) (bool, error) {
 	had, err := db.GetEngine(ctx).ID(id).Where("user_id = ?", userID).Delete(&WebAuthnCredential{})
diff --git a/models/auth/webauthn_test.go b/models/auth/webauthn_test.go
index a648319226..d7d848f71e 100644
--- a/models/auth/webauthn_test.go
+++ b/models/auth/webauthn_test.go
@@ -64,3 +64,14 @@ func TestCreateCredential(t *testing.T) {
 
 	unittest.AssertExistsAndLoadBean(t, &auth_model.WebAuthnCredential{Name: "WebAuthn Created Credential", UserID: 1})
 }
+
+func TestUpdateCredentialName(t *testing.T) {
+	assert.NoError(t, unittest.PrepareTestDatabase())
+
+	// credential 1 belongs to user 32, so another user's update must not change it
+	assert.NoError(t, auth_model.UpdateCredentialName(t.Context(), 1, 2, "Hijacked"))
+	unittest.AssertExistsAndLoadBean(t, &auth_model.WebAuthnCredential{ID: 1, Name: "WebAuthn credential"})
+
+	assert.NoError(t, auth_model.UpdateCredentialName(t.Context(), 1, 32, "Renamed Key"))
+	unittest.AssertExistsAndLoadBean(t, &auth_model.WebAuthnCredential{ID: 1, Name: "Renamed Key", LowerName: "renamed key", UserID: 32})
+}
diff --git a/routers/web/user/setting/security/webauthn.go b/routers/web/user/setting/security/webauthn.go
index 4db7b47879..5e93abbbf1 100644
--- a/routers/web/user/setting/security/webauthn.go
+++ b/routers/web/user/setting/security/webauthn.go
@@ -7,6 +7,7 @@ import (
 	"errors"
 	"net/http"
 	"strconv"
+	"strings"
 	"time"
 
 	"gitea.dev/models/auth"
@@ -125,6 +126,52 @@ func WebauthnRegisterPost(ctx *context.Context) {
 	ctx.JSON(http.StatusCreated, cred)
 }
 
+// WebauthnRename renames a security key by id
+func WebauthnRename(ctx *context.Context) {
+	if user_model.IsFeatureDisabledWithLoginType(ctx.Doer, setting.UserFeatureManageMFA) {
+		ctx.HTTPError(http.StatusNotFound)
+		return
+	}
+
+	if ctx.HasError() { // form binding validation error
+		ctx.JSONError(ctx.GetErrMsg())
+		return
+	}
+
+	cred, err := auth.GetWebAuthnCredentialByID(ctx, ctx.FormInt64("id"))
+	if err != nil && !auth.IsErrWebAuthnCredentialNotExist(err) {
+		ctx.ServerError("GetWebAuthnCredentialByID", err)
+		return
+	}
+	if cred == nil || cred.UserID != ctx.Doer.ID { // hide other users' keys as not found
+		ctx.JSONErrorNotFound()
+		return
+	}
+
+	form := web.GetForm(ctx).(*forms.WebauthnRenameForm)
+	form.Name = strings.TrimSpace(form.Name)
+	if form.Name == "" { // "Required" binding only rejects an exactly empty value, not whitespace
+		ctx.JSONError(ctx.Locale.TrString("settings.webauthn_nickname") + ctx.Locale.TrString("form.require_error"))
+		return
+	}
+	sameName, err := auth.GetWebAuthnCredentialByName(ctx, ctx.Doer.ID, form.Name)
+	if err != nil && !auth.IsErrWebAuthnCredentialNotExist(err) {
+		ctx.ServerError("GetWebAuthnCredentialByName", err)
+		return
+	}
+	if sameName != nil && sameName.ID != cred.ID { // renaming a key to its own name with different casing is allowed
+		ctx.JSONError(ctx.Tr("settings.webauthn_nickname_taken"))
+		return
+	}
+
+	if err := auth.UpdateCredentialName(ctx, cred.ID, ctx.Doer.ID, form.Name); err != nil {
+		ctx.ServerError("UpdateCredentialName", err)
+		return
+	}
+	ctx.Flash.Success(ctx.Tr("settings.webauthn_rename_key_success"))
+	ctx.JSONRedirect(setting.AppSubURL + "/user/settings/security")
+}
+
 // WebauthnDelete deletes an security key by id
 func WebauthnDelete(ctx *context.Context) {
 	if user_model.IsFeatureDisabledWithLoginType(ctx.Doer, setting.UserFeatureManageMFA) {
diff --git a/routers/web/web.go b/routers/web/web.go
index 0bfae44e86..f8e48f99fb 100644
--- a/routers/web/web.go
+++ b/routers/web/web.go
@@ -652,6 +652,7 @@ func registerWebRoutes(m *web.Router, webAuth *AuthMiddleware) {
 			m.Group("/webauthn", func() {
 				m.Post("/request_register", web.Bind(forms.WebauthnRegistrationForm{}), security.WebAuthnRegister)
 				m.Post("/register", security.WebauthnRegisterPost)
+				m.Post("/rename", web.Bind(forms.WebauthnRenameForm{}), security.WebauthnRename)
 				m.Post("/delete", security.WebauthnDelete)
 			})
 			m.Group("/openid", func() {
diff --git a/services/forms/user_form.go b/services/forms/user_form.go
index 75275fb5c7..95f3ae0b16 100644
--- a/services/forms/user_form.go
+++ b/services/forms/user_form.go
@@ -419,6 +419,17 @@ func (f *WebauthnRegistrationForm) Validate(req *http.Request, errs binding.Erro
 	return middleware.Validate(errs, ctx.Data, f, ctx.Locale)
 }
 
+// WebauthnRenameForm for renaming an existing WebAuthn credential
+type WebauthnRenameForm struct {
+	Name string `binding:"Required;MaxSize(255)" locale:"settings.webauthn_nickname"` // 255 is the size of the name column
+}
+
+// Validate validates the fields
+func (f *WebauthnRenameForm) Validate(req *http.Request, errs binding.Errors) binding.Errors {
+	ctx := context.GetValidateContext(req)
+	return middleware.Validate(errs, ctx.Data, f, ctx.Locale)
+}
+
 // PackageSettingForm form for package settings
 type PackageSettingForm struct {
 	Action   string
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:19:43Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Show template, locale, test diff

`git diff $(git merge-base main HEAD)..HEAD -- templates/ options/ tests/`

```text
diff --git a/options/locale/locale_en-US.json b/options/locale/locale_en-US.json
index 9373aa83a9..694d0bf99b 100644
--- a/options/locale/locale_en-US.json
+++ b/options/locale/locale_en-US.json
@@ -919,6 +919,9 @@
   "settings.webauthn_desc": "Security keys are hardware devices containing cryptographic keys. They can be used for two-factor authentication. Security keys must support the <a rel=\"noreferrer\" target=\"_blank\" href=\"%s\">WebAuthn Authenticator</a> standard.",
   "settings.webauthn_register_key": "Add Security Key",
   "settings.webauthn_nickname": "Nickname",
+  "settings.webauthn_rename_key": "Rename Security Key",
+  "settings.webauthn_rename_key_success": "The security key has been renamed.",
+  "settings.webauthn_nickname_taken": "Another security key already uses this nickname.",
   "settings.webauthn_delete_key": "Remove Security Key",
   "settings.webauthn_delete_key_desc": "If you remove a security key, you can no longer sign in with it. Continue?",
   "settings.webauthn_key_loss_warning": "If you lose your security keys, you will lose access to your account.",
diff --git a/templates/user/settings/security/webauthn.tmpl b/templates/user/settings/security/webauthn.tmpl
index 1bf0159d37..6bfaf11d6b 100644
--- a/templates/user/settings/security/webauthn.tmpl
+++ b/templates/user/settings/security/webauthn.tmpl
@@ -16,6 +16,13 @@
 					</div>
 				</div>
 				<div class="item-trailing">
+					<button class="ui tiny button show-modal"
+						data-modal="#rename-webauthn-modal"
+						data-modal-form.url="{{$.Link}}/webauthn/rename?id={{.ID}}"
+						data-modal-rename-webauthn-nickname="{{.Name}}"
+					>
+						{{svg "octicon-pencil"}} {{ctx.Locale.Tr "settings.webauthn_rename_key"}}
+					</button>
 					<button class="ui red tiny button link-action" data-modal-confirm="#delete-registration" data-url="{{$.Link}}/webauthn/delete?id={{.ID}}">
 					{{ctx.Locale.Tr "settings.delete_key"}}
 					</button>
@@ -30,6 +37,18 @@
 		</div>
 		<button id="register-webauthn" class="ui primary button">{{svg "octicon-key"}} {{ctx.Locale.Tr "settings.webauthn_register_key"}}</button>
 	</div>
+	<div class="ui small modal" id="rename-webauthn-modal">
+		<div class="header">{{svg "octicon-pencil"}} {{ctx.Locale.Tr "settings.webauthn_rename_key"}}</div>
+		<form class="ui form form-fetch-action" method="post">
+			<div class="content">
+				<div class="required field">
+					<label for="rename-webauthn-nickname">{{ctx.Locale.Tr "settings.webauthn_nickname"}}</label>
+					<input id="rename-webauthn-nickname" name="name" type="text" maxlength="255" required>
+				</div>
+			</div>
+			{{template "base/modal_actions_confirm" (dict "ModalButtonTypes" "confirm")}}
+		</form>
+	</div>
 	<div class="ui small modal" id="delete-registration">
 		<div class="header">
 			{{svg "octicon-trash"}}
diff --git a/tests/integration/user_settings_test.go b/tests/integration/user_settings_test.go
index d837da71cd..4df3f9f534 100644
--- a/tests/integration/user_settings_test.go
+++ b/tests/integration/user_settings_test.go
@@ -4,15 +4,21 @@
 package integration
 
 import (
+	"fmt"
 	"net/http"
+	"net/http/httptest"
 	"strings"
 	"testing"
 
+	auth_model "gitea.dev/models/auth"
+	"gitea.dev/models/unittest"
 	"gitea.dev/modules/container"
 	"gitea.dev/modules/setting"
 	"gitea.dev/modules/test"
+	"gitea.dev/modules/translation"
 	"gitea.dev/tests"
 
+	"github.com/go-webauthn/webauthn/webauthn"
 	"github.com/stretchr/testify/assert"
 )
 
@@ -269,6 +275,64 @@ func TestUserSettingsSecurity(t *testing.T) {
 	})
 }
 
+func TestUserSettingsWebAuthnRename(t *testing.T) {
+	defer tests.PrepareTestEnv(t)()
+
+	session := loginUser(t, "user2") // log in before adding keys, otherwise login would require the second factor
+	own, err := auth_model.CreateCredential(t.Context(), 2, "Laptop Key", &webauthn.Credential{ID: []byte("rename-1")})
+	assert.NoError(t, err)
+	_, err = auth_model.CreateCredential(t.Context(), 2, "Phone Key", &webauthn.Credential{ID: []byte("rename-2")})
+	assert.NoError(t, err)
+	other, err := auth_model.CreateCredential(t.Context(), 4, "Other User Key", &webauthn.Credential{ID: []byte("rename-3")})
+	assert.NoError(t, err)
+
+	rename := func(id int64, name string, expectedStatus int) *httptest.ResponseRecorder {
+		req := NewRequestWithValues(t, "POST", fmt.Sprintf("/user/settings/security/webauthn/rename?id=%d", id), map[string]string{"name": name})
+		return session.MakeRequest(t, req, expectedStatus)
+	}
+
+	t.Run("Success", func(t *testing.T) {
+		resp := rename(own.ID, "Work Laptop", http.StatusOK)
+		assert.Equal(t, "/user/settings/security", *test.ParseJSONRedirect(resp.Body.Bytes()).Redirect)
+		cred := unittest.AssertExistsAndLoadBean(t, &auth_model.WebAuthnCredential{ID: own.ID, Name: "Work Laptop"})
+		assert.Equal(t, []byte("rename-1"), cred.CredentialID) // the credential itself is untouched
+	})
+
+	t.Run("CaseChangeOfOwnName", func(t *testing.T) {
+		rename(own.ID, "WORK LAPTOP", http.StatusOK)
+		unittest.AssertExistsAndLoadBean(t, &auth_model.WebAuthnCredential{ID: own.ID, Name: "WORK LAPTOP"})
+	})
+
+	t.Run("InvalidNames", func(t *testing.T) {
+		for _, name := range []string{"", "   ", strings.Repeat("a", 256)} {
+			resp := rename(own.ID, name, http.StatusBadRequest)
+			assert.NotEmpty(t, test.ParseJSONError(resp.Body.Bytes()).ErrorMessage)
+		}
+		unittest.AssertExistsAndLoadBean(t, &auth_model.WebAuthnCredential{ID: own.ID, Name: "WORK LAPTOP"})
+	})
+
+	t.Run("SurroundingSpacesTrimmed", func(t *testing.T) {
+		rename(own.ID, "  Spaced Key  ", http.StatusOK)
+		unittest.AssertExistsAndLoadBean(t, &auth_model.WebAuthnCredential{ID: own.ID, Name: "Spaced Key"})
+		rename(own.ID, "WORK LAPTOP", http.StatusOK) // restore the name the following subtests expect
+	})
+
+	t.Run("NameTaken", func(t *testing.T) {
+		resp := rename(own.ID, "phone key", http.StatusBadRequest)
+		assert.Equal(t, translation.NewLocale("en-US").TrString("settings.webauthn_nickname_taken"), test.ParseJSONError(resp.Body.Bytes()).ErrorMessage)
+		unittest.AssertExistsAndLoadBean(t, &auth_model.WebAuthnCredential{ID: own.ID, Name: "WORK LAPTOP"})
+	})
+
+	t.Run("OtherUsersKey", func(t *testing.T) {
+		rename(other.ID, "Stolen", http.StatusNotFound)
+		unittest.AssertExistsAndLoadBean(t, &auth_model.WebAuthnCredential{ID: other.ID, Name: "Other User Key"})
+	})
+
+	t.Run("NonexistentKey", func(t *testing.T) {
+		rename(99999, "Missing", http.StatusNotFound)
+	})
+}
+
 func TestUserSettingsApplications(t *testing.T) {
 	defer tests.PrepareTestEnv(t)()
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:19:43Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Read webauthn model

`cat -n models/auth/webauthn.go`

```text
     1	// Copyright 2020 The Gitea Authors. All rights reserved.
     2	// SPDX-License-Identifier: MIT
     3	
     4	package auth
     5	
     6	import (
     7		"context"
     8		"fmt"
     9		"strings"
    10	
    11		"gitea.dev/models/db"
    12		"gitea.dev/modules/timeutil"
    13		"gitea.dev/modules/util"
    14	
    15		"github.com/go-webauthn/webauthn/protocol"
    16		"github.com/go-webauthn/webauthn/webauthn"
    17	)
    18	
    19	// ErrWebAuthnCredentialNotExist represents a "ErrWebAuthnCRedentialNotExist" kind of error.
    20	type ErrWebAuthnCredentialNotExist struct {
    21		ID           int64
    22		CredentialID []byte
    23	}
    24	
    25	func (err ErrWebAuthnCredentialNotExist) Error() string {
    26		if len(err.CredentialID) == 0 {
    27			return fmt.Sprintf("WebAuthn credential does not exist [id: %d]", err.ID)
    28		}
    29		return fmt.Sprintf("WebAuthn credential does not exist [credential_id: %x]", err.CredentialID)
    30	}
    31	
    32	// Unwrap unwraps this as a ErrNotExist err
    33	func (err ErrWebAuthnCredentialNotExist) Unwrap() error {
    34		return util.ErrNotExist
    35	}
    36	
    37	// IsErrWebAuthnCredentialNotExist checks if an error is a ErrWebAuthnCredentialNotExist.
    38	func IsErrWebAuthnCredentialNotExist(err error) bool {
    39		_, ok := err.(ErrWebAuthnCredentialNotExist)
    40		return ok
    41	}
    42	
    43	// WebAuthnCredential represents the WebAuthn credential data for a public-key
    44	// credential conformant to WebAuthn Level 1
    45	type WebAuthnCredential struct {
    46		ID              int64 `xorm:"pk autoincr"`
    47		Name            string
    48		LowerName       string `xorm:"unique(s)"`
    49		UserID          int64  `xorm:"INDEX unique(s)"`
    50		CredentialID    []byte `xorm:"INDEX VARBINARY(1024)"`
    51		PublicKey       []byte
    52		AttestationType string
    53		AAGUID          []byte
    54		SignCount       uint32 `xorm:"BIGINT"`
    55		CloneWarning    bool
    56		CreatedUnix     timeutil.TimeStamp `xorm:"INDEX created"`
    57		UpdatedUnix     timeutil.TimeStamp `xorm:"INDEX updated"`
    58	}
    59	
    60	func init() {
    61		db.RegisterModel(new(WebAuthnCredential))
    62	}
    63	
    64	// TableName returns a better table name for WebAuthnCredential
    65	func (cred WebAuthnCredential) TableName() string {
    66		return "webauthn_credential"
    67	}
    68	
    69	// UpdateSignCount will update the database value of SignCount
    70	func (cred *WebAuthnCredential) UpdateSignCount(ctx context.Context) error {
    71		_, err := db.GetEngine(ctx).ID(cred.ID).Cols("sign_count").Update(cred)
    72		return err
    73	}
    74	
    75	// BeforeInsert will be invoked by XORM before updating a record
    76	func (cred *WebAuthnCredential) BeforeInsert() {
    77		cred.LowerName = strings.ToLower(cred.Name)
    78	}
    79	
    80	// BeforeUpdate will be invoked by XORM before updating a record
    81	func (cred *WebAuthnCredential) BeforeUpdate() {
    82		cred.LowerName = strings.ToLower(cred.Name)
    83	}
    84	
    85	// AfterLoad is invoked from XORM after setting the values of all fields of this object.
    86	func (cred *WebAuthnCredential) AfterLoad() {
    87		cred.LowerName = strings.ToLower(cred.Name)
    88	}
    89	
    90	// WebAuthnCredentialList is a list of *WebAuthnCredential
    91	type WebAuthnCredentialList []*WebAuthnCredential
    92	
    93	// newCredentialFlagsFromAuthenticatorFlags is copied from https://github.com/go-webauthn/webauthn/pull/337
    94	// to convert protocol.AuthenticatorFlags to webauthn.CredentialFlags
    95	func newCredentialFlagsFromAuthenticatorFlags(flags protocol.AuthenticatorFlags) webauthn.CredentialFlags {
    96		return webauthn.CredentialFlags{
    97			UserPresent:    flags.HasUserPresent(),
    98			UserVerified:   flags.HasUserVerified(),
    99			BackupEligible: flags.HasBackupEligible(),
   100			BackupState:    flags.HasBackupState(),
   101		}
   102	}
   103	
   104	// ToCredentials will convert all WebAuthnCredentials to webauthn.Credentials
   105	func (list WebAuthnCredentialList) ToCredentials(defaultAuthFlags ...protocol.AuthenticatorFlags) []webauthn.Credential {
   106		// TODO: at the moment, Gitea doesn't store or check the flags
   107		// so we need to use the default flags from the authenticator to make the login validation pass
   108		// In the future, we should:
   109		// 1. store the flags when registering the credential
   110		// 2. provide the stored flags when converting the credentials (for login)
   111		// 3. for old users, still use this fallback to the default flags
   112		defAuthFlags := util.OptionalArg(defaultAuthFlags)
   113		creds := make([]webauthn.Credential, 0, len(list))
   114		for _, cred := range list {
   115			creds = append(creds, webauthn.Credential{
   116				ID:              cred.CredentialID,
   117				PublicKey:       cred.PublicKey,
   118				AttestationType: cred.AttestationType,
   119				Flags:           newCredentialFlagsFromAuthenticatorFlags(defAuthFlags),
   120				Authenticator: webauthn.Authenticator{
   121					AAGUID:       cred.AAGUID,
   122					SignCount:    cred.SignCount,
   123					CloneWarning: cred.CloneWarning,
   124				},
   125			})
   126		}
   127		return creds
   128	}
   129	
   130	// GetWebAuthnCredentialsByUID returns all WebAuthn credentials of the given user
   131	func GetWebAuthnCredentialsByUID(ctx context.Context, uid int64) (WebAuthnCredentialList, error) {
   132		creds := make(WebAuthnCredentialList, 0)
   133		return creds, db.GetEngine(ctx).Where("user_id = ?", uid).Find(&creds)
   134	}
   135	
   136	// ExistsWebAuthnCredentialsForUID returns if the given user has credentials
   137	func ExistsWebAuthnCredentialsForUID(ctx context.Context, uid int64) (bool, error) {
   138		return db.GetEngine(ctx).Where("user_id = ?", uid).Exist(&WebAuthnCredential{})
   139	}
   140	
   141	// GetWebAuthnCredentialByName returns WebAuthn credential by id
   142	func GetWebAuthnCredentialByName(ctx context.Context, uid int64, name string) (*WebAuthnCredential, error) {
   143		cred := new(WebAuthnCredential)
   144		if found, err := db.GetEngine(ctx).Where("user_id = ? AND lower_name = ?", uid, strings.ToLower(name)).Get(cred); err != nil {
   145			return nil, err
   146		} else if !found {
   147			return nil, ErrWebAuthnCredentialNotExist{}
   148		}
   149		return cred, nil
   150	}
   151	
   152	// GetWebAuthnCredentialByID returns WebAuthn credential by id
   153	func GetWebAuthnCredentialByID(ctx context.Context, id int64) (*WebAuthnCredential, error) {
   154		cred := new(WebAuthnCredential)
   155		if found, err := db.GetEngine(ctx).ID(id).Get(cred); err != nil {
   156			return nil, err
   157		} else if !found {
   158			return nil, ErrWebAuthnCredentialNotExist{ID: id}
   159		}
   160		return cred, nil
   161	}
   162	
   163	// HasWebAuthnRegistrationsByUID returns whether a given user has WebAuthn registrations
   164	func HasWebAuthnRegistrationsByUID(ctx context.Context, uid int64) (bool, error) {
   165		return db.GetEngine(ctx).Where("user_id = ?", uid).Exist(&WebAuthnCredential{})
   166	}
   167	
   168	// GetWebAuthnCredentialByCredID returns WebAuthn credential by credential ID
   169	func GetWebAuthnCredentialByCredID(ctx context.Context, userID int64, credID []byte) (*WebAuthnCredential, error) {
   170		cred := new(WebAuthnCredential)
   171		if found, err := db.GetEngine(ctx).Where("user_id = ? AND credential_id = ?", userID, credID).Get(cred); err != nil {
   172			return nil, err
   173		} else if !found {
   174			return nil, ErrWebAuthnCredentialNotExist{CredentialID: credID}
   175		}
   176		return cred, nil
   177	}
   178	
   179	// CreateCredential will create a new WebAuthnCredential from the given Credential
   180	func CreateCredential(ctx context.Context, userID int64, name string, cred *webauthn.Credential) (*WebAuthnCredential, error) {
   181		c := &WebAuthnCredential{
   182			UserID:          userID,
   183			Name:            name,
   184			CredentialID:    cred.ID,
   185			PublicKey:       cred.PublicKey,
   186			AttestationType: cred.AttestationType,
   187			AAGUID:          cred.Authenticator.AAGUID,
   188			SignCount:       cred.Authenticator.SignCount,
   189			CloneWarning:    false,
   190		}
   191	
   192		if err := db.Insert(ctx, c); err != nil {
   193			return nil, err
   194		}
   195		return c, nil
   196	}
   197	
   198	// UpdateCredentialName will rename the WebAuthnCredential with the given id if it belongs to userID
   199	func UpdateCredentialName(ctx context.Context, id, userID int64, name string) error {
   200		_, err := db.GetEngine(ctx).ID(id).Where("user_id = ?", userID).Cols("name", "lower_name").Update(&WebAuthnCredential{Name: name})
   201		return err
   202	}
   203	
   204	// DeleteCredential will delete WebAuthnCredential
   205	func DeleteCredential(ctx context.Context, id, userID int64) (bool, error) {
   206		had, err := db.GetEngine(ctx).ID(id).Where("user_id = ?", userID).Delete(&WebAuthnCredential{})
   207		return had > 0, err
   208	}
```
</details></tool-use>

_**Agent (claude-opus-5 2026-09-15 02:19:43Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Find CSRF token injection in frontend

`ls web_src/js/modules/ | head -20; echo "=== csrf anywhere in web_src ==="; grep -rn "csrf" web_src/js/ --include='*.ts' -i | head -25`

```text
action-status-icon.test.ts
action-status-icon.ts
clipboard.ts
codeeditor
devtest.ts
diff-file.test.ts
diff-file.ts
errors.test.ts
errors.ts
favicon-status.test.ts
favicon-status.ts
fetch.test.ts
fetch.ts
fomantic
fomantic.ts
gitea-actions.ts
i18n.test.ts
i18n.ts
init.ts
observer.ts
=== csrf anywhere in web_src ===
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:19:44Z)**_

Now let me read the CSRF mechanism and the modal button code.

_**Agent - sidechain (claude-opus-5 2026-09-15 02:19:44Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Read webauthn router handler

`cat -n routers/web/user/setting/security/webauthn.go`

```text
     1	// Copyright 2018 The Gitea Authors. All rights reserved.
     2	// SPDX-License-Identifier: MIT
     3	
     4	package security
     5	
     6	import (
     7		"errors"
     8		"net/http"
     9		"strconv"
    10		"strings"
    11		"time"
    12	
    13		"gitea.dev/models/auth"
    14		user_model "gitea.dev/models/user"
    15		wa "gitea.dev/modules/auth/webauthn"
    16		"gitea.dev/modules/log"
    17		"gitea.dev/modules/session"
    18		"gitea.dev/modules/setting"
    19		"gitea.dev/modules/web"
    20		"gitea.dev/services/context"
    21		"gitea.dev/services/forms"
    22	
    23		"github.com/go-webauthn/webauthn/protocol"
    24		"github.com/go-webauthn/webauthn/webauthn"
    25	)
    26	
    27	// WebAuthnRegister initializes the webauthn registration procedure
    28	func WebAuthnRegister(ctx *context.Context) {
    29		if user_model.IsFeatureDisabledWithLoginType(ctx.Doer, setting.UserFeatureManageMFA) {
    30			ctx.HTTPError(http.StatusNotFound)
    31			return
    32		}
    33	
    34		form := web.GetForm(ctx).(*forms.WebauthnRegistrationForm)
    35		if form.Name == "" {
    36			// Set name to the hexadecimal of the current time
    37			form.Name = strconv.FormatInt(time.Now().UnixNano(), 16)
    38		}
    39	
    40		cred, err := auth.GetWebAuthnCredentialByName(ctx, ctx.Doer.ID, form.Name)
    41		if err != nil && !auth.IsErrWebAuthnCredentialNotExist(err) {
    42			ctx.ServerError("GetWebAuthnCredentialsByUID", err)
    43			return
    44		}
    45		if cred != nil {
    46			ctx.HTTPError(http.StatusConflict, "Name already taken")
    47			return
    48		}
    49	
    50		_ = ctx.Session.Delete("webauthnRegistration")
    51		if err := ctx.Session.Set("webauthnName", form.Name); err != nil {
    52			ctx.ServerError("Unable to set session key for webauthnName", err)
    53			return
    54		}
    55	
    56		webAuthnUser := wa.NewWebAuthnUser(ctx, ctx.Doer)
    57		credentialOptions, sessionData, err := wa.WebAuthn.BeginRegistration(webAuthnUser, webauthn.WithAuthenticatorSelection(protocol.AuthenticatorSelection{
    58			ResidentKey: protocol.ResidentKeyRequirementRequired,
    59		}))
    60		if err != nil {
    61			ctx.ServerError("Unable to BeginRegistration", err)
    62			return
    63		}
    64	
    65		// Save the session data as marshaled JSON
    66		if err = ctx.Session.Set("webauthnRegistration", sessionData); err != nil {
    67			ctx.ServerError("Unable to set session", err)
    68			return
    69		}
    70	
    71		ctx.JSON(http.StatusOK, credentialOptions)
    72	}
    73	
    74	// WebauthnRegisterPost receives the response of the security key
    75	func WebauthnRegisterPost(ctx *context.Context) {
    76		if user_model.IsFeatureDisabledWithLoginType(ctx.Doer, setting.UserFeatureManageMFA) {
    77			ctx.HTTPError(http.StatusNotFound)
    78			return
    79		}
    80	
    81		name, ok := ctx.Session.Get("webauthnName").(string)
    82		if !ok || name == "" {
    83			ctx.ServerError("Get webauthnName", errors.New("no webauthnName"))
    84			return
    85		}
    86	
    87		// Load the session data
    88		sessionData, ok := ctx.Session.Get("webauthnRegistration").(*webauthn.SessionData)
    89		if !ok || sessionData == nil {
    90			ctx.ServerError("Get registration", errors.New("no registration"))
    91			return
    92		}
    93		defer func() {
    94			_ = ctx.Session.Delete("webauthnRegistration")
    95		}()
    96	
    97		// Verify that the challenge succeeded
    98		webAuthnUser := wa.NewWebAuthnUser(ctx, ctx.Doer)
    99		cred, err := wa.WebAuthn.FinishRegistration(webAuthnUser, *sessionData, ctx.Req)
   100		if err != nil {
   101			if pErr, ok := err.(*protocol.Error); ok {
   102				log.Error("Unable to finish registration due to error: %v\nDevInfo: %s", pErr, pErr.DevInfo)
   103			}
   104			ctx.ServerError("CreateCredential", err)
   105			return
   106		}
   107	
   108		dbCred, err := auth.GetWebAuthnCredentialByName(ctx, ctx.Doer.ID, name)
   109		if err != nil && !auth.IsErrWebAuthnCredentialNotExist(err) {
   110			ctx.ServerError("GetWebAuthnCredentialsByUID", err)
   111			return
   112		}
   113		if dbCred != nil {
   114			ctx.HTTPError(http.StatusConflict, "Name already taken")
   115			return
   116		}
   117	
   118		// Create the credential
   119		_, err = auth.CreateCredential(ctx, ctx.Doer.ID, name, cred)
   120		if err != nil {
   121			ctx.ServerError("CreateCredential", err)
   122			return
   123		}
   124		_ = ctx.Session.Delete("webauthnName")
   125		_ = ctx.Session.Set(session.KeyUserHasTwoFactorAuth, true)
   126		ctx.JSON(http.StatusCreated, cred)
   127	}
   128	
   129	// WebauthnRename renames a security key by id
   130	func WebauthnRename(ctx *context.Context) {
   131		if user_model.IsFeatureDisabledWithLoginType(ctx.Doer, setting.UserFeatureManageMFA) {
   132			ctx.HTTPError(http.StatusNotFound)
   133			return
   134		}
   135	
   136		if ctx.HasError() { // form binding validation error
   137			ctx.JSONError(ctx.GetErrMsg())
   138			return
   139		}
   140	
   141		cred, err := auth.GetWebAuthnCredentialByID(ctx, ctx.FormInt64("id"))
   142		if err != nil && !auth.IsErrWebAuthnCredentialNotExist(err) {
   143			ctx.ServerError("GetWebAuthnCredentialByID", err)
   144			return
   145		}
   146		if cred == nil || cred.UserID != ctx.Doer.ID { // hide other users' keys as not found
   147			ctx.JSONErrorNotFound()
   148			return
   149		}
   150	
   151		form := web.GetForm(ctx).(*forms.WebauthnRenameForm)
   152		form.Name = strings.TrimSpace(form.Name)
   153		if form.Name == "" { // "Required" binding only rejects an exactly empty value, not whitespace
   154			ctx.JSONError(ctx.Locale.TrString("settings.webauthn_nickname") + ctx.Locale.TrString("form.require_error"))
   155			return
   156		}
   157		sameName, err := auth.GetWebAuthnCredentialByName(ctx, ctx.Doer.ID, form.Name)
   158		if err != nil && !auth.IsErrWebAuthnCredentialNotExist(err) {
   159			ctx.ServerError("GetWebAuthnCredentialByName", err)
   160			return
   161		}
   162		if sameName != nil && sameName.ID != cred.ID { // renaming a key to its own name with different casing is allowed
   163			ctx.JSONError(ctx.Tr("settings.webauthn_nickname_taken"))
   164			return
   165		}
   166	
   167		if err := auth.UpdateCredentialName(ctx, cred.ID, ctx.Doer.ID, form.Name); err != nil {
   168			ctx.ServerError("UpdateCredentialName", err)
   169			return
   170		}
   171		ctx.Flash.Success(ctx.Tr("settings.webauthn_rename_key_success"))
   172		ctx.JSONRedirect(setting.AppSubURL + "/user/settings/security")
   173	}
   174	
   175	// WebauthnDelete deletes an security key by id
   176	func WebauthnDelete(ctx *context.Context) {
   177		if user_model.IsFeatureDisabledWithLoginType(ctx.Doer, setting.UserFeatureManageMFA) {
   178			ctx.HTTPError(http.StatusNotFound)
   179			return
   180		}
   181	
   182		if _, err := auth.DeleteCredential(ctx, ctx.FormInt64("id"), ctx.Doer.ID); err != nil {
   183			ctx.ServerError("GetWebAuthnCredentialByID", err)
   184			return
   185		}
   186		ctx.JSONRedirect(setting.AppSubURL + "/user/settings/security")
   187	}
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:19:45Z)**_

Now the build and vet.

_**Agent - sidechain (claude-opus-5 2026-09-15 02:19:45Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Read fetch module for CSRF

`cat web_src/js/modules/fetch.ts`

```text
import {isObject} from '../utils.ts';
import type {RequestOpts} from '../types.ts';

// fetch wrapper, use below method name functions and the `data` option to pass in data
// which will automatically set an appropriate headers. For JSON content, only object
// and array types are currently supported.
export function request(url: string, {method = 'GET', data, headers = {}, ...other}: RequestOpts = {}): Promise<Response> {
  let body: string | FormData | URLSearchParams | undefined;
  let contentType: string | undefined;
  if (data instanceof FormData || data instanceof URLSearchParams) {
    body = data;
  } else if (isObject(data) || Array.isArray(data)) {
    contentType = 'application/json';
    body = JSON.stringify(data);
  }

  headers = new Headers(headers);
  if (!headers.has('content-type') && contentType) {
    headers.set('content-type', contentType);
  }
  return fetch(url, { // eslint-disable-line no-restricted-globals
    method,
    headers,
    ...other,
    ...(body && {body}),
  });
}

export const GET = (url: string, opts?: RequestOpts) => request(url, {method: 'GET', ...opts});
export const POST = (url: string, opts?: RequestOpts) => request(url, {method: 'POST', ...opts});
export const PATCH = (url: string, opts?: RequestOpts) => request(url, {method: 'PATCH', ...opts});
export const PUT = (url: string, opts?: RequestOpts) => request(url, {method: 'PUT', ...opts});
export const DELETE = (url: string, opts?: RequestOpts) => request(url, {method: 'DELETE', ...opts});
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:19:46Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Read common-button.ts

`cat -n web_src/js/features/common-button.ts`

```text
     1	import {addDelegatedEventListener, hideElem, isElemVisible, showElem, toggleElem} from '../utils/dom.ts';
     2	import {showFomanticModal} from '../modules/fomantic/modal.ts';
     3	import {camelize} from 'vue';
     4	import {applyAutoFocus} from './common-page.ts';
     5	
     6	export function initGlobalButtonClickOnEnter(): void {
     7	  addDelegatedEventListener(document, 'keypress', 'div.ui.button, span.ui.button', (el, e: KeyboardEvent) => {
     8	    if (e.code === 'Space' || e.code === 'Enter') {
     9	      e.preventDefault();
    10	      el.click();
    11	    }
    12	  });
    13	}
    14	
    15	function onShowPanelClick(el: HTMLElement, e: MouseEvent) {
    16	  // a '.show-panel' element can show a panel, by `data-panel="selector"`
    17	  // if it has "toggle" class, it toggles the panel
    18	  e.preventDefault();
    19	  const sel = el.getAttribute('data-panel')!;
    20	  const elems = el.classList.contains('toggle') ? toggleElem(sel) : showElem(sel);
    21	  for (const elem of elems) {
    22	    if (isElemVisible(elem as HTMLElement)) {
    23	      applyAutoFocus(elem);
    24	    }
    25	  }
    26	}
    27	
    28	function onHidePanelClick(el: HTMLElement, e: MouseEvent) {
    29	  // a `.hide-panel` element can hide a panel, by `data-panel="selector"` or `data-panel-closest="selector"`
    30	  e.preventDefault();
    31	  let sel = el.getAttribute('data-panel');
    32	  if (sel) {
    33	    hideElem(sel);
    34	    return;
    35	  }
    36	  sel = el.getAttribute('data-panel-closest');
    37	  if (sel) {
    38	    hideElem((el.parentNode as HTMLElement).closest(sel)!);
    39	    return;
    40	  }
    41	  throw new Error('no panel to hide'); // should never happen, otherwise there is a bug in code
    42	}
    43	
    44	export type ElementWithAssignableProperties = {
    45	  nodeName: string;
    46	  getAttribute: (name: string) => string | null;
    47	  setAttribute: (name: string, value: string) => void;
    48	} & Record<string, any>;
    49	
    50	export function assignElementProperty(el: ElementWithAssignableProperties, kebabName: string, val: string) {
    51	  if (el.nodeName === 'FORM') {
    52	    // HINT: GOLANG-HTML-TEMPLATE-URL-ESCAPING: a special case for Golang HTML template escaping.
    53	    // Golang HTML template only handles some "known" attribute names as URL (e.g.: when the name is "action" or contains "url")
    54	    // To prevent template developers from making mistakes like `data-modal-form.action="?k={{ValueWithSpecialChars}}" (no escaping),
    55	    // here we use `data-modal-form.url="?k={{ValueWithSpecialChars}}", then the value gets correctly escaped by Golang HTML template.
    56	    if (kebabName === 'action') throw new Error(`don't assign element property "action" by value, use "data-modal-form.url" instead`);
    57	    if (kebabName === 'url') kebabName = 'action';
    58	  }
    59	  const camelizedName = camelize(kebabName);
    60	  const old = el[camelizedName];
    61	  if (typeof old === 'boolean') {
    62	    el[camelizedName] = val === 'true';
    63	  } else if (typeof old === 'number') {
    64	    el[camelizedName] = parseFloat(val);
    65	  } else if (typeof old === 'string') {
    66	    el[camelizedName] = val;
    67	  } else if (old?.nodeName) {
    68	    // "form" has an edge case: its "<input name=action>" element overwrites the "action" property, we can only set attribute
    69	    el.setAttribute(kebabName, val);
    70	  } else {
    71	    // in the future, maybe we could introduce a better typing system if it is really needed
    72	    throw new Error(`cannot assign element property "${camelizedName}" by value "${val}"`);
    73	  }
    74	}
    75	
    76	function onShowModalClick(el: HTMLElement, e: MouseEvent) {
    77	  // A ".show-modal" button will show a modal dialog defined by its "data-modal" attribute.
    78	  // Each "data-modal-{target}" attribute will be filled to target element's value or text-content.
    79	  // * First, try to query '#target'
    80	  // * Then, try to query '[name=target]'
    81	  // * Then, try to query '.target'
    82	  // * Then, try to query 'target' as HTML tag
    83	  // If there's a ".{prop-name}" part like "data-modal-input.value", the "input" element's "value" property will be set,
    84	  // the "prop-name" will be camel-cased to "propName" (e.g.: "data-modal-input.read-only" for "readOnly" property).
    85	  //
    86	  // HINT: GOLANG-HTML-TEMPLATE-URL-ESCAPING: Form element's "action" property must be set by "data-modal-form.url"
    87	  // to make the template variables get correctly escaped in the URL.
    88	  e.preventDefault();
    89	  const modalSelector = el.getAttribute('data-modal')!;
    90	  const elModal = document.querySelector(modalSelector);
    91	  if (!elModal) throw new Error('no modal for this action');
    92	
    93	  const modalAttrPrefix = 'data-modal-';
    94	  for (const attrib of el.attributes) {
    95	    if (!attrib.name.startsWith(modalAttrPrefix)) {
    96	      continue;
    97	    }
    98	
    99	    const attrTargetCombo = attrib.name.substring(modalAttrPrefix.length);
   100	    const [attrTargetName, attrTargetProp] = attrTargetCombo.split('.');
   101	    // try to find target by: "#target" -> "[name=target]" -> ".target" -> "<target> tag", and then try the modal itself
   102	    const attrTarget = elModal.querySelector(`#${attrTargetName}`) ||
   103	      elModal.querySelector(`[name=${CSS.escape(attrTargetName)}]`) ||
   104	      elModal.querySelector(`.${attrTargetName}`) ||
   105	      elModal.querySelector(attrTargetName) ||
   106	      (elModal.matches(attrTargetName) || elModal.matches(`#${attrTargetName}`) || elModal.matches(`.${attrTargetName}`) ? elModal : null);
   107	    if (!attrTarget) {
   108	      if (!window.config.runModeIsProd) throw new Error(`attr target "${attrTargetCombo}" not found for modal`);
   109	      continue;
   110	    }
   111	
   112	    if (attrTargetProp) {
   113	      assignElementProperty(attrTarget, attrTargetProp, attrib.value);
   114	    } else if (attrTarget.matches('input, textarea')) {
   115	      (attrTarget as HTMLInputElement | HTMLTextAreaElement).value = attrib.value; // FIXME: add more supports like checkbox
   116	    } else {
   117	      attrTarget.textContent = attrib.value; // FIXME: it should be more strict here, only handle div/span/p
   118	    }
   119	  }
   120	
   121	  showFomanticModal(elModal);
   122	}
   123	
   124	export function initGlobalButtons(): void {
   125	  // There are many "cancel button" elements in modal dialogs, Fomantic UI expects they are button-like elements but never submit a form.
   126	  // However, Gitea misuses the modal dialog and put the cancel buttons inside forms, so we must prevent the form submission.
   127	  // There are a few cancel buttons in non-modal forms, and there are some dynamically created forms (eg: the "Edit Issue Content")
   128	  addDelegatedEventListener(document, 'click', 'form button.ui.cancel.button', (_ /* el */, e) => e.preventDefault());
   129	
   130	  // Ideally these "button" events should be handled by registerGlobalEventFunc
   131	  // Refactoring would involve too many changes, so at the moment, just use the global event listener.
   132	  addDelegatedEventListener(document, 'click', '.show-panel, .hide-panel, .show-modal', (el, e: MouseEvent) => {
   133	    if (el.classList.contains('show-panel')) {
   134	      onShowPanelClick(el, e);
   135	    } else if (el.classList.contains('hide-panel')) {
   136	      onHidePanelClick(el, e);
   137	    } else if (el.classList.contains('show-modal')) {
   138	      onShowModalClick(el, e);
   139	    }
   140	  });
   141	}
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:19:46Z)**_

Now let me examine the test helpers the review hinges on.

_**Agent - sidechain (claude-opus-5 2026-09-15 02:19:47Z)**_

Let me read the JS that handles these modal attributes.

_**Agent - sidechain (claude-opus-5 2026-09-15 02:20:29Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Build all Go packages

`go build ./... 2>&1 | tail -40; echo "---EXIT:${PIPESTATUS[0]}"`

```text
---EXIT:
```
</details></tool-use>

_**Agent (claude-opus-5 2026-09-15 02:19:47Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Read fetch module

`cat web_src/js/modules/fetch.ts`

```text
import {isObject} from '../utils.ts';
import type {RequestOpts} from '../types.ts';

// fetch wrapper, use below method name functions and the `data` option to pass in data
// which will automatically set an appropriate headers. For JSON content, only object
// and array types are currently supported.
export function request(url: string, {method = 'GET', data, headers = {}, ...other}: RequestOpts = {}): Promise<Response> {
  let body: string | FormData | URLSearchParams | undefined;
  let contentType: string | undefined;
  if (data instanceof FormData || data instanceof URLSearchParams) {
    body = data;
  } else if (isObject(data) || Array.isArray(data)) {
    contentType = 'application/json';
    body = JSON.stringify(data);
  }

  headers = new Headers(headers);
  if (!headers.has('content-type') && contentType) {
    headers.set('content-type', contentType);
  }
  return fetch(url, { // eslint-disable-line no-restricted-globals
    method,
    headers,
    ...other,
    ...(body && {body}),
  });
}

export const GET = (url: string, opts?: RequestOpts) => request(url, {method: 'GET', ...opts});
export const POST = (url: string, opts?: RequestOpts) => request(url, {method: 'POST', ...opts});
export const PATCH = (url: string, opts?: RequestOpts) => request(url, {method: 'PATCH', ...opts});
export const PUT = (url: string, opts?: RequestOpts) => request(url, {method: 'PUT', ...opts});
export const DELETE = (url: string, opts?: RequestOpts) => request(url, {method: 'DELETE', ...opts});
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:19:48Z)**_

Now let me locate the xorm source to settle the `BeforeUpdate` ordering question definitively.

_**Agent - sidechain (claude-opus-5 2026-09-15 02:19:48Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Read NewRequestWithValues helper

`grep -n "func NewRequestWithValues" -A 20 tests/integration/integration_test.go`

```text
236:func NewRequestWithValues(t testing.TB, method, urlStr string, values map[string]string) *RequestWrapper {
237-	t.Helper()
238-	urlValues := url.Values{}
239-	for key, value := range values {
240-		urlValues[key] = []string{value}
241-	}
242-	return NewRequestWithURLValues(t, method, urlStr, urlValues)
243-}
244-
245-func NewRequestWithURLValues(t testing.TB, method, urlStr string, urlValues url.Values) *RequestWrapper {
246-	t.Helper()
247-	return NewRequestWithBody(t, method, urlStr, strings.NewReader(urlValues.Encode())).
248-		SetHeader("Content-Type", "application/x-www-form-urlencoded")
249-}
250-
251-func NewRequestWithJSON(t testing.TB, method, urlStr string, v any) *RequestWrapper {
252-	t.Helper()
253-
254-	jsonBytes, err := json.Marshal(v)
255-	assert.NoError(t, err)
256-	return NewRequestWithBody(t, method, urlStr, bytes.NewBuffer(jsonBytes)).
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:19:48Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Read common-button.ts

`cat -n web_src/js/features/common-button.ts`

```text
     1	import {addDelegatedEventListener, hideElem, isElemVisible, showElem, toggleElem} from '../utils/dom.ts';
     2	import {showFomanticModal} from '../modules/fomantic/modal.ts';
     3	import {camelize} from 'vue';
     4	import {applyAutoFocus} from './common-page.ts';
     5	
     6	export function initGlobalButtonClickOnEnter(): void {
     7	  addDelegatedEventListener(document, 'keypress', 'div.ui.button, span.ui.button', (el, e: KeyboardEvent) => {
     8	    if (e.code === 'Space' || e.code === 'Enter') {
     9	      e.preventDefault();
    10	      el.click();
    11	    }
    12	  });
    13	}
    14	
    15	function onShowPanelClick(el: HTMLElement, e: MouseEvent) {
    16	  // a '.show-panel' element can show a panel, by `data-panel="selector"`
    17	  // if it has "toggle" class, it toggles the panel
    18	  e.preventDefault();
    19	  const sel = el.getAttribute('data-panel')!;
    20	  const elems = el.classList.contains('toggle') ? toggleElem(sel) : showElem(sel);
    21	  for (const elem of elems) {
    22	    if (isElemVisible(elem as HTMLElement)) {
    23	      applyAutoFocus(elem);
    24	    }
    25	  }
    26	}
    27	
    28	function onHidePanelClick(el: HTMLElement, e: MouseEvent) {
    29	  // a `.hide-panel` element can hide a panel, by `data-panel="selector"` or `data-panel-closest="selector"`
    30	  e.preventDefault();
    31	  let sel = el.getAttribute('data-panel');
    32	  if (sel) {
    33	    hideElem(sel);
    34	    return;
    35	  }
    36	  sel = el.getAttribute('data-panel-closest');
    37	  if (sel) {
    38	    hideElem((el.parentNode as HTMLElement).closest(sel)!);
    39	    return;
    40	  }
    41	  throw new Error('no panel to hide'); // should never happen, otherwise there is a bug in code
    42	}
    43	
    44	export type ElementWithAssignableProperties = {
    45	  nodeName: string;
    46	  getAttribute: (name: string) => string | null;
    47	  setAttribute: (name: string, value: string) => void;
    48	} & Record<string, any>;
    49	
    50	export function assignElementProperty(el: ElementWithAssignableProperties, kebabName: string, val: string) {
    51	  if (el.nodeName === 'FORM') {
    52	    // HINT: GOLANG-HTML-TEMPLATE-URL-ESCAPING: a special case for Golang HTML template escaping.
    53	    // Golang HTML template only handles some "known" attribute names as URL (e.g.: when the name is "action" or contains "url")
    54	    // To prevent template developers from making mistakes like `data-modal-form.action="?k={{ValueWithSpecialChars}}" (no escaping),
    55	    // here we use `data-modal-form.url="?k={{ValueWithSpecialChars}}", then the value gets correctly escaped by Golang HTML template.
    56	    if (kebabName === 'action') throw new Error(`don't assign element property "action" by value, use "data-modal-form.url" instead`);
    57	    if (kebabName === 'url') kebabName = 'action';
    58	  }
    59	  const camelizedName = camelize(kebabName);
    60	  const old = el[camelizedName];
    61	  if (typeof old === 'boolean') {
    62	    el[camelizedName] = val === 'true';
    63	  } else if (typeof old === 'number') {
    64	    el[camelizedName] = parseFloat(val);
    65	  } else if (typeof old === 'string') {
    66	    el[camelizedName] = val;
    67	  } else if (old?.nodeName) {
    68	    // "form" has an edge case: its "<input name=action>" element overwrites the "action" property, we can only set attribute
    69	    el.setAttribute(kebabName, val);
    70	  } else {
    71	    // in the future, maybe we could introduce a better typing system if it is really needed
    72	    throw new Error(`cannot assign element property "${camelizedName}" by value "${val}"`);
    73	  }
    74	}
    75	
    76	function onShowModalClick(el: HTMLElement, e: MouseEvent) {
    77	  // A ".show-modal" button will show a modal dialog defined by its "data-modal" attribute.
    78	  // Each "data-modal-{target}" attribute will be filled to target element's value or text-content.
    79	  // * First, try to query '#target'
    80	  // * Then, try to query '[name=target]'
    81	  // * Then, try to query '.target'
    82	  // * Then, try to query 'target' as HTML tag
    83	  // If there's a ".{prop-name}" part like "data-modal-input.value", the "input" element's "value" property will be set,
    84	  // the "prop-name" will be camel-cased to "propName" (e.g.: "data-modal-input.read-only" for "readOnly" property).
    85	  //
    86	  // HINT: GOLANG-HTML-TEMPLATE-URL-ESCAPING: Form element's "action" property must be set by "data-modal-form.url"
    87	  // to make the template variables get correctly escaped in the URL.
    88	  e.preventDefault();
    89	  const modalSelector = el.getAttribute('data-modal')!;
    90	  const elModal = document.querySelector(modalSelector);
    91	  if (!elModal) throw new Error('no modal for this action');
    92	
    93	  const modalAttrPrefix = 'data-modal-';
    94	  for (const attrib of el.attributes) {
    95	    if (!attrib.name.startsWith(modalAttrPrefix)) {
    96	      continue;
    97	    }
    98	
    99	    const attrTargetCombo = attrib.name.substring(modalAttrPrefix.length);
   100	    const [attrTargetName, attrTargetProp] = attrTargetCombo.split('.');
   101	    // try to find target by: "#target" -> "[name=target]" -> ".target" -> "<target> tag", and then try the modal itself
   102	    const attrTarget = elModal.querySelector(`#${attrTargetName}`) ||
   103	      elModal.querySelector(`[name=${CSS.escape(attrTargetName)}]`) ||
   104	      elModal.querySelector(`.${attrTargetName}`) ||
   105	      elModal.querySelector(attrTargetName) ||
   106	      (elModal.matches(attrTargetName) || elModal.matches(`#${attrTargetName}`) || elModal.matches(`.${attrTargetName}`) ? elModal : null);
   107	    if (!attrTarget) {
   108	      if (!window.config.runModeIsProd) throw new Error(`attr target "${attrTargetCombo}" not found for modal`);
   109	      continue;
   110	    }
   111	
   112	    if (attrTargetProp) {
   113	      assignElementProperty(attrTarget, attrTargetProp, attrib.value);
   114	    } else if (attrTarget.matches('input, textarea')) {
   115	      (attrTarget as HTMLInputElement | HTMLTextAreaElement).value = attrib.value; // FIXME: add more supports like checkbox
   116	    } else {
   117	      attrTarget.textContent = attrib.value; // FIXME: it should be more strict here, only handle div/span/p
   118	    }
   119	  }
   120	
   121	  showFomanticModal(elModal);
   122	}
   123	
   124	export function initGlobalButtons(): void {
   125	  // There are many "cancel button" elements in modal dialogs, Fomantic UI expects they are button-like elements but never submit a form.
   126	  // However, Gitea misuses the modal dialog and put the cancel buttons inside forms, so we must prevent the form submission.
   127	  // There are a few cancel buttons in non-modal forms, and there are some dynamically created forms (eg: the "Edit Issue Content")
   128	  addDelegatedEventListener(document, 'click', 'form button.ui.cancel.button', (_ /* el */, e) => e.preventDefault());
   129	
   130	  // Ideally these "button" events should be handled by registerGlobalEventFunc
   131	  // Refactoring would involve too many changes, so at the moment, just use the global event listener.
   132	  addDelegatedEventListener(document, 'click', '.show-panel, .hide-panel, .show-modal', (el, e: MouseEvent) => {
   133	    if (el.classList.contains('show-panel')) {
   134	      onShowPanelClick(el, e);
   135	    } else if (el.classList.contains('hide-panel')) {
   136	      onHidePanelClick(el, e);
   137	    } else if (el.classList.contains('show-modal')) {
   138	      onShowModalClick(el, e);
   139	    }
   140	  });
   141	}
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:19:48Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Show diff for route, form, model, template

`git diff $(git merge-base main HEAD)..HEAD -- routers/web/web.go services/forms/user_form.go models/auth/webauthn.go templates/user/settings/security/webauthn.tmpl options/locale/locale_en-US.json`

```text
diff --git a/models/auth/webauthn.go b/models/auth/webauthn.go
index 11297556d2..b37e9f51ef 100644
--- a/models/auth/webauthn.go
+++ b/models/auth/webauthn.go
@@ -195,6 +195,12 @@ func CreateCredential(ctx context.Context, userID int64, name string, cred *weba
 	return c, nil
 }
 
+// UpdateCredentialName will rename the WebAuthnCredential with the given id if it belongs to userID
+func UpdateCredentialName(ctx context.Context, id, userID int64, name string) error {
+	_, err := db.GetEngine(ctx).ID(id).Where("user_id = ?", userID).Cols("name", "lower_name").Update(&WebAuthnCredential{Name: name})
+	return err
+}
+
 // DeleteCredential will delete WebAuthnCredential
 func DeleteCredential(ctx context.Context, id, userID int64) (bool, error) {
 	had, err := db.GetEngine(ctx).ID(id).Where("user_id = ?", userID).Delete(&WebAuthnCredential{})
diff --git a/options/locale/locale_en-US.json b/options/locale/locale_en-US.json
index 9373aa83a9..694d0bf99b 100644
--- a/options/locale/locale_en-US.json
+++ b/options/locale/locale_en-US.json
@@ -919,6 +919,9 @@
   "settings.webauthn_desc": "Security keys are hardware devices containing cryptographic keys. They can be used for two-factor authentication. Security keys must support the <a rel=\"noreferrer\" target=\"_blank\" href=\"%s\">WebAuthn Authenticator</a> standard.",
   "settings.webauthn_register_key": "Add Security Key",
   "settings.webauthn_nickname": "Nickname",
+  "settings.webauthn_rename_key": "Rename Security Key",
+  "settings.webauthn_rename_key_success": "The security key has been renamed.",
+  "settings.webauthn_nickname_taken": "Another security key already uses this nickname.",
   "settings.webauthn_delete_key": "Remove Security Key",
   "settings.webauthn_delete_key_desc": "If you remove a security key, you can no longer sign in with it. Continue?",
   "settings.webauthn_key_loss_warning": "If you lose your security keys, you will lose access to your account.",
diff --git a/routers/web/web.go b/routers/web/web.go
index 0bfae44e86..f8e48f99fb 100644
--- a/routers/web/web.go
+++ b/routers/web/web.go
@@ -652,6 +652,7 @@ func registerWebRoutes(m *web.Router, webAuth *AuthMiddleware) {
 			m.Group("/webauthn", func() {
 				m.Post("/request_register", web.Bind(forms.WebauthnRegistrationForm{}), security.WebAuthnRegister)
 				m.Post("/register", security.WebauthnRegisterPost)
+				m.Post("/rename", web.Bind(forms.WebauthnRenameForm{}), security.WebauthnRename)
 				m.Post("/delete", security.WebauthnDelete)
 			})
 			m.Group("/openid", func() {
diff --git a/services/forms/user_form.go b/services/forms/user_form.go
index 75275fb5c7..95f3ae0b16 100644
--- a/services/forms/user_form.go
+++ b/services/forms/user_form.go
@@ -419,6 +419,17 @@ func (f *WebauthnRegistrationForm) Validate(req *http.Request, errs binding.Erro
 	return middleware.Validate(errs, ctx.Data, f, ctx.Locale)
 }
 
+// WebauthnRenameForm for renaming an existing WebAuthn credential
+type WebauthnRenameForm struct {
+	Name string `binding:"Required;MaxSize(255)" locale:"settings.webauthn_nickname"` // 255 is the size of the name column
+}
+
+// Validate validates the fields
+func (f *WebauthnRenameForm) Validate(req *http.Request, errs binding.Errors) binding.Errors {
+	ctx := context.GetValidateContext(req)
+	return middleware.Validate(errs, ctx.Data, f, ctx.Locale)
+}
+
 // PackageSettingForm form for package settings
 type PackageSettingForm struct {
 	Action   string
diff --git a/templates/user/settings/security/webauthn.tmpl b/templates/user/settings/security/webauthn.tmpl
index 1bf0159d37..6bfaf11d6b 100644
--- a/templates/user/settings/security/webauthn.tmpl
+++ b/templates/user/settings/security/webauthn.tmpl
@@ -16,6 +16,13 @@
 					</div>
 				</div>
 				<div class="item-trailing">
+					<button class="ui tiny button show-modal"
+						data-modal="#rename-webauthn-modal"
+						data-modal-form.url="{{$.Link}}/webauthn/rename?id={{.ID}}"
+						data-modal-rename-webauthn-nickname="{{.Name}}"
+					>
+						{{svg "octicon-pencil"}} {{ctx.Locale.Tr "settings.webauthn_rename_key"}}
+					</button>
 					<button class="ui red tiny button link-action" data-modal-confirm="#delete-registration" data-url="{{$.Link}}/webauthn/delete?id={{.ID}}">
 					{{ctx.Locale.Tr "settings.delete_key"}}
 					</button>
@@ -30,6 +37,18 @@
 		</div>
 		<button id="register-webauthn" class="ui primary button">{{svg "octicon-key"}} {{ctx.Locale.Tr "settings.webauthn_register_key"}}</button>
 	</div>
+	<div class="ui small modal" id="rename-webauthn-modal">
+		<div class="header">{{svg "octicon-pencil"}} {{ctx.Locale.Tr "settings.webauthn_rename_key"}}</div>
+		<form class="ui form form-fetch-action" method="post">
+			<div class="content">
+				<div class="required field">
+					<label for="rename-webauthn-nickname">{{ctx.Locale.Tr "settings.webauthn_nickname"}}</label>
+					<input id="rename-webauthn-nickname" name="name" type="text" maxlength="255" required>
+				</div>
+			</div>
+			{{template "base/modal_actions_confirm" (dict "ModalButtonTypes" "confirm")}}
+		</form>
+	</div>
 	<div class="ui small modal" id="delete-registration">
 		<div class="header">
 			{{svg "octicon-trash"}}
```
</details></tool-use>

_**Agent (claude-opus-5 2026-09-15 02:19:48Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Find server-side CSRF middleware

`grep -rn "Csrf\|CSRF" services/context/*.go routers/web/web.go modules/web/*.go 2>/dev/null | head -25`

```text
(Bash completed with no output)
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:19:49Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Read webauthn template

`cat -n templates/user/settings/security/webauthn.tmpl`

```text
     1	<h4 class="ui top attached header">{{ctx.Locale.Tr "settings.webauthn"}}</h4>
     2	<div class="ui attached segment">
     3		<p>{{ctx.Locale.Tr "settings.webauthn_desc" "https://w3c.github.io/webauthn/#webauthn-authenticator"}}</p>
     4		<p>{{ctx.Locale.Tr "settings.webauthn_key_loss_warning"}} {{ctx.Locale.Tr "settings.webauthn_alternative_tip"}}</p>
     5		{{template "user/auth/webauthn_error" .}}
     6		<div class="flex-divided-list items-with-main">
     7			{{range .WebAuthnCredentials}}
     8				<div class="item">
     9					<div class="item-leading">
    10						{{svg "octicon-key" 32}}
    11					</div>
    12					<div class="item-main">
    13						<div class="item-title">{{.Name}}</div>
    14						<div class="item-body">
    15							<i>{{ctx.Locale.Tr "settings.added_on" (DateUtils.AbsoluteShort .CreatedUnix)}}</i>
    16						</div>
    17					</div>
    18					<div class="item-trailing">
    19						<button class="ui tiny button show-modal"
    20							data-modal="#rename-webauthn-modal"
    21							data-modal-form.url="{{$.Link}}/webauthn/rename?id={{.ID}}"
    22							data-modal-rename-webauthn-nickname="{{.Name}}"
    23						>
    24							{{svg "octicon-pencil"}} {{ctx.Locale.Tr "settings.webauthn_rename_key"}}
    25						</button>
    26						<button class="ui red tiny button link-action" data-modal-confirm="#delete-registration" data-url="{{$.Link}}/webauthn/delete?id={{.ID}}">
    27						{{ctx.Locale.Tr "settings.delete_key"}}
    28						</button>
    29					</div>
    30				</div>
    31			{{end}}
    32		</div>
    33		<div class="ui form">
    34			<div class="required field">
    35				<label for="nickname">{{ctx.Locale.Tr "settings.webauthn_nickname"}}</label>
    36				<input id="nickname" name="nickname" type="text" required>
    37			</div>
    38			<button id="register-webauthn" class="ui primary button">{{svg "octicon-key"}} {{ctx.Locale.Tr "settings.webauthn_register_key"}}</button>
    39		</div>
    40		<div class="ui small modal" id="rename-webauthn-modal">
    41			<div class="header">{{svg "octicon-pencil"}} {{ctx.Locale.Tr "settings.webauthn_rename_key"}}</div>
    42			<form class="ui form form-fetch-action" method="post">
    43				<div class="content">
    44					<div class="required field">
    45						<label for="rename-webauthn-nickname">{{ctx.Locale.Tr "settings.webauthn_nickname"}}</label>
    46						<input id="rename-webauthn-nickname" name="name" type="text" maxlength="255" required>
    47					</div>
    48				</div>
    49				{{template "base/modal_actions_confirm" (dict "ModalButtonTypes" "confirm")}}
    50			</form>
    51		</div>
    52		<div class="ui small modal" id="delete-registration">
    53			<div class="header">
    54				{{svg "octicon-trash"}}
    55				{{ctx.Locale.Tr "settings.webauthn_delete_key"}}
    56			</div>
    57			<div class="content">
    58				<p>{{ctx.Locale.Tr "settings.webauthn_delete_key_desc"}}</p>
    59			</div>
    60			{{template "base/modal_actions_confirm" .}}
    61		</div>
    62	</div>
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:19:49Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Search for csrf handling in integration test harness

`grep -n "_csrf" tests/integration/integration_test.go`

```text
(Bash completed with no output)
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:19:49Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Find webauthn routes in web.go

`grep -n "webauthn" routers/web/web.go`

```text
592:		m.Group("/webauthn", func() {
652:			m.Group("/webauthn", func() {
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:19:50Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Find xorm version and module cache path

`grep -n "xorm" go.mod && ls $(go env GOMODCACHE)/xorm.io/ 2>/dev/null`

```text
120:	xorm.io/builder v0.3.13
121:	xorm.io/xorm v1.4.1
builder@v0.3.13
xorm@v1.4.1
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:19:51Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Read unittest helper source

`grep -rn "func AssertExistsAndLoadBean" models/unittest/ && sed -n '1,120p' models/unittest/unit_tests.go`

```text
models/unittest/unit_tests.go:79:func AssertExistsAndLoadBean[T any](t TestingT, bean T, conditions ...any) T {
// Copyright 2016 The Gitea Authors. All rights reserved.
// SPDX-License-Identifier: MIT

package unittest

import (
	"context"
	"fmt"
	"math"
	"os"
	"strings"

	"gitea.dev/models/db"

	"github.com/stretchr/testify/assert"
	"github.com/stretchr/testify/require"
	"xorm.io/builder"
)

// Code in this file is mainly used by unittest.CheckConsistencyFor, which is not in the unit test for various reasons.
// In the future if we can decouple CheckConsistencyFor into separate unit test code, then this file can be moved into unittest package too.

// NonexistentID an ID that will never exist
const NonexistentID = int64(math.MaxInt64)

type TestingT interface {
	require.TestingT
	assert.TestingT
	Context() context.Context
}

type testCond struct {
	query any
	args  []any
}

type testOrderBy string

// Cond create a condition with arguments for a test
func Cond(query any, args ...any) any {
	return &testCond{query: query, args: args}
}

// OrderBy creates "ORDER BY" a test query
func OrderBy(orderBy string) any {
	return testOrderBy(orderBy)
}

func whereOrderConditions(e db.Engine, conditions []any) db.Engine {
	orderBy := "id" // query must have the "ORDER BY", otherwise the result is not deterministic. FIXME: some tables do not have "id" column
	for _, condition := range conditions {
		switch cond := condition.(type) {
		case *testCond:
			e = e.Where(cond.query, cond.args...)
		case testOrderBy:
			orderBy = string(cond)
		default:
			e = e.Where(cond)
		}
	}
	return e.OrderBy(orderBy)
}

func getBeanIfExists(t TestingT, bean any, conditions ...any) (bool, error) {
	e := db.GetEngine(t.Context())
	return whereOrderConditions(e, conditions).Get(bean)
}

func GetBean[T any](t TestingT, bean T, conditions ...any) (ret T) {
	exists, err := getBeanIfExists(t, bean, conditions...)
	require.NoError(t, err)
	if exists {
		return bean
	}
	return ret
}

// AssertExistsAndLoadBean assert that a bean exists and load it from the test database
func AssertExistsAndLoadBean[T any](t TestingT, bean T, conditions ...any) T {
	exists, err := getBeanIfExists(t, bean, conditions...)
	require.NoError(t, err)
	require.True(t, exists,
		"Expected to find %+v (of type %T, with conditions %+v), but did not",
		bean, bean, conditions)
	return bean
}

// AssertExistsAndLoadMap assert that a row exists and load it from the test database
func AssertExistsAndLoadMap(t TestingT, table string, conditions ...any) map[string]string {
	e := db.GetEngine(t.Context()).Table(table)
	res, err := whereOrderConditions(e, conditions).Query()
	assert.NoError(t, err)
	assert.Len(t, res, 1,
		"Expected to find one row in %s (with conditions %+v), but found %d",
		table, conditions, len(res),
	)

	if len(res) == 1 {
		rec := map[string]string{}
		for k, v := range res[0] {
			rec[k] = string(v)
		}
		return rec
	}
	return nil
}

// GetCount get the count of a bean
func GetCount(t TestingT, bean any, conditions ...any) int {
	e := db.GetEngine(t.Context())
	for _, condition := range conditions {
		switch cond := condition.(type) {
		case *testCond:
			e = e.Where(cond.query, cond.args...)
		default:
			e = e.Where(cond)
		}
	}
	count, err := e.Count(bean)
	assert.NoError(t, err)
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:19:52Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Show route group context

`sed -n 575,700p routers/web/web.go`

```text
			m.Group("/register", func() {
				m.Combo("").
					Get(auth.RegisterOpenID, openIDSignUpEnabled).
					Post(web.Bind(forms.SignUpOpenIDForm{}), auth.RegisterOpenIDPost)
			}, openIDSignUpEnabled)
		}, openIDSignInEnabled)
		m.Get("/sign_up", auth.SignUp)
		m.Post("/sign_up", web.Bind(forms.RegisterForm{}), auth.SignUpPost)
		m.Get("/link_account", auth.LinkAccount)
		m.Post("/link_account_signin", web.Bind(forms.SignInForm{}), auth.LinkAccountPostSignIn)
		m.Post("/link_account_signup", web.Bind(forms.RegisterForm{}), auth.LinkAccountPostRegister)
		m.Group("/two_factor", func() {
			m.Get("", auth.TwoFactor)
			m.Post("", web.Bind(forms.TwoFactorAuthForm{}), auth.TwoFactorPost)
			m.Get("/scratch", auth.TwoFactorScratch)
			m.Post("/scratch", web.Bind(forms.TwoFactorScratchAuthForm{}), auth.TwoFactorScratchPost)
		})
		m.Group("/webauthn", func() {
			m.Get("", auth.WebAuthn)
			m.Get("/passkey/assertion", auth.WebAuthnPasskeyAssertion)
			m.Post("/passkey/login", auth.WebAuthnPasskeyLogin)
			m.Get("/assertion", auth.WebAuthnLoginAssertion)
			m.Post("/assertion", auth.WebAuthnLoginAssertionPost)
		})
	}, reqSignOut)

	m.Any("/user/events", routing.MarkLongPolling(), events.Events)

	m.Group("/login/oauth", func() {
		m.Group("", func() {
			m.Get("/authorize", web.Bind(forms.AuthorizationForm{}), auth.AuthorizeOAuth)
			m.Post("/grant", web.Bind(forms.GrantApplicationForm{}), auth.GrantApplicationOAuth)
			// TODO manage redirection
			m.Post("/authorize", web.Bind(forms.AuthorizationForm{}), auth.AuthorizeOAuth)
		}, reqSignIn)

		m.Group("", func() {
			m.Methods("GET, POST, OPTIONS", "/userinfo", auth.InfoOAuth)
			m.Methods("POST, OPTIONS", "/access_token", web.Bind(forms.AccessTokenForm{}), auth.AccessTokenOAuth)
			m.Methods("GET, OPTIONS", "/keys", auth.OIDCKeys)
			m.Methods("POST, OPTIONS", "/introspect", web.Bind(forms.IntrospectTokenForm{}), auth.IntrospectOAuth)
		}, optionsCorsHandler(), webAuth.AllowOAuth2, optSignInFromAnyOrigin)
	}, oauth2Enabled)

	m.Group("/user/settings", func() {
		m.Get("", user_setting.Profile)
		m.Post("", web.Bind(forms.UpdateProfileForm{}), user_setting.ProfilePost)
		m.Post("/update_preferences", user_setting.UpdatePreferences)
		m.Get("/change_password", auth.MustChangePassword)
		m.Post("/change_password", web.Bind(forms.MustChangePasswordForm{}), auth.MustChangePasswordPost)
		m.Post("/avatar", web.Bind(forms.AvatarForm{}), user_setting.AvatarPost)
		m.Post("/avatar/delete", user_setting.DeleteAvatar)
		m.Group("/account", func() {
			m.Combo("").Get(user_setting.Account).Post(web.Bind(forms.ChangePasswordForm{}), user_setting.AccountPost)
			m.Post("/email", web.Bind(forms.AddEmailForm{}), user_setting.EmailPost)
			m.Post("/email/delete", user_setting.DeleteEmail)
			m.Post("/delete", user_setting.DeleteAccount)
		})
		m.Group("/appearance", func() {
			m.Get("", user_setting.Appearance)
			m.Post("/language", web.Bind(forms.UpdateLanguageForm{}), user_setting.UpdateUserLang)
			m.Post("/hidden_comments", user_setting.UpdateUserHiddenComments)
			m.Post("/theme", web.Bind(forms.UpdateThemeForm{}), user_setting.UpdateUIThemePost)
		})
		m.Group("/notifications", func() {
			m.Get("", user_setting.Notifications)
			m.Post("/email", user_setting.NotificationsEmailPost)
			m.Post("/actions", user_setting.NotificationsActionsEmailPost)
		})
		m.Group("/security", func() {
			m.Get("", security.Security)
			m.Group("/two_factor", func() {
				m.Post("/regenerate_scratch", security.RegenerateScratchTwoFactor)
				m.Post("/disable", security.DisableTwoFactor)
				m.Get("/enroll", security.EnrollTwoFactor)
				m.Post("/enroll", web.Bind(forms.TwoFactorAuthForm{}), security.EnrollTwoFactorPost)
			})
			m.Group("/webauthn", func() {
				m.Post("/request_register", web.Bind(forms.WebauthnRegistrationForm{}), security.WebAuthnRegister)
				m.Post("/register", security.WebauthnRegisterPost)
				m.Post("/rename", web.Bind(forms.WebauthnRenameForm{}), security.WebauthnRename)
				m.Post("/delete", security.WebauthnDelete)
			})
			m.Group("/openid", func() {
				m.Post("", web.Bind(forms.AddOpenIDForm{}), security.OpenIDPost)
				m.Post("/delete", security.DeleteOpenID)
				m.Post("/toggle_visibility", security.ToggleOpenIDVisibility)
			}, openIDSignInEnabled)
			m.Post("/account_link", security.DeleteAccountLink)
		})

		m.Group("/applications", func() {
			// oauth2 applications
			m.Group("/oauth2", func() {
				m.Get("/{id}", user_setting.OAuth2ApplicationShow)
				m.Post("/{id}", web.Bind(forms.EditOAuth2ApplicationForm{}), user_setting.OAuthApplicationsEdit)
				m.Post("/{id}/regenerate_secret", user_setting.OAuthApplicationsRegenerateSecret)
				m.Post("", web.Bind(forms.EditOAuth2ApplicationForm{}), user_setting.OAuthApplicationsPost)
				m.Post("/{id}/delete", user_setting.DeleteOAuth2Application)
				m.Post("/{id}/revoke/{grantId}", user_setting.RevokeOAuth2Grant)
			}, oauth2Enabled)

			// access token applications
			m.Combo("").Get(user_setting.Applications).
				Post(web.Bind(forms.NewAccessTokenForm{}), user_setting.ApplicationsPost)
			m.Post("/delete", user_setting.DeleteApplication)
		})

		m.Combo("/keys").Get(user_setting.Keys).
			Post(web.Bind(forms.AddKeyForm{}), user_setting.KeysPost)
		m.Post("/keys/delete", user_setting.DeleteKey)
		m.Group("/packages", func() {
			m.Get("", user_setting.Packages)
			m.Group("/rules", func() {
				m.Group("/add", func() {
					m.Get("", user_setting.PackagesRuleAdd)
					m.Post("", web.Bind(forms.PackageCleanupRuleForm{}), user_setting.PackagesRuleAddPost)
				})
				m.Group("/{id}", func() {
					m.Get("", user_setting.PackagesRuleEdit)
					m.Post("", web.Bind(forms.PackageCleanupRuleForm{}), user_setting.PackagesRuleEditPost)
					m.Get("/preview", user_setting.PackagesRulePreview)
				})
			})
			m.Group("/cargo", func() {
				m.Post("/initialize", user_setting.InitializeCargoIndex)
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:19:53Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Find CSRF handling in integration session helpers

`grep -rn "_csrf\|CsrfToken\|csrf" tests/integration/integration_test.go tests/integration/session_test.go 2>/dev/null | head -40; echo "---files---"; ls tests/integration | grep -i "session\|integration_test"`

```text
---files---
create_no_session_test.go
integration_test.go
session_test.go
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:19:53Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Show webauthn model funcs

`sed -n 160,230p models/auth/webauthn.go`

```text
	return cred, nil
}

// HasWebAuthnRegistrationsByUID returns whether a given user has WebAuthn registrations
func HasWebAuthnRegistrationsByUID(ctx context.Context, uid int64) (bool, error) {
	return db.GetEngine(ctx).Where("user_id = ?", uid).Exist(&WebAuthnCredential{})
}

// GetWebAuthnCredentialByCredID returns WebAuthn credential by credential ID
func GetWebAuthnCredentialByCredID(ctx context.Context, userID int64, credID []byte) (*WebAuthnCredential, error) {
	cred := new(WebAuthnCredential)
	if found, err := db.GetEngine(ctx).Where("user_id = ? AND credential_id = ?", userID, credID).Get(cred); err != nil {
		return nil, err
	} else if !found {
		return nil, ErrWebAuthnCredentialNotExist{CredentialID: credID}
	}
	return cred, nil
}

// CreateCredential will create a new WebAuthnCredential from the given Credential
func CreateCredential(ctx context.Context, userID int64, name string, cred *webauthn.Credential) (*WebAuthnCredential, error) {
	c := &WebAuthnCredential{
		UserID:          userID,
		Name:            name,
		CredentialID:    cred.ID,
		PublicKey:       cred.PublicKey,
		AttestationType: cred.AttestationType,
		AAGUID:          cred.Authenticator.AAGUID,
		SignCount:       cred.Authenticator.SignCount,
		CloneWarning:    false,
	}

	if err := db.Insert(ctx, c); err != nil {
		return nil, err
	}
	return c, nil
}

// UpdateCredentialName will rename the WebAuthnCredential with the given id if it belongs to userID
func UpdateCredentialName(ctx context.Context, id, userID int64, name string) error {
	_, err := db.GetEngine(ctx).ID(id).Where("user_id = ?", userID).Cols("name", "lower_name").Update(&WebAuthnCredential{Name: name})
	return err
}

// DeleteCredential will delete WebAuthnCredential
func DeleteCredential(ctx context.Context, id, userID int64) (bool, error) {
	had, err := db.GetEngine(ctx).ID(id).Where("user_id = ?", userID).Delete(&WebAuthnCredential{})
	return had > 0, err
}
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:19:54Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Find TestSession methods

`grep -rn "func (s \*TestSession) MakeRequest\|func (s \*TestSession)" tests/integration/*.go | head -20`

```text
tests/integration/integration_test.go:91:func (s *TestSession) GetRawCookie(name string) *http.Cookie {
tests/integration/integration_test.go:104:func (s *TestSession) GetSiteCookie(name string) string {
tests/integration/integration_test.go:113:func (s *TestSession) GetCookieFlashMessage() *middleware.Flash {
tests/integration/integration_test.go:118:func (s *TestSession) MakeRequest(t testing.TB, rw *RequestWrapper, expectedStatus int) *httptest.ResponseRecorder {
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:19:54Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Find usages of form.require_error

`grep -rn "form.require_error" --include=*.go --include=*.tmpl --include=*.json . | head -50`

```text
(eval):1: no matches found: --include=*.go
```
</details></tool-use>

_**Agent (claude-opus-5 2026-09-15 02:19:54Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Find CSRF/same-site protection server-side

`grep -rn "Sec-Fetch-Site\|SecFetch\|SameSite\|csrf" --include='*.go' modules/ services/ routers/ | grep -iv "test" | head -30`

```text
modules/web/middleware/cookie.go:72:		SameSite: setting.SessionConfig.SameSite,
modules/web/middleware/cookie.go:96:		SameSite: setting.SessionConfig.SameSite,
modules/setting/session.go:34:	// SameSite declares if your cookie should be restricted to a first-party or same-site context. Valid strings are "none", "lax", "strict". Default is "lax"
modules/setting/session.go:35:	SameSite http.SameSite
modules/setting/session.go:40:	SameSite:    http.SameSiteLaxMode,
modules/setting/session.go:62:		SessionConfig.SameSite = http.SameSiteNoneMode
modules/setting/session.go:66:		SessionConfig.SameSite = http.SameSiteLaxMode
services/context/context.go:189:			// FIXME: GLOBAL-PARSE-FORM: this ParseMultipartForm was used for parsing the csrf token from multipart/form-data
services/context/context.go:190:			// We have dropped the csrf token, so ideally this global ParseMultipartForm should be removed.
routers/web/web.go:322://   - With CORS middleware: CORS middleware does the preflight request handling, the requests has Sec-Fetch-Site header.
routers/web/web.go:324://   - For non-browser client requests: git clone via http, no Sec-Fetch-Site header.
routers/common/middleware.go:148:		SameSite:       setting.SessionConfig.SameSite,
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:19:55Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Find JSON helper definitions

`grep -rn "JSONErrorNotFound\|func (ctx \*Context) JSONError\|JSONRedirect" modules/ services/context/ | head -30`

```text
modules/test/utils.go:28:// It also works for JSONRedirect: `{"redirect": "..."}`
modules/test/utils.go:56:func ParseJSONRedirect(buf []byte) (ret struct {
services/context/context.go:249:func (ctx *Context) JSONRedirect(redirect string) {
services/context/context.go:257:func (ctx *Context) JSONError(msg any) {
services/context/context.go:268:func (ctx *Context) JSONErrorNotFound(optMsg ...string) {
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:19:56Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Read TestSession MakeRequest

`sed -n 80,180p tests/integration/integration_test.go`

```text
	flag.Parse()
	if flag.Lookup("test.list").Value.String() != "" {
		os.Exit(m.Run())
	}
	os.Exit(testMain(m))
}

type TestSession struct {
	jar http.CookieJar
}

func (s *TestSession) GetRawCookie(name string) *http.Cookie {
	baseURL, err := url.Parse(setting.AppURL)
	if err != nil {
		return nil
	}
	for _, c := range s.jar.Cookies(baseURL) {
		if c.Name == name {
			return c
		}
	}
	return nil
}

func (s *TestSession) GetSiteCookie(name string) string {
	c := s.GetRawCookie(name)
	if c != nil {
		v, _ := url.QueryUnescape(c.Value)
		return v
	}
	return ""
}

func (s *TestSession) GetCookieFlashMessage() *middleware.Flash {
	cookie := s.GetSiteCookie(gitea_context.CookieNameFlash)
	return middleware.ParseCookieFlashMessage(cookie)
}

func (s *TestSession) MakeRequest(t testing.TB, rw *RequestWrapper, expectedStatus int) *httptest.ResponseRecorder {
	t.Helper()
	if s == nil {
		return MakeRequest(t, rw, expectedStatus)
	}
	req := rw.Request
	baseURL, err := url.Parse(setting.AppURL)
	assert.NoError(t, err)
	for _, c := range s.jar.Cookies(baseURL) {
		req.AddCookie(c)
	}
	resp := MakeRequest(t, rw, expectedStatus)

	ch := http.Header{}
	ch.Add("Cookie", strings.Join(resp.Header()["Set-Cookie"], ";"))
	cr := http.Request{Header: ch}
	s.jar.SetCookies(baseURL, cr.Cookies())

	return resp
}

const userPassword = "password"

func emptyTestSession(t testing.TB) *TestSession {
	t.Helper()
	jar, err := cookiejar.New(nil)
	assert.NoError(t, err)

	return &TestSession{jar: jar}
}

func getUserToken(t testing.TB, userName string, scope ...auth.AccessTokenScope) string {
	return getTokenForLoggedInUser(t, loginUser(t, userName), scope...)
}

func loginUser(t testing.TB, userName string) *TestSession {
	t.Helper()

	return loginUserWithPassword(t, userName, userPassword)
}

func loginUserWithPassword(t testing.TB, userName, password string) *TestSession {
	t.Helper()
	req := NewRequestWithValues(t, "POST", "/user/login", map[string]string{
		"user_name": userName,
		"password":  password,
	})
	resp := MakeRequest(t, req, http.StatusSeeOther)

	ch := http.Header{}
	ch.Add("Cookie", strings.Join(resp.Header()["Set-Cookie"], ";"))
	cr := http.Request{Header: ch}

	session := emptyTestSession(t)

	baseURL, err := url.Parse(setting.AppURL)
	assert.NoError(t, err)
	session.jar.SetCookies(baseURL, cr.Cookies())

	return session
}

// token has to be unique this counter take care of
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:19:57Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Search CSRF in test modules

`grep -rn "csrf\|Csrf\|CSRF" modules/test/*.go tests/*.go 2>/dev/null | head -30`

```text
(Bash completed with no output)
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:20:00Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Read xorm Update implementation head

`cd $(go env GOMODCACHE)/xorm.io/xorm@v1.4.1 && sed -n '1,140p' session_update.go`

```text
// Copyright 2016 The Xorm Authors. All rights reserved.
// Use of this source code is governed by a BSD-style
// license that can be found in the LICENSE file.

package xorm

import (
	"reflect"

	"xorm.io/builder"
	"xorm.io/xorm/internal/statements"
	"xorm.io/xorm/internal/utils"
	"xorm.io/xorm/schemas"
)

// enumerated all errors
var (
	ErrNoColumnsTobeUpdated = statements.ErrNoColumnsTobeUpdated
)

func (session *Session) genAutoCond(condiBean any) (builder.Cond, error) {
	if session.statement.NoAutoCondition {
		return builder.NewCond(), nil
	}

	if c, ok := condiBean.(map[string]any); ok {
		eq := make(builder.Eq)
		for k, v := range c {
			eq[session.engine.Quote(k)] = v
		}

		if session.statement.RefTable != nil {
			if col := session.statement.RefTable.DeletedColumn(); col != nil && !session.statement.GetUnscoped() { // tag "deleted" is enabled
				return eq.And(session.statement.CondDeleted(col)), nil
			}
		}
		return eq, nil
	}

	ct := reflect.TypeOf(condiBean)
	k := ct.Kind()
	if k == reflect.Ptr {
		k = ct.Elem().Kind()
	}
	if k != reflect.Struct {
		return nil, ErrConditionType
	}

	condTable, err := session.engine.TableInfo(condiBean)
	if err != nil {
		return nil, err
	}
	return session.statement.BuildConds(condTable, condiBean, true, true, false, true, false)
}

// Update records, bean's non-empty fields are updated contents,
// condiBean' non-empty filds are conditions
// CAUTION:
//
//	1.bool will defaultly be updated content nor conditions
//	 You should call UseBool if you have bool to use.
//	2.float32 & float64 may be not inexact as conditions
func (session *Session) Update(bean any, condiBean ...any) (int64, error) {
	if session.isAutoClose {
		defer session.Close()
	}

	defer cleanupProcessorsClosures(&session.afterClosures)

	defer session.resetStatement()

	if session.statement.LastError != nil {
		return 0, session.statement.LastError
	}

	v := utils.ReflectValue(bean)
	t := v.Type()

	// handle before update processors
	for _, closure := range session.beforeClosures {
		closure(bean)
	}
	cleanupProcessorsClosures(&session.beforeClosures) // cleanup after used
	if processor, ok := bean.(BeforeUpdateProcessor); ok {
		processor.BeforeUpdate()
	}
	// --

	var colNames []string
	var args []any
	var autoUpdateClosures []func(any)
	var err error
	isMap := t.Kind() == reflect.Map
	isStruct := t.Kind() == reflect.Struct
	switch {
	case isStruct:
		if err := session.statement.SetRefBean(bean); err != nil {
			return 0, err
		}

		if len(session.statement.TableName()) == 0 {
			return 0, ErrTableNotFound
		}

		if session.statement.ColumnStr() == "" {
			colNames, args, err = session.statement.BuildUpdates(v, false, false,
				false, false, true)
		} else {
			colNames, args, autoUpdateClosures, err = session.genUpdateColumns(bean)
		}
		if err != nil {
			return 0, err
		}
	case isMap:
		colNames = make([]string, 0)
		args = make([]any, 0)
		bValue := reflect.Indirect(reflect.ValueOf(bean))

		for _, v := range bValue.MapKeys() {
			colNames = append(colNames, session.engine.Quote(v.String())+" = ?")
			args = append(args, bValue.MapIndex(v).Interface())
		}
	default:
		return 0, ErrParamsType
	}

	table := session.statement.RefTable

	if session.statement.UseAutoTime && table != nil && table.Updated != "" {
		if !session.statement.ColumnMap.Contain(table.Updated) &&
			!session.statement.OmitColumnMap.Contain(table.Updated) {
			colNames = append(colNames, session.engine.Quote(table.Updated)+" = ?")
			col := table.UpdatedColumn()
			val, t, err := session.engine.nowTime(col)
			if err != nil {
				return 0, err
			}
			if session.engine.dialect.URI().DBType == schemas.ORACLE {
				args = append(args, t)
			} else {
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:19:59Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Find group closing brace

`awk 'NR>=618 && NR<=800' routers/web/web.go | grep -n "^	}" | head -20`

```text
120:	}, reqSignIn, user_setting.SettingsCtxData)
138:	})
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:19:58Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Find CSRF references in frontend

`grep -rni "csrf" web_src/js/ --include=*.ts --include=*.vue -l | head -50`

```text
(eval):1: no matches found: --include=*.ts
```
</details></tool-use>

_**Agent (claude-opus-5 2026-09-15 02:19:59Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Read Sec-Fetch-Site protection middleware

`sed -n '300,380p' routers/web/web.go`

```text
	// Get user from session if logged in.
	webAuth := newWebAuthMiddleware()
	mid = append(mid, webAuth.MiddlewareHandler)

	if setting.API.EnableSwagger {
		// Note: The route is here but no in API routes because it renders a web page
		routes.Get("/api/swagger", append(mid, misc.Swagger)...) // Render V1 by default
	}

	mid = append(mid, goGet)
	mid = append(mid, common.PageGlobalData)
	mid = append(mid, common.BlockExpensive(), common.QoS(), web.RouterMockPoint(RouterMockPointBeforeWebRoutes))

	webRoutes := web.NewRouter()
	webRoutes.AfterRouting(mid...)
	registerWebRoutes(webRoutes, webAuth)

	routes.Mount("", webRoutes)
	return routes
}

// optSignInFromAnyOrigin means that the user can (optionally) be signed in from any origin (no cross-origin protection)
//   - With CORS middleware: CORS middleware does the preflight request handling, the requests has Sec-Fetch-Site header.
//     The CORS mechanism already protects cross-origin requests, and the CrossOriginProtection has no "allowed origin" list, so disable CrossOriginProtection.
//   - For non-browser client requests: git clone via http, no Sec-Fetch-Site header.
//     Such requests are not cross-origin requests, so disable CrossOriginProtection.
var optSignInFromAnyOrigin = verifyAuthWithOptions(&common.VerifyOptions{DisableCrossOriginProtection: true})

// registerWebRoutes register routes
func registerWebRoutes(m *web.Router, webAuth *AuthMiddleware) {
	// required to be signed in or signed out
	reqSignIn := verifyAuthWithOptions(&common.VerifyOptions{SignInRequired: true})
	reqSignOut := verifyAuthWithOptions(&common.VerifyOptions{SignOutRequired: true})
	// optional sign in (if signed in, use the user as doer, if not, no doer)
	optSignIn := verifyAuthWithOptions(&common.VerifyOptions{SignInRequired: setting.Service.RequireSignInViewStrict})
	optExploreSignIn := verifyAuthWithOptions(&common.VerifyOptions{SignInRequired: setting.Service.RequireSignInViewStrict || setting.Service.Explore.RequireSigninView})

	validation.AddBindingRules()

	openIDSignInEnabled := func(ctx *context.Context) {
		if !setting.Service.EnableOpenIDSignIn {
			ctx.HTTPError(http.StatusForbidden)
			return
		}
	}

	openIDSignUpEnabled := func(ctx *context.Context) {
		if !setting.Service.EnableOpenIDSignUp {
			ctx.HTTPError(http.StatusForbidden)
			return
		}
	}

	oauth2Enabled := func(ctx *context.Context) {
		if !setting.OAuth2.Enabled {
			ctx.HTTPError(http.StatusForbidden)
			return
		}
	}

	reqMilestonesDashboardPageEnabled := func(ctx *context.Context) {
		if !setting.Service.ShowMilestonesDashboardPage {
			ctx.HTTPError(http.StatusForbidden)
			return
		}
	}

	// webhooksEnabled requires webhooks to be enabled by admin.
	webhooksEnabled := func(ctx *context.Context) {
		if setting.DisableWebhooks {
			ctx.HTTPError(http.StatusForbidden)
			return
		}
	}

	starsEnabled := func(ctx *context.Context) {
		if setting.Repository.DisableStars {
			ctx.HTTPError(http.StatusForbidden)
			return
		}
	}
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:20:01Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Grep for hook invocation points in update

`cd $(go env GOMODCACHE)/xorm.io/xorm@v1.4.1 && grep -n "BeforeUpdate\|BeforeSet\|beforeClosures\|BuildUpdates" session_update.go | head -50`

```text
80:	for _, closure := range session.beforeClosures {
83:	cleanupProcessorsClosures(&session.beforeClosures) // cleanup after used
84:	if processor, ok := bean.(BeforeUpdateProcessor); ok {
85:		processor.BeforeUpdate()
106:			colNames, args, err = session.statement.BuildUpdates(v, false, false,
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:19:59Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Show rest of settings group

`sed -n 700,780p routers/web/web.go`

```text
				m.Post("/initialize", user_setting.InitializeCargoIndex)
				m.Post("/rebuild", user_setting.RebuildCargoIndex)
			})
			m.Post("/chef/regenerate_keypair", user_setting.RegenerateChefKeyPair)
		}, packagesEnabled)

		m.Group("/actions", func() {
			m.Get("", misc.LocationRedirect("./actions/general"))
			m.Group("/general", func() {
				m.Get("", shared_actions.GeneralSettings)
				m.Post("", shared_actions.UpdateGeneralSettings)
			})
			addSettingsRunnersRoutes()
			addSettingsSecretsRoutes()
			addSettingsVariablesRoutes()
			addSettingsScopedWorkflowsRoutes()
		}, actions.MustEnableActions)

		m.Get("/organization", user_setting.Organization)
		m.Get("/repos", user_setting.Repos)
		m.Post("/repos/unadopted", user_setting.AdoptOrDeleteRepository)

		m.Group("/hooks", func() {
			m.Get("", user_setting.Webhooks)
			m.Post("/delete", user_setting.DeleteWebhook)
			addWebhookAddRoutes()
			m.Group("/{id}", func() {
				m.Get("", repo_setting.WebHooksEdit)
				m.Post("/replay/{uuid}", repo_setting.ReplayWebhook)
			})
			addWebhookEditRoutes()
		}, webhooksEnabled)

		m.Group("/blocked_users", func() {
			m.Get("", user_setting.BlockedUsers)
			m.Post("", web.Bind(forms.BlockUserForm{}), user_setting.BlockedUsersPost)
		})
	}, reqSignIn, user_setting.SettingsCtxData)

	m.Group("/user", func() {
		m.Get("/activate", auth.Activate)
		m.Post("/activate", auth.ActivatePost)
		m.Any("/activate_email", auth.ActivateEmail)
		m.Get("/avatar/{username}/{size}", user.AvatarByUsernameSize)
		m.Get("/recover_account", auth.ResetPasswd)
		m.Post("/recover_account", auth.ResetPasswdPost)
		m.Get("/forgot_password", auth.ForgotPasswd)
		m.Post("/forgot_password", auth.ForgotPasswdPost)
		m.Get("/logout", auth.SignOut)
		m.Get("/stopwatches", reqSignIn, user.GetStopwatches)
		m.Get("/search_candidates", optExploreSignIn, user.SearchCandidates)
		m.Group("/oauth2", func() {
			m.Get("/{provider}", auth.SignInOAuth)
			m.Get("/{provider}/callback", auth.SignInOAuthCallback)
		})
	})
	// ***** END: User *****

	m.Get("/avatar/{hash}", user.AvatarByEmailHash)

	adminReq := verifyAuthWithOptions(&common.VerifyOptions{SignInRequired: true, AdminRequired: true})

	// ***** START: Admin *****
	m.Group("/-/admin", func() {
		m.Get("", admin.Dashboard)
		m.Get("/system_status", admin.SystemStatus)
		m.Post("", web.Bind(forms.AdminDashboardForm{}), admin.DashboardPost)

		m.Get("/self_check", admin.SelfCheck)
		m.Post("/self_check", admin.SelfCheckPost)

		m.Group("/config", func() {
			m.Get("", admin.Config)
			m.Post("", admin.ChangeConfig)
			m.Post("/test_mail", admin.SendTestMail)
			m.Post("/test_cache", admin.TestCache)
			m.Get("/settings", admin.ConfigSettings)
		})

		m.Group("/monitor", func() {
			m.Get("/stats", admin.MonitorStats)
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:20:00Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Find CSRF middleware server-side

`grep -rn "X-Gitea-Fetch-Action\|Sec-Fetch-Site\|csrf" services/context/*.go modules/web/**/*.go routers/web/web.go 2>/dev/null | head -60`

```text
services/context/base_test.go:41:	req.Header.Add("X-Gitea-Fetch-Action", "1")
services/context/base.go:164:	if b.Req.Header.Get("X-Gitea-Fetch-Action") != "" {
services/context/context.go:189:			// FIXME: GLOBAL-PARSE-FORM: this ParseMultipartForm was used for parsing the csrf token from multipart/form-data
services/context/context.go:190:			// We have dropped the csrf token, so ideally this global ParseMultipartForm should be removed.
modules/web/middleware/cookie.go:38:	if req.Header.Get("X-Gitea-Fetch-Action") != "" {
routers/web/web.go:322://   - With CORS middleware: CORS middleware does the preflight request handling, the requests has Sec-Fetch-Site header.
routers/web/web.go:324://   - For non-browser client requests: git clone via http, no Sec-Fetch-Site header.
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:20:01Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Find usages of require_error

`grep -rn "require_error" --include='*.go' --include='*.tmpl' --include='*.json' --include='*.ts' . | head -50`

```text
routers/web/admin/auths.go:215:		return nil, errors.New(ctx.Locale.TrString("form.SSPISeparatorReplacement") + ctx.Locale.TrString("form.require_error"))
routers/web/user/setting/security/webauthn.go:154:		ctx.JSONError(ctx.Locale.TrString("settings.webauthn_nickname") + ctx.Locale.TrString("form.require_error"))
options/locale/locale_de-DE.json:540:  "form.require_error": " darf nicht leer sein.",
options/locale/locale_fi-FI.json:298:  "form.require_error": " ei voi olla tyhjä.",
options/locale/locale_is-IS.json:279:  "form.require_error": " Getur ekki verið tómt.",
options/locale/locale_ru-RU.json:407:  "form.require_error": " не может быть пустым.",
options/locale/locale_pl-PL.json:322:  "form.require_error": " nie może być puste.",
options/locale/locale_lv-LV.json:411:  "form.require_error": " nedrīkst būt tukšs.",
options/locale/locale_sv-SE.json:252:  "form.require_error": "får inte vara tomt",
options/locale/locale_id-ID.json:269:  "form.require_error": " tidak boleh kosong.",
options/locale/locale_hu-HU.json:246:  "form.require_error": " nem lehet üres",
options/locale/locale_nl-NL.json:320:  "form.require_error": "kan niet leeg zijn.",
options/locale/locale_tr-TR.json:527:  "form.require_error": " boş olamaz.",
options/locale/locale_en-US.json:540:  "form.require_error": " cannot be empty.",
options/locale/locale_fa-IR.json:317:  "form.require_error": " نمی تواند خالی باشد.",
options/locale/locale_el-GR.json:404:  "form.require_error": " δεν μπορεί να είναι κενό.",
options/locale/locale_it-IT.json:318:  "form.require_error": " non può essere vuoto.",
options/locale/locale_si-LK.json:310:  "form.require_error": " හිස් විය නොහැක.",
options/locale/locale_cs-CZ.json:445:  "form.require_error": " nemůže být prázdný.",
options/locale/locale_pt-BR.json:483:  "form.require_error": " não pode estar em branco.",
options/locale/locale_ko-KR.json:540:  "form.require_error": " 비어 있을 수 없습니다.",
options/locale/locale_ja-JP.json:540:  "form.require_error": "は空にできません。",
options/locale/locale_zh-TW.json:450:  "form.require_error": " 不能為空。",
options/locale/locale_es-ES.json:399:  "form.require_error": " no puede estar vacío.",
options/locale/locale_ga-IE.json:540:  "form.require_error": " ní féidir a bheith folamh.",
options/locale/locale_uk-UA.json:460:  "form.require_error": " не може бути порожнім.",
options/locale/locale_zh-CN.json:540:  "form.require_error": "不能为空。",
options/locale/locale_sk-SK.json:389:  "form.require_error": " nemôže byť prázdne.",
options/locale/locale_pt-PT.json:540:  "form.require_error": " não pode estar em branco.",
options/locale/locale_fr-FR.json:540:  "form.require_error": " ne peut être vide.",
tests/integration/repo_branch_test.go:56:			FlashMessage:   translation.NewLocale("en-US").TrString("form.NewBranchName") + translation.NewLocale("en-US").TrString("form.require_error"),
modules/web/middleware/binding.go:131:		data["ErrorMsg"] = trName + l.TrString("form.require_error")
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:20:01Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Show JSON helpers

`sed -n '240,290p' services/context/context.go`

```text
// GetErrMsg returns error message in form validation.
func (ctx *Context) GetErrMsg() string {
	msg, _ := ctx.Data["ErrorMsg"].(string)
	if msg == "" {
		msg = "invalid form data"
	}
	return msg
}

func (ctx *Context) JSONRedirect(redirect string) {
	ctx.JSON(http.StatusOK, map[string]any{"redirect": redirect})
}

func (ctx *Context) JSONOK() {
	ctx.JSON(http.StatusOK, map[string]any{"ok": true}) // this is only a dummy response, frontend seldom uses it
}

func (ctx *Context) JSONError(msg any) {
	switch v := msg.(type) {
	case string:
		ctx.JSON(http.StatusBadRequest, map[string]any{"errorMessage": v, "renderFormat": "text"})
	case template.HTML:
		ctx.JSON(http.StatusBadRequest, map[string]any{"errorMessage": v, "renderFormat": "html"})
	default:
		panic(fmt.Sprintf("unsupported type: %T", msg))
	}
}

func (ctx *Context) JSONErrorNotFound(optMsg ...string) {
	msg := util.OptionalArg(optMsg)
	if msg == "" {
		msg = ctx.Locale.TrString("error.not_found")
	}
	ctx.JSON(http.StatusNotFound, map[string]any{"errorMessage": msg, "renderFormat": "text"})
}
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:20:02Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Find CSRF middleware wiring

`grep -rn "CsrfProtect\|csrfProtect\|PrepareCSRF\|csrf.Protect\|DisableCSRF\|IsProd\|skipCsrf" modules/web/middleware/*.go services/context/*.go routers/common/*.go 2>/dev/null | grep -i csrf | head -30`

```text
(Bash completed with no output)
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:20:03Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Read web.go CSRF-ish middleware

`sed -n '300,420p' routers/web/web.go`

```text
	// Get user from session if logged in.
	webAuth := newWebAuthMiddleware()
	mid = append(mid, webAuth.MiddlewareHandler)

	if setting.API.EnableSwagger {
		// Note: The route is here but no in API routes because it renders a web page
		routes.Get("/api/swagger", append(mid, misc.Swagger)...) // Render V1 by default
	}

	mid = append(mid, goGet)
	mid = append(mid, common.PageGlobalData)
	mid = append(mid, common.BlockExpensive(), common.QoS(), web.RouterMockPoint(RouterMockPointBeforeWebRoutes))

	webRoutes := web.NewRouter()
	webRoutes.AfterRouting(mid...)
	registerWebRoutes(webRoutes, webAuth)

	routes.Mount("", webRoutes)
	return routes
}

// optSignInFromAnyOrigin means that the user can (optionally) be signed in from any origin (no cross-origin protection)
//   - With CORS middleware: CORS middleware does the preflight request handling, the requests has Sec-Fetch-Site header.
//     The CORS mechanism already protects cross-origin requests, and the CrossOriginProtection has no "allowed origin" list, so disable CrossOriginProtection.
//   - For non-browser client requests: git clone via http, no Sec-Fetch-Site header.
//     Such requests are not cross-origin requests, so disable CrossOriginProtection.
var optSignInFromAnyOrigin = verifyAuthWithOptions(&common.VerifyOptions{DisableCrossOriginProtection: true})

// registerWebRoutes register routes
func registerWebRoutes(m *web.Router, webAuth *AuthMiddleware) {
	// required to be signed in or signed out
	reqSignIn := verifyAuthWithOptions(&common.VerifyOptions{SignInRequired: true})
	reqSignOut := verifyAuthWithOptions(&common.VerifyOptions{SignOutRequired: true})
	// optional sign in (if signed in, use the user as doer, if not, no doer)
	optSignIn := verifyAuthWithOptions(&common.VerifyOptions{SignInRequired: setting.Service.RequireSignInViewStrict})
	optExploreSignIn := verifyAuthWithOptions(&common.VerifyOptions{SignInRequired: setting.Service.RequireSignInViewStrict || setting.Service.Explore.RequireSigninView})

	validation.AddBindingRules()

	openIDSignInEnabled := func(ctx *context.Context) {
		if !setting.Service.EnableOpenIDSignIn {
			ctx.HTTPError(http.StatusForbidden)
			return
		}
	}

	openIDSignUpEnabled := func(ctx *context.Context) {
		if !setting.Service.EnableOpenIDSignUp {
			ctx.HTTPError(http.StatusForbidden)
			return
		}
	}

	oauth2Enabled := func(ctx *context.Context) {
		if !setting.OAuth2.Enabled {
			ctx.HTTPError(http.StatusForbidden)
			return
		}
	}

	reqMilestonesDashboardPageEnabled := func(ctx *context.Context) {
		if !setting.Service.ShowMilestonesDashboardPage {
			ctx.HTTPError(http.StatusForbidden)
			return
		}
	}

	// webhooksEnabled requires webhooks to be enabled by admin.
	webhooksEnabled := func(ctx *context.Context) {
		if setting.DisableWebhooks {
			ctx.HTTPError(http.StatusForbidden)
			return
		}
	}

	starsEnabled := func(ctx *context.Context) {
		if setting.Repository.DisableStars {
			ctx.HTTPError(http.StatusForbidden)
			return
		}
	}

	lfsServerEnabled := func(ctx *context.Context) {
		if !setting.LFS.StartServer {
			ctx.HTTPError(http.StatusNotFound)
			return
		}
	}

	federationEnabled := func(ctx *context.Context) {
		if !setting.Federation.Enabled {
			ctx.HTTPError(http.StatusNotFound)
			return
		}
	}

	dlSourceEnabled := func(ctx *context.Context) {
		if setting.Repository.DisableDownloadSourceArchives {
			ctx.HTTPError(http.StatusNotFound)
			return
		}
	}

	sitemapEnabled := func(ctx *context.Context) {
		if !setting.Other.EnableSitemap {
			ctx.HTTPError(http.StatusNotFound)
			return
		}
	}

	packagesEnabled := func(ctx *context.Context) {
		if !setting.Packages.Enabled {
			ctx.HTTPError(http.StatusForbidden)
			return
		}
	}

	feedEnabled := func(ctx *context.Context) {
		if !setting.Other.EnableFeed {
			ctx.HTTPError(http.StatusNotFound)
			return
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:20:03Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
List go files mentioning csrf

`grep -rln "csrf" --include=*.go modules/ services/context/ routers/ | head -20`

```text
(eval):1: no matches found: --include=*.go
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:20:04Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Find Sec-Fetch-Site handling

`grep -rn "SecFetchSite\|Sec-Fetch-Site" --include=*.go . | head -40`

```text
(eval):1: no matches found: --include=*.go
```
</details></tool-use>

_**Agent (claude-opus-5 2026-09-15 02:20:04Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Check enclosing route group for webauthn rename

`sed -n '600,665p' routers/web/web.go`

```text
	m.Any("/user/events", routing.MarkLongPolling(), events.Events)

	m.Group("/login/oauth", func() {
		m.Group("", func() {
			m.Get("/authorize", web.Bind(forms.AuthorizationForm{}), auth.AuthorizeOAuth)
			m.Post("/grant", web.Bind(forms.GrantApplicationForm{}), auth.GrantApplicationOAuth)
			// TODO manage redirection
			m.Post("/authorize", web.Bind(forms.AuthorizationForm{}), auth.AuthorizeOAuth)
		}, reqSignIn)

		m.Group("", func() {
			m.Methods("GET, POST, OPTIONS", "/userinfo", auth.InfoOAuth)
			m.Methods("POST, OPTIONS", "/access_token", web.Bind(forms.AccessTokenForm{}), auth.AccessTokenOAuth)
			m.Methods("GET, OPTIONS", "/keys", auth.OIDCKeys)
			m.Methods("POST, OPTIONS", "/introspect", web.Bind(forms.IntrospectTokenForm{}), auth.IntrospectOAuth)
		}, optionsCorsHandler(), webAuth.AllowOAuth2, optSignInFromAnyOrigin)
	}, oauth2Enabled)

	m.Group("/user/settings", func() {
		m.Get("", user_setting.Profile)
		m.Post("", web.Bind(forms.UpdateProfileForm{}), user_setting.ProfilePost)
		m.Post("/update_preferences", user_setting.UpdatePreferences)
		m.Get("/change_password", auth.MustChangePassword)
		m.Post("/change_password", web.Bind(forms.MustChangePasswordForm{}), auth.MustChangePasswordPost)
		m.Post("/avatar", web.Bind(forms.AvatarForm{}), user_setting.AvatarPost)
		m.Post("/avatar/delete", user_setting.DeleteAvatar)
		m.Group("/account", func() {
			m.Combo("").Get(user_setting.Account).Post(web.Bind(forms.ChangePasswordForm{}), user_setting.AccountPost)
			m.Post("/email", web.Bind(forms.AddEmailForm{}), user_setting.EmailPost)
			m.Post("/email/delete", user_setting.DeleteEmail)
			m.Post("/delete", user_setting.DeleteAccount)
		})
		m.Group("/appearance", func() {
			m.Get("", user_setting.Appearance)
			m.Post("/language", web.Bind(forms.UpdateLanguageForm{}), user_setting.UpdateUserLang)
			m.Post("/hidden_comments", user_setting.UpdateUserHiddenComments)
			m.Post("/theme", web.Bind(forms.UpdateThemeForm{}), user_setting.UpdateUIThemePost)
		})
		m.Group("/notifications", func() {
			m.Get("", user_setting.Notifications)
			m.Post("/email", user_setting.NotificationsEmailPost)
			m.Post("/actions", user_setting.NotificationsActionsEmailPost)
		})
		m.Group("/security", func() {
			m.Get("", security.Security)
			m.Group("/two_factor", func() {
				m.Post("/regenerate_scratch", security.RegenerateScratchTwoFactor)
				m.Post("/disable", security.DisableTwoFactor)
				m.Get("/enroll", security.EnrollTwoFactor)
				m.Post("/enroll", web.Bind(forms.TwoFactorAuthForm{}), security.EnrollTwoFactorPost)
			})
			m.Group("/webauthn", func() {
				m.Post("/request_register", web.Bind(forms.WebauthnRegistrationForm{}), security.WebAuthnRegister)
				m.Post("/register", security.WebauthnRegisterPost)
				m.Post("/rename", web.Bind(forms.WebauthnRenameForm{}), security.WebauthnRename)
				m.Post("/delete", security.WebauthnDelete)
			})
			m.Group("/openid", func() {
				m.Post("", web.Bind(forms.AddOpenIDForm{}), security.OpenIDPost)
				m.Post("/delete", security.DeleteOpenID)
				m.Post("/toggle_visibility", security.ToggleOpenIDVisibility)
			}, openIDSignInEnabled)
			m.Post("/account_link", security.DeleteAccountLink)
		})
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:20:05Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Read webauthn model

`sed -n 1,230p models/auth/webauthn.go`

```text
// Copyright 2020 The Gitea Authors. All rights reserved.
// SPDX-License-Identifier: MIT

package auth

import (
	"context"
	"fmt"
	"strings"

	"gitea.dev/models/db"
	"gitea.dev/modules/timeutil"
	"gitea.dev/modules/util"

	"github.com/go-webauthn/webauthn/protocol"
	"github.com/go-webauthn/webauthn/webauthn"
)

// ErrWebAuthnCredentialNotExist represents a "ErrWebAuthnCRedentialNotExist" kind of error.
type ErrWebAuthnCredentialNotExist struct {
	ID           int64
	CredentialID []byte
}

func (err ErrWebAuthnCredentialNotExist) Error() string {
	if len(err.CredentialID) == 0 {
		return fmt.Sprintf("WebAuthn credential does not exist [id: %d]", err.ID)
	}
	return fmt.Sprintf("WebAuthn credential does not exist [credential_id: %x]", err.CredentialID)
}

// Unwrap unwraps this as a ErrNotExist err
func (err ErrWebAuthnCredentialNotExist) Unwrap() error {
	return util.ErrNotExist
}

// IsErrWebAuthnCredentialNotExist checks if an error is a ErrWebAuthnCredentialNotExist.
func IsErrWebAuthnCredentialNotExist(err error) bool {
	_, ok := err.(ErrWebAuthnCredentialNotExist)
	return ok
}

// WebAuthnCredential represents the WebAuthn credential data for a public-key
// credential conformant to WebAuthn Level 1
type WebAuthnCredential struct {
	ID              int64 `xorm:"pk autoincr"`
	Name            string
	LowerName       string `xorm:"unique(s)"`
	UserID          int64  `xorm:"INDEX unique(s)"`
	CredentialID    []byte `xorm:"INDEX VARBINARY(1024)"`
	PublicKey       []byte
	AttestationType string
	AAGUID          []byte
	SignCount       uint32 `xorm:"BIGINT"`
	CloneWarning    bool
	CreatedUnix     timeutil.TimeStamp `xorm:"INDEX created"`
	UpdatedUnix     timeutil.TimeStamp `xorm:"INDEX updated"`
}

func init() {
	db.RegisterModel(new(WebAuthnCredential))
}

// TableName returns a better table name for WebAuthnCredential
func (cred WebAuthnCredential) TableName() string {
	return "webauthn_credential"
}

// UpdateSignCount will update the database value of SignCount
func (cred *WebAuthnCredential) UpdateSignCount(ctx context.Context) error {
	_, err := db.GetEngine(ctx).ID(cred.ID).Cols("sign_count").Update(cred)
	return err
}

// BeforeInsert will be invoked by XORM before updating a record
func (cred *WebAuthnCredential) BeforeInsert() {
	cred.LowerName = strings.ToLower(cred.Name)
}

// BeforeUpdate will be invoked by XORM before updating a record
func (cred *WebAuthnCredential) BeforeUpdate() {
	cred.LowerName = strings.ToLower(cred.Name)
}

// AfterLoad is invoked from XORM after setting the values of all fields of this object.
func (cred *WebAuthnCredential) AfterLoad() {
	cred.LowerName = strings.ToLower(cred.Name)
}

// WebAuthnCredentialList is a list of *WebAuthnCredential
type WebAuthnCredentialList []*WebAuthnCredential

// newCredentialFlagsFromAuthenticatorFlags is copied from https://github.com/go-webauthn/webauthn/pull/337
// to convert protocol.AuthenticatorFlags to webauthn.CredentialFlags
func newCredentialFlagsFromAuthenticatorFlags(flags protocol.AuthenticatorFlags) webauthn.CredentialFlags {
	return webauthn.CredentialFlags{
		UserPresent:    flags.HasUserPresent(),
		UserVerified:   flags.HasUserVerified(),
		BackupEligible: flags.HasBackupEligible(),
		BackupState:    flags.HasBackupState(),
	}
}

// ToCredentials will convert all WebAuthnCredentials to webauthn.Credentials
func (list WebAuthnCredentialList) ToCredentials(defaultAuthFlags ...protocol.AuthenticatorFlags) []webauthn.Credential {
	// TODO: at the moment, Gitea doesn't store or check the flags
	// so we need to use the default flags from the authenticator to make the login validation pass
	// In the future, we should:
	// 1. store the flags when registering the credential
	// 2. provide the stored flags when converting the credentials (for login)
	// 3. for old users, still use this fallback to the default flags
	defAuthFlags := util.OptionalArg(defaultAuthFlags)
	creds := make([]webauthn.Credential, 0, len(list))
	for _, cred := range list {
		creds = append(creds, webauthn.Credential{
			ID:              cred.CredentialID,
			PublicKey:       cred.PublicKey,
			AttestationType: cred.AttestationType,
			Flags:           newCredentialFlagsFromAuthenticatorFlags(defAuthFlags),
			Authenticator: webauthn.Authenticator{
				AAGUID:       cred.AAGUID,
				SignCount:    cred.SignCount,
				CloneWarning: cred.CloneWarning,
			},
		})
	}
	return creds
}

// GetWebAuthnCredentialsByUID returns all WebAuthn credentials of the given user
func GetWebAuthnCredentialsByUID(ctx context.Context, uid int64) (WebAuthnCredentialList, error) {
	creds := make(WebAuthnCredentialList, 0)
	return creds, db.GetEngine(ctx).Where("user_id = ?", uid).Find(&creds)
}

// ExistsWebAuthnCredentialsForUID returns if the given user has credentials
func ExistsWebAuthnCredentialsForUID(ctx context.Context, uid int64) (bool, error) {
	return db.GetEngine(ctx).Where("user_id = ?", uid).Exist(&WebAuthnCredential{})
}

// GetWebAuthnCredentialByName returns WebAuthn credential by id
func GetWebAuthnCredentialByName(ctx context.Context, uid int64, name string) (*WebAuthnCredential, error) {
	cred := new(WebAuthnCredential)
	if found, err := db.GetEngine(ctx).Where("user_id = ? AND lower_name = ?", uid, strings.ToLower(name)).Get(cred); err != nil {
		return nil, err
	} else if !found {
		return nil, ErrWebAuthnCredentialNotExist{}
	}
	return cred, nil
}

// GetWebAuthnCredentialByID returns WebAuthn credential by id
func GetWebAuthnCredentialByID(ctx context.Context, id int64) (*WebAuthnCredential, error) {
	cred := new(WebAuthnCredential)
	if found, err := db.GetEngine(ctx).ID(id).Get(cred); err != nil {
		return nil, err
	} else if !found {
		return nil, ErrWebAuthnCredentialNotExist{ID: id}
	}
	return cred, nil
}

// HasWebAuthnRegistrationsByUID returns whether a given user has WebAuthn registrations
func HasWebAuthnRegistrationsByUID(ctx context.Context, uid int64) (bool, error) {
	return db.GetEngine(ctx).Where("user_id = ?", uid).Exist(&WebAuthnCredential{})
}

// GetWebAuthnCredentialByCredID returns WebAuthn credential by credential ID
func GetWebAuthnCredentialByCredID(ctx context.Context, userID int64, credID []byte) (*WebAuthnCredential, error) {
	cred := new(WebAuthnCredential)
	if found, err := db.GetEngine(ctx).Where("user_id = ? AND credential_id = ?", userID, credID).Get(cred); err != nil {
		return nil, err
	} else if !found {
		return nil, ErrWebAuthnCredentialNotExist{CredentialID: credID}
	}
	return cred, nil
}

// CreateCredential will create a new WebAuthnCredential from the given Credential
func CreateCredential(ctx context.Context, userID int64, name string, cred *webauthn.Credential) (*WebAuthnCredential, error) {
	c := &WebAuthnCredential{
		UserID:          userID,
		Name:            name,
		CredentialID:    cred.ID,
		PublicKey:       cred.PublicKey,
		AttestationType: cred.AttestationType,
		AAGUID:          cred.Authenticator.AAGUID,
		SignCount:       cred.Authenticator.SignCount,
		CloneWarning:    false,
	}

	if err := db.Insert(ctx, c); err != nil {
		return nil, err
	}
	return c, nil
}

// UpdateCredentialName will rename the WebAuthnCredential with the given id if it belongs to userID
func UpdateCredentialName(ctx context.Context, id, userID int64, name string) error {
	_, err := db.GetEngine(ctx).ID(id).Where("user_id = ?", userID).Cols("name", "lower_name").Update(&WebAuthnCredential{Name: name})
	return err
}

// DeleteCredential will delete WebAuthnCredential
func DeleteCredential(ctx context.Context, id, userID int64) (bool, error) {
	had, err := db.GetEngine(ctx).ID(id).Where("user_id = ?", userID).Delete(&WebAuthnCredential{})
	return had > 0, err
}
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:20:06Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
List go files mentioning csrf (bash)

`grep -rli "csrf" --include="*.go" modules services routers | head -30`

```text
services/context/context.go
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:20:06Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Read webauthn router

`cat -n routers/web/user/setting/security/webauthn.go`

```text
     1	// Copyright 2018 The Gitea Authors. All rights reserved.
     2	// SPDX-License-Identifier: MIT
     3	
     4	package security
     5	
     6	import (
     7		"errors"
     8		"net/http"
     9		"strconv"
    10		"strings"
    11		"time"
    12	
    13		"gitea.dev/models/auth"
    14		user_model "gitea.dev/models/user"
    15		wa "gitea.dev/modules/auth/webauthn"
    16		"gitea.dev/modules/log"
    17		"gitea.dev/modules/session"
    18		"gitea.dev/modules/setting"
    19		"gitea.dev/modules/web"
    20		"gitea.dev/services/context"
    21		"gitea.dev/services/forms"
    22	
    23		"github.com/go-webauthn/webauthn/protocol"
    24		"github.com/go-webauthn/webauthn/webauthn"
    25	)
    26	
    27	// WebAuthnRegister initializes the webauthn registration procedure
    28	func WebAuthnRegister(ctx *context.Context) {
    29		if user_model.IsFeatureDisabledWithLoginType(ctx.Doer, setting.UserFeatureManageMFA) {
    30			ctx.HTTPError(http.StatusNotFound)
    31			return
    32		}
    33	
    34		form := web.GetForm(ctx).(*forms.WebauthnRegistrationForm)
    35		if form.Name == "" {
    36			// Set name to the hexadecimal of the current time
    37			form.Name = strconv.FormatInt(time.Now().UnixNano(), 16)
    38		}
    39	
    40		cred, err := auth.GetWebAuthnCredentialByName(ctx, ctx.Doer.ID, form.Name)
    41		if err != nil && !auth.IsErrWebAuthnCredentialNotExist(err) {
    42			ctx.ServerError("GetWebAuthnCredentialsByUID", err)
    43			return
    44		}
    45		if cred != nil {
    46			ctx.HTTPError(http.StatusConflict, "Name already taken")
    47			return
    48		}
    49	
    50		_ = ctx.Session.Delete("webauthnRegistration")
    51		if err := ctx.Session.Set("webauthnName", form.Name); err != nil {
    52			ctx.ServerError("Unable to set session key for webauthnName", err)
    53			return
    54		}
    55	
    56		webAuthnUser := wa.NewWebAuthnUser(ctx, ctx.Doer)
    57		credentialOptions, sessionData, err := wa.WebAuthn.BeginRegistration(webAuthnUser, webauthn.WithAuthenticatorSelection(protocol.AuthenticatorSelection{
    58			ResidentKey: protocol.ResidentKeyRequirementRequired,
    59		}))
    60		if err != nil {
    61			ctx.ServerError("Unable to BeginRegistration", err)
    62			return
    63		}
    64	
    65		// Save the session data as marshaled JSON
    66		if err = ctx.Session.Set("webauthnRegistration", sessionData); err != nil {
    67			ctx.ServerError("Unable to set session", err)
    68			return
    69		}
    70	
    71		ctx.JSON(http.StatusOK, credentialOptions)
    72	}
    73	
    74	// WebauthnRegisterPost receives the response of the security key
    75	func WebauthnRegisterPost(ctx *context.Context) {
    76		if user_model.IsFeatureDisabledWithLoginType(ctx.Doer, setting.UserFeatureManageMFA) {
    77			ctx.HTTPError(http.StatusNotFound)
    78			return
    79		}
    80	
    81		name, ok := ctx.Session.Get("webauthnName").(string)
    82		if !ok || name == "" {
    83			ctx.ServerError("Get webauthnName", errors.New("no webauthnName"))
    84			return
    85		}
    86	
    87		// Load the session data
    88		sessionData, ok := ctx.Session.Get("webauthnRegistration").(*webauthn.SessionData)
    89		if !ok || sessionData == nil {
    90			ctx.ServerError("Get registration", errors.New("no registration"))
    91			return
    92		}
    93		defer func() {
    94			_ = ctx.Session.Delete("webauthnRegistration")
    95		}()
    96	
    97		// Verify that the challenge succeeded
    98		webAuthnUser := wa.NewWebAuthnUser(ctx, ctx.Doer)
    99		cred, err := wa.WebAuthn.FinishRegistration(webAuthnUser, *sessionData, ctx.Req)
   100		if err != nil {
   101			if pErr, ok := err.(*protocol.Error); ok {
   102				log.Error("Unable to finish registration due to error: %v\nDevInfo: %s", pErr, pErr.DevInfo)
   103			}
   104			ctx.ServerError("CreateCredential", err)
   105			return
   106		}
   107	
   108		dbCred, err := auth.GetWebAuthnCredentialByName(ctx, ctx.Doer.ID, name)
   109		if err != nil && !auth.IsErrWebAuthnCredentialNotExist(err) {
   110			ctx.ServerError("GetWebAuthnCredentialsByUID", err)
   111			return
   112		}
   113		if dbCred != nil {
   114			ctx.HTTPError(http.StatusConflict, "Name already taken")
   115			return
   116		}
   117	
   118		// Create the credential
   119		_, err = auth.CreateCredential(ctx, ctx.Doer.ID, name, cred)
   120		if err != nil {
   121			ctx.ServerError("CreateCredential", err)
   122			return
   123		}
   124		_ = ctx.Session.Delete("webauthnName")
   125		_ = ctx.Session.Set(session.KeyUserHasTwoFactorAuth, true)
   126		ctx.JSON(http.StatusCreated, cred)
   127	}
   128	
   129	// WebauthnRename renames a security key by id
   130	func WebauthnRename(ctx *context.Context) {
   131		if user_model.IsFeatureDisabledWithLoginType(ctx.Doer, setting.UserFeatureManageMFA) {
   132			ctx.HTTPError(http.StatusNotFound)
   133			return
   134		}
   135	
   136		if ctx.HasError() { // form binding validation error
   137			ctx.JSONError(ctx.GetErrMsg())
   138			return
   139		}
   140	
   141		cred, err := auth.GetWebAuthnCredentialByID(ctx, ctx.FormInt64("id"))
   142		if err != nil && !auth.IsErrWebAuthnCredentialNotExist(err) {
   143			ctx.ServerError("GetWebAuthnCredentialByID", err)
   144			return
   145		}
   146		if cred == nil || cred.UserID != ctx.Doer.ID { // hide other users' keys as not found
   147			ctx.JSONErrorNotFound()
   148			return
   149		}
   150	
   151		form := web.GetForm(ctx).(*forms.WebauthnRenameForm)
   152		form.Name = strings.TrimSpace(form.Name)
   153		if form.Name == "" { // "Required" binding only rejects an exactly empty value, not whitespace
   154			ctx.JSONError(ctx.Locale.TrString("settings.webauthn_nickname") + ctx.Locale.TrString("form.require_error"))
   155			return
   156		}
   157		sameName, err := auth.GetWebAuthnCredentialByName(ctx, ctx.Doer.ID, form.Name)
   158		if err != nil && !auth.IsErrWebAuthnCredentialNotExist(err) {
   159			ctx.ServerError("GetWebAuthnCredentialByName", err)
   160			return
   161		}
   162		if sameName != nil && sameName.ID != cred.ID { // renaming a key to its own name with different casing is allowed
   163			ctx.JSONError(ctx.Tr("settings.webauthn_nickname_taken"))
   164			return
   165		}
   166	
   167		if err := auth.UpdateCredentialName(ctx, cred.ID, ctx.Doer.ID, form.Name); err != nil {
   168			ctx.ServerError("UpdateCredentialName", err)
   169			return
   170		}
   171		ctx.Flash.Success(ctx.Tr("settings.webauthn_rename_key_success"))
   172		ctx.JSONRedirect(setting.AppSubURL + "/user/settings/security")
   173	}
   174	
   175	// WebauthnDelete deletes an security key by id
   176	func WebauthnDelete(ctx *context.Context) {
   177		if user_model.IsFeatureDisabledWithLoginType(ctx.Doer, setting.UserFeatureManageMFA) {
   178			ctx.HTTPError(http.StatusNotFound)
   179			return
   180		}
   181	
   182		if _, err := auth.DeleteCredential(ctx, ctx.FormInt64("id"), ctx.Doer.ID); err != nil {
   183			ctx.ServerError("GetWebAuthnCredentialByID", err)
   184			return
   185		}
   186		ctx.JSONRedirect(setting.AppSubURL + "/user/settings/security")
   187	}
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:20:08Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Find cross-origin protection implementation

`grep -rn "CrossOriginProtection" --include='*.go' . | head -40`

```text
routers/web/web.go:170:	crossOriginProtection := http.NewCrossOriginProtection()
routers/web/web.go:212:		if !options.SignOutRequired && !options.DisableCrossOriginProtection {
routers/web/web.go:323://     The CORS mechanism already protects cross-origin requests, and the CrossOriginProtection has no "allowed origin" list, so disable CrossOriginProtection.
routers/web/web.go:325://     Such requests are not cross-origin requests, so disable CrossOriginProtection.
routers/web/web.go:326:var optSignInFromAnyOrigin = verifyAuthWithOptions(&common.VerifyOptions{DisableCrossOriginProtection: true})
routers/common/auth.go:44:	DisableCrossOriginProtection bool
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:20:08Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Show csrf refs in context.go

`grep -rni "csrf" services/context/context.go`

```text
services/context/context.go:189:			// FIXME: GLOBAL-PARSE-FORM: this ParseMultipartForm was used for parsing the csrf token from multipart/form-data
services/context/context.go:190:			// We have dropped the csrf token, so ideally this global ParseMultipartForm should be removed.
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:20:28Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Read genUpdateColumns

`cd $(go env GOMODCACHE)/xorm.io/xorm@v1.4.1 && grep -n "func (session \*Session) genUpdateColumns" -A 60 session_update.go`

```text
246:func (session *Session) genUpdateColumns(bean any) ([]string, []any, []func(any), error) {
247-	table := session.statement.RefTable
248-	colNames := make([]string, 0, len(table.ColumnsSeq()))
249-	args := make([]any, 0, len(table.ColumnsSeq()))
250-	autoUpdateClosures := make([]func(any), 0)
251-
252-	for _, col := range table.Columns() {
253-		if !col.IsVersion && !col.IsCreated && !col.IsUpdated {
254-			if session.statement.OmitColumnMap.Contain(col.Name) {
255-				continue
256-			}
257-		}
258-		if col.MapType == schemas.ONLYFROMDB {
259-			continue
260-		}
261-
262-		fieldValuePtr, err := col.ValueOf(bean)
263-		if err != nil {
264-			return nil, nil, nil, err
265-		}
266-		fieldValue := *fieldValuePtr
267-
268-		if col.IsAutoIncrement && utils.IsValueZero(fieldValue) {
269-			continue
270-		}
271-
272-		if col.IsDeleted && !session.statement.GetUnscoped() {
273-			continue
274-		}
275-		if col.IsCreated && !session.statement.ColumnMap.Contain(col.Name) {
276-			continue
277-		}
278-
279-		// if only update specify columns
280-		if len(session.statement.ColumnMap) > 0 && !session.statement.ColumnMap.Contain(col.Name) {
281-			continue
282-		}
283-
284-		switch {
285-		case session.statement.IncrColumns.IsColExist(col.Name):
286-			continue
287-		case session.statement.DecrColumns.IsColExist(col.Name):
288-			continue
289-		case session.statement.ExprColumns.IsColExist(col.Name):
290-			continue
291-		}
292-
293-		// !evalphobia! set fieldValue as nil when column is nullable and zero-value
294-		if getFlagForColumn(session.statement.NullableMap, col) {
295-			if col.Nullable && utils.IsValueZero(fieldValue) {
296-				var nilValue *int
297-				fieldValue = reflect.ValueOf(nilValue)
298-			}
299-		}
300-
301-		switch {
302-		case col.IsUpdated && session.statement.UseAutoTime: /*&& isZero(fieldValue.Interface())*/
303-			// if time is non-empty, then set to auto time
304-			val, t, err := session.engine.nowTime(col)
305-			if err != nil {
306-				return nil, nil, nil, err
```
</details></tool-use>

_**Agent (claude-opus-5 2026-09-15 02:20:09Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Find closing middleware of user settings group

`sed -n '665,700p' routers/web/web.go | grep -n "}, \|reqSignIn\|ctxDataSet" `

```text
11:			}, oauth2Enabled)
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:20:09Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
List all go files mentioning csrf repo-wide

`grep -rli "csrf" --include="*.go" . 2>/dev/null | head -30`

```text
tests/integration/auth_oauth2_test.go
services/context/context.go
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:20:10Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Read common-button.ts modal handling

`sed -n 1,140p web_src/js/features/common-button.ts`

```text
import {addDelegatedEventListener, hideElem, isElemVisible, showElem, toggleElem} from '../utils/dom.ts';
import {showFomanticModal} from '../modules/fomantic/modal.ts';
import {camelize} from 'vue';
import {applyAutoFocus} from './common-page.ts';

export function initGlobalButtonClickOnEnter(): void {
  addDelegatedEventListener(document, 'keypress', 'div.ui.button, span.ui.button', (el, e: KeyboardEvent) => {
    if (e.code === 'Space' || e.code === 'Enter') {
      e.preventDefault();
      el.click();
    }
  });
}

function onShowPanelClick(el: HTMLElement, e: MouseEvent) {
  // a '.show-panel' element can show a panel, by `data-panel="selector"`
  // if it has "toggle" class, it toggles the panel
  e.preventDefault();
  const sel = el.getAttribute('data-panel')!;
  const elems = el.classList.contains('toggle') ? toggleElem(sel) : showElem(sel);
  for (const elem of elems) {
    if (isElemVisible(elem as HTMLElement)) {
      applyAutoFocus(elem);
    }
  }
}

function onHidePanelClick(el: HTMLElement, e: MouseEvent) {
  // a `.hide-panel` element can hide a panel, by `data-panel="selector"` or `data-panel-closest="selector"`
  e.preventDefault();
  let sel = el.getAttribute('data-panel');
  if (sel) {
    hideElem(sel);
    return;
  }
  sel = el.getAttribute('data-panel-closest');
  if (sel) {
    hideElem((el.parentNode as HTMLElement).closest(sel)!);
    return;
  }
  throw new Error('no panel to hide'); // should never happen, otherwise there is a bug in code
}

export type ElementWithAssignableProperties = {
  nodeName: string;
  getAttribute: (name: string) => string | null;
  setAttribute: (name: string, value: string) => void;
} & Record<string, any>;

export function assignElementProperty(el: ElementWithAssignableProperties, kebabName: string, val: string) {
  if (el.nodeName === 'FORM') {
    // HINT: GOLANG-HTML-TEMPLATE-URL-ESCAPING: a special case for Golang HTML template escaping.
    // Golang HTML template only handles some "known" attribute names as URL (e.g.: when the name is "action" or contains "url")
    // To prevent template developers from making mistakes like `data-modal-form.action="?k={{ValueWithSpecialChars}}" (no escaping),
    // here we use `data-modal-form.url="?k={{ValueWithSpecialChars}}", then the value gets correctly escaped by Golang HTML template.
    if (kebabName === 'action') throw new Error(`don't assign element property "action" by value, use "data-modal-form.url" instead`);
    if (kebabName === 'url') kebabName = 'action';
  }
  const camelizedName = camelize(kebabName);
  const old = el[camelizedName];
  if (typeof old === 'boolean') {
    el[camelizedName] = val === 'true';
  } else if (typeof old === 'number') {
    el[camelizedName] = parseFloat(val);
  } else if (typeof old === 'string') {
    el[camelizedName] = val;
  } else if (old?.nodeName) {
    // "form" has an edge case: its "<input name=action>" element overwrites the "action" property, we can only set attribute
    el.setAttribute(kebabName, val);
  } else {
    // in the future, maybe we could introduce a better typing system if it is really needed
    throw new Error(`cannot assign element property "${camelizedName}" by value "${val}"`);
  }
}

function onShowModalClick(el: HTMLElement, e: MouseEvent) {
  // A ".show-modal" button will show a modal dialog defined by its "data-modal" attribute.
  // Each "data-modal-{target}" attribute will be filled to target element's value or text-content.
  // * First, try to query '#target'
  // * Then, try to query '[name=target]'
  // * Then, try to query '.target'
  // * Then, try to query 'target' as HTML tag
  // If there's a ".{prop-name}" part like "data-modal-input.value", the "input" element's "value" property will be set,
  // the "prop-name" will be camel-cased to "propName" (e.g.: "data-modal-input.read-only" for "readOnly" property).
  //
  // HINT: GOLANG-HTML-TEMPLATE-URL-ESCAPING: Form element's "action" property must be set by "data-modal-form.url"
  // to make the template variables get correctly escaped in the URL.
  e.preventDefault();
  const modalSelector = el.getAttribute('data-modal')!;
  const elModal = document.querySelector(modalSelector);
  if (!elModal) throw new Error('no modal for this action');

  const modalAttrPrefix = 'data-modal-';
  for (const attrib of el.attributes) {
    if (!attrib.name.startsWith(modalAttrPrefix)) {
      continue;
    }

    const attrTargetCombo = attrib.name.substring(modalAttrPrefix.length);
    const [attrTargetName, attrTargetProp] = attrTargetCombo.split('.');
    // try to find target by: "#target" -> "[name=target]" -> ".target" -> "<target> tag", and then try the modal itself
    const attrTarget = elModal.querySelector(`#${attrTargetName}`) ||
      elModal.querySelector(`[name=${CSS.escape(attrTargetName)}]`) ||
      elModal.querySelector(`.${attrTargetName}`) ||
      elModal.querySelector(attrTargetName) ||
      (elModal.matches(attrTargetName) || elModal.matches(`#${attrTargetName}`) || elModal.matches(`.${attrTargetName}`) ? elModal : null);
    if (!attrTarget) {
      if (!window.config.runModeIsProd) throw new Error(`attr target "${attrTargetCombo}" not found for modal`);
      continue;
    }

    if (attrTargetProp) {
      assignElementProperty(attrTarget, attrTargetProp, attrib.value);
    } else if (attrTarget.matches('input, textarea')) {
      (attrTarget as HTMLInputElement | HTMLTextAreaElement).value = attrib.value; // FIXME: add more supports like checkbox
    } else {
      attrTarget.textContent = attrib.value; // FIXME: it should be more strict here, only handle div/span/p
    }
  }

  showFomanticModal(elModal);
}

export function initGlobalButtons(): void {
  // There are many "cancel button" elements in modal dialogs, Fomantic UI expects they are button-like elements but never submit a form.
  // However, Gitea misuses the modal dialog and put the cancel buttons inside forms, so we must prevent the form submission.
  // There are a few cancel buttons in non-modal forms, and there are some dynamically created forms (eg: the "Edit Issue Content")
  addDelegatedEventListener(document, 'click', 'form button.ui.cancel.button', (_ /* el */, e) => e.preventDefault());

  // Ideally these "button" events should be handled by registerGlobalEventFunc
  // Refactoring would involve too many changes, so at the moment, just use the global event listener.
  addDelegatedEventListener(document, 'click', '.show-panel, .hide-panel, .show-modal', (el, e: MouseEvent) => {
    if (el.classList.contains('show-panel')) {
      onShowPanelClick(el, e);
    } else if (el.classList.contains('hide-panel')) {
      onHidePanelClick(el, e);
    } else if (el.classList.contains('show-modal')) {
      onShowModalClick(el, e);
    }
  });
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:20:10Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Read verifyAuthWithOptions

`sed -n '160,250p' routers/web/web.go`

```text
		if ctx.Doer == nil {
			// ensure the session uid is deleted
			_ = ctx.Session.Delete("uid")
		}
	}
	return webAuth
}

// verifyAuthWithOptions checks authentication according to options
func verifyAuthWithOptions(options *common.VerifyOptions) func(ctx *context.Context) {
	crossOriginProtection := http.NewCrossOriginProtection()

	return func(ctx *context.Context) {
		// Check prohibit login users.
		if ctx.IsSigned {
			if !ctx.Doer.IsActive && setting.Service.RegisterEmailConfirm {
				ctx.Data["Title"] = ctx.Tr("auth.active_your_account")
				ctx.HTML(http.StatusOK, "user/auth/activate")
				return
			}
			if !ctx.Doer.IsActive || ctx.Doer.ProhibitLogin {
				log.Info("Failed authentication attempt for %s from %s", ctx.Doer.Name, ctx.RemoteAddr())
				ctx.Data["Title"] = ctx.Tr("auth.prohibit_login")
				ctx.HTML(http.StatusOK, "user/auth/prohibit_login")
				return
			}

			if ctx.Doer.MustChangePassword {
				if ctx.Req.URL.Path != "/user/settings/change_password" {
					if strings.HasPrefix(ctx.Req.UserAgent(), "git") {
						ctx.HTTPError(http.StatusUnauthorized, ctx.Locale.TrString("auth.must_change_password"))
						return
					}
					ctx.Data["Title"] = ctx.Tr("auth.must_change_password")
					ctx.Data["ChangePasscodeLink"] = setting.AppSubURL + "/user/change_password"
					middleware.SetRedirectToCookie(ctx.Resp, setting.AppSubURL+ctx.Req.URL.RequestURI())
					ctx.Redirect(setting.AppSubURL + "/user/settings/change_password")
					return
				}
			} else if ctx.Req.URL.Path == "/user/settings/change_password" {
				// make sure that the form cannot be accessed by users who don't need this
				ctx.Redirect(setting.AppSubURL + "/")
				return
			}
		}

		// When a signed-in user visits a page that requires sign-out (e.g.: "/user/login"), redirect to home (or alternate location)
		if options.SignOutRequired && ctx.IsSigned && ctx.Req.URL.RequestURI() != "/" {
			ctx.RedirectToCurrentSite(ctx.FormString("redirect_to"))
			return
		}

		if !options.SignOutRequired && !options.DisableCrossOriginProtection {
			if err := crossOriginProtection.Check(ctx.Req); err != nil {
				http.Error(ctx.Resp, err.Error(), http.StatusForbidden)
				return
			}
		}

		if options.SignInRequired {
			if !ctx.IsSigned {
				ctx.Redirect(middleware.RedirectLinkUserLogin(ctx.Req))
				return
			} else if !ctx.Doer.IsActive && setting.Service.RegisterEmailConfirm {
				ctx.Data["Title"] = ctx.Tr("auth.active_your_account")
				ctx.HTML(http.StatusOK, "user/auth/activate")
				return
			}
		}

		// Redirect to log in page if auto-signin info is provided and has not signed in.
		if !options.SignOutRequired && !ctx.IsSigned && ctx.GetSiteCookie(setting.CookieRememberName) != "" {
			ctx.Redirect(middleware.RedirectLinkUserLogin(ctx.Req))
			return
		}

		if options.AdminRequired {
			if !ctx.Doer.IsAdmin {
				ctx.HTTPError(http.StatusForbidden)
				return
			}
			ctx.Data["PageIsAdmin"] = true
		}
	}
}

func ctxDataSet(args ...any) func(ctx *context.Context) {
	return func(ctx *context.Context) {
		for i := 0; i < len(args); i += 2 {
			ctx.Data[args[i].(string)] = args[i+1]
		}
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:20:29Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Check UseAutoTime default

`cd $(go env GOMODCACHE)/xorm.io/xorm@v1.4.1 && grep -rn "UseAutoTime" internal/statements/*.go session.go | head -20`

```text
internal/statements/statement.go:74:	UseAutoTime     bool
internal/statements/statement.go:149:	statement.UseAutoTime = true
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:20:11Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Compare JSONError argument conventions

`grep -rn "JSONError(ctx.Tr(" --include='*.go' routers/ | wc -l; echo "---TrString---"; grep -rn "JSONError(ctx.Locale.TrString(" --include='*.go' routers/ | wc -l; echo "---samples Tr---"; grep -rn "JSONError(ctx.Tr(" --include='*.go' routers/ | head -10; echo "---samples TrString---"; grep -rn "JSONError(ctx.Locale.TrString(" --include='*.go' routers/ | head -10`

```text
124
---TrString---
2
---samples Tr---
routers/web/admin/config.go:157:			ctx.JSONError(ctx.Tr("admin.config.set_setting_failed", key))
routers/web/admin/config.go:167:				ctx.JSONError(ctx.Tr("admin.config.set_setting_failed", key))
routers/web/user/package.go:473:			ctx.JSONError(ctx.Tr("packages.settings.unlink.error"))
routers/web/user/package.go:485:			ctx.JSONError(ctx.Tr("packages.settings.link.repo_not_found", form.RepoName))
routers/web/user/package.go:493:		ctx.JSONError(ctx.Tr("packages.settings.link.error"))
routers/web/user/setting/keys.go:271:			ctx.JSONError(ctx.Tr("settings.ssh_externally_managed"))
routers/web/user/setting/account.go:248:			ctx.JSONError(ctx.Tr("form.user_not_exist"))
routers/web/user/setting/account.go:250:			ctx.JSONError(ctx.Tr("form.unsupported_login_type"))
routers/web/user/setting/account.go:252:			ctx.JSONError(ctx.Tr("form.unset_password"))
routers/web/user/setting/account.go:254:			ctx.JSONError(ctx.Tr("form.enterred_invalid_password"))
---samples TrString---
routers/web/repo/editor.go:131:		ctx.JSONError(ctx.Locale.TrString("error.not_found"))
routers/web/user/setting/security/webauthn.go:154:		ctx.JSONError(ctx.Locale.TrString("settings.webauthn_nickname") + ctx.Locale.TrString("form.require_error"))
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:20:12Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Read binding validation middleware

`sed -n '1,170p' modules/web/middleware/binding.go`

```text
// Copyright 2014 The Gogs Authors. All rights reserved.
// Copyright 2019 The Gitea Authors. All rights reserved.
// SPDX-License-Identifier: MIT

package middleware

import (
	"reflect"
	"strings"

	"gitea.dev/modules/translation"
	"gitea.dev/modules/util"
	"gitea.dev/modules/validation"

	"gitea.com/go-chi/binding"
)

// Form form binding interface
type Form interface {
	binding.Validator
}

func init() {
	binding.SetNameMapper(util.ToSnakeCase)
}

// AssignForm assign form values back to the template data.
func AssignForm(form any, data map[string]any) {
	typ := reflect.TypeOf(form)
	val := reflect.ValueOf(form)

	for typ.Kind() == reflect.Pointer {
		typ = typ.Elem()
		val = val.Elem()
	}

	for i := 0; i < typ.NumField(); i++ {
		field := typ.Field(i)

		fieldName := field.Tag.Get("form")
		// Allow ignored fields in the struct
		if fieldName == "-" {
			continue
		} else if len(fieldName) == 0 {
			fieldName = util.ToSnakeCase(field.Name)
		}

		data[fieldName] = val.Field(i).Interface()
	}
}

func getRuleBody(field reflect.StructField, prefix string) string {
	for rule := range strings.SplitSeq(field.Tag.Get("binding"), ";") {
		if strings.HasPrefix(rule, prefix) {
			return rule[len(prefix) : len(rule)-1]
		}
	}
	return ""
}

// GetSize get size int form tag
func GetSize(field reflect.StructField) string {
	return getRuleBody(field, "Size(")
}

// GetMinSize get minimal size in form tag
func GetMinSize(field reflect.StructField) string {
	return getRuleBody(field, "MinSize(")
}

// GetMaxSize get max size in form tag
func GetMaxSize(field reflect.StructField) string {
	return getRuleBody(field, "MaxSize(")
}

// GetInclude get include in form tag
func GetInclude(field reflect.StructField) string {
	return getRuleBody(field, "Include(")
}

func ReportValidationError(errs binding.Errors, data map[string]any, fieldName, classification, errorMsg string) binding.Errors {
	errs.Add([]string{fieldName}, classification, errorMsg)

	data["HasError"] = true
	data["ErrorMsg"] = fieldName + ": " + errorMsg
	data["Err_"+fieldName] = true
	// there is already a reported validation error, so no need to generate default error messages in Validate()
	data["HasErrorFormValidation"] = true
	return errs
}

func Validate(errs binding.Errors, data map[string]any, f Form, l translation.Locale) binding.Errors {
	// try to restore the form's values as much as possible,
	// especially for RenderWithErrDeprecated to re-render the form with errors
	AssignForm(f, data)

	if errs.Len() == 0 || data["HasErrorFormValidation"] == true {
		return errs
	}

	// if HasError=true, then must set default error message
	// because still a lot of places use `ctx.Data["ErrorMsg"].(string)` even if the error fields can't be found
	data["HasError"] = true
	data["ErrorMsg"] = l.TrString("form.unknown_error")

	typ := reflect.TypeOf(f)
	if typ.Kind() == reflect.Pointer {
		typ = typ.Elem()
	}

	field, fieldExists := typ.FieldByName(errs[0].FieldNames[0])
	if !fieldExists {
		return errs
	}

	if field.Tag.Get("form") == "-" {
		return errs
	}

	data["Err_"+field.Name] = true

	trName := field.Tag.Get("locale")
	if len(trName) == 0 {
		trName = l.TrString("form." + field.Name)
	} else {
		trName = l.TrString(trName)
	}

	switch errs[0].Classification {
	case binding.ERR_REQUIRED:
		data["ErrorMsg"] = trName + l.TrString("form.require_error")
	case binding.ERR_ALPHA_DASH:
		data["ErrorMsg"] = trName + l.TrString("form.alpha_dash_error")
	case binding.ERR_ALPHA_DASH_DOT:
		data["ErrorMsg"] = trName + l.TrString("form.alpha_dash_dot_error")
	case validation.ErrGitRefName:
		data["ErrorMsg"] = trName + l.TrString("form.git_ref_name_error")
	case binding.ERR_SIZE:
		data["ErrorMsg"] = trName + l.TrString("form.size_error", GetSize(field))
	case binding.ERR_MIN_SIZE:
		data["ErrorMsg"] = trName + l.TrString("form.min_size_error", GetMinSize(field))
	case binding.ERR_MAX_SIZE:
		data["ErrorMsg"] = trName + l.TrString("form.max_size_error", GetMaxSize(field))
	case binding.ERR_EMAIL:
		data["ErrorMsg"] = trName + l.TrString("form.email_error")
	case binding.ERR_URL:
		data["ErrorMsg"] = trName + l.TrString("form.url_error", errs[0].Message)
	case binding.ERR_INCLUDE:
		data["ErrorMsg"] = trName + l.TrString("form.include_error", GetInclude(field))
	case validation.ErrGlobPattern:
		data["ErrorMsg"] = trName + l.TrString("form.glob_pattern_error", errs[0].Message)
	case validation.ErrRegexPattern:
		data["ErrorMsg"] = trName + l.TrString("form.regex_pattern_error", errs[0].Message)
	case validation.ErrUsername:
		data["ErrorMsg"] = trName + l.TrString("form.username_error")
	case validation.ErrInvalidGroupTeamMap:
		data["ErrorMsg"] = trName + l.TrString("form.invalid_group_team_map_error", errs[0].Message)
	case validation.ErrInvalidBadgeSlug:
		data["ErrorMsg"] = trName + l.TrString("form.invalid_slug_error")
	default:
		msg := errs[0].Classification
		if msg != "" && errs[0].Message != "" {
			msg += ": "
		}

		msg += errs[0].Message
		if msg == "" {
			msg = l.TrString("form.unknown_error")
		}
		data["ErrorMsg"] = trName + ": " + msg
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:20:12Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Grep for credential name usage

`grep -rn "Name\b" --include=*.go models/auth/webauthn.go | head -40; echo "=== callers of GetWebAuthnCredentialByName ==="; grep -rn "GetWebAuthnCredentialByName" --include=*.go . ; echo "=== reads of cred.Name / .LowerName ==="; grep -rn "\.LowerName" --include=*.go --include=*.tmpl . | grep -i webauthn`

```text
(eval):1: no matches found: --include=*.go
=== callers of GetWebAuthnCredentialByName ===
(eval):1: no matches found: --include=*.go
```
</details></tool-use>

_**Agent (claude-opus-5 2026-09-15 02:20:14Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Locate group-closing middleware line

`awk 'NR>=618 && NR<=730 && /^\t\}, /' routers/web/web.go; echo "--- with line numbers ---"; grep -n "^	}, " routers/web/web.go | awk -F: '$1>=618 && $1<=760'`

```text
--- with line numbers ---
737:	}, reqSignIn, user_setting.SettingsCtxData)
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:20:15Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Search csrf in web_src and read context.go section

`grep -rni "csrf" web_src/ --include="*.ts" --include="*.js" 2>/dev/null | head -30; echo "=== context.go 170-230 ==="; sed -n 170,230p services/context/context.go`

```text
=== context.go 170-230 ===
			// PageData is passed by reference, and it will be rendered to `window.config.pageData` in `head.tmpl` for JavaScript modules
			ctx.PageData = map[string]any{}
			ctx.Data["PageData"] = ctx.PageData

			// get the last flash message from cookie
			lastFlashCookie, lastFlashMsg := middleware.GetSiteCookieFlashMessage(ctx, ctx.Req, CookieNameFlash)
			if vals, _ := url.ParseQuery(lastFlashCookie); len(vals) > 0 {
				ctx.Data["Flash"] = lastFlashMsg // store last Flash message into the template data, to render it
			}

			// if there are new messages in the ctx.Flash, write them into cookie
			ctx.Resp.Before(func(resp ResponseWriter) {
				if val := ctx.Flash.Encode(); val != "" {
					middleware.SetSiteCookie(ctx.Resp, CookieNameFlash, val, 0)
				} else if lastFlashCookie != "" {
					middleware.SetSiteCookie(ctx.Resp, CookieNameFlash, "", -1)
				}
			})

			// FIXME: GLOBAL-PARSE-FORM: this ParseMultipartForm was used for parsing the csrf token from multipart/form-data
			// We have dropped the csrf token, so ideally this global ParseMultipartForm should be removed.
			// When removing this, we need to avoid regressions in the handler functions because Golang's http framework is quite fragile
			// and developers sometimes need to manually parse the form before accessing some values.
			if ctx.Req.Method == http.MethodPost && strings.Contains(ctx.Req.Header.Get("Content-Type"), "multipart/form-data") {
				if !ctx.ParseMultipartForm() {
					return
				}
			}

			httpcache.SetCacheControlInHeader(ctx.Resp.Header(), &httpcache.CacheControlOptions{})

			ctx.Data["SystemConfig"] = setting.Config()

			ctx.Data["ShowTwoFactorRequiredMessage"] = ctx.DoerNeedTwoFactorAuth()

			// FIXME: do we really always need these setting? There should be someway to have to avoid having to always set these
			ctx.Data["DisableMigrations"] = setting.Repository.DisableMigrations
			ctx.Data["DisableStars"] = setting.Repository.DisableStars
			ctx.Data["EnableActions"] = setting.Actions.Enabled && !unit.TypeActions.UnitGlobalDisabled()

			ctx.Data["AllLangs"] = translation.AllLangs()

			next.ServeHTTP(ctx.Resp, ctx.Req)
		})
	}
}

func (ctx *Context) DoerNeedTwoFactorAuth() bool {
	if !setting.TwoFactorAuthEnforced {
		return false
	}
	return ctx.Session.Get(session.KeyUserHasTwoFactorAuth) == false
}

// HasError returns true if error occurs in form validation.
// Attention: this function changes ctx.Data and ctx.Flash
// If HasError is called, then before Redirect, the error message should be stored by ctx.Flash.Error(ctx.GetErrMsg()) again.
func (ctx *Context) HasError() bool {
	hasErr, _ := ctx.Data["HasError"].(bool)
	hasErr = hasErr || ctx.Flash.ErrorMsg != ""
	if !hasErr {
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:20:15Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Read user settings route group

`sed -n '600,680p' routers/web/web.go`

```text
	m.Any("/user/events", routing.MarkLongPolling(), events.Events)

	m.Group("/login/oauth", func() {
		m.Group("", func() {
			m.Get("/authorize", web.Bind(forms.AuthorizationForm{}), auth.AuthorizeOAuth)
			m.Post("/grant", web.Bind(forms.GrantApplicationForm{}), auth.GrantApplicationOAuth)
			// TODO manage redirection
			m.Post("/authorize", web.Bind(forms.AuthorizationForm{}), auth.AuthorizeOAuth)
		}, reqSignIn)

		m.Group("", func() {
			m.Methods("GET, POST, OPTIONS", "/userinfo", auth.InfoOAuth)
			m.Methods("POST, OPTIONS", "/access_token", web.Bind(forms.AccessTokenForm{}), auth.AccessTokenOAuth)
			m.Methods("GET, OPTIONS", "/keys", auth.OIDCKeys)
			m.Methods("POST, OPTIONS", "/introspect", web.Bind(forms.IntrospectTokenForm{}), auth.IntrospectOAuth)
		}, optionsCorsHandler(), webAuth.AllowOAuth2, optSignInFromAnyOrigin)
	}, oauth2Enabled)

	m.Group("/user/settings", func() {
		m.Get("", user_setting.Profile)
		m.Post("", web.Bind(forms.UpdateProfileForm{}), user_setting.ProfilePost)
		m.Post("/update_preferences", user_setting.UpdatePreferences)
		m.Get("/change_password", auth.MustChangePassword)
		m.Post("/change_password", web.Bind(forms.MustChangePasswordForm{}), auth.MustChangePasswordPost)
		m.Post("/avatar", web.Bind(forms.AvatarForm{}), user_setting.AvatarPost)
		m.Post("/avatar/delete", user_setting.DeleteAvatar)
		m.Group("/account", func() {
			m.Combo("").Get(user_setting.Account).Post(web.Bind(forms.ChangePasswordForm{}), user_setting.AccountPost)
			m.Post("/email", web.Bind(forms.AddEmailForm{}), user_setting.EmailPost)
			m.Post("/email/delete", user_setting.DeleteEmail)
			m.Post("/delete", user_setting.DeleteAccount)
		})
		m.Group("/appearance", func() {
			m.Get("", user_setting.Appearance)
			m.Post("/language", web.Bind(forms.UpdateLanguageForm{}), user_setting.UpdateUserLang)
			m.Post("/hidden_comments", user_setting.UpdateUserHiddenComments)
			m.Post("/theme", web.Bind(forms.UpdateThemeForm{}), user_setting.UpdateUIThemePost)
		})
		m.Group("/notifications", func() {
			m.Get("", user_setting.Notifications)
			m.Post("/email", user_setting.NotificationsEmailPost)
			m.Post("/actions", user_setting.NotificationsActionsEmailPost)
		})
		m.Group("/security", func() {
			m.Get("", security.Security)
			m.Group("/two_factor", func() {
				m.Post("/regenerate_scratch", security.RegenerateScratchTwoFactor)
				m.Post("/disable", security.DisableTwoFactor)
				m.Get("/enroll", security.EnrollTwoFactor)
				m.Post("/enroll", web.Bind(forms.TwoFactorAuthForm{}), security.EnrollTwoFactorPost)
			})
			m.Group("/webauthn", func() {
				m.Post("/request_register", web.Bind(forms.WebauthnRegistrationForm{}), security.WebAuthnRegister)
				m.Post("/register", security.WebauthnRegisterPost)
				m.Post("/rename", web.Bind(forms.WebauthnRenameForm{}), security.WebauthnRename)
				m.Post("/delete", security.WebauthnDelete)
			})
			m.Group("/openid", func() {
				m.Post("", web.Bind(forms.AddOpenIDForm{}), security.OpenIDPost)
				m.Post("/delete", security.DeleteOpenID)
				m.Post("/toggle_visibility", security.ToggleOpenIDVisibility)
			}, openIDSignInEnabled)
			m.Post("/account_link", security.DeleteAccountLink)
		})

		m.Group("/applications", func() {
			// oauth2 applications
			m.Group("/oauth2", func() {
				m.Get("/{id}", user_setting.OAuth2ApplicationShow)
				m.Post("/{id}", web.Bind(forms.EditOAuth2ApplicationForm{}), user_setting.OAuthApplicationsEdit)
				m.Post("/{id}/regenerate_secret", user_setting.OAuthApplicationsRegenerateSecret)
				m.Post("", web.Bind(forms.EditOAuth2ApplicationForm{}), user_setting.OAuthApplicationsPost)
				m.Post("/{id}/delete", user_setting.DeleteOAuth2Application)
				m.Post("/{id}/revoke/{grantId}", user_setting.RevokeOAuth2Grant)
			}, oauth2Enabled)

			// access token applications
			m.Combo("").Get(user_setting.Applications).
				Post(web.Bind(forms.NewAccessTokenForm{}), user_setting.ApplicationsPost)
			m.Post("/delete", user_setting.DeleteApplication)
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:20:16Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Read cookie middleware

`sed -n '1,60p' modules/web/middleware/cookie.go`

```text
// Copyright 2020 The Macaron Authors
// Copyright 2020 The Gitea Authors. All rights reserved.
// SPDX-License-Identifier: MIT

package middleware

import (
	"net/http"
	"net/url"
	"strings"

	"gitea.dev/modules/session"
	"gitea.dev/modules/setting"
	"gitea.dev/modules/util"
)

const (
	CookieWebBannerDismissed = "gitea_disbnr"
	CookieTheme              = "gitea_theme"
	cookieRedirectTo         = "redirect_to"
)

func GetRedirectToCookie(req *http.Request) string {
	return GetSiteCookie(req, cookieRedirectTo)
}

// SetRedirectToCookie convenience function to set the RedirectTo cookie consistently
func SetRedirectToCookie(resp http.ResponseWriter, value string) {
	SetSiteCookie(resp, cookieRedirectTo, value, 0)
}

// DeleteRedirectToCookie convenience function to delete most cookies consistently
func DeleteRedirectToCookie(resp http.ResponseWriter) {
	SetSiteCookie(resp, cookieRedirectTo, "", -1)
}

func RedirectLinkUserLogin(req *http.Request) string {
	if req.Header.Get("X-Gitea-Fetch-Action") != "" {
		// when building the redirect link for a fetch request, the current link might be a partial page,
		// so we only redirect to the login page without redirect_to parameter
		return setting.AppSubURL + "/user/login"
	}
	return setting.AppSubURL + "/user/login?redirect_to=" + url.QueryEscape(setting.AppSubURL+req.URL.RequestURI())
}

// GetSiteCookie returns given cookie value from request header.
func GetSiteCookie(req *http.Request, name string) string {
	cookie, err := req.Cookie(name)
	if err != nil {
		return ""
	}
	val, _ := url.QueryUnescape(cookie.Value)
	return val
}

// SetSiteCookie returns given cookie value from request header.
func SetSiteCookie(resp http.ResponseWriter, name, value string, maxAge int) {
	// Previous versions would use a cookie path with a trailing /.
	// These are more specific than cookies without a trailing /, so
	// we need to delete these if they exist.
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:20:22Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Find closing middleware of user settings group

`awk 'NR>=618 && NR<=740' routers/web/web.go | grep -n "reqSignIn\|^	})" | head -20`

```text
120:	}, reqSignIn, user_setting.SettingsCtxData)
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:20:22Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Read end of settings group

`sed -n '700,740p' routers/web/web.go`

```text
				m.Post("/initialize", user_setting.InitializeCargoIndex)
				m.Post("/rebuild", user_setting.RebuildCargoIndex)
			})
			m.Post("/chef/regenerate_keypair", user_setting.RegenerateChefKeyPair)
		}, packagesEnabled)

		m.Group("/actions", func() {
			m.Get("", misc.LocationRedirect("./actions/general"))
			m.Group("/general", func() {
				m.Get("", shared_actions.GeneralSettings)
				m.Post("", shared_actions.UpdateGeneralSettings)
			})
			addSettingsRunnersRoutes()
			addSettingsSecretsRoutes()
			addSettingsVariablesRoutes()
			addSettingsScopedWorkflowsRoutes()
		}, actions.MustEnableActions)

		m.Get("/organization", user_setting.Organization)
		m.Get("/repos", user_setting.Repos)
		m.Post("/repos/unadopted", user_setting.AdoptOrDeleteRepository)

		m.Group("/hooks", func() {
			m.Get("", user_setting.Webhooks)
			m.Post("/delete", user_setting.DeleteWebhook)
			addWebhookAddRoutes()
			m.Group("/{id}", func() {
				m.Get("", repo_setting.WebHooksEdit)
				m.Post("/replay/{uuid}", repo_setting.ReplayWebhook)
			})
			addWebhookEditRoutes()
		}, webhooksEnabled)

		m.Group("/blocked_users", func() {
			m.Get("", user_setting.BlockedUsers)
			m.Post("", web.Bind(forms.BlockUserForm{}), user_setting.BlockedUsersPost)
		})
	}, reqSignIn, user_setting.SettingsCtxData)

	m.Group("/user", func() {
		m.Get("/activate", auth.Activate)
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:20:21Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Find frontend handling of JSON error render format

`grep -rn "renderFormat\|errorMessage" web_src/js/ | head -30`

```text
web_src/js/markup/common.ts:1:import {errorMessage} from '../modules/errors.ts';
web_src/js/markup/common.ts:7:  errorNode.textContent = errorMessage(err);
web_src/js/markup/tasklist.ts:79:          showErrorToast(data.errorMessage);
web_src/js/markup/render-iframe.ts:2:import {errorMessage} from '../modules/errors.ts';
web_src/js/markup/render-iframe.ts:14:    console.error(`Failed to parse link: ${link}, error: ${errorMessage(e)}`);
web_src/js/components/RepoRecentCommits.vue:23:import {errorMessage} from '../modules/errors.ts';
web_src/js/components/RepoRecentCommits.vue:78:    errorText.value = errorMessage(err);
web_src/js/features/repo-issue.ts:1:import {errorMessage} from '../modules/errors.ts';
web_src/js/features/repo-issue.ts:428:      showErrorToast(errorMessage(error));
web_src/js/features/common-fetch-action.ts:4:import {errorMessage, errorName} from '../modules/errors.ts';
web_src/js/features/common-fetch-action.ts:98:  if (respJson?.errorMessage) {
web_src/js/features/common-fetch-action.ts:100:    // but at the moment, as a new approach, we only use "errorMessage" here, backend can use JSONError() to respond.
web_src/js/features/common-fetch-action.ts:101:    showErrorToast(respJson.errorMessage, {useHtmlBody: respJson.renderFormat === 'html'});
web_src/js/features/common-fetch-action.ts:144:      showErrorToast(`Error: ${errorMessage(err)}`);
web_src/js/features/file-view.ts:5:import {errorMessage} from '../modules/errors.ts';
web_src/js/features/file-view.ts:34:    errorMsg = errorMessage(e);
web_src/js/features/repo-issue-sidebar-combolist.ts:5:import {errorMessage} from '../modules/errors.ts';
web_src/js/features/repo-issue-sidebar-combolist.ts:133:      showErrorToast(`Failed to update to backend: ${errorMessage(e)}`);
web_src/js/features/dropzone.ts:7:import {errorMessage} from '../modules/errors.ts';
web_src/js/features/dropzone.ts:150:      showErrorToast(`Failed to load attachments: ${errorMessage(error)}`);
web_src/js/components/ContextPopup.vue:10:  errorMessage?: string,
web_src/js/components/ContextPopup.vue:44:      {{ errorMessage }}
web_src/js/features/user-auth-webauthn.ts:3:import {errorMessage} from '../modules/errors.ts';
web_src/js/features/user-auth-webauthn.ts:84:    webAuthnError('general', errorMessage(err));
web_src/js/features/user-auth-webauthn.ts:108:      webAuthnError('general', errorMessage(err));
web_src/js/features/user-auth-webauthn.ts:118:      webAuthnError('general', errorMessage(err));
web_src/js/features/user-auth-webauthn.ts:266:    webAuthnError('unknown', errorMessage(err));
web_src/js/features/admin/config.ts:5:import {errorMessage} from '../../modules/errors.ts';
web_src/js/features/admin/config.ts:29:      if (json.errorMessage) throw new Error(json.errorMessage);
web_src/js/features/admin/config.ts:31:      showTemporaryTooltip(el, errorMessage(ex));
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:20:22Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Read custom binding rules

`sed -n '1,80p' modules/validation/binding.go`

```text
// Copyright 2017 The Gitea Authors. All rights reserved.
// SPDX-License-Identifier: MIT

package validation

import (
	"fmt"
	"io"
	"regexp"
	"strings"

	"gitea.dev/modules/auth"
	"gitea.dev/modules/git"
	"gitea.dev/modules/glob"
	"gitea.dev/modules/json"

	"gitea.com/go-chi/binding"
)

const (
	// ErrGitRefName is git reference name error
	ErrGitRefName = "GitRefNameError"
	// ErrGlobPattern is returned when glob pattern is invalid
	ErrGlobPattern = "GlobPattern"
	// ErrRegexPattern is returned when a regex pattern is invalid
	ErrRegexPattern = "RegexPattern"
	// ErrUsername is username error
	ErrUsername = "UsernameError"
	// ErrInvalidGroupTeamMap is returned when a group team mapping is invalid
	ErrInvalidGroupTeamMap = "InvalidGroupTeamMap"
	// ErrInvalidBadgeSlug is returned when a badge slug is invalid
	ErrInvalidBadgeSlug = "InvalidBadgeSlug"
)

type jsonProvider struct{}

func (j jsonProvider) Marshal(v any) ([]byte, error) { return json.Marshal(v) }

func (j jsonProvider) Unmarshal(data []byte, v any) error { return json.Unmarshal(data, v) }

func (j jsonProvider) NewDecoder(reader io.Reader) binding.JSONDecoder {
	return json.NewDecoder(reader)
}

func (j jsonProvider) NewEncoder(writer io.Writer) binding.JSONEncoder {
	return json.NewEncoder(writer)
}

// AddBindingRules adds additional binding rules
func AddBindingRules() {
	binding.JSONProvider = jsonProvider{}
	addGitRefNameBindingRule()
	addValidURLBindingRule()
	addValidSiteURLBindingRule()
	addGlobPatternRule()
	addRegexPatternRule()
	addGlobOrRegexPatternRule()
	addUsernamePatternRule()
	addValidGroupTeamMapRule()
	addSlugPatternRule()
}

func addGitRefNameBindingRule() {
	// Git refname validation rule
	binding.AddRule(&binding.Rule{
		IsMatch: func(rule string) bool {
			return rule == "GitRefName"
		},
		IsValid: func(errs binding.Errors, name string, val any) (bool, binding.Errors) {
			str := fmt.Sprintf("%v", val)

			if !git.IsValidRefPattern(str) {
				errs.Add([]string{name}, ErrGitRefName, "GitRefName")
				return false, errs
			}
			return true, errs
		},
	})
}
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:20:22Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Read AssertExistsAndLoadBean

`grep -rn "func AssertExistsAndLoadBean" -A 15 models/unittest/consistency.go models/unittest/unit_tests.go 2>/dev/null`

```text
models/unittest/unit_tests.go:79:func AssertExistsAndLoadBean[T any](t TestingT, bean T, conditions ...any) T {
models/unittest/unit_tests.go-80-	exists, err := getBeanIfExists(t, bean, conditions...)
models/unittest/unit_tests.go-81-	require.NoError(t, err)
models/unittest/unit_tests.go-82-	require.True(t, exists,
models/unittest/unit_tests.go-83-		"Expected to find %+v (of type %T, with conditions %+v), but did not",
models/unittest/unit_tests.go-84-		bean, bean, conditions)
models/unittest/unit_tests.go-85-	return bean
models/unittest/unit_tests.go-86-}
models/unittest/unit_tests.go-87-
models/unittest/unit_tests.go-88-// AssertExistsAndLoadMap assert that a row exists and load it from the test database
models/unittest/unit_tests.go-89-func AssertExistsAndLoadMap(t TestingT, table string, conditions ...any) map[string]string {
models/unittest/unit_tests.go-90-	e := db.GetEngine(t.Context()).Table(table)
models/unittest/unit_tests.go-91-	res, err := whereOrderConditions(e, conditions).Query()
models/unittest/unit_tests.go-92-	assert.NoError(t, err)
models/unittest/unit_tests.go-93-	assert.Len(t, res, 1,
models/unittest/unit_tests.go-94-		"Expected to find one row in %s (with conditions %+v), but found %d",
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:20:22Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Grep credential name call sites

`echo "=== GetWebAuthnCredentialByName callers ==="; grep -rn "GetWebAuthnCredentialByName" --include="*.go" . ; echo; echo "=== webauthn cred .Name reads in tmpl ==="; grep -rn "webauthn" --include="*.tmpl" templates/ ; echo; echo "=== UpdateCredentialName callers ==="; grep -rn "UpdateCredentialName" --include="*.go" .`

```text
=== GetWebAuthnCredentialByName callers ===
routers/web/user/setting/security/webauthn.go:40:	cred, err := auth.GetWebAuthnCredentialByName(ctx, ctx.Doer.ID, form.Name)
routers/web/user/setting/security/webauthn.go:108:	dbCred, err := auth.GetWebAuthnCredentialByName(ctx, ctx.Doer.ID, name)
routers/web/user/setting/security/webauthn.go:157:	sameName, err := auth.GetWebAuthnCredentialByName(ctx, ctx.Doer.ID, form.Name)
routers/web/user/setting/security/webauthn.go:159:		ctx.ServerError("GetWebAuthnCredentialByName", err)
models/auth/webauthn.go:141:// GetWebAuthnCredentialByName returns WebAuthn credential by id
models/auth/webauthn.go:142:func GetWebAuthnCredentialByName(ctx context.Context, uid int64, name string) (*WebAuthnCredential, error) {

=== webauthn cred .Name reads in tmpl ===
templates/user/settings/security/webauthn.tmpl:1:<h4 class="ui top attached header">{{ctx.Locale.Tr "settings.webauthn"}}</h4>
templates/user/settings/security/webauthn.tmpl:3:	<p>{{ctx.Locale.Tr "settings.webauthn_desc" "https://w3c.github.io/webauthn/#webauthn-authenticator"}}</p>
templates/user/settings/security/webauthn.tmpl:4:	<p>{{ctx.Locale.Tr "settings.webauthn_key_loss_warning"}} {{ctx.Locale.Tr "settings.webauthn_alternative_tip"}}</p>
templates/user/settings/security/webauthn.tmpl:5:	{{template "user/auth/webauthn_error" .}}
templates/user/settings/security/webauthn.tmpl:20:						data-modal="#rename-webauthn-modal"
templates/user/settings/security/webauthn.tmpl:21:						data-modal-form.url="{{$.Link}}/webauthn/rename?id={{.ID}}"
templates/user/settings/security/webauthn.tmpl:22:						data-modal-rename-webauthn-nickname="{{.Name}}"
templates/user/settings/security/webauthn.tmpl:24:						{{svg "octicon-pencil"}} {{ctx.Locale.Tr "settings.webauthn_rename_key"}}
templates/user/settings/security/webauthn.tmpl:26:					<button class="ui red tiny button link-action" data-modal-confirm="#delete-registration" data-url="{{$.Link}}/webauthn/delete?id={{.ID}}">
templates/user/settings/security/webauthn.tmpl:35:			<label for="nickname">{{ctx.Locale.Tr "settings.webauthn_nickname"}}</label>
templates/user/settings/security/webauthn.tmpl:38:		<button id="register-webauthn" class="ui primary button">{{svg "octicon-key"}} {{ctx.Locale.Tr "settings.webauthn_register_key"}}</button>
templates/user/settings/security/webauthn.tmpl:40:	<div class="ui small modal" id="rename-webauthn-modal">
templates/user/settings/security/webauthn.tmpl:41:		<div class="header">{{svg "octicon-pencil"}} {{ctx.Locale.Tr "settings.webauthn_rename_key"}}</div>
templates/user/settings/security/webauthn.tmpl:45:					<label for="rename-webauthn-nickname">{{ctx.Locale.Tr "settings.webauthn_nickname"}}</label>
templates/user/settings/security/webauthn.tmpl:46:					<input id="rename-webauthn-nickname" name="name" type="text" maxlength="255" required>
templates/user/settings/security/webauthn.tmpl:55:			{{ctx.Locale.Tr "settings.webauthn_delete_key"}}
templates/user/settings/security/webauthn.tmpl:58:			<p>{{ctx.Locale.Tr "settings.webauthn_delete_key_desc"}}</p>
templates/user/auth/signin_openid.tmpl:41:				{{template "user/auth/webauthn_error" .}}
templates/user/settings/security/security.tmpl:6:		{{template "user/settings/security/webauthn" .}}
templates/user/auth/webauthn_error.tmpl:1:<div id="webauthn-error" class="ui negative message tw-hidden">
templates/user/auth/webauthn_error.tmpl:2:	<div class="header">{{ctx.Locale.Tr "webauthn_error"}}</div>
templates/user/auth/webauthn_error.tmpl:3:	<div id="webauthn-error-msg" class="tw-pt-2"></div>
templates/user/auth/webauthn_error.tmpl:5:		<div data-webauthn-error-msg="browser">{{ctx.Locale.Tr "webauthn_unsupported_browser"}}</div>
templates/user/auth/webauthn_error.tmpl:6:		<div data-webauthn-error-msg="unknown">{{ctx.Locale.Tr "webauthn_error_unknown"}}</div>
templates/user/auth/webauthn_error.tmpl:7:		<div data-webauthn-error-msg="insecure">{{ctx.Locale.Tr "webauthn_error_insecure"}}</div>
templates/user/auth/webauthn_error.tmpl:8:		<div data-webauthn-error-msg="unable-to-process">{{ctx.Locale.Tr "webauthn_error_unable_to_process"}}</div>
templates/user/auth/webauthn_error.tmpl:9:		<div data-webauthn-error-msg="duplicated">{{ctx.Locale.Tr "webauthn_error_duplicated"}}</div>
templates/user/auth/webauthn_error.tmpl:10:		<div data-webauthn-error-msg="empty">{{ctx.Locale.Tr "webauthn_error_empty"}}</div>
templates/user/auth/webauthn_error.tmpl:11:		<div data-webauthn-error-msg="timeout">{{ctx.Locale.Tr "webauthn_error_timeout"}}</div>
templates/user/auth/webauthn.tmpl:2:<div role="main" aria-label="{{.Title}}" class="page-content user signin webauthn-prompt">
templates/user/auth/webauthn.tmpl:5:			{{template "user/auth/webauthn_error" .}}
templates/user/auth/webauthn.tmpl:9:				<h3>{{ctx.Locale.Tr "webauthn_insert_key"}}</h3>
templates/user/auth/webauthn.tmpl:11:				<p>{{ctx.Locale.Tr "webauthn_sign_in"}}</p>
templates/user/auth/webauthn.tmpl:15:				{{ctx.Locale.Tr "webauthn_press_button"}}
templates/user/auth/webauthn.tmpl:19:					<a href="{{AppSubUrl}}/user/two_factor">{{ctx.Locale.Tr "webauthn_use_twofa"}}</a>
templates/user/auth/signin_inner.tmpl:64:			{{template "user/auth/webauthn_error" .}}

=== UpdateCredentialName callers ===
routers/web/user/setting/security/webauthn.go:167:	if err := auth.UpdateCredentialName(ctx, cred.ID, ctx.Doer.ID, form.Name); err != nil {
routers/web/user/setting/security/webauthn.go:168:		ctx.ServerError("UpdateCredentialName", err)
models/auth/webauthn.go:198:// UpdateCredentialName will rename the WebAuthnCredential with the given id if it belongs to userID
models/auth/webauthn.go:199:func UpdateCredentialName(ctx context.Context, id, userID int64, name string) error {
models/auth/webauthn_test.go:68:func TestUpdateCredentialName(t *testing.T) {
models/auth/webauthn_test.go:72:	assert.NoError(t, auth_model.UpdateCredentialName(t.Context(), 1, 2, "Hijacked"))
models/auth/webauthn_test.go:75:	assert.NoError(t, auth_model.UpdateCredentialName(t.Context(), 1, 32, "Renamed Key"))
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:20:23Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Read webauthn model top

`sed -n 1,120p models/auth/webauthn.go`

```text
// Copyright 2020 The Gitea Authors. All rights reserved.
// SPDX-License-Identifier: MIT

package auth

import (
	"context"
	"fmt"
	"strings"

	"gitea.dev/models/db"
	"gitea.dev/modules/timeutil"
	"gitea.dev/modules/util"

	"github.com/go-webauthn/webauthn/protocol"
	"github.com/go-webauthn/webauthn/webauthn"
)

// ErrWebAuthnCredentialNotExist represents a "ErrWebAuthnCRedentialNotExist" kind of error.
type ErrWebAuthnCredentialNotExist struct {
	ID           int64
	CredentialID []byte
}

func (err ErrWebAuthnCredentialNotExist) Error() string {
	if len(err.CredentialID) == 0 {
		return fmt.Sprintf("WebAuthn credential does not exist [id: %d]", err.ID)
	}
	return fmt.Sprintf("WebAuthn credential does not exist [credential_id: %x]", err.CredentialID)
}

// Unwrap unwraps this as a ErrNotExist err
func (err ErrWebAuthnCredentialNotExist) Unwrap() error {
	return util.ErrNotExist
}

// IsErrWebAuthnCredentialNotExist checks if an error is a ErrWebAuthnCredentialNotExist.
func IsErrWebAuthnCredentialNotExist(err error) bool {
	_, ok := err.(ErrWebAuthnCredentialNotExist)
	return ok
}

// WebAuthnCredential represents the WebAuthn credential data for a public-key
// credential conformant to WebAuthn Level 1
type WebAuthnCredential struct {
	ID              int64 `xorm:"pk autoincr"`
	Name            string
	LowerName       string `xorm:"unique(s)"`
	UserID          int64  `xorm:"INDEX unique(s)"`
	CredentialID    []byte `xorm:"INDEX VARBINARY(1024)"`
	PublicKey       []byte
	AttestationType string
	AAGUID          []byte
	SignCount       uint32 `xorm:"BIGINT"`
	CloneWarning    bool
	CreatedUnix     timeutil.TimeStamp `xorm:"INDEX created"`
	UpdatedUnix     timeutil.TimeStamp `xorm:"INDEX updated"`
}

func init() {
	db.RegisterModel(new(WebAuthnCredential))
}

// TableName returns a better table name for WebAuthnCredential
func (cred WebAuthnCredential) TableName() string {
	return "webauthn_credential"
}

// UpdateSignCount will update the database value of SignCount
func (cred *WebAuthnCredential) UpdateSignCount(ctx context.Context) error {
	_, err := db.GetEngine(ctx).ID(cred.ID).Cols("sign_count").Update(cred)
	return err
}

// BeforeInsert will be invoked by XORM before updating a record
func (cred *WebAuthnCredential) BeforeInsert() {
	cred.LowerName = strings.ToLower(cred.Name)
}

// BeforeUpdate will be invoked by XORM before updating a record
func (cred *WebAuthnCredential) BeforeUpdate() {
	cred.LowerName = strings.ToLower(cred.Name)
}

// AfterLoad is invoked from XORM after setting the values of all fields of this object.
func (cred *WebAuthnCredential) AfterLoad() {
	cred.LowerName = strings.ToLower(cred.Name)
}

// WebAuthnCredentialList is a list of *WebAuthnCredential
type WebAuthnCredentialList []*WebAuthnCredential

// newCredentialFlagsFromAuthenticatorFlags is copied from https://github.com/go-webauthn/webauthn/pull/337
// to convert protocol.AuthenticatorFlags to webauthn.CredentialFlags
func newCredentialFlagsFromAuthenticatorFlags(flags protocol.AuthenticatorFlags) webauthn.CredentialFlags {
	return webauthn.CredentialFlags{
		UserPresent:    flags.HasUserPresent(),
		UserVerified:   flags.HasUserVerified(),
		BackupEligible: flags.HasBackupEligible(),
		BackupState:    flags.HasBackupState(),
	}
}

// ToCredentials will convert all WebAuthnCredentials to webauthn.Credentials
func (list WebAuthnCredentialList) ToCredentials(defaultAuthFlags ...protocol.AuthenticatorFlags) []webauthn.Credential {
	// TODO: at the moment, Gitea doesn't store or check the flags
	// so we need to use the default flags from the authenticator to make the login validation pass
	// In the future, we should:
	// 1. store the flags when registering the credential
	// 2. provide the stored flags when converting the credentials (for login)
	// 3. for old users, still use this fallback to the default flags
	defAuthFlags := util.OptionalArg(defaultAuthFlags)
	creds := make([]webauthn.Credential, 0, len(list))
	for _, cred := range list {
		creds = append(creds, webauthn.Credential{
			ID:              cred.CredentialID,
			PublicKey:       cred.PublicKey,
			AttestationType: cred.AttestationType,
			Flags:           newCredentialFlagsFromAuthenticatorFlags(defAuthFlags),
			Authenticator: webauthn.Authenticator{
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:20:23Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Read webauthn template and modal actions

`cat -n templates/user/settings/security/webauthn.tmpl; echo "=== modal_actions_confirm ==="; cat -n templates/base/modal_actions_confirm.tmpl`

```text
     1	<h4 class="ui top attached header">{{ctx.Locale.Tr "settings.webauthn"}}</h4>
     2	<div class="ui attached segment">
     3		<p>{{ctx.Locale.Tr "settings.webauthn_desc" "https://w3c.github.io/webauthn/#webauthn-authenticator"}}</p>
     4		<p>{{ctx.Locale.Tr "settings.webauthn_key_loss_warning"}} {{ctx.Locale.Tr "settings.webauthn_alternative_tip"}}</p>
     5		{{template "user/auth/webauthn_error" .}}
     6		<div class="flex-divided-list items-with-main">
     7			{{range .WebAuthnCredentials}}
     8				<div class="item">
     9					<div class="item-leading">
    10						{{svg "octicon-key" 32}}
    11					</div>
    12					<div class="item-main">
    13						<div class="item-title">{{.Name}}</div>
    14						<div class="item-body">
    15							<i>{{ctx.Locale.Tr "settings.added_on" (DateUtils.AbsoluteShort .CreatedUnix)}}</i>
    16						</div>
    17					</div>
    18					<div class="item-trailing">
    19						<button class="ui tiny button show-modal"
    20							data-modal="#rename-webauthn-modal"
    21							data-modal-form.url="{{$.Link}}/webauthn/rename?id={{.ID}}"
    22							data-modal-rename-webauthn-nickname="{{.Name}}"
    23						>
    24							{{svg "octicon-pencil"}} {{ctx.Locale.Tr "settings.webauthn_rename_key"}}
    25						</button>
    26						<button class="ui red tiny button link-action" data-modal-confirm="#delete-registration" data-url="{{$.Link}}/webauthn/delete?id={{.ID}}">
    27						{{ctx.Locale.Tr "settings.delete_key"}}
    28						</button>
    29					</div>
    30				</div>
    31			{{end}}
    32		</div>
    33		<div class="ui form">
    34			<div class="required field">
    35				<label for="nickname">{{ctx.Locale.Tr "settings.webauthn_nickname"}}</label>
    36				<input id="nickname" name="nickname" type="text" required>
    37			</div>
    38			<button id="register-webauthn" class="ui primary button">{{svg "octicon-key"}} {{ctx.Locale.Tr "settings.webauthn_register_key"}}</button>
    39		</div>
    40		<div class="ui small modal" id="rename-webauthn-modal">
    41			<div class="header">{{svg "octicon-pencil"}} {{ctx.Locale.Tr "settings.webauthn_rename_key"}}</div>
    42			<form class="ui form form-fetch-action" method="post">
    43				<div class="content">
    44					<div class="required field">
    45						<label for="rename-webauthn-nickname">{{ctx.Locale.Tr "settings.webauthn_nickname"}}</label>
    46						<input id="rename-webauthn-nickname" name="name" type="text" maxlength="255" required>
    47					</div>
    48				</div>
    49				{{template "base/modal_actions_confirm" (dict "ModalButtonTypes" "confirm")}}
    50			</form>
    51		</div>
    52		<div class="ui small modal" id="delete-registration">
    53			<div class="header">
    54				{{svg "octicon-trash"}}
    55				{{ctx.Locale.Tr "settings.webauthn_delete_key"}}
    56			</div>
    57			<div class="content">
    58				<p>{{ctx.Locale.Tr "settings.webauthn_delete_key_desc"}}</p>
    59			</div>
    60			{{template "base/modal_actions_confirm" .}}
    61		</div>
    62	</div>
=== modal_actions_confirm ===
     1	{{/* Two buttons (negative, positive):
     2	* ModalButtonTypes: "yes" (default) or "confirm"
     3	* ModalButtonCancelText
     4	* ModalButtonOkText
     5	
     6	Single danger button (GitHub-like):
     7	* ModalButtonDangerText "This action will destroy your data"
     8	
     9	The ".ok.button" and ".cancel.button" selectors are also used by Fomantic Modal internally
    10	*/}}
    11	<div class="actions">
    12		{{if .ModalButtonDangerText}}
    13			<button class="ui danger red ok button">{{.ModalButtonDangerText}}</button>
    14		{{else}}
    15			{{$textNegative := ctx.Locale.Tr "modal.no"}}
    16			{{$textPositive := ctx.Locale.Tr "modal.yes"}}
    17			{{if eq .ModalButtonTypes "confirm"}}
    18				{{$textNegative = ctx.Locale.Tr "modal.cancel"}}
    19				{{$textPositive = ctx.Locale.Tr "modal.confirm"}}
    20			{{end}}
    21			{{if .ModalButtonCancelText}}{{$textNegative = .ModalButtonCancelText}}{{end}}
    22			{{if .ModalButtonOkText}}{{$textPositive = .ModalButtonOkText}}{{end}}
    23			<button class="ui cancel button">{{svg "octicon-x"}} {{$textNegative}}</button>
    24			<button class="ui primary ok button">{{svg "octicon-check"}} {{$textPositive}}</button>
    25		{{end}}
    26	</div>
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:20:24Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Find form-fetch-action JS

`grep -rn "form-fetch-action" web_src/js/ | head -30`

```text
web_src/js/components/PullRequestMergeForm.vue:108:    <form class="ui form form-fetch-action" v-if="showActionForm" :action="mergeForm.baseLink+'/merge'" method="post">
web_src/js/modules/fomantic/modal.ts:74:  // "form-fetch-action" can handle network errors gracefully,
web_src/js/modules/fomantic/modal.ts:76:  if (elForm.matches('.form-fetch-action')) return false;
web_src/js/features/common-fetch-action.ts:397:  addDelegatedEventListener<HTMLFormElement, SubmitEvent>(document, 'submit', '.form-fetch-action', async (el, e) => {
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:20:25Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Find HasError/GetErrMsg

`grep -rn "HasError\|GetErrMsg" services/context/*.go modules/web/*.go | head -30`

```text
services/context/context.go:224:// HasError returns true if error occurs in form validation.
services/context/context.go:226:// If HasError is called, then before Redirect, the error message should be stored by ctx.Flash.Error(ctx.GetErrMsg()) again.
services/context/context.go:227:func (ctx *Context) HasError() bool {
services/context/context.go:228:	hasErr, _ := ctx.Data["HasError"].(bool)
services/context/context.go:234:		ctx.Flash.ErrorMsg = ctx.GetErrMsg()
services/context/context.go:240:// GetErrMsg returns error message in form validation.
services/context/context.go:241:func (ctx *Context) GetErrMsg() string {
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:20:28Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Read common-fetch-action.ts submit handling

`sed -n 280,410p web_src/js/features/common-fetch-action.ts`

```text
    }
  }
  return {targets, cmdInnerHTML, cmdMorph};
}

async function handleFetchActionSuccessSync(el: Element, successSync: string, respText: string) {
  const res = execPseudoSelectorCommands(el, successSync);
  if (!res.targets.length) throw new Error(`Fetch-sync command "${successSync}" did not find any target element to update`);
  if (res.targets.length > 1) throw new Error(`Fetch-sync command "${successSync}" found multiple target elements, which is not supported`);
  const target = res.targets[0];
  if (res.cmdMorph) {
    Idiomorph.morph(target, respText, {morphStyle: res.cmdInnerHTML ? 'innerHTML' : 'outerHTML'});
  } else if (res.cmdInnerHTML) {
    target.innerHTML = respText;
  } else {
    target.outerHTML = respText;
  }
  await fetchActionReloadOutdatedElements();
}

async function fetchActionReloadOutdatedElements() {
  const outdatedElems: HTMLElement[] = [];
  for (const outdated of document.querySelectorAll<HTMLElement>('[data-fetch-trigger~="fetch-reload"]')) {
    if (!outdated.id) throw new Error(`Elements with "fetch-reload" trigger must have an id to be reloaded after fetch sync: ${outdated.outerHTML.substring(0, 100)}`);
    outdatedElems.push(outdated);
  }
  if (!outdatedElems.length) return;

  const resp = await GET(window.location.href);
  if (!resp.ok) {
    showErrorToast(`Failed to reload page content after fetch action: ${resp.status} ${resp.statusText}`);
    return;
  }
  const newPageHtml = await resp.text();
  const newPageDom = parseDom(newPageHtml, 'text/html');
  for (const oldEl of outdatedElems) {
    // eslint-disable-next-line unicorn/prefer-query-selector
    const newEl = newPageDom.getElementById(oldEl.id);
    if (newEl) {
      oldEl.replaceWith(newEl);
    } else {
      oldEl.remove();
    }
  }
}

function initFetchActionTriggerEvery(el: HTMLElement, trigger: string) {
  const interval = trigger.substring('every '.length);
  const match = /^(\d+)(ms|s)$/.exec(interval);
  if (!match) throw new Error(`Invalid interval format: ${interval}`);

  const num = parseInt(match[1], 10), unit = match[2];
  const intervalMs = unit === 's' ? num * 1000 : num;
  activePageTimerRefresh({
    interval: () => document.contains(el) ? intervalMs : 0, // only continue if the element is still in the document
    async callback() { await performFetchActionTrigger(el, 'every') },
  });
}

function initFetchActionTrigger(el: HTMLElement) {
  const trigger = el.getAttribute('data-fetch-trigger');

  // this trigger is managed internally, only triggered after fetch sync success, not triggered by event or timer
  if (trigger === 'fetch-reload') return;

  if (trigger === 'load') {
    performFetchActionTrigger(el, trigger);
  } else if (trigger === 'change') {
    el.addEventListener('change', () => performFetchActionTrigger(el, trigger));
  } else if (trigger?.startsWith('every ')) {
    initFetchActionTriggerEvery(el, trigger);
  } else if (!trigger || trigger === 'click') {
    el.addEventListener('click', (e) => {
      e.preventDefault();
      performFetchActionTrigger(el, 'click');
    });
  } else {
    throw new Error(`Unsupported fetch trigger: ${trigger}`);
  }
}

export function initGlobalFetchAction() {
  // The "fetch-action" framework is a general approach for elements to trigger fetch requests:
  // show confirm dialog (if any), show loading indicators, send fetch request, and redirect or update UI after success.
  //
  // If you need more fine-grained control more details, sometimes it's clearer to write the logic in JavaScript, instead of using this generic framework.
  //
  // Attributes:
  //
  // * data-fetch-method: the HTTP method to use
  //   * default to "GET" for "data-fetch-url" actions, "POST" for "link-action" elements
  //   * this attribute is ignored, the method will be determined by the form's "method" attribute, and default to "GET"
  //
  // * data-fetch-url: the URL for the request
  //
  // * data-fetch-trigger: the event to trigger the fetch action, can be:
  //   * "click", "change" (user-initiated events)
  //   * "load" (triggered on page load)
  //   * "every 5s" (also support "ms" unit)
  //   * "fetch-reload" (only triggered by fetch sync success to reload outdated content)
  //
  // * data-fetch-indicator: the loading indicator element selector, it uses the same syntax as "data-fetch-sync" to find the element(s)
  //
  // * data-fetch-sync: when the response is text (html), the pseudo selectors/commands defined in "data-fetch-sync"
  //   will be used to update the content in the current page. It only supports some simple syntaxes that we need.
  //   "$" prefix means it is our private command (for special logic), the selectors are run one by one from current element.
  //   * "$this": replace the current element with the response
  //   * "$innerHTML": replace innerHTML of the current element with the response, instead of replacing the whole element (outerHTML)
  //   * "$morph": use morph algorithm to update the target element
  //   * "$body #the-id .the-class": query the selector one by one from body
  //   * "$closest(tr) td": pseudo command can help to find the target element in a more flexible way
  //
  // * data-modal-confirm: a "confirm modal dialog" will be shown before taking action.
  //   * it can be a string for the content of the modal dialog
  //   * it has "-header" and "-content" variants to set the header and content of the "confirm modal"
  //   * it can refer an existing modal element by "#the-modal-id"

  addDelegatedEventListener<HTMLFormElement, SubmitEvent>(document, 'submit', '.form-fetch-action', async (el, e) => {
    // "fetch-action" will use the form's data to send the request
    e.preventDefault();
    await submitFormFetchAction(el, {formSubmitter: e.submitter});
  });

  addDelegatedEventListener(document, 'click', '.link-action', async (el, e) => {
    // `<a class="link-action" data-url="...">` is a shorthand for
    // `<a data-fetch-trigger="click" data-fetch-method="post" data-fetch-url="..." data-fetch-indicator="$this">`
    e.preventDefault();
    await performLinkFetchAction(el);
  });

  registerGlobalSelectorFunc('[data-fetch-url]', initFetchActionTrigger);
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:20:29Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Find form-fetch-action handler

`grep -rn "form-fetch-action" web_src/js/ | head -20`

```text
web_src/js/components/PullRequestMergeForm.vue:108:    <form class="ui form form-fetch-action" v-if="showActionForm" :action="mergeForm.baseLink+'/merge'" method="post">
web_src/js/modules/fomantic/modal.ts:74:  // "form-fetch-action" can handle network errors gracefully,
web_src/js/modules/fomantic/modal.ts:76:  if (elForm.matches('.form-fetch-action')) return false;
web_src/js/features/common-fetch-action.ts:397:  addDelegatedEventListener<HTMLFormElement, SubmitEvent>(document, 'submit', '.form-fetch-action', async (el, e) => {
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:20:29Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Read HasError/GetErrMsg

`sed -n 215,255p services/context/context.go`

```text
}

func (ctx *Context) DoerNeedTwoFactorAuth() bool {
	if !setting.TwoFactorAuthEnforced {
		return false
	}
	return ctx.Session.Get(session.KeyUserHasTwoFactorAuth) == false
}

// HasError returns true if error occurs in form validation.
// Attention: this function changes ctx.Data and ctx.Flash
// If HasError is called, then before Redirect, the error message should be stored by ctx.Flash.Error(ctx.GetErrMsg()) again.
func (ctx *Context) HasError() bool {
	hasErr, _ := ctx.Data["HasError"].(bool)
	hasErr = hasErr || ctx.Flash.ErrorMsg != ""
	if !hasErr {
		return false
	}
	if ctx.Flash.ErrorMsg == "" {
		ctx.Flash.ErrorMsg = ctx.GetErrMsg()
	}
	ctx.Data["Flash"] = ctx.Flash
	return hasErr
}

// GetErrMsg returns error message in form validation.
func (ctx *Context) GetErrMsg() string {
	msg, _ := ctx.Data["ErrorMsg"].(string)
	if msg == "" {
		msg = "invalid form data"
	}
	return msg
}

func (ctx *Context) JSONRedirect(redirect string) {
	ctx.JSON(http.StatusOK, map[string]any{"redirect": redirect})
}

func (ctx *Context) JSONOK() {
	ctx.JSON(http.StatusOK, map[string]any{"ok": true}) // this is only a dummy response, frontend seldom uses it
}
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:20:30Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Find CSRF injection in fetch helpers

`grep -rn "csrf" --include="*.ts" web_src/js/features/common-fetch-action.ts web_src/js/modules/fetch.ts 2>/dev/null | head -30`

```text
(Bash completed with no output)
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:20:31Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Read unit_tests.go helpers

`sed -n 1,110p models/unittest/unit_tests.go`

```text
// Copyright 2016 The Gitea Authors. All rights reserved.
// SPDX-License-Identifier: MIT

package unittest

import (
	"context"
	"fmt"
	"math"
	"os"
	"strings"

	"gitea.dev/models/db"

	"github.com/stretchr/testify/assert"
	"github.com/stretchr/testify/require"
	"xorm.io/builder"
)

// Code in this file is mainly used by unittest.CheckConsistencyFor, which is not in the unit test for various reasons.
// In the future if we can decouple CheckConsistencyFor into separate unit test code, then this file can be moved into unittest package too.

// NonexistentID an ID that will never exist
const NonexistentID = int64(math.MaxInt64)

type TestingT interface {
	require.TestingT
	assert.TestingT
	Context() context.Context
}

type testCond struct {
	query any
	args  []any
}

type testOrderBy string

// Cond create a condition with arguments for a test
func Cond(query any, args ...any) any {
	return &testCond{query: query, args: args}
}

// OrderBy creates "ORDER BY" a test query
func OrderBy(orderBy string) any {
	return testOrderBy(orderBy)
}

func whereOrderConditions(e db.Engine, conditions []any) db.Engine {
	orderBy := "id" // query must have the "ORDER BY", otherwise the result is not deterministic. FIXME: some tables do not have "id" column
	for _, condition := range conditions {
		switch cond := condition.(type) {
		case *testCond:
			e = e.Where(cond.query, cond.args...)
		case testOrderBy:
			orderBy = string(cond)
		default:
			e = e.Where(cond)
		}
	}
	return e.OrderBy(orderBy)
}

func getBeanIfExists(t TestingT, bean any, conditions ...any) (bool, error) {
	e := db.GetEngine(t.Context())
	return whereOrderConditions(e, conditions).Get(bean)
}

func GetBean[T any](t TestingT, bean T, conditions ...any) (ret T) {
	exists, err := getBeanIfExists(t, bean, conditions...)
	require.NoError(t, err)
	if exists {
		return bean
	}
	return ret
}

// AssertExistsAndLoadBean assert that a bean exists and load it from the test database
func AssertExistsAndLoadBean[T any](t TestingT, bean T, conditions ...any) T {
	exists, err := getBeanIfExists(t, bean, conditions...)
	require.NoError(t, err)
	require.True(t, exists,
		"Expected to find %+v (of type %T, with conditions %+v), but did not",
		bean, bean, conditions)
	return bean
}

// AssertExistsAndLoadMap assert that a row exists and load it from the test database
func AssertExistsAndLoadMap(t TestingT, table string, conditions ...any) map[string]string {
	e := db.GetEngine(t.Context()).Table(table)
	res, err := whereOrderConditions(e, conditions).Query()
	assert.NoError(t, err)
	assert.Len(t, res, 1,
		"Expected to find one row in %s (with conditions %+v), but found %d",
		table, conditions, len(res),
	)

	if len(res) == 1 {
		rec := map[string]string{}
		for k, v := range res[0] {
			rec[k] = string(v)
		}
		return rec
	}
	return nil
}

// GetCount get the count of a bean
func GetCount(t TestingT, bean any, conditions ...any) int {
	e := db.GetEngine(t.Context())
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:20:34Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Read router webauthn.go and check trailing whitespace

`cat -A routers/web/user/setting/security/webauthn.go | grep -n ' \$$' | head; echo "=== file ==="; sed -n '1,200p' routers/web/user/setting/security/webauthn.go`

```text
=== file ===
// Copyright 2018 The Gitea Authors. All rights reserved.
// SPDX-License-Identifier: MIT

package security

import (
	"errors"
	"net/http"
	"strconv"
	"strings"
	"time"

	"gitea.dev/models/auth"
	user_model "gitea.dev/models/user"
	wa "gitea.dev/modules/auth/webauthn"
	"gitea.dev/modules/log"
	"gitea.dev/modules/session"
	"gitea.dev/modules/setting"
	"gitea.dev/modules/web"
	"gitea.dev/services/context"
	"gitea.dev/services/forms"

	"github.com/go-webauthn/webauthn/protocol"
	"github.com/go-webauthn/webauthn/webauthn"
)

// WebAuthnRegister initializes the webauthn registration procedure
func WebAuthnRegister(ctx *context.Context) {
	if user_model.IsFeatureDisabledWithLoginType(ctx.Doer, setting.UserFeatureManageMFA) {
		ctx.HTTPError(http.StatusNotFound)
		return
	}

	form := web.GetForm(ctx).(*forms.WebauthnRegistrationForm)
	if form.Name == "" {
		// Set name to the hexadecimal of the current time
		form.Name = strconv.FormatInt(time.Now().UnixNano(), 16)
	}

	cred, err := auth.GetWebAuthnCredentialByName(ctx, ctx.Doer.ID, form.Name)
	if err != nil && !auth.IsErrWebAuthnCredentialNotExist(err) {
		ctx.ServerError("GetWebAuthnCredentialsByUID", err)
		return
	}
	if cred != nil {
		ctx.HTTPError(http.StatusConflict, "Name already taken")
		return
	}

	_ = ctx.Session.Delete("webauthnRegistration")
	if err := ctx.Session.Set("webauthnName", form.Name); err != nil {
		ctx.ServerError("Unable to set session key for webauthnName", err)
		return
	}

	webAuthnUser := wa.NewWebAuthnUser(ctx, ctx.Doer)
	credentialOptions, sessionData, err := wa.WebAuthn.BeginRegistration(webAuthnUser, webauthn.WithAuthenticatorSelection(protocol.AuthenticatorSelection{
		ResidentKey: protocol.ResidentKeyRequirementRequired,
	}))
	if err != nil {
		ctx.ServerError("Unable to BeginRegistration", err)
		return
	}

	// Save the session data as marshaled JSON
	if err = ctx.Session.Set("webauthnRegistration", sessionData); err != nil {
		ctx.ServerError("Unable to set session", err)
		return
	}

	ctx.JSON(http.StatusOK, credentialOptions)
}

// WebauthnRegisterPost receives the response of the security key
func WebauthnRegisterPost(ctx *context.Context) {
	if user_model.IsFeatureDisabledWithLoginType(ctx.Doer, setting.UserFeatureManageMFA) {
		ctx.HTTPError(http.StatusNotFound)
		return
	}

	name, ok := ctx.Session.Get("webauthnName").(string)
	if !ok || name == "" {
		ctx.ServerError("Get webauthnName", errors.New("no webauthnName"))
		return
	}

	// Load the session data
	sessionData, ok := ctx.Session.Get("webauthnRegistration").(*webauthn.SessionData)
	if !ok || sessionData == nil {
		ctx.ServerError("Get registration", errors.New("no registration"))
		return
	}
	defer func() {
		_ = ctx.Session.Delete("webauthnRegistration")
	}()

	// Verify that the challenge succeeded
	webAuthnUser := wa.NewWebAuthnUser(ctx, ctx.Doer)
	cred, err := wa.WebAuthn.FinishRegistration(webAuthnUser, *sessionData, ctx.Req)
	if err != nil {
		if pErr, ok := err.(*protocol.Error); ok {
			log.Error("Unable to finish registration due to error: %v\nDevInfo: %s", pErr, pErr.DevInfo)
		}
		ctx.ServerError("CreateCredential", err)
		return
	}

	dbCred, err := auth.GetWebAuthnCredentialByName(ctx, ctx.Doer.ID, name)
	if err != nil && !auth.IsErrWebAuthnCredentialNotExist(err) {
		ctx.ServerError("GetWebAuthnCredentialsByUID", err)
		return
	}
	if dbCred != nil {
		ctx.HTTPError(http.StatusConflict, "Name already taken")
		return
	}

	// Create the credential
	_, err = auth.CreateCredential(ctx, ctx.Doer.ID, name, cred)
	if err != nil {
		ctx.ServerError("CreateCredential", err)
		return
	}
	_ = ctx.Session.Delete("webauthnName")
	_ = ctx.Session.Set(session.KeyUserHasTwoFactorAuth, true)
	ctx.JSON(http.StatusCreated, cred)
}

// WebauthnRename renames a security key by id
func WebauthnRename(ctx *context.Context) {
	if user_model.IsFeatureDisabledWithLoginType(ctx.Doer, setting.UserFeatureManageMFA) {
		ctx.HTTPError(http.StatusNotFound)
		return
	}

	if ctx.HasError() { // form binding validation error
		ctx.JSONError(ctx.GetErrMsg())
		return
	}

	cred, err := auth.GetWebAuthnCredentialByID(ctx, ctx.FormInt64("id"))
	if err != nil && !auth.IsErrWebAuthnCredentialNotExist(err) {
		ctx.ServerError("GetWebAuthnCredentialByID", err)
		return
	}
	if cred == nil || cred.UserID != ctx.Doer.ID { // hide other users' keys as not found
		ctx.JSONErrorNotFound()
		return
	}

	form := web.GetForm(ctx).(*forms.WebauthnRenameForm)
	form.Name = strings.TrimSpace(form.Name)
	if form.Name == "" { // "Required" binding only rejects an exactly empty value, not whitespace
		ctx.JSONError(ctx.Locale.TrString("settings.webauthn_nickname") + ctx.Locale.TrString("form.require_error"))
		return
	}
	sameName, err := auth.GetWebAuthnCredentialByName(ctx, ctx.Doer.ID, form.Name)
	if err != nil && !auth.IsErrWebAuthnCredentialNotExist(err) {
		ctx.ServerError("GetWebAuthnCredentialByName", err)
		return
	}
	if sameName != nil && sameName.ID != cred.ID { // renaming a key to its own name with different casing is allowed
		ctx.JSONError(ctx.Tr("settings.webauthn_nickname_taken"))
		return
	}

	if err := auth.UpdateCredentialName(ctx, cred.ID, ctx.Doer.ID, form.Name); err != nil {
		ctx.ServerError("UpdateCredentialName", err)
		return
	}
	ctx.Flash.Success(ctx.Tr("settings.webauthn_rename_key_success"))
	ctx.JSONRedirect(setting.AppSubURL + "/user/settings/security")
}

// WebauthnDelete deletes an security key by id
func WebauthnDelete(ctx *context.Context) {
	if user_model.IsFeatureDisabledWithLoginType(ctx.Doer, setting.UserFeatureManageMFA) {
		ctx.HTTPError(http.StatusNotFound)
		return
	}

	if _, err := auth.DeleteCredential(ctx, ctx.FormInt64("id"), ctx.Doer.ID); err != nil {
		ctx.ServerError("GetWebAuthnCredentialByID", err)
		return
	}
	ctx.JSONRedirect(setting.AppSubURL + "/user/settings/security")
}
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:20:32Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Read form-fetch-action submit handler

`sed -n 330,420p web_src/js/features/common-fetch-action.ts`

```text
  const num = parseInt(match[1], 10), unit = match[2];
  const intervalMs = unit === 's' ? num * 1000 : num;
  activePageTimerRefresh({
    interval: () => document.contains(el) ? intervalMs : 0, // only continue if the element is still in the document
    async callback() { await performFetchActionTrigger(el, 'every') },
  });
}

function initFetchActionTrigger(el: HTMLElement) {
  const trigger = el.getAttribute('data-fetch-trigger');

  // this trigger is managed internally, only triggered after fetch sync success, not triggered by event or timer
  if (trigger === 'fetch-reload') return;

  if (trigger === 'load') {
    performFetchActionTrigger(el, trigger);
  } else if (trigger === 'change') {
    el.addEventListener('change', () => performFetchActionTrigger(el, trigger));
  } else if (trigger?.startsWith('every ')) {
    initFetchActionTriggerEvery(el, trigger);
  } else if (!trigger || trigger === 'click') {
    el.addEventListener('click', (e) => {
      e.preventDefault();
      performFetchActionTrigger(el, 'click');
    });
  } else {
    throw new Error(`Unsupported fetch trigger: ${trigger}`);
  }
}

export function initGlobalFetchAction() {
  // The "fetch-action" framework is a general approach for elements to trigger fetch requests:
  // show confirm dialog (if any), show loading indicators, send fetch request, and redirect or update UI after success.
  //
  // If you need more fine-grained control more details, sometimes it's clearer to write the logic in JavaScript, instead of using this generic framework.
  //
  // Attributes:
  //
  // * data-fetch-method: the HTTP method to use
  //   * default to "GET" for "data-fetch-url" actions, "POST" for "link-action" elements
  //   * this attribute is ignored, the method will be determined by the form's "method" attribute, and default to "GET"
  //
  // * data-fetch-url: the URL for the request
  //
  // * data-fetch-trigger: the event to trigger the fetch action, can be:
  //   * "click", "change" (user-initiated events)
  //   * "load" (triggered on page load)
  //   * "every 5s" (also support "ms" unit)
  //   * "fetch-reload" (only triggered by fetch sync success to reload outdated content)
  //
  // * data-fetch-indicator: the loading indicator element selector, it uses the same syntax as "data-fetch-sync" to find the element(s)
  //
  // * data-fetch-sync: when the response is text (html), the pseudo selectors/commands defined in "data-fetch-sync"
  //   will be used to update the content in the current page. It only supports some simple syntaxes that we need.
  //   "$" prefix means it is our private command (for special logic), the selectors are run one by one from current element.
  //   * "$this": replace the current element with the response
  //   * "$innerHTML": replace innerHTML of the current element with the response, instead of replacing the whole element (outerHTML)
  //   * "$morph": use morph algorithm to update the target element
  //   * "$body #the-id .the-class": query the selector one by one from body
  //   * "$closest(tr) td": pseudo command can help to find the target element in a more flexible way
  //
  // * data-modal-confirm: a "confirm modal dialog" will be shown before taking action.
  //   * it can be a string for the content of the modal dialog
  //   * it has "-header" and "-content" variants to set the header and content of the "confirm modal"
  //   * it can refer an existing modal element by "#the-modal-id"

  addDelegatedEventListener<HTMLFormElement, SubmitEvent>(document, 'submit', '.form-fetch-action', async (el, e) => {
    // "fetch-action" will use the form's data to send the request
    e.preventDefault();
    await submitFormFetchAction(el, {formSubmitter: e.submitter});
  });

  addDelegatedEventListener(document, 'click', '.link-action', async (el, e) => {
    // `<a class="link-action" data-url="...">` is a shorthand for
    // `<a data-fetch-trigger="click" data-fetch-method="post" data-fetch-url="..." data-fetch-indicator="$this">`
    e.preventDefault();
    await performLinkFetchAction(el);
  });

  registerGlobalSelectorFunc('[data-fetch-url]', initFetchActionTrigger);
}
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:20:36Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Check added lines for trailing whitespace

`git diff $(git merge-base main HEAD)..HEAD | grep -n '^+.*[ 	]$' | head -20; echo "=== done trailing ws ==="`

```text
=== done trailing ws ===
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:20:33Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Check fetch module CSRF

`grep -rn "csrf" -i web_src/js/modules/fetch.ts | head -20`

```text
(Bash completed with no output)
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:20:33Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Build all Go packages with exit code

`go build ./... > /tmp/claude-1000/-home-mantmant-Documents-CSCI435-gitea/f1c2aa49-dab6-4b47-9277-85993d369c13/scratchpad/build.log 2>&1; echo "EXIT=$?"; tail -40 /tmp/claude-1000/-home-mantmant-Documents-CSCI435-gitea/f1c2aa49-dab6-4b47-9277-85993d369c13/scratchpad/build.log`
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:20:35Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Read common-button.ts

`sed -n 1,120p web_src/js/features/common-button.ts`

```text
import {addDelegatedEventListener, hideElem, isElemVisible, showElem, toggleElem} from '../utils/dom.ts';
import {showFomanticModal} from '../modules/fomantic/modal.ts';
import {camelize} from 'vue';
import {applyAutoFocus} from './common-page.ts';

export function initGlobalButtonClickOnEnter(): void {
  addDelegatedEventListener(document, 'keypress', 'div.ui.button, span.ui.button', (el, e: KeyboardEvent) => {
    if (e.code === 'Space' || e.code === 'Enter') {
      e.preventDefault();
      el.click();
    }
  });
}

function onShowPanelClick(el: HTMLElement, e: MouseEvent) {
  // a '.show-panel' element can show a panel, by `data-panel="selector"`
  // if it has "toggle" class, it toggles the panel
  e.preventDefault();
  const sel = el.getAttribute('data-panel')!;
  const elems = el.classList.contains('toggle') ? toggleElem(sel) : showElem(sel);
  for (const elem of elems) {
    if (isElemVisible(elem as HTMLElement)) {
      applyAutoFocus(elem);
    }
  }
}

function onHidePanelClick(el: HTMLElement, e: MouseEvent) {
  // a `.hide-panel` element can hide a panel, by `data-panel="selector"` or `data-panel-closest="selector"`
  e.preventDefault();
  let sel = el.getAttribute('data-panel');
  if (sel) {
    hideElem(sel);
    return;
  }
  sel = el.getAttribute('data-panel-closest');
  if (sel) {
    hideElem((el.parentNode as HTMLElement).closest(sel)!);
    return;
  }
  throw new Error('no panel to hide'); // should never happen, otherwise there is a bug in code
}

export type ElementWithAssignableProperties = {
  nodeName: string;
  getAttribute: (name: string) => string | null;
  setAttribute: (name: string, value: string) => void;
} & Record<string, any>;

export function assignElementProperty(el: ElementWithAssignableProperties, kebabName: string, val: string) {
  if (el.nodeName === 'FORM') {
    // HINT: GOLANG-HTML-TEMPLATE-URL-ESCAPING: a special case for Golang HTML template escaping.
    // Golang HTML template only handles some "known" attribute names as URL (e.g.: when the name is "action" or contains "url")
    // To prevent template developers from making mistakes like `data-modal-form.action="?k={{ValueWithSpecialChars}}" (no escaping),
    // here we use `data-modal-form.url="?k={{ValueWithSpecialChars}}", then the value gets correctly escaped by Golang HTML template.
    if (kebabName === 'action') throw new Error(`don't assign element property "action" by value, use "data-modal-form.url" instead`);
    if (kebabName === 'url') kebabName = 'action';
  }
  const camelizedName = camelize(kebabName);
  const old = el[camelizedName];
  if (typeof old === 'boolean') {
    el[camelizedName] = val === 'true';
  } else if (typeof old === 'number') {
    el[camelizedName] = parseFloat(val);
  } else if (typeof old === 'string') {
    el[camelizedName] = val;
  } else if (old?.nodeName) {
    // "form" has an edge case: its "<input name=action>" element overwrites the "action" property, we can only set attribute
    el.setAttribute(kebabName, val);
  } else {
    // in the future, maybe we could introduce a better typing system if it is really needed
    throw new Error(`cannot assign element property "${camelizedName}" by value "${val}"`);
  }
}

function onShowModalClick(el: HTMLElement, e: MouseEvent) {
  // A ".show-modal" button will show a modal dialog defined by its "data-modal" attribute.
  // Each "data-modal-{target}" attribute will be filled to target element's value or text-content.
  // * First, try to query '#target'
  // * Then, try to query '[name=target]'
  // * Then, try to query '.target'
  // * Then, try to query 'target' as HTML tag
  // If there's a ".{prop-name}" part like "data-modal-input.value", the "input" element's "value" property will be set,
  // the "prop-name" will be camel-cased to "propName" (e.g.: "data-modal-input.read-only" for "readOnly" property).
  //
  // HINT: GOLANG-HTML-TEMPLATE-URL-ESCAPING: Form element's "action" property must be set by "data-modal-form.url"
  // to make the template variables get correctly escaped in the URL.
  e.preventDefault();
  const modalSelector = el.getAttribute('data-modal')!;
  const elModal = document.querySelector(modalSelector);
  if (!elModal) throw new Error('no modal for this action');

  const modalAttrPrefix = 'data-modal-';
  for (const attrib of el.attributes) {
    if (!attrib.name.startsWith(modalAttrPrefix)) {
      continue;
    }

    const attrTargetCombo = attrib.name.substring(modalAttrPrefix.length);
    const [attrTargetName, attrTargetProp] = attrTargetCombo.split('.');
    // try to find target by: "#target" -> "[name=target]" -> ".target" -> "<target> tag", and then try the modal itself
    const attrTarget = elModal.querySelector(`#${attrTargetName}`) ||
      elModal.querySelector(`[name=${CSS.escape(attrTargetName)}]`) ||
      elModal.querySelector(`.${attrTargetName}`) ||
      elModal.querySelector(attrTargetName) ||
      (elModal.matches(attrTargetName) || elModal.matches(`#${attrTargetName}`) || elModal.matches(`.${attrTargetName}`) ? elModal : null);
    if (!attrTarget) {
      if (!window.config.runModeIsProd) throw new Error(`attr target "${attrTargetCombo}" not found for modal`);
      continue;
    }

    if (attrTargetProp) {
      assignElementProperty(attrTarget, attrTargetProp, attrib.value);
    } else if (attrTarget.matches('input, textarea')) {
      (attrTarget as HTMLInputElement | HTMLTextAreaElement).value = attrib.value; // FIXME: add more supports like checkbox
    } else {
      attrTarget.textContent = attrib.value; // FIXME: it should be more strict here, only handle div/span/p
    }
  }
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:20:36Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Read flash middleware

`cat -n modules/web/middleware/flash.go`

```text
     1	// Copyright 2020 The Gitea Authors. All rights reserved.
     2	// SPDX-License-Identifier: MIT
     3	
     4	package middleware
     5	
     6	import (
     7		"fmt"
     8		"html/template"
     9		"net/http"
    10		"net/url"
    11	
    12		"gitea.dev/modules/reqctx"
    13	)
    14	
    15	// Flash represents a one time data transfer between two requests.
    16	type Flash struct {
    17		DataStore reqctx.RequestDataStore
    18		url.Values
    19		ErrorMsg, WarningMsg, InfoMsg, SuccessMsg string
    20	}
    21	
    22	func (f *Flash) set(name, msg string, current ...bool) {
    23		if f.Values == nil {
    24			f.Values = make(map[string][]string)
    25		}
    26		showInCurrentPage := len(current) > 0 && current[0]
    27		if showInCurrentPage {
    28			// assign it to the context data, then the template can use ".Flash.XxxMsg" to render the message
    29			f.DataStore.GetData()["Flash"] = f
    30		} else {
    31			// the message map will be saved into the cookie and be shown in next response (a new page response which decodes the cookie)
    32			f.Set(name, msg)
    33		}
    34	}
    35	
    36	func flashMsgStringOrHTML(msg any) string {
    37		switch v := msg.(type) {
    38		case string:
    39			return v
    40		case template.HTML:
    41			return string(v)
    42		}
    43		panic(fmt.Sprintf("unknown type: %T", msg))
    44	}
    45	
    46	// Error sets error message
    47	func (f *Flash) Error(msg any, current ...bool) {
    48		f.ErrorMsg = flashMsgStringOrHTML(msg)
    49		f.set("error", f.ErrorMsg, current...)
    50	}
    51	
    52	// Warning sets warning message
    53	func (f *Flash) Warning(msg any, current ...bool) {
    54		f.WarningMsg = flashMsgStringOrHTML(msg)
    55		f.set("warning", f.WarningMsg, current...)
    56	}
    57	
    58	// Info sets info message
    59	func (f *Flash) Info(msg any, current ...bool) {
    60		f.InfoMsg = flashMsgStringOrHTML(msg)
    61		f.set("info", f.InfoMsg, current...)
    62	}
    63	
    64	// Success sets success message
    65	func (f *Flash) Success(msg any, current ...bool) {
    66		f.SuccessMsg = flashMsgStringOrHTML(msg)
    67		f.set("success", f.SuccessMsg, current...)
    68	}
    69	
    70	func ParseCookieFlashMessage(val string) *Flash {
    71		if vals, _ := url.ParseQuery(val); len(vals) > 0 {
    72			return &Flash{
    73				Values:     vals,
    74				ErrorMsg:   vals.Get("error"),
    75				SuccessMsg: vals.Get("success"),
    76				InfoMsg:    vals.Get("info"),
    77				WarningMsg: vals.Get("warning"),
    78			}
    79		}
    80		return nil
    81	}
    82	
    83	func GetSiteCookieFlashMessage(dataStore reqctx.RequestDataStore, req *http.Request, cookieName string) (string, *Flash) {
    84		// Get the last flash message from cookie
    85		lastFlashCookie := GetSiteCookie(req, cookieName)
    86		lastFlashMsg := ParseCookieFlashMessage(lastFlashCookie)
    87		if lastFlashMsg != nil {
    88			lastFlashMsg.DataStore = dataStore
    89			return lastFlashCookie, lastFlashMsg
    90		}
    91		return lastFlashCookie, nil
    92	}
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:20:36Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Read common-fetch-action.ts

`cat web_src/js/features/common-fetch-action.ts 2>/dev/null | head -120`

```text
import {GET, request} from '../modules/fetch.ts';
import {hideToastsAll, showErrorToast} from '../modules/toast.ts';
import {activePageTimerRefresh, addDelegatedEventListener, createElementFromHTML} from '../utils/dom.ts';
import {errorMessage, errorName} from '../modules/errors.ts';
import {confirmModal, createConfirmModal} from './comp/ConfirmModal.ts';
import {ignoreAreYouSure} from '../vendor/jquery.are-you-sure.ts';
import {registerGlobalSelectorFunc} from '../modules/observer.ts';
import {Idiomorph} from 'idiomorph';
import {parseDom} from '../utils.ts';
import {html} from '../utils/html.ts';

const {appSubUrl, runModeIsProd} = window.config;

type FetchActionOpts = {
  method: string;
  url: string;
  headers?: HeadersInit;
  body?: FormData;
  formSubmitter?: HTMLElement | null;

  // pseudo selectors/commands to update the current page with the response text when the response is text (html)
  // e.g.: "$this", "$innerHTML", "$closest(tr) td .the-class", "$body #the-id"
  successSync: string;

  // the loading indicator element selector, it uses the same syntax as "data-fetch-sync" to find the element(s)
  // empty means no loading indicator, "$this" means the element itself
  loadingIndicator: string;
};

// fetchActionDoRedirect does real redirection to bypass the browser's limitations of "location"
// more details are in the backend's fetch-redirect handler
function fetchActionDoRedirect(redirect: string) {
  // In production, if the link can be directly navigated by browser, we just do normal redirection, which is faster.
  // Otherwise, need to use backend to do redirection:
  // * Also do so in development, to make sure the redirection logic is always tested by real users
  const needBackendHelp = redirect.includes('#');
  if (runModeIsProd && !needBackendHelp) {
    window.location.assign(redirect);
    return;
  }

  // use backend to do redirection, which can bypass the browser's limitations of "location"
  const form = createElementFromHTML<HTMLFormElement>(html`<form method="post"></form>`);
  form.action = `${appSubUrl}/-/fetch-redirect?redirect=${encodeURIComponent(redirect)}`;
  document.body.append(form);
  form.submit();
}

function toggleLoadingIndicator(el: HTMLElement, opt: FetchActionOpts, isLoading: boolean) {
  const loadingIndicatorElems = opt.loadingIndicator ? execPseudoSelectorCommands(el, opt.loadingIndicator).targets : [];
  for (const indicatorEl of loadingIndicatorElems) {
    if (isLoading) {
      // for button or input element, we can directly disable it, it looks better than adding a loading spinner
      if ('disabled' in indicatorEl) {
        indicatorEl.disabled = true;
      } else {
        indicatorEl.classList.add('is-loading');
        if (indicatorEl.clientHeight < 50) indicatorEl.classList.add('loading-icon-2px');
      }
    } else {
      if ('disabled' in indicatorEl) {
        indicatorEl.disabled = false;
      } else {
        indicatorEl.classList.remove('is-loading', 'loading-icon-2px');
      }
    }
  }
}

export async function handleFetchActionSuccessJson(el: HTMLElement, respJson: any) {
  ignoreAreYouSure(el); // ignore the areYouSure check before reloading
  const redirect = respJson?.redirect;
  if (typeof redirect === 'string' && redirect) {
    fetchActionDoRedirect(redirect);
  } else {
    // reserved behavior, in the future, there can be more fields to introduce more behaviors
    window.location.reload();
  }
}

async function handleFetchActionSuccess(el: HTMLElement, opt: FetchActionOpts, resp: Response) {
  const isRespJson = resp.headers.get('content-type')?.includes('application/json');
  const respText = await resp.text();
  const respJson = isRespJson ? JSON.parse(respText) : null;
  if (isRespJson) {
    await handleFetchActionSuccessJson(el, respJson);
  } else if (opt.successSync) {
    await handleFetchActionSuccessSync(el, opt.successSync, respText);
  } else {
    showErrorToast(`Unsupported fetch action response, expected JSON but got: ${respText.substring(0, 200)}`);
  }
}

async function handleFetchActionError(resp: Response) {
  const isRespJson = resp.headers.get('content-type')?.includes('application/json');
  const respText = await resp.text();
  const respJson = isRespJson ? JSON.parse(respText) : null;
  if (respJson?.errorMessage) {
    // the code was quite messy, sometimes the backend uses "err", sometimes it uses "error", and even "user_error"
    // but at the moment, as a new approach, we only use "errorMessage" here, backend can use JSONError() to respond.
    showErrorToast(respJson.errorMessage, {useHtmlBody: respJson.renderFormat === 'html'});
  } else {
    showErrorToast(`Error ${resp.status} ${resp.statusText}. Response: ${respText.substring(0, 200)}`);
  }
}

function buildFetchActionUrl(el: HTMLElement, opt: FetchActionOpts) {
  let url = opt.url;
  if ('name' in el && 'value' in el) {
    // ref: https://htmx.org/attributes/hx-get/
    // If the element with the hx-get attribute also has a value, this will be included as a parameter
    const name = (el as HTMLInputElement).name;
    const val = (el as HTMLInputElement).value;
    const u = new URL(url, window.location.href);
    if (name && !u.searchParams.has(name)) {
      u.searchParams.set(name, val);
      url = u.href;
    }
  }
  return url;
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:20:37Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Find JSONError definitions

`grep -rn "JSONError\b\|JSONErrorNotFound" services/context/context.go services/context/*.go | head -20`

```text
services/context/context.go:257:func (ctx *Context) JSONError(msg any) {
services/context/context.go:268:func (ctx *Context) JSONErrorNotFound(optMsg ...string) {
services/context/context.go:257:func (ctx *Context) JSONError(msg any) {
services/context/context.go:268:func (ctx *Context) JSONErrorNotFound(optMsg ...string) {
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:20:37Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Look for CSRF handling in fetch action

`grep -rn "csrf\|Csrf\|CSRF" web_src/js/features/common-fetch-action.ts | head -20; echo "==="; grep -rn "submitFormFetchAction" -A40 web_src/js/features/common-fetch-action.ts | sed -n 1,80p`

```text
===
web_src/js/features/common-fetch-action.ts:191:export async function submitFormFetchAction(formEl: HTMLFormElement, opts: SubmitFormFetchActionOpts = {}) {
web_src/js/features/common-fetch-action.ts-192-  hideToastsAll();
web_src/js/features/common-fetch-action.ts-193-  await performActionRequest(formEl, prepareFormFetchActionOpts(formEl, opts));
web_src/js/features/common-fetch-action.ts-194-}
web_src/js/features/common-fetch-action.ts-195-
web_src/js/features/common-fetch-action.ts-196-async function confirmFetchAction(el: HTMLElement) {
web_src/js/features/common-fetch-action.ts-197-  let elModal: HTMLElement | null = null;
web_src/js/features/common-fetch-action.ts-198-  const dataModalConfirm = el.getAttribute('data-modal-confirm') || '';
web_src/js/features/common-fetch-action.ts-199-  if (dataModalConfirm.startsWith('#')) {
web_src/js/features/common-fetch-action.ts-200-    // eslint-disable-next-line unicorn/prefer-query-selector
web_src/js/features/common-fetch-action.ts-201-    elModal = document.getElementById(dataModalConfirm.substring(1));
web_src/js/features/common-fetch-action.ts-202-    if (elModal) {
web_src/js/features/common-fetch-action.ts-203-      elModal = createElementFromHTML(elModal.outerHTML);
web_src/js/features/common-fetch-action.ts-204-      elModal.removeAttribute('id');
web_src/js/features/common-fetch-action.ts-205-    }
web_src/js/features/common-fetch-action.ts-206-  }
web_src/js/features/common-fetch-action.ts-207-  if (!elModal) {
web_src/js/features/common-fetch-action.ts-208-    const modalConfirmContent = dataModalConfirm || el.getAttribute('data-modal-confirm-content') || '';
web_src/js/features/common-fetch-action.ts-209-    if (modalConfirmContent) {
web_src/js/features/common-fetch-action.ts-210-      const isRisky = el.classList.contains('red') || el.classList.contains('negative');
web_src/js/features/common-fetch-action.ts-211-      elModal = createConfirmModal({
web_src/js/features/common-fetch-action.ts-212-        header: el.getAttribute('data-modal-confirm-header') || '',
web_src/js/features/common-fetch-action.ts-213-        content: modalConfirmContent,
web_src/js/features/common-fetch-action.ts-214-        confirmButtonColor: isRisky ? 'red' : 'primary',
web_src/js/features/common-fetch-action.ts-215-      });
web_src/js/features/common-fetch-action.ts-216-    }
web_src/js/features/common-fetch-action.ts-217-  }
web_src/js/features/common-fetch-action.ts-218-  if (!elModal) return true;
web_src/js/features/common-fetch-action.ts-219-  return await confirmModal(elModal);
web_src/js/features/common-fetch-action.ts-220-}
web_src/js/features/common-fetch-action.ts-221-
web_src/js/features/common-fetch-action.ts-222-async function performLinkFetchAction(el: HTMLElement) {
web_src/js/features/common-fetch-action.ts-223-  hideToastsAll();
web_src/js/features/common-fetch-action.ts-224-  await performActionRequest(el, {
web_src/js/features/common-fetch-action.ts-225-    method: el.getAttribute('data-fetch-method') || 'POST', // by default, the method is POST for link-action
web_src/js/features/common-fetch-action.ts-226-    url: el.getAttribute('data-url')!,
web_src/js/features/common-fetch-action.ts-227-    loadingIndicator: el.getAttribute('data-fetch-indicator') ?? '$this', // by default, the link-action itself is the loading indicator
web_src/js/features/common-fetch-action.ts-228-    successSync: el.getAttribute('data-fetch-sync') ?? '', // by default, no fetch sync for link-action
web_src/js/features/common-fetch-action.ts-229-  });
web_src/js/features/common-fetch-action.ts-230-}
web_src/js/features/common-fetch-action.ts-231-
--
web_src/js/features/common-fetch-action.ts:400:    await submitFormFetchAction(el, {formSubmitter: e.submitter});
web_src/js/features/common-fetch-action.ts-401-  });
web_src/js/features/common-fetch-action.ts-402-
web_src/js/features/common-fetch-action.ts-403-  addDelegatedEventListener(document, 'click', '.link-action', async (el, e) => {
web_src/js/features/common-fetch-action.ts-404-    // `<a class="link-action" data-url="...">` is a shorthand for
web_src/js/features/common-fetch-action.ts-405-    // `<a data-fetch-trigger="click" data-fetch-method="post" data-fetch-url="..." data-fetch-indicator="$this">`
web_src/js/features/common-fetch-action.ts-406-    e.preventDefault();
web_src/js/features/common-fetch-action.ts-407-    await performLinkFetchAction(el);
web_src/js/features/common-fetch-action.ts-408-  });
web_src/js/features/common-fetch-action.ts-409-
web_src/js/features/common-fetch-action.ts-410-  registerGlobalSelectorFunc('[data-fetch-url]', initFetchActionTrigger);
web_src/js/features/common-fetch-action.ts-411-}
```
</details></tool-use>

_**Agent (claude-opus-5 2026-09-15 02:20:38Z)**_

Interim, from my own independent checks while the fan-out runs — two things that *look* like findings but aren't:

**CSRF is a non-issue here.** This fork has removed CSRF tokens entirely (`services/context/context.go:189-190`: *"We have dropped the csrf token"*) in favour of `Sec-Fetch-Site`-based cross-origin protection (`routers/web/web.go:321-326`). The new route sits inside the `/user/settings` group that closes at `routers/web/web.go:737` with `reqSignIn, user_setting.SettingsCtxData` — same chain as `webauthn/delete` and `webauthn/register`. So the form correctly has no token, matching every other settings template.

**The modal prefill does work.** I suspected `data-modal-rename-webauthn-nickname="{{.Name}}"` wouldn't find `<input id="rename-webauthn-nickname">`, but `web_src/js/features/common-button.ts:101-105` tries `#target` *first*, then `[name=target]`, then `.target`, then tag. The id matches on the first try and line 118 assigns `.value` since the target is an `input`.

Eight adversarial lenses (authz/IDOR, frontend wiring, xorm/DB integrity, WebAuthn semantics, injection, test adversary, convention drift, plus one agent that actually builds and mutation-tests it) are running now, scoped strictly to the added lines, with a skeptic panel behind them. I'll report once they land.

---

_**User (2026-09-15 02:20:38Z)**_

I should note that this is not going to be that serious of a check. So dont go too deep...

---

_**Agent - sidechain (claude-opus-5 2026-09-15 02:20:48Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Locate xorm version and module cache

`grep -n "xorm.io/xorm " go.mod; ls $(go env GOMODCACHE)/xorm.io/ 2>/dev/null | head`

```text
121:	xorm.io/xorm v1.4.1
builder@v0.3.13
xorm@v1.4.1
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:20:40Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Read rest of common-fetch-action.ts

`sed -n 120,260p web_src/js/features/common-fetch-action.ts`

```text
  return url;
}

async function performActionRequest(el: HTMLElement, opt: FetchActionOpts) {
  const attrIsLoading = 'data-fetch-is-loading';
  if (el.getAttribute(attrIsLoading)) return;
  if (!await confirmFetchAction(opt.formSubmitter ?? el)) return;

  el.setAttribute(attrIsLoading, 'true');
  toggleLoadingIndicator(el, opt, true);

  try {
    const url = buildFetchActionUrl(el, opt);
    const headers = new Headers(opt.headers);
    headers.set('X-Gitea-Fetch-Action', '1');
    const resp = await request(url, {method: opt.method, body: opt.body, headers});
    if (resp.ok) {
      await handleFetchActionSuccess(el, opt, resp);
      return;
    }
    await handleFetchActionError(resp);
  } catch (err) {
    if (errorName(err) !== 'AbortError') {
      console.error(`Fetch action request error:`, err);
      showErrorToast(`Error: ${errorMessage(err)}`);
    }
  } finally {
    toggleLoadingIndicator(el, opt, false);
    el.removeAttribute(attrIsLoading);
  }
}

type SubmitFormFetchActionOpts = {
  formSubmitter?: HTMLElement | null;
  formData?: FormData;
};

function prepareFormFetchActionOpts(formEl: HTMLFormElement, opts: SubmitFormFetchActionOpts = {}): FetchActionOpts {
  const formMethodUpper = formEl.getAttribute('method')?.toUpperCase() || 'GET';
  const formActionUrl = formEl.getAttribute('action') || window.location.href;
  const formData = opts.formData ?? new FormData(formEl);
  const [submitterName, submitterValue] = [opts.formSubmitter?.getAttribute('name'), opts.formSubmitter?.getAttribute('value')];
  if (submitterName) {
    formData.append(submitterName, submitterValue || '');
  }

  let reqUrl = formActionUrl;
  let reqBody: FormData | undefined;
  if (formMethodUpper === 'GET') {
    const params = new URLSearchParams();
    for (const [key, value] of formData) {
      params.append(key, value as string);
    }
    const pos = reqUrl.indexOf('?');
    if (pos !== -1) {
      reqUrl = reqUrl.slice(0, pos);
    }
    reqUrl += `?${params.toString()}`;
  } else {
    reqBody = formData;
  }
  return {
    method: formMethodUpper,
    url: reqUrl,
    body: reqBody,
    formSubmitter: opts.formSubmitter,
    loadingIndicator: '$this', // for form submit, by default, the loading indicator is the whole form
    successSync: formEl.getAttribute('data-fetch-sync') ?? '', // by default, no fetch sync for form submit
  };
}

export async function submitFormFetchAction(formEl: HTMLFormElement, opts: SubmitFormFetchActionOpts = {}) {
  hideToastsAll();
  await performActionRequest(formEl, prepareFormFetchActionOpts(formEl, opts));
}

async function confirmFetchAction(el: HTMLElement) {
  let elModal: HTMLElement | null = null;
  const dataModalConfirm = el.getAttribute('data-modal-confirm') || '';
  if (dataModalConfirm.startsWith('#')) {
    // eslint-disable-next-line unicorn/prefer-query-selector
    elModal = document.getElementById(dataModalConfirm.substring(1));
    if (elModal) {
      elModal = createElementFromHTML(elModal.outerHTML);
      elModal.removeAttribute('id');
    }
  }
  if (!elModal) {
    const modalConfirmContent = dataModalConfirm || el.getAttribute('data-modal-confirm-content') || '';
    if (modalConfirmContent) {
      const isRisky = el.classList.contains('red') || el.classList.contains('negative');
      elModal = createConfirmModal({
        header: el.getAttribute('data-modal-confirm-header') || '',
        content: modalConfirmContent,
        confirmButtonColor: isRisky ? 'red' : 'primary',
      });
    }
  }
  if (!elModal) return true;
  return await confirmModal(elModal);
}

async function performLinkFetchAction(el: HTMLElement) {
  hideToastsAll();
  await performActionRequest(el, {
    method: el.getAttribute('data-fetch-method') || 'POST', // by default, the method is POST for link-action
    url: el.getAttribute('data-url')!,
    loadingIndicator: el.getAttribute('data-fetch-indicator') ?? '$this', // by default, the link-action itself is the loading indicator
    successSync: el.getAttribute('data-fetch-sync') ?? '', // by default, no fetch sync for link-action
  });
}

type FetchActionTriggerType = 'click' | 'change' | 'every' | 'load' | 'fetch-reload';

export async function performFetchActionTrigger(el: HTMLElement, triggerType: FetchActionTriggerType) {
  const isUserInitiated = triggerType === 'click' || triggerType === 'change';
  // for user initiated action, by default, the loading indicator is the element itself, otherwise no loading indicator
  const defaultLoadingIndicator = isUserInitiated ? '$this' : '';

  if (isUserInitiated) hideToastsAll();
  await performActionRequest(el, {
    method: el.getAttribute('data-fetch-method') || 'GET', // by default, the method is GET for fetch trigger action
    url: el.getAttribute('data-fetch-url')!,
    loadingIndicator: el.getAttribute('data-fetch-indicator') ?? defaultLoadingIndicator,
    successSync: el.getAttribute('data-fetch-sync') ?? '$this', // by default, the response will replace the current element
  });
}

type PseudoSelectorCommandResult = {
  targets: Element[];
  cmdInnerHTML: boolean;
  cmdMorph: boolean;
};

export function execPseudoSelectorCommands(el: Element, fullCommand: string): PseudoSelectorCommandResult {
  const cmds = fullCommand.split(' ').map((s) => s.trim()).filter(Boolean) || [];
  let targets = [el], cmdInnerHTML = false, cmdMorph = false;
  for (const cmd of cmds) {
    if (cmd === '$this') {
      targets = [el];
    } else if (cmd === '$body') {
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:20:40Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Check csrf in fetch module

`grep -rn "csrf" web_src/js/modules/fetch.ts`

```text
(Bash completed with no output)
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:20:41Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Read fetch module

`cat -n web_src/js/modules/fetch.ts`

```text
     1	import {isObject} from '../utils.ts';
     2	import type {RequestOpts} from '../types.ts';
     3	
     4	// fetch wrapper, use below method name functions and the `data` option to pass in data
     5	// which will automatically set an appropriate headers. For JSON content, only object
     6	// and array types are currently supported.
     7	export function request(url: string, {method = 'GET', data, headers = {}, ...other}: RequestOpts = {}): Promise<Response> {
     8	  let body: string | FormData | URLSearchParams | undefined;
     9	  let contentType: string | undefined;
    10	  if (data instanceof FormData || data instanceof URLSearchParams) {
    11	    body = data;
    12	  } else if (isObject(data) || Array.isArray(data)) {
    13	    contentType = 'application/json';
    14	    body = JSON.stringify(data);
    15	  }
    16	
    17	  headers = new Headers(headers);
    18	  if (!headers.has('content-type') && contentType) {
    19	    headers.set('content-type', contentType);
    20	  }
    21	  return fetch(url, { // eslint-disable-line no-restricted-globals
    22	    method,
    23	    headers,
    24	    ...other,
    25	    ...(body && {body}),
    26	  });
    27	}
    28	
    29	export const GET = (url: string, opts?: RequestOpts) => request(url, {method: 'GET', ...opts});
    30	export const POST = (url: string, opts?: RequestOpts) => request(url, {method: 'POST', ...opts});
    31	export const PATCH = (url: string, opts?: RequestOpts) => request(url, {method: 'PATCH', ...opts});
    32	export const PUT = (url: string, opts?: RequestOpts) => request(url, {method: 'PUT', ...opts});
    33	export const DELETE = (url: string, opts?: RequestOpts) => request(url, {method: 'DELETE', ...opts});
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:20:42Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Read JSONError and flash cookie write

`sed -n 250,290p services/context/context.go; echo ---; grep -rn "CookieFlash\|flashCookie\|SetSiteCookie(.*Flash" services/context/*.go modules/web/middleware/*.go | head -20`

```text
	ctx.JSON(http.StatusOK, map[string]any{"redirect": redirect})
}

func (ctx *Context) JSONOK() {
	ctx.JSON(http.StatusOK, map[string]any{"ok": true}) // this is only a dummy response, frontend seldom uses it
}

func (ctx *Context) JSONError(msg any) {
	switch v := msg.(type) {
	case string:
		ctx.JSON(http.StatusBadRequest, map[string]any{"errorMessage": v, "renderFormat": "text"})
	case template.HTML:
		ctx.JSON(http.StatusBadRequest, map[string]any{"errorMessage": v, "renderFormat": "html"})
	default:
		panic(fmt.Sprintf("unsupported type: %T", msg))
	}
}

func (ctx *Context) JSONErrorNotFound(optMsg ...string) {
	msg := util.OptionalArg(optMsg)
	if msg == "" {
		msg = ctx.Locale.TrString("error.not_found")
	}
	ctx.JSON(http.StatusNotFound, map[string]any{"errorMessage": msg, "renderFormat": "text"})
}
---
modules/web/middleware/flash.go:70:func ParseCookieFlashMessage(val string) *Flash {
modules/web/middleware/flash.go:83:func GetSiteCookieFlashMessage(dataStore reqctx.RequestDataStore, req *http.Request, cookieName string) (string, *Flash) {
modules/web/middleware/flash.go:86:	lastFlashMsg := ParseCookieFlashMessage(lastFlashCookie)
services/context/context.go:175:			lastFlashCookie, lastFlashMsg := middleware.GetSiteCookieFlashMessage(ctx, ctx.Req, CookieNameFlash)
services/context/context.go:183:					middleware.SetSiteCookie(ctx.Resp, CookieNameFlash, val, 0)
services/context/context.go:185:					middleware.SetSiteCookie(ctx.Resp, CookieNameFlash, "", -1)
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:20:44Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Find Flash.Success + JSONRedirect patterns

`grep -rn "Flash.Success" routers/web/user/setting/ | head -20; echo "--- JSONRedirect+Flash combos:"; grep -rn -B3 "JSONRedirect" routers/web/ | grep -n "Flash.Success" | head -20`

```text
routers/web/user/setting/applications.go:111:	ctx.Flash.Success(ctx.Tr("settings.generate_token_success"))
routers/web/user/setting/applications.go:122:		ctx.Flash.Success(ctx.Tr("settings.delete_token_success"))
routers/web/user/setting/profile.go:120:	ctx.Flash.Success(ctx.Tr("settings.update_profile_success"))
routers/web/user/setting/profile.go:181:		ctx.Flash.Success(ctx.Tr("settings.update_avatar_success"))
routers/web/user/setting/profile.go:378:		ctx.Flash.Success(ctx.Tr("settings.theme_update_success"))
routers/web/user/setting/profile.go:410:	ctx.Flash.Success(translation.NewLocale(ctx.Doer.Language).TrString("settings.update_language_success"))
routers/web/user/setting/profile.go:423:	ctx.Flash.Success(ctx.Tr("settings.saved_successfully"))
routers/web/user/setting/account.go:95:			ctx.Flash.Success(ctx.Tr("settings.change_password_success"))
routers/web/user/setting/account.go:209:		ctx.Flash.Success(ctx.Tr("settings.add_email_success"))
routers/web/user/setting/account.go:234:	ctx.Flash.Success(ctx.Tr("settings.email_deletion_success"))
routers/web/user/setting/webhooks.go:43:		ctx.Flash.Success(ctx.Tr("repo.settings.webhook_deletion_success"))
routers/web/user/setting/security/openid.go:108:	ctx.Flash.Success(ctx.Tr("settings.add_openid_success"))
routers/web/user/setting/security/openid.go:130:	ctx.Flash.Success(ctx.Tr("settings.openid_deletion_success"))
routers/web/user/setting/adopt.go:50:		ctx.Flash.Success(ctx.Tr("repo.adopt_preexisting_success", dir))
routers/web/user/setting/adopt.go:56:		ctx.Flash.Success(ctx.Tr("repo.delete_preexisting_success", dir))
routers/web/user/setting/security/security.go:61:			ctx.Flash.Success(ctx.Tr("settings.remove_account_link_success"))
routers/web/user/setting/security/webauthn.go:171:	ctx.Flash.Success(ctx.Tr("settings.webauthn_rename_key_success"))
routers/web/user/setting/oauth2_common.go:63:	ctx.Flash.Success(ctx.Tr("settings.create_oauth2_application_success"), true)
routers/web/user/setting/oauth2_common.go:129:	ctx.Flash.Success(ctx.Tr("settings.update_oauth2_application_success"))
routers/web/user/setting/oauth2_common.go:154:	ctx.Flash.Success(ctx.Tr("settings.update_oauth2_application_success"), true)
--- JSONRedirect+Flash combos:
1:routers/web/admin/hooks.go-67-		ctx.Flash.Success(ctx.Tr("repo.settings.webhook_deletion_success"))
7:routers/web/admin/packages.go-96-	ctx.Flash.Success(ctx.Tr("packages.settings.delete.version.success"))
16:routers/web/admin/emails.go-180-	ctx.Flash.Success(ctx.Tr("admin.emails.deletion_success"))
28:routers/web/user/setting/oauth2_common.go-165-	ctx.Flash.Success(ctx.Tr("settings.remove_oauth2_application_success"))
33:routers/web/user/setting/oauth2_common.go-176-	ctx.Flash.Success(ctx.Tr("settings.revoke_oauth2_grant_success"))
37:routers/web/admin/badges.go-81-	ctx.Flash.Success(ctx.Tr("admin.badges.new_success", b.Slug))
42:routers/web/admin/badges.go-162-	ctx.Flash.Success(ctx.Tr("admin.badges.update_success"))
61:routers/web/admin/auths.go-465-	ctx.Flash.Success(ctx.Tr("admin.auths.deletion_success"))
63:routers/web/user/setting/applications.go-122-		ctx.Flash.Success(ctx.Tr("settings.delete_token_success"))
69:routers/web/org/members.go-118-			ctx.Flash.Success(ctx.Tr("form.organization_leave_success", org.DisplayName()))
71:routers/web/user/setting/webhooks.go-43-		ctx.Flash.Success(ctx.Tr("repo.settings.webhook_deletion_success"))
85:routers/web/org/teams.go-619-		ctx.Flash.Success(ctx.Tr("org.teams.delete_team_success"))
96:routers/web/org/setting.go-145-	ctx.Flash.Success(ctx.Tr("org.settings.delete_successful", ctx.Org.Organization.Name))
99:routers/web/org/setting.go-178-		ctx.Flash.Success(ctx.Tr("repo.settings.webhook_deletion_success"))
106:routers/web/org/setting.go-232-	ctx.Flash.Success(ctx.Tr("org.settings.rename_success", oldOrgName, newOrgName))
121:routers/web/org/setting.go-257-	ctx.Flash.Success(ctx.Tr("org.settings.change_visibility_success", ctx.Org.Organization.Name))
125:routers/web/user/package.go-477-		ctx.Flash.Success(ctx.Tr("packages.settings.unlink.success"))
130:routers/web/user/package.go-497-	ctx.Flash.Success(ctx.Tr("packages.settings.link.success"))
134:routers/web/user/setting/security/webauthn.go-171-	ctx.Flash.Success(ctx.Tr("settings.webauthn_rename_key_success"))
143:routers/web/user/setting/security/openid.go-130-	ctx.Flash.Success(ctx.Tr("settings.openid_deletion_success"))
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:20:45Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Find web.Bind implementation

`grep -rn "func Bind" modules/web/*.go services/context/*.go | head; echo "=== bind impl ==="; sed -n '1,80p' modules/web/route.go 2>/dev/null | head -5; grep -rn "func Bind" -A 40 modules/web/binding.go 2>/dev/null | head -60`

```text
modules/web/router.go:23:func Bind[T any](_ T) http.HandlerFunc {
=== bind impl ===
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:20:45Z)**_

`go build ./...` exit 0, no output. Now vet on the three changed packages.

_**Agent - sidechain (claude-opus-5 2026-09-15 02:20:46Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Find ctx.HasError usages

`grep -rn "ctx.HasError()" --include='*.go' routers/ | head -40`

```text
routers/install/install.go:200:	if ctx.HasError() {
routers/web/admin/badges.go:59:	if ctx.HasError() {
routers/web/admin/badges.go:147:	if ctx.HasError() {
routers/web/admin/auths.go:300:	if ctx.HasError() {
routers/web/admin/auths.go:384:	if ctx.HasError() {
routers/web/user/setting/security/openid.go:31:	if ctx.HasError() {
routers/web/user/setting/profile.go:62:	if ctx.HasError() {
routers/web/user/setting/profile.go:360:	if ctx.HasError() {
routers/web/user/setting/account.go:64:	if ctx.HasError() {
routers/web/user/setting/account.go:178:	if ctx.HasError() {
routers/web/admin/users.go:122:	if ctx.HasError() {
routers/web/admin/users.go:339:	if ctx.HasError() {
routers/web/user/setting/applications.go:59:	if ctx.HasError() {
routers/web/auth/auth.go:291:	if ctx.HasError() {
routers/web/auth/auth.go:554:	if ctx.HasError() {
routers/web/user/setting/security/webauthn.go:136:	if ctx.HasError() { // form binding validation error
routers/web/org/org_labels.go:100:	if ctx.HasError() {
routers/web/shared/user/block.go:64:	if ctx.HasError() {
routers/web/user/setting/security/2fa.go:213:	if ctx.HasError() {
routers/web/user/setting/oauth2_common.go:43:	if ctx.HasError() {
routers/web/user/setting/oauth2_common.go:97:	if ctx.HasError() {
routers/web/shared/packages/packages.go:102:	if ctx.HasError() {
routers/web/auth/linkaccount.go:127:	if ctx.HasError() {
routers/web/auth/linkaccount.go:205:	if ctx.HasError() {
routers/web/repo/wiki.go:661:	if ctx.HasError() {
routers/web/repo/wiki.go:717:	if ctx.HasError() {
routers/web/org/setting.go:67:	if ctx.HasError() {
routers/web/org/setting.go:202:	if ctx.HasError() {
routers/web/auth/password.go:258:	if ctx.HasError() {
routers/web/user/setting/keys.go:53:	if ctx.HasError() {
routers/web/repo/fork.go:153:	if ctx.HasError() {
routers/web/shared/actions/variables.go:120:	if ctx.HasError() { // form binding validation error
routers/web/shared/actions/variables.go:145:	if ctx.HasError() { // form binding validation error
routers/web/repo/setting/deploy_key.go:49:	if ctx.HasError() {
routers/web/shared/label/label.go:15:	if ctx.HasError() {
routers/web/auth/openid.go:78:	if ctx.HasError() {
routers/web/org/projects.go:153:	if ctx.HasError() {
routers/web/org/projects.go:272:	if ctx.HasError() {
routers/web/repo/milestone.go:113:	if ctx.HasError() {
routers/web/repo/milestone.go:169:	if ctx.HasError() {
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:20:46Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Find CSRF token injection in JS

`grep -rn "_csrf\|csrfToken\|X-Csrf" web_src/js/ | head -30`

```text
(Bash completed with no output)
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:20:46Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Read fetch module

`cat web_src/js/modules/fetch.ts`

```text
import {isObject} from '../utils.ts';
import type {RequestOpts} from '../types.ts';

// fetch wrapper, use below method name functions and the `data` option to pass in data
// which will automatically set an appropriate headers. For JSON content, only object
// and array types are currently supported.
export function request(url: string, {method = 'GET', data, headers = {}, ...other}: RequestOpts = {}): Promise<Response> {
  let body: string | FormData | URLSearchParams | undefined;
  let contentType: string | undefined;
  if (data instanceof FormData || data instanceof URLSearchParams) {
    body = data;
  } else if (isObject(data) || Array.isArray(data)) {
    contentType = 'application/json';
    body = JSON.stringify(data);
  }

  headers = new Headers(headers);
  if (!headers.has('content-type') && contentType) {
    headers.set('content-type', contentType);
  }
  return fetch(url, { // eslint-disable-line no-restricted-globals
    method,
    headers,
    ...other,
    ...(body && {body}),
  });
}

export const GET = (url: string, opts?: RequestOpts) => request(url, {method: 'GET', ...opts});
export const POST = (url: string, opts?: RequestOpts) => request(url, {method: 'POST', ...opts});
export const PATCH = (url: string, opts?: RequestOpts) => request(url, {method: 'PATCH', ...opts});
export const PUT = (url: string, opts?: RequestOpts) => request(url, {method: 'PUT', ...opts});
export const DELETE = (url: string, opts?: RequestOpts) => request(url, {method: 'DELETE', ...opts});
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:20:47Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Grep csrf in js

`grep -rn "csrf" --include=*.ts web_src/js/ | head -40`

```text
(eval):1: no matches found: --include=*.ts
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:20:50Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Vet the three changed packages

`go vet ./models/auth/ ./routers/web/user/setting/security/ ./services/forms/ > /tmp/claude-1000/-home-mantmant-Documents-CSCI435-gitea/f1c2aa49-dab6-4b47-9277-85993d369c13/scratchpad/vet.log 2>&1; echo "EXIT=$?"; cat /tmp/claude-1000/-home-mantmant-Documents-CSCI435-gitea/f1c2aa49-dab6-4b47-9277-85993d369c13/scratchpad/vet.log`

```text
The user doesn't want to proceed with this tool use. The tool use was rejected (eg. if it was a file edit, the new_string was NOT written to the file). STOP what you are doing and wait for the user to tell you how to proceed.
```
</details></tool-use>

_**Agent (claude-opus-5 2026-09-15 02:20:48Z)**_

<tool-use data-tool-type="unknown" data-tool-name="ToolSearch"><details>
<summary>Tool use: **ToolSearch**</summary>

**Input:**

- max_results: `1`
- query: `select:TaskStop`

**Result:**

```

```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:20:49Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Read webauthn template and modal actions

`cat -n templates/user/settings/security/webauthn.tmpl && echo "=== modal_actions_confirm ===" && cat -n templates/base/modal_actions_confirm.tmpl`

```text
     1	<h4 class="ui top attached header">{{ctx.Locale.Tr "settings.webauthn"}}</h4>
     2	<div class="ui attached segment">
     3		<p>{{ctx.Locale.Tr "settings.webauthn_desc" "https://w3c.github.io/webauthn/#webauthn-authenticator"}}</p>
     4		<p>{{ctx.Locale.Tr "settings.webauthn_key_loss_warning"}} {{ctx.Locale.Tr "settings.webauthn_alternative_tip"}}</p>
     5		{{template "user/auth/webauthn_error" .}}
     6		<div class="flex-divided-list items-with-main">
     7			{{range .WebAuthnCredentials}}
     8				<div class="item">
     9					<div class="item-leading">
    10						{{svg "octicon-key" 32}}
    11					</div>
    12					<div class="item-main">
    13						<div class="item-title">{{.Name}}</div>
    14						<div class="item-body">
    15							<i>{{ctx.Locale.Tr "settings.added_on" (DateUtils.AbsoluteShort .CreatedUnix)}}</i>
    16						</div>
    17					</div>
    18					<div class="item-trailing">
    19						<button class="ui tiny button show-modal"
    20							data-modal="#rename-webauthn-modal"
    21							data-modal-form.url="{{$.Link}}/webauthn/rename?id={{.ID}}"
    22							data-modal-rename-webauthn-nickname="{{.Name}}"
    23						>
    24							{{svg "octicon-pencil"}} {{ctx.Locale.Tr "settings.webauthn_rename_key"}}
    25						</button>
    26						<button class="ui red tiny button link-action" data-modal-confirm="#delete-registration" data-url="{{$.Link}}/webauthn/delete?id={{.ID}}">
    27						{{ctx.Locale.Tr "settings.delete_key"}}
    28						</button>
    29					</div>
    30				</div>
    31			{{end}}
    32		</div>
    33		<div class="ui form">
    34			<div class="required field">
    35				<label for="nickname">{{ctx.Locale.Tr "settings.webauthn_nickname"}}</label>
    36				<input id="nickname" name="nickname" type="text" required>
    37			</div>
    38			<button id="register-webauthn" class="ui primary button">{{svg "octicon-key"}} {{ctx.Locale.Tr "settings.webauthn_register_key"}}</button>
    39		</div>
    40		<div class="ui small modal" id="rename-webauthn-modal">
    41			<div class="header">{{svg "octicon-pencil"}} {{ctx.Locale.Tr "settings.webauthn_rename_key"}}</div>
    42			<form class="ui form form-fetch-action" method="post">
    43				<div class="content">
    44					<div class="required field">
    45						<label for="rename-webauthn-nickname">{{ctx.Locale.Tr "settings.webauthn_nickname"}}</label>
    46						<input id="rename-webauthn-nickname" name="name" type="text" maxlength="255" required>
    47					</div>
    48				</div>
    49				{{template "base/modal_actions_confirm" (dict "ModalButtonTypes" "confirm")}}
    50			</form>
    51		</div>
    52		<div class="ui small modal" id="delete-registration">
    53			<div class="header">
    54				{{svg "octicon-trash"}}
    55				{{ctx.Locale.Tr "settings.webauthn_delete_key"}}
    56			</div>
    57			<div class="content">
    58				<p>{{ctx.Locale.Tr "settings.webauthn_delete_key_desc"}}</p>
    59			</div>
    60			{{template "base/modal_actions_confirm" .}}
    61		</div>
    62	</div>
=== modal_actions_confirm ===
     1	{{/* Two buttons (negative, positive):
     2	* ModalButtonTypes: "yes" (default) or "confirm"
     3	* ModalButtonCancelText
     4	* ModalButtonOkText
     5	
     6	Single danger button (GitHub-like):
     7	* ModalButtonDangerText "This action will destroy your data"
     8	
     9	The ".ok.button" and ".cancel.button" selectors are also used by Fomantic Modal internally
    10	*/}}
    11	<div class="actions">
    12		{{if .ModalButtonDangerText}}
    13			<button class="ui danger red ok button">{{.ModalButtonDangerText}}</button>
    14		{{else}}
    15			{{$textNegative := ctx.Locale.Tr "modal.no"}}
    16			{{$textPositive := ctx.Locale.Tr "modal.yes"}}
    17			{{if eq .ModalButtonTypes "confirm"}}
    18				{{$textNegative = ctx.Locale.Tr "modal.cancel"}}
    19				{{$textPositive = ctx.Locale.Tr "modal.confirm"}}
    20			{{end}}
    21			{{if .ModalButtonCancelText}}{{$textNegative = .ModalButtonCancelText}}{{end}}
    22			{{if .ModalButtonOkText}}{{$textPositive = .ModalButtonOkText}}{{end}}
    23			<button class="ui cancel button">{{svg "octicon-x"}} {{$textNegative}}</button>
    24			<button class="ui primary ok button">{{svg "octicon-check"}} {{$textPositive}}</button>
    25		{{end}}
    26	</div>
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:20:50Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Grep csrf in web_src js

`grep -rni "csrf" web_src/js/ | head -40`

```text
(Bash completed with no output)
```
</details></tool-use>

_**Agent - sidechain (claude-opus-5 2026-09-15 02:20:50Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Find CSRF Go implementation

`grep -rln "csrf" --include="*.go" . | head -20`

```text
services/context/context.go
```
</details></tool-use>

_**Agent (claude-opus-5 2026-09-15 02:20:50Z)**_

<tool-use data-tool-type="unknown" data-tool-name="TaskStop"><details>
<summary>Tool use: **TaskStop**</summary>

```text
{"message":"Successfully stopped task: wdswt5slj (Adversarially attack the WebAuthn passkey-rename diff (changed lines only) across 8 lenses, then verify each finding with skeptics)","task_id":"wdswt5slj","task_type":"local_workflow","command":"Adversarially attack the WebAuthn passkey-rename diff (changed lines only) across 8 lenses, then verify each finding with skeptics"}
```
</details></tool-use>

_**Agent - sidechain (2026-09-15 02:20:50Z)**_

[Request interrupted by user]

_**Agent - sidechain (2026-09-15 02:20:50Z)**_

[Request interrupted by user]

_**Agent - sidechain (2026-09-15 02:20:50Z)**_

[Request interrupted by user]

_**Agent - sidechain (2026-09-15 02:20:50Z)**_

[Request interrupted by user]

_**Agent - sidechain (2026-09-15 02:20:50Z)**_

[Request interrupted by user]

_**Agent - sidechain (2026-09-15 02:20:50Z)**_

[Request interrupted by user]

_**Agent - sidechain (2026-09-15 02:20:50Z)**_

[Request interrupted by user for tool use]

_**Agent - sidechain (2026-09-15 02:20:50Z)**_

[Request interrupted by user]

_**Agent (claude-opus-5 2026-09-15 02:20:52Z)**_

Stopped the deep pass. Quick targeted checks instead:

_**Agent (claude-opus-5 2026-09-15 02:20:55Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Check JSON helpers and locale strings

`echo "=== JSONErrorNotFound ===" && grep -rn "func (ctx \*Context) JSONErrorNotFound\|func.*JSONErrorNotFound\|func.*JSONError\b\|func.*JSONRedirect" services/context/*.go; echo "=== form.require_error text ==="; grep -n '"form.require_error"\|"settings.webauthn_nickname"' options/locale/locale_en-US.json`

```text
=== JSONErrorNotFound ===
services/context/context.go:249:func (ctx *Context) JSONRedirect(redirect string) {
services/context/context.go:257:func (ctx *Context) JSONError(msg any) {
services/context/context.go:268:func (ctx *Context) JSONErrorNotFound(optMsg ...string) {
=== form.require_error text ===
540:  "form.require_error": " cannot be empty.",
921:  "settings.webauthn_nickname": "Nickname",
```
</details></tool-use>

_**Agent (claude-opus-5 2026-09-15 02:20:57Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Check HasError JSONError convention

`echo "=== ctx.HasError + JSONError pattern elsewhere ===" && grep -rn -B1 -A2 "ctx.HasError()" routers/web/ | grep -A2 -B1 "JSONError" | head -30`

```text
=== ctx.HasError + JSONError pattern elsewhere ===
routers/web/user/setting/security/webauthn.go:136:	if ctx.HasError() { // form binding validation error
routers/web/user/setting/security/webauthn.go-137-		ctx.JSONError(ctx.GetErrMsg())
routers/web/user/setting/security/webauthn.go-138-		return
routers/web/admin/badges.go-58-
routers/web/admin/badges.go:59:	if ctx.HasError() {
routers/web/admin/badges.go-60-		ctx.JSONError(ctx.GetErrMsg())
routers/web/admin/badges.go-61-		return
--
--
routers/web/admin/badges.go:147:	if ctx.HasError() {
routers/web/admin/badges.go-148-		ctx.JSONError(ctx.GetErrMsg())
routers/web/admin/badges.go-149-		return
routers/web/user/setting/applications.go-58-
--
routers/web/org/setting.go:202:	if ctx.HasError() {
routers/web/org/setting.go-203-		ctx.JSONError(ctx.GetErrMsg())
routers/web/org/setting.go-204-		return
routers/web/org/org.go-50-
--
routers/web/shared/actions/variables.go:120:	if ctx.HasError() { // form binding validation error
routers/web/shared/actions/variables.go-121-		ctx.JSONError(ctx.GetErrMsg())
routers/web/shared/actions/variables.go-122-		return
--
--
routers/web/shared/actions/variables.go:145:	if ctx.HasError() { // form binding validation error
routers/web/shared/actions/variables.go-146-		ctx.JSONError(ctx.GetErrMsg())
routers/web/shared/actions/variables.go-147-		return
routers/web/org/org_labels.go-99-	form := web.GetForm(ctx).(*forms.InitializeLabelsForm)
--
routers/web/shared/label/label.go:15:	if ctx.HasError() {
```
</details></tool-use>

_**Agent (claude-opus-5 2026-09-15 02:21:04Z)**_

<tool-use data-tool-type="shell" data-tool-name="Bash"><details>
<summary>Tool use: **Bash**</summary>
Check how require_error is normally composed

`grep -rn "require_error" --include='*.go' . | head`

```text
routers/web/admin/auths.go:215:		return nil, errors.New(ctx.Locale.TrString("form.SSPISeparatorReplacement") + ctx.Locale.TrString("form.require_error"))
routers/web/user/setting/security/webauthn.go:154:		ctx.JSONError(ctx.Locale.TrString("settings.webauthn_nickname") + ctx.Locale.TrString("form.require_error"))
tests/integration/repo_branch_test.go:56:			FlashMessage:   translation.NewLocale("en-US").TrString("form.NewBranchName") + translation.NewLocale("en-US").TrString("form.require_error"),
modules/web/middleware/binding.go:131:		data["ErrorMsg"] = trName + l.TrString("form.require_error")
```
</details></tool-use>

_**Agent (claude-opus-5 2026-09-15 02:21:46Z)**_

Stopped the deep pass — here's the right-sized version.

## Verdict: the security-critical parts are sound

I checked the things that actually matter for a passkey feature, and they hold up:

- **Ownership** is enforced twice — `cred.UserID != ctx.Doer.ID` in the handler (`webauthn.go:145`) *and* `.Where("user_id = ?", userID)` in `UpdateCredentialName` (`models/auth/webauthn.go:200`). The unit test exercises the cross-user case for real: `AssertExistsAndLoadBean` turns the bean's fields into SQL conditions, so it's a genuine DB assertion, not a vacuous one.
- **No CSRF hole.** This fork dropped CSRF tokens entirely (`services/context/context.go:189-190`) in favour of `Sec-Fetch-Site` cross-origin protection (`routers/web/web.go:321-326`). The new route sits in the `/user/settings` group closing at `web.go:737` with `reqSignIn` — identical chain to `webauthn/delete`. The form correctly carries no token, matching every other settings template.
- **Credential material is untouched.** `Cols("name", "lower_name")` bounds the SET clause, and `BeforeUpdate()` (`webauthn.go:154-156`) fills `lower_name`. Nothing can zero out `credential_id`/`public_key`/`sign_count`.
- **The modal prefill works.** I initially suspected `data-modal-rename-webauthn-nickname` wouldn't find `<input id="rename-webauthn-nickname">`, but `common-button.ts:101-105` tries `#target` first and assigns `.value` at line 118.
- Handler shape matches house style: `ctx.HasError()` → `ctx.JSONError(ctx.GetErrMsg())` is the pattern in `admin/badges.go:59`, `shared/actions/variables.go:120`, `org/setting.go:202`. The error-string concatenation matches `modules/web/middleware/binding.go:131` exactly, and `form.require_error` is `" cannot be empty."` with a leading space, so it renders "Nickname cannot be empty."

## Three small things

**1. Subtests depend on running in order** (`tests/integration/user_settings_test.go`). `SurroundingSpacesTrimmed` ends with a rename back to `"WORK LAPTOP"` *specifically so later subtests pass*, and `InvalidNames` asserts `Name: "WORK LAPTOP"` which only holds if `CaseChangeOfOwnName` ran first. So `go test -run 'TestUserSettingsWebAuthnRename/NameTaken'` fails on its own — and AGENTS.md documents single-test runs as the normal workflow. Cheap fix: have each subtest set up the name it needs.

**2. Concurrent rename → 500.** The uniqueness check is a SELECT then an UPDATE with no transaction, and there's a real DB unique index on `(lower_name, user_id)` (`models/auth/webauthn.go:47-48`). Two renames to the same name at once means one hits the constraint, and the raw error goes to `ctx.ServerError` → 500 plus a logged stack. Narrow (you'd have to race yourself in two tabs), but the fix is just treating a constraint violation as the same `webauthn_nickname_taken` error.

**3. Interior whitespace isn't normalized.** Leading/trailing is trimmed, but `"My  Key"` and `"My Key"` both pass the uniqueness check and look identical in the key list — as does anything using NBSP. For a list where the name is the only way you tell your security keys apart, collapsing internal runs of whitespace is worth the one line.

None of these are merge blockers. #1 is the one I'd actually ask them to fix, since it'll bite whoever debugs that test next.
